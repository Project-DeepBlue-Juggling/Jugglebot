# tools/probes/ — Reusable Empirical-Probe Harnesses

Probes fall into two families:

1. **Offline drivers** — scripts that drive the MPC / controller stack in
   a production-faithful way from recorded telemetry, to reproduce
   hardware failures, characterise edge cases, or empirically confirm
   threshold values before they get encoded in tests.
2. **Live observers** — scripts that record system state alongside a live
   session (real robot, real ROS2 stack, real MPC) so a later analysis
   pass can attribute failures to hardware bottlenecks (CPU saturation,
   thermal / DVFS throttling, memory bandwidth, etc).

**These probes are committed to the repo** — distinct from one-off
exploratory probes which still go to `/tmp/probe_*.py`. The split is
codified in `CLAUDE.md`'s "Empirical probe before writing tests" rule.

## Why committed probes exist

Logbook entries and test docstrings reference probes by path. When the
probe lives in `/tmp/`, any reboot wipes the reference. Reusable
harnesses — anything a future investigation is likely to re-run or
adapt — go here so the references stay live.

## Probe conventions

- **Outputs**: probes write CSV / JSON / report files to `temp/probes/`
  (auto-gitignored under the umbrella `temp/` rule). Never write
  run outputs inside `tools/`. Prefer a **timestamped** filename so
  successive runs in one session accumulate rather than clobber each
  other (`fk_convergence_bag_check.py`, `cone_bag_decode.py` and
  `hand_stroke_timeline.py` all do this).
- **Committed reference fixtures** live in `tools/probes/data/`. A probe
  whose gate replays a recording under gitignored `temp/` needs a
  committed cut of it, or the gate is unrunnable on a fresh clone — and
  that cut needs a committed *recipe* (an explicit-output regeneration
  flag), since a fixture nobody can re-baseline is only half a live
  reference. This is the one exception to "no outputs inside `tools/`":
  reference data, not run output, and written only by an explicit flag.
  **Exception, by fixture SHAPE, not by probe**: a fixture that is a
  handful of *measured constants* rather than a data cut is written as an
  importable `.py` under `tests/`, next to the test that reads it —
  `possession_verdict_bag_check.py --emit-fixtures` →
  `tests/ros/possession_fixtures.py`, `toss_record_miner.py
  --emit-fixture` → `tests/ros/toss_record_fixtures.py`. The reason is
  reviewability: a census of a dozen numbers is something a human checks
  and argues with in a diff, and burying it in JSON under `tools/` makes
  a re-baseline look like a blob swap. Data *cuts* (thousands of samples)
  stay in `tools/probes/data/` — e.g.
  `hand_sensor_replay_fixture.json`. Either way the recipe rule is
  unchanged and absolute.
- **Header docstring**: each probe's top-of-file docstring states what
  the probe replays / characterises, the logbook entry that motivated
  it, and the test (if any) whose empirical recipe it underpins.
- **README row**: every committed probe gets a row in § "Available
  probes" below, in the **same commit** that adds the probe. A probe
  nobody can find from this file is a probe nobody re-runs — Phase 0 of
  the 2026-07 refactor programme had to backfill a missing
  `canbridge_reboot_latch_probe.py` row for exactly that reason.
- **Self-contained**: probes set up `sys.path` themselves so they run
  with `python tools/probes/<name>.py ...` from the repo root without
  needing the project venv to be on `PYTHONPATH`.
- **Offline drivers — no hardware**: read recorded telemetry CSVs from
  `temp/logs/` and drive the MPC stack via Python imports, not ROS2 /
  ZeroMQ. Reproducibility against the pinned dependency stack is the
  whole point.
- **Live observers — hardware-allowed, side-effect-free**: may launch
  alongside a real session, but must only *observe* (read `/proc`,
  spawn `tegrastats` / `pidstat`, attach `py-spy`). Never command the
  motors, never send ROS2 messages — that's `tests/hardware/` territory.
  (Sole carve-out: the GUI verification harness `gui_synthetic_stack.py`
  publishes *synthetic telemetry* ROS2 messages, guarded in code three
  ways — it refuses to start unless the ROS graph is EMPTY, it re-checks
  the graph at ~1 Hz while running and on any foreign node immediately
  disables all publishers and tears itself down, and it never publishes
  on a topic any node treats as a command.)

## Available probes

### Offline drivers

| Probe | Purpose | Motivating logbook entry |
|-------|---------|--------------------------|
| `replay_hardware_csv.py` | Production-faithful replay of a recorded `mpc_*.csv` through `MPCController` exactly as `controller/runner.py` does. Reproduces solver-failure scenarios (CTE singletons, cascades, walk-forward emission discontinuities) offline against the pinned dependency stack. | `logbook/2026-05-20-walk-forward-singleton-emission-jerk.md` (Task 2 of post-warm-start-fix arc; logbook landing alongside the fix) |
| `cone_bag_decode.py` | Decodes the catching cone's `/cone/*` topics straight out of rosbags into a one-command per-session evidence table: `/cone/heartbeat` count, `connected` (n_true/n_total), `time_synced` (n_true/n_total + **was it synced from the FIRST heartbeat?** — the column that settles "did the cone ever sync this session"), `last_catch_seq` range → `taps` (wrap-aware, **reboot-aware** genuine tap count), `/cone/catch_event` count, `/cone/timing_result` count. The `taps` vs `catch_ev` cross-check is load-bearing: `taps` is what the CONE believed happened, `catch_ev` is what the ROS graph actually PUBLISHED — a divergence (` !` in the table) means catch frames were lost in between. Handles both cone firmware eras: the modern `have_any_catch`-gated seq (pre-first-catch `last_catch_seq` is a placeholder) and the legacy May firmware that never set the flag at all (falls back to a plain wrap-aware delta) — `taps == catch_ev` on 17/17 sessions of 2026-07-12 **and** 3/3 legacy `cone_test_*` bags with catches. **Read that 17/17 with its condition attached**: it validates the cone→ROS transport only *because no catch followed either in-session cone reboot* (`18-34-09`, `18-44-56`, both `*`-marked) — a cone MCU reboot resets `last_catch_seq` to 0, and the probe now detects the reset (`have_any_catch` clearing, or an implausible >64-tap single-heartbeat step), restarts the accumulator and marks the row, instead of bridging straight across it and inventing ~250 phantom taps. A genuine count of **zero** is reported as `0`, never as "topic absent" (`-`) — the distinction is drawn from the bag's channel list, because collapsing a real 0 into "absent" silently suppressed the exact frames-lost case the probe exists to detect (cone counted N taps, ROS graph published ZERO `catch_event`s — i.e. the `catch_correlation_node` orphan). `-` now means the bag has no channel for that topic at all. Offline and read-only (opens `.mcap` files only; no node, no socket, no hardware) and needs NO ROS2 — `mcap_ros2` decodes the schemas embedded in the bag, so it runs on a non-ROS2 clone. `--bag` / `--all` (`--match <date>` to narrow; a full ~230-bag sweep takes ~6 min — cost is chunk decompression, not decode) / `--json` (copy to `temp/probes/`). Degrades gracefully on unreadable bags (per-bag `ERROR:` row, sweep continues) and on a missing `--root` (clean error, not a traceback). | `logbook/2026-07-12-gui-cone-catch-correlation-regression.md` (the `timing` column is ground truth for that fix: `/cone/timing_result` is **0** in every session before the rebuild, non-zero after) + `logbook/2026-07-12-cone-sync-udp-link-contention.md` (this probe REFUTED the "cone only syncs right after a colcon build" model — `sync@1st` is `Y` for every session from 15:57 on, straight across the 19:17 rebuild boundary and for three further launches 45 min–3 h later. It then refuted that entry's OWN first attribution of the two dead morning sessions to a co-binding bench harness: the `connected` fractions and the `*` reboot markers show the same dropout-then-reboot signature in sessions with **no bench process running at all**, and a partial-connectivity session (`20-03-36`, 155/593) is a shape an all-or-nothing UDP bind race cannot produce — pointing at cone-side power/wiring intermittency instead) |
| `bench_leg_plant_id.py` | Leg-plant system-ID from recorded `bench_leg_sysid.py --mode strokes` CSVs — offline, no hardware, no new bench runs. Two fits per gain point: (a) the **plant**, by regressing the rigid-body torque balance `2πJ·a = Kt·iq − τ_c·sgn(v) − b·v` on the (a, v, iq) triples → `J_eff`, `τ_c`, `b`; and (b) the **tracking-error budget**, decomposing `err = cmd_u0 − encoder` into `τ_d·v + k_a·a + k_f·sgn(v) + c` → how much of the error is even *attackable by feedback* (the delay term is not: it is gain-independent, and much of it is the metric's own sample-and-hold, since `cmd_u0` is the knot at the START of the segment the firmware is interpolating THROUGH). Prints the cascade table `ω_v = vel_gain/(2πJ)` vs `ω_p = pos_gain` — the ratio that actually decides whether a gain point rings, and which the gain ladder never measured. **Result: `J_eff` = 0.7–1.2 × J_rotor across five independent fits (two highest-R² both at 0.97×), refuting the unsourced "J_eff ~10× rotor" guess the tuning methodology had carried since 2026-04-27 by an order of magnitude; `τ_c` = 0.88–1.22 A, bracketing the 2026-04-27 friction bench's 1.094 A — reproduced from unrelated data.** Caveats in the header docstring — `a` is a numerical derivative so this is ±~20%, good enough to settle a 10× hypothesis and size a feedforward, not a precision inertia measurement; and the bench leg is UNLOADED (no gravity, coupling or hand reaction), so these are a floor, not the loaded-robot budget. `python tools/probes/bench_leg_plant_id.py --all`. | `logbook/2026-07-13-leg-plant-id-and-the-units-bug.md` (the session that ended the leg gain hunt: the "accuracy knee" that motivated the retune was a rev→mm units bug, and the plant this probe identifies shows the cascade was already healthy at production gains) |
| `toss_trace_synth.py` | Synthetic-trace generator + end-to-end verification matrix for the Phase-3 toss-choreography checker (`tests/hardware/toss_trace_recorder.py check`): generates the 2 happy-path traces (waived dry ending `Toss MISSED`; un-waived `REJECTED_NO_BALL`) plus the faithful `ABORTED_NO_RELEASE` variant (dispatch eaten, THROWING reached, no stroke — must banner, SKIP exactly DT-9/DT-12 with DT-5 still evaluated via the THROWING proxy, and exit 0) plus BOTH continuous happy-path traces — `cont_happy` (3-cycle `TossContinuous` session, every cycle CAUGHT: the `ACTION_STAY` teardown) and `cont_dry` (every cycle MISSED: the `SAFE_ABORT` teardown, which is the shape the runbook's MANDATORY CONT-STEP-1 pre-flight actually produces and whose ~10 rev retract lands inside every dwell window CS-3 measures) — plus ONE violation trace per invariant (25 cases, DT-1..14 + RJ-1..4 + CS-1..6 with CS-3 carrying two, one per half — each flips exactly the ordering/property its invariant checks), then `--verify` runs the REAL checker on all 30 and asserts the matrix: happy/variant ⇒ exit 0, every non-SKIP invariant PASS; violation ⇒ exit 1, exactly the targeted invariant FAILs, zero collateral. Includes the empirically-confirmed 2026-07-25 audit inversion (`viol_dt5`: hand cmd-echo changed BEFORE the announcement) which the pre-fix DT-5 silently PASSed — the fixed checker's pre-announcement negative scan hard-FAILs it. Pure stdlib, no hardware, no ROS. `python tools/probes/toss_trace_synth.py --all --verify`, exit 0 = matrix clean. Traces to `temp/probes/toss_trace_synth/`. Re-run after ANY checker change. **2026-07-29**: `viol_dt7` had gone VACUOUS (DT-7 stopped being an "exactly one dynamic_target" count when it was reworked to bound the pre-position STREAM to 30 mm of the nominated point) and the matrix reported MISMATCH at HEAD; the case now injects a target that WANDERS to (95, 0, 170), which is what DT-7 actually guards. **2026-07-29 (Phase F finalize)**: added `cont_dry` and `viol_cs3_hand`. The CS-1..CS-6 matrix had only ever been shown clean on an all-CAUGHT session, while the runbook makes an all-MISSED dry trace the mandatory gate BEFORE any ball flies — an instrument validated on one side only. Adding `cont_dry` immediately surfaced two real CS-3 defects (a hand bound inherited from DT-5's idle window, and a baseline that landed on the retract ramp), both fixed in the checker. | `logbook/2026-07-25-toss-phase3-prep-trace-harness.md` (Phase 3 prep of `plans/active/single-ball-toss.md`); CS-1..CS-6 added for Phase F (`toss_continuous`) |
| `uplot_nan_gap_probe.js` | Node-driven regression probe for the GUI telemetry charts' gap semantics: extracts the SHIPPED `nanGaps` hook verbatim from `ros_ws/gui/js/telemetry-charts.js` and drives the REAL vendored `ros_ws/gui/lib/uPlot.iife.min.js` linear path builder with it, asserting NaN runs render as pen-up gaps (never false bridging lines) — 21 assertions incl. edge runs, all-NaN, single-point NaN, null+NaN merge, dashed-stroke invariance. Pins uPlot-1.6.31-specific behaviour (gap scanner honours strict `null` only; the chart store's Float64Array columns can only carry NaN). Re-run on any uPlot upgrade or `nanGaps`/series-wiring change. `node tools/probes/uplot_nan_gap_probe.js`, exit 0 = pass. | `logbook/2026-07-11-gui-leg-setpoint-echo-poscmd.md` (review HIGH #1: false bridging ramp across stale windows) |
| `fk_convergence_bag_check.py` | Offline FK-convergence verdict for a recorded session: replays every `/robot_state` encoder vector and every `/leg_setpoint_echo` commanded vector from a rosbag through the PRODUCTION `leg_lengths_to_pose` and reports, per topic, how many reconstructions raise under (a) the production default criterion — **the verdict, must be 0** — and (b) the historical absolute-only criterion (`rtol=0, stall_ceiling_mm=0`) — **must be > 0 or the verdict is vacuous**, because it counts how many vectors in that session actually reached the round-off floor; a session whose poses all happen to land under 1e-10 mm reads identically to a genuinely fixed one, so the probe reports that case as a loud `VACUOUS` banner rather than a PASS. Also reports the residual band of the historical failures, the worst residual accepted at the default, and the worst Newton iteration count (50 before the fix, 5 after). Baseline on bag `2026-07-25_15-17-48`: `/robot_state` **514/28953 (1.775 %)** and `/leg_setpoint_echo` **37/10453 (0.354 %)** fail under the historical criterion, 0 under the default; worst accepted residual 9.07e-10 mm; ~92 s per bag (cost is chunk decompression). Strictly offline and read-only — opens `.mcap` files only, no node, no socket, no hardware — and needs NO ROS2 (`mcap_ros2` decodes the schemas embedded in the bag). `--bag` / `--all` (`--match <date>`) / `--json` (→ `temp/probes/`). Exit 0 = PASS, 1 = a reconstruction still fails or a bag was unreadable. | `plans/archived/fk-convergence-tolerance.md` Phases 0–1 — the bare absolute `tol=1e-10` mm was below the reachable round-off floor at part of the workspace, and `trajectory_node`'s seed path turned that into a hard "not streaming until a valid state" refusal (26 consecutive ERRORs in 286 ms on 2026-07-24 09:08:55). The failure is spatially **clustered**, not rare: ±1 encoder dead-band around the burst pose fails **96.8 %** of the time, vs 0 % at `z=50`/`z=110` flat — which is why it bursts. Consuming test: `tests/motion/test_fk_convergence.py` (both its hardware fixtures were extracted with this probe's decode path) |
| `levelling_tilt_bag_check.py` | Offline levelling-frame (C-LEVEL-1) verdict for a recorded session: FK-reconstructs the **commanded** platform pose from every `/leg_setpoint_echo` vector and reports the commanded-tilt **plateau structure** — every run where `rx` sits still, with start time, duration and value in degrees — then, given the session's published offset (`--offset TILT_X TILT_Y`, radians, from the orchestrator's `Gravity offset published:` line), scores three numbers. **`park_rx`/`park_ry` (the longest plateau — where the platform RESTS) is the verdict**, and it is the whole fix in one number: pre-migration the platform parked at plan-frame `rx = 0` while corrected surfaces asked for `-0.7788 deg`, so PASS is `park` within `--tol` (default 0.05 deg) of `(-tilt_x, -tilt_y)`. `settle_rx` is the plateau **furthest from the park in the correction's own direction** (the catch settle — not the last, because post-migration the session ends parked at the corrected neutral; and sign-aware rather than a hard-coded `min()`, which returns the PARK for a positive-rx correction). `peak_above_park` is REPORTED and **never gated**: post-fix it equals the platform's physical peak tilt against gravity and equals `(16/81)*rate*|tdir_x|*lead` = **0.789132 deg per second of catch lead** exactly, so any fixed threshold on it is really a threshold on the lead — the retired `+3.099 deg` ABORT would have fired on a healthy system at any lead >= 3.93 s. The probe prints `implied lead` instead, which on a healthy post-fix session recovers the toss's actual catch lead (the pre-fix bag reads 2.94 s against a true 3.70 s). **Read the header docstring's CAVEAT before scoring**: commanded `rx` is NOT flat across a catch goal and the plan's original "flat to +-0.05 deg across the whole goal" criterion is unreachable — `build_catch` ramps a tilt-through-seat residual along `catch_pose[3:5]`, which with a correction loaded is the correction itself, settling the platform `0.5*0.07*0.15 = 0.005250 rad = 0.3008 deg` OFF gravity-level at ball contact. **Validated on BOTH shapes, which is the acceptance criterion for a verdict instrument.** FLAG side, the real pre-fix bag `2026-07-25_15-17-48` (toss window `--t0 165 --t1 200`, 4.5 s): `park +0.0000 deg` (the defect), `peak +2.3224` above a `0.0000` park, `settle -1.0775` against the closed form `-1.078408` — **err 0.0009 deg**, which is what confirms the through-seat attribution against hardware; 0 FK failures; VERDICT FAIL, correctly, because that bag predates the migration. ACCEPT side, `--self-check` (no bag needed, ~3 s): three sessions synthesised through the PRODUCTION `planner.build_catch` — post-fix must PASS, pre-fix must FAIL, and post-fix contaminated by a 30 s pre-`go_home` ACTIVATE hold must FAIL **with the ambiguity note**, because that hold is commanded at plan-frame `rx ~ 0` and is *correctly* uncorrected (a seed is never corrected), so on a healthy session it can win the longest-plateau vote and read exactly like the pre-fix frame. Hence `--t0` after the first `go_home` is mandatory in CHECK LVL-3. Without the ACCEPT half the instrument would have scored a working fix as FAILED at the bench. Strictly offline and read-only (opens `.mcap` only; no node, no socket, no hardware) and needs NO ROS2. `--bag` / `--root` / `--offset` / `--tol` / `--plateau-tol` / `--plateau-min-s` / `--t0` / `--t1` / `--json` (timestamped, into `temp/probes/`) / `--self-check`. Exit 0 = PASS or STRUCTURE-ONLY (no `--offset`), 1 = park outside tol or unreadable bag. | `plans/parked/levelling-frame-contract.md` Phases 1-2 (and Phase 4, which this is the verdict instrument for) — `trajectory_node` applied the correction on some pose-ingest paths and not others, so the toss's positioning `go_to_pose` parked at plan-frame `rx = 0` while the pre-tilt `catch/dynamic_target` asked for `-0.7788 deg` and `build_catch` planned a min-jerk reach between the two frames, arriving 0.7 s before release. Contract: `ros_ws/docs/levelling_frame.md`. Consuming tests: `tests/ros/test_levelling_frame.py`, `tests/motion/test_levelling.py`, `tests/motion/test_levelling_probe.py` (the two-sided self-check, guarded in CI) |
| `catch_reach_replay.py` | Offline reproduction of a recorded **catch reach** through the production `planner.build_catch`, plus the *install census* that says how many plans spanned it. Three outputs. (1) **Install census** — `move_seq`, `plan_kind`, `last_rejection`, accepted `target_feedback` and `catch/dynamic_target` inside the plan's own window, plus plan-end drift (`t + plan_time_remaining_s`). Five clauses, none sufficient alone. **The load-bearing ones are single-valued `move_seq`, single-valued `plan_kind` (a `HoldPlan` reads `hold`, which is how the emitter backstop / escalation hold / freeze-in-place become visible — they bump nothing else) and zero accepted `target_feedback`.** Plan-end drift is NOT decisive and is not described as such: `_plan_and_install_catch` derives its lead from a fixed `arrival_perf`, so a re-plan to the SAME arrival leaves the plan end untouched — the reference bag's own post-release burst runs `move_seq` 53→64 with 14 accepted feedbacks and drifts **1.02 ms**, LESS than the 1.9 ms across the genuinely zero-install window. There is likewise no status-vs-diagnostics rate advantage: one 5 Hz timer publishes both (1453 messages each over 290.6 s). A rejection only counts if it was RAISED inside the window — `last_rejection` is a lifetime latch that never clears, so gating on "non-empty" would fail every reach after one unrelated session rejection. (2) **The `peak_leg_*` verdict** — it rebuilds the plan and compares the `FeasibilityReport` peaks against what the bag published (`14.2 / 142.4 / 3950`, identical at the published precision — rebuilt `14.2401 / 142.4440 / 3949.7`; the decisive half is the `0.0 / 0.0 / 0` the preceding `go_to_pose` install wrote, which proves the field is WRITTEN). The field is **per-plan, not stale**; it repeats across *these* catch installs because a catch plan's predicted peaks are the fixed-shape 0.15 s through-seat decay's peaks **whenever the reach is small enough for that decay to dominate**. Not a general property: a reload-sized reach runs `82.5→30.1→24.7→15.2→14.0 mm/s` over leads 0.8→8.0 s, and the same bag's two reload catch installs published `23.6` / `23.8` against the toss catches' `14.2` — so predicted peaks MOVING between two catch installs is normal, not evidence of a non-catch plan in between. (3) **The replay** — `build_catch` seeded only from recorded inputs (FK seed pose, `levelling.correct_pose` of the wire target, arrival = `landing − _PRETILT_EARLY_S`), sampled and differenced against FK of `/leg_setpoint_echo`; ONE scalar is fitted (the emit→echo pipeline lag) and the zero-lag residual is reported alongside. All **seven** catch reaches in `2026-07-25_15-17-48` reproduce: 5 self-toss pre-tilts (rx rms 0.005–0.006°, max ≤ 0.039°, lag +10.5 ms) and both reload pre-tilts (rx rms 0.003°, max 0.009°, lag +9.5 ms). The headline closed form is the **amplification** `(16/81)·rate·T / \|tilt\|` = peak *unrequested* excursion ÷ requested displacement — direction-only, so it blows up as the requested tilt → 0: **3.76 for the 0.78° toss pre-tilt, 0.17 for the 11.08° reload pre-tilt**, one code path, 22× apart. That is the whole answer to "why is an 11° reach clean and a 0.78° one not". Verdict tolerance is `max(--tol-deg, --tol-frac × commanded span)` — a purely absolute bound scores the clean 11° reload as NOT-REPRODUCED on a 0.5 % error. **Validated on BOTH shapes, which is the acceptance criterion for a verdict instrument**: the FLAG case (`--toss 2`, the excursion) and the ACCEPT case (`--thrower ball_butler --toss 2`, the clean 11° reload) both read REPRODUCED / exit 0, and `--self-check` case 7 compares all **nine** mirrored rows against their production sources so a change to any of them fails the instrument loudly instead of silently re-scoring old physics. Two of those rows are the 2026-07-26 seat-rate split: `PLANNER_DEFAULT_THROUGH_SEAT_RATE_RADPS` (0.0 — the LIVE default, so raising it off zero breaks the self-check) and `THROUGH_SEAT_RATE_RADPS` (0.07 — a **capture record** of what the reference bag ran at, pinned to itself so nobody "helpfully" syncs it to the new default and makes every pre-2026-07-26 reach score NOT-REPRODUCED). **(4) The C-CATCH-1 counterfactual (added 2026-07-26).** Every scored row also rebuilds the SAME recorded inputs through the post-fix path (`build_fixed`: seat aimed at the gravity-referenced wire tilt, arrival rate left to the planner so `ros_ws/docs/catch_arrival_contract.md`'s bound applies) and prints the delta — unrequested (wrong-side) excursion, settle, segment count, predicted peaks, and whether the `2.5·|d|/T` bound binds. On the reference session the self-toss reach goes `2.3239° → 0.0000°` unrequested excursion with the settle landing dead on the target. **Updated 2026-07-26:** `_CATCH_TILT_THROUGH_RATE_RADPS` now ships at `0.0` (the builder manufactures no motion; an explicit `tilt_through_rate_radps` is still honoured verbatim), so `build_fixed` — which deliberately tracks the shipping planner — now rebuilds the reload reach with **no seat at all**: `3 → 2` segments, arrival rate `0.070000 → 0.000000 rad/s`, settle `+1.8235/−10.9330° → +1.7741/−10.6363°` (exactly the target), predicted `23.8/139.7/3873 → 29.0/37.9/170` mm/s·s²·s³. The earlier "keeps its seat, aim rotated `4.0997°`, settle moved `0.0215°`" reading was the counterfactual at the `0.07` rate and is retained in `ros_ws/docs/catch_arrival_contract.md` § Consequences, because separating the **frame** fix's effect from the **zero-rate** decision's effect is what a bench disagreement needs. REPORTED, never gated — the verdict is about reproducing what the robot DID run, which is why `build_replay` deliberately keeps rebuilding the PRE-fix plan (it passes the arrival rate explicitly, which under C-CATCH-1 means caller-requested and therefore unbounded); a probe that could not express the physics a capture contains could not reproduce it. Self-check cases 9 and 10 make that two-sided and pin the bound factor to `min_s ψ/|φ| = 5/2`. `/gravity_offset` is resolved at the INSTALL, not at end-of-bag, so a mid-session re-`level` cannot score earlier reaches with a later correction. Strictly offline and read-only (opens `.mcap` only; no node, no socket, no hardware) and needs NO ROS2. `--bag` / `--root` / `--toss` / `--thrower` / `--list` / `--offset` (defaults to the bag's `/gravity_offset`) / `--tol-deg` / `--tol-frac` / `--pre` / `--post` / `--json` / `--csv` (both timestamped, into `temp/probes/`) / `--self-check`. Exit 0 = reproduced and the census is self-consistent, 1 = not reproduced, contradictory census, or unreadable bag — **a non-reproduction is a publishable result, not a probe bug**. | `plans/parked/catch-reach-degenerate-overshoot.md` Phase 0 — a `−0.77878°` catch target produced a `+2.32°` commanded excursion in the OPPOSITE direction, settled at `−1.0784°` (1.385× the target), with a leg acc/jerk spike at release − 0.6 s. All three are one plan doing what `build_catch` specifies: the quintic acquiring its specified terminal arrival rate (`φ = −4s³+7s⁴−3s⁵`, extremum `−16/81` at `s = 2/3`), the through-seat overshoot `1 + 0.5·rate·decay/\|tilt\|`, and the 0.15 s through-seat decay segment (10× the reach's acceleration, 113× its jerk). Consuming test: `tests/motion/test_catch_reach_replay_probe.py` (the model-vs-production-planner self-check, guarded in CI). Sibling: `levelling_tilt_bag_check.py`, whose FK reconstruction this reuses |
| `possession_verdict_bag_check.py` | Replays every `/balls` message in a rosbag through the **production** possession source (`jugglebot.ball_possession.TrackerArrivalSource`, constructed exactly as `reload_coordinator_node` constructs it) and prints, per destination-tagged track that reached `CAUGHT`, the estimate, the ARRIVAL error, the REPORT-only plane drop and the verdict the coordinator would mint. Turns a capture into the two numbers the runbook's `POSS-1` row needs — how many CAUGHT verdicts the gate minted vs how many catches the operator counted by eye — without re-running the sitting. Also **owns the committed test fixtures**: `--emit-fixtures` regenerates `tests/ros/possession_fixtures.py` (all 35 tagged-track CAUGHT estimates of 2026-07-27, 17 self-toss + 18 reload) so the contract's tests are scored on measured ground truth rather than synthetic points. `--self-check` is the two-sided instrument acceptance and needs **no bag**: ACCEPT side (all 17 real catches confirmed), REFUSE side (0/18 corrupt tracks confirmed), the >10x population separation, the bound sitting strictly between them, the retention pin, the 2026-07-23 second-session negative, and **two drift guards mirroring the node's own tolerance and catch point** — 8/8, exit 0 (run 2026-07-28). The reference point defaults to the ACTIVE catch point because a bag does not carry the live goal; `--ref X Y Z` scores a displaced-catch session. Strictly offline and read-only (opens `.mcap` only), `mcap_ros2` imported lazily so the scoring half runs without the bag reader. Outputs a timestamped CSV to `temp/probes/`. | `the anomaly-fixes orchestration prompt (completed, deleted 2026-08-15)` § Follow-on programme, item **B** (cross-referenced from `plans/parked/catch-reach-degenerate-overshoot.md`, whose `CCATCH-2t` row this un-blocks) — the CAUGHT gate was **structurally always False**: it ANDed `\|z − catch_z\| <= 150 mm` onto a quantity that is a dead-reckoned free-fall extrapolation (the tracker declares CAUGHT *because the marker vanished*), so all 17 self-toss catches of 2026-07-27 read `z` error **305–1007 mm** against `xy` error **0.30–3.88 mm** and every one scored MISSED. Contract: `ros_ws/docs/ball_possession_contract.md` (C-POSSESS-1). Consuming test: `tests/ros/test_ball_possession.py`; consuming runbook row: `tests/hardware/session_anomaly_fixes.md` § SECTION POSS |
| `hand_sensor_verdict_replay.py` | Replays a session's **hand ball sensor** through the production possession verdict: reads `/hand_telemetry` + `/throw_announcements` out of a rosbag, feeds every sample into the shipped `jugglebot.ball_possession.HandBallSensorSource` (same `JB_BD_*` windows the node constructs it with) and prints, per announced throw, the sensor's label resolved with **full hindsight** (both windows closed) — `CAUGHT` / `BOUNCE` (arrived then left) / `MISSED` (window closed with no empty→held edge) / `UNKNOWN` (the sensor could not look, so the merge falls back to the tracker). That is the verdict the coordinator would have minted for every label **except `BOUNCE`**: the coordinator answers on the first confirmed tick, where retention is still UNKNOWN and is contract-forbidden from vetoing, so a `BOUNCE` row is a live `CAUGHT` plus the C-POSSESS-1 § 7 reporting residual — which is exactly why it is worth counting offline, being the one count no live log carries — with the **catch-event time** (`arrival edge − predicted landing`), plus the whole-bag sensor ledger (sample count, `ball_held_valid` fraction, transition counts, held-segment durations, sub-second seat-then-leave events). This is the instrument that SIZED the shipped windows: over the three 2026-08-10 retest bags (`16-04-26` / `16-13-48` / `16-30-44`; **203,922 samples, 100 % `ball_held_valid`**, 81 announcements) it labels **35 CAUGHT / 46 MISSED / 0 BOUNCE / 0 UNKNOWN**, with the catch band **+137…+798 ms** (median +399) against a nearest non-catch edge at **+3194 ms** — a ~4x separation, which is what `arrival_lead_s 0.20` / `arrival_window_s 1.50` sit inside. Its transition counts reconcile **exactly** with an independent hand count on all three bags (5/6, 24/23, 38/39 rises/falls). `--merge-tracker {refuse,confirm}` prints the MERGED counterfactual, making the restored-verdict count visible (under the measured pessimistic tracker the pre-2026-08-10 machine mints **zero** catches on these bags; the sensor restores all 35). `--self-check` is the two-sided acceptance and needs **no bag** — 10/10, exit 0 (run 2026-08-10): both ends of the measured catch band ACCEPT, the +3194 ms re-seat REFUSES, the 0.999 s seat-then-leave reads BOUNCE while a hold past the window reads CAUGHT, a **1.55 s hold at the shipped 1.50 s window reads CAUGHT** (the absolute widen guard — the relative case above it follows the constant and can only catch a narrowing), a blind window reads UNKNOWN, a ball that never left raises no edge, and the fallback rule is checked in both directions. `--emit-fixture --t0 --t1` owns the committed cut `tools/probes/data/hand_sensor_replay_fixture.json` (16,444 samples of `2026-08-10_16-30-44` over `[140, 305) s`, carrying the band-maximum +798 ms catch, five further catches, two misses and all three seat-then-leave events) and stores its own regeneration recipe inside the file. Strictly offline and read-only (opens `.mcap` only; no node, no socket, no hardware), needs **no ROS 2**, `mcap_ros2` imported lazily so the scoring half runs without a bag. `--json` writes a timestamped report to `temp/probes/`. | `logbook/2026-08-10-sensor-truth-possession.md` — the phase that made the hand sensor the PRIMARY possession source and flipped `toss_require_ball_evidence` to `true`. Contract: `ros_ws/docs/ball_possession_contract.md` (C-POSSESS-1 §§ 3.2, 3.3). Consuming test: `tests/ros/test_hand_sensor_replay.py`. Sibling: `possession_verdict_bag_check.py`, which does the same job for the TRACKER half of the merged verdict |
| `toss_record_miner.py` | **Turns a sitting into the per-toss corpus** — the MEASUREMENT half of `toss_record/1`. Reads one bag and emits one record per self-toss (`thrower_name == target_id == the robot`, which is what separates our tosses from the BallButler reload throws riding the same topic into the same cup): the sensor block and its label, the mocap arrival offset, the provenance the bag witnesses (`uptime_ms_at_release`, tilt-map identity, bridge/platform FW), and — when the coordinator was publishing them — the `/toss/record` DECLARATIONS joined on `announce_throw_time_ros` within 5 ms. The PLANT block (`hand_stroke_timeline`) is **deliberately not wired in 2a**: § 3.4 requires the row builder to be imported rather than re-implemented, and mapping its session-relative clock onto the record's ROS instants without a reference to validate against risks writing plausible WRONG plant numbers into a corpus — the exact failure class the phase exists to prevent. Field mapping recorded in the plan's § 10. **The inversion is the point (plan § 3.3): the miner must produce the WHOLE record alone, in degraded form, and a declaration only upgrades fields from mined to declared.** The reference bag proves it — `2026-08-10_16-30-44` predates every line of this phase and carries no `/toss/record`, no `/rosout`, no `/catch/*` and no action feedback, and the miner still produces 31 full rows from it, every one `mined-only`. **Nothing is re-implemented**: the label and the sensor edges come from the installed `jugglebot.toss_record.label_from_sensor` the node imports too, the announced-ball latch from `toss_record.latch_announced_ball` (extracted verbatim from `reload_coordinator_node`), the arrival estimator from `ball_arrival_offset.fit_plane_crossing_full` — so the live and offline definitions of "caught" cannot drift, which is the whole D11 argument. Reads the bag in **two passes** because a sitting carries ~137k `/mocap_data` and ~93k `/balls` messages and the miner only ever looks at ±1.2 s around each landing (the single-pass first cut did not finish in 7 minutes). Its default fit plane is the **8a cup plane** (809.08 mm = `GEOM_INITIAL_HEIGHT + DEFAULT_ACTIVE_Z + HAND_CATCH_OFFSET`), not `ball_arrival_offset`'s 1000 mm, and a declared `catch_point_global_mm[2]` more than 5 mm from the plane actually fitted is reported LOUDLY per row — for a vertical toss `v_xy ≈ 0` so a wrong plane looks perfect right up until a displaced throw. `--self-check` is the two-sided no-bag acceptance (**22/22, exit 0, run 2026-08-10**): both ends of the measured catch band ACCEPT, an arrival past the shipped 1.5 s window REFUSES (absolute, so a widening fails), the measured departure band labels, a missing departure edge reads `NO_RELEASE`, retention is guarded on both sides, a blind window reads `UNKNOWN` and never collapses, the RAW vs DEBOUNCED departure lag is asserted at the measured ~240 ms, a bounce-out is not mistaken for a departure, and the schema round-trip + join rules are checked including that a D+M conflict is RECORDED rather than resolved. `--emit-fixture` owns the committed census `tests/ros/toss_record_fixtures.py` (the `possession_fixtures.py` shape — an importable `.py` of measured constants, diffable in review, carrying its own regeneration recipe). Strictly offline and read-only (opens `.mcap` only), needs no ROS 2, `mcap` imported lazily so the scoring half runs without a bag; `--jsonl` writes a timestamped corpus + `_meta.json` to `temp/probes/`. | `logbook/2026-08-10-toss-selftuning-build.md` § Phase 2a. Plan: `plans/active/toss-selftuning.md` §§ 3.3, 3.4, D10–D13. Consuming test: `tests/ros/test_toss_record_miner.py` (plus `tests/motion/test_toss_record.py` for the pure module). Siblings: `hand_sensor_verdict_replay.py` (the same sensor stream through the LIVE possession verdict) and `ball_arrival_offset.py` (the arrival estimator this imports) |
| `ball_arrival_offset.py` | **Where each thrown ball actually ARRIVED.** For every `/throw_announcements` entry in a rosbag it reads the raw mocap marker stream (`/mocap_data`, unlabelled markers only — the ball carries no rigid body) and reports the ball's `(x, y)` at a horizontal plane (`--plane`, default **1000 mm**) as a least-squares fit of `x(z)`/`y(z)` over the DESCENDING samples in `[plane, plane+band]`, plus `n` and the fitted `z` span. Exists because the 2026-07-27 sitting could not decide its own headline question — three bounce-outs off a stationary 10.8° rim against a measured 26–39 mm BB warm-up drift in arrival `x` — and the A/B that settles it compares outcomes **at matched arrival offsets**, a number the session did not produce (the sitting's own gap list: *"Nothing asks for the ball's measured arrival point... it appears in no log line"*). **Three design points are load-bearing, not stylistic**: the fit is cut at the first sub-plane sample so a bounce-out's post-contact excursion (up to ~70 mm, and in a different direction on different balls) cannot bias a track by an amount that *correlates with the outcome being scored*; it is a fit rather than a nearest-sample read because mocap drops the ball for tens of ms exactly when a track is worst (ball 11 was lost at `z = 1111` and never re-acquired), and `n < 5` is flagged `SPARSE` rather than silently trusted; and a lateral gate removes the room's fixed reflector at `(−537.1, 412.9, 1163.7)`, which sits squarely in the arrival band. `--group NAME=lo-hi` (repeatable) + `--discard N` produce the per-block mean/sd and the matched-arrival check the SEAT-EXP decision rule reads — expressed against the **standard error of the difference of means**, not a flat threshold, because at the measured 9–11 mm sd and `n = 12`/arm a fixed 5 mm gate fires on identically-aimed blocks 17–28 % of the time. It also prints a `PLATEAU (last 8 scored)` mean, which is the statistic `SEAT-EXP-2.2` gates on: the all-scored mean still carries the warm-up excursion and is biased ~10 mm toward passing (`−28.8` vs `−38.9` on the reference capture). **Note `--group` selects on the ANNOUNCEMENT index**, which counts throws that produced no ball (4 of 18 on the reference capture), so the runbook requires one bag per arm rather than counted ranges. **Validated two-sided against the sitting (2026-07-28)**: at `--plane 1050` it reproduces all three published bounce-out entry points — ball 6 `(−6.6, 16.2)` vs `(−7.2, 16.1)`, ball 9 `(−18.5, 3.9)` vs `(−18.8, 3.6)`, ball 11 `(−21.0, 7.6)` vs `(−21.5, 7.4)`, worst error **0.6 mm** — and the population mean `−35.11 mm` vs the published `−34.9`. **Scope that 0.6 mm to what it measures** (bounce-out entry points, at `--plane 1050`) — it is agreement with one published quantity, not this estimator's accuracy: against the sitting's published `x@z1000` series the probe differs by up to **12.7 mm** (attempt 2, `−21.6` published vs `−8.9` here) and by 3.0–3.9 mm on three others, because two defensible estimators on sparse tracks disagree by far more than 0.6 mm. That disagreement is between-estimators and systematic, so it cancels within a sitting (this probe is deterministic and scores both arms) — which is exactly why the decision rule is within-sitting and `SEAT-EXP-6` is expressed against its own standard error rather than a flat millimetre gate. **One number deliberately does NOT reproduce and is recorded rather than buried**: the sitting's *"attempt 3 dropped at −10.7, attempt 4 caught at −14.5, 3.8 mm apart"* reads **−11.507 / −11.525 here, 0.02 mm apart** — the old pair came from an ad-hoc fit whose window is unrecorded and attempt 3 is the capture's sparsest track (3 samples, all above `z = 1100`). The qualitative reading is unchanged and sharper, but it is why **no absolute number from the hand analysis is a gate** and the SEAT-EXP rule is within-sitting with both arms scored by this probe. The floor census (new resting clusters after each attempt) is **REPORT-only** and its measured failure mode is documented in the header: it over-counts a rolling ball and under-counts two balls resting within `--floor-radius` (attempt 3's rested 111 mm from attempt 2's, so it merges and reads 0). Deliberately NOT tuned to make that capture come out right. `--self-check` is the no-bag acceptance: **8 cases**, two-sided (recovers a known crossing to 0.084 mm; proves the post-contact cut holds — as a real MUTATION check, grafting the bounce-out back INSIDE the fit band and requiring the naive fit that kept those samples to move (481 mm here), after a 2026-07-28 review found the original graft sat at `z = 700`, i.e. *below* the plane where the band filter drops it either way, so a probe with the cut deleted scored `8/8 PASS, exit 0`; re-verified both ways — mutant now `FAIL (1 case)` exit 1, unmutated `PASS` exit 0; extrapolates a sparse high-only track; REFUSES a track that never enters the band; sample-sd `n−1`; a one-sample block reports NO sd rather than 0.0, which would make the instrument's own data loss decisive; and **two cases on the sitting's real numbers** — the burn-in trend gate must REFUSE the measured warm-up (`−7.13 mm/throw`) and ACCEPT the measured plateau (`−1.75`), while the criterion the sitting first proposed (*successive change < 10 mm*) is pinned as one that FIRES ON A GOOD PLATEAU (largest successive change there is 15.6 mm), so it cannot be re-adopted from the prose). Strictly offline and read-only, `mcap_ros2` imported lazily, timestamped CSV to `temp/probes/`. | `plans/parked/catch-reach-degenerate-overshoot.md` follow-on phase C — the seat rate became the YAML key `trajectory_op.catch_seat_rate_radps` so the A/B is a config toggle; this probe is what makes "matched arrival offsets" a measurement. Evidence base: `logbook/2026-07-28-anomaly-fixes-validation-sitting.md`. Consuming test: `tests/motion/test_ball_arrival_offset_probe.py`; consuming runbook rows: `tests/hardware/session_anomaly_fixes.md` SECTION SEAT-EXP (`SEAT-EXP-4`, `SEAT-EXP-6`, `SEAT-EXP-7.1`) |
| `hand_stroke_timeline.py` | Per-throw hand stroke timeline from a `toss_trace_*.jsonl` **or** a session rosbag: reconstructs the commanded-vs-measured hand profile around every throw and emits stroke start, the modelled release instant (two independent estimates — see below), the observed crossing of the modelled release position `x2`, the `pos_cmd` truncation instant, **every from-rest quintic seed** in the window, the instant `pos_cmd` reaches the stroke end `x3` and how long it held there (`stroke_end`) plus the first command that took it back below (`post_stroke_cmd`), the coasting peak, the pos-loop pull-back velocity, the dip bottom and depth in rev / mm / % of effective stroke, **how far below the stroke end `x3` the hand ended up** (`dip_below_x3` — the gated row), the recovery instant, the first downward command of any kind (`first_neg_cmd`) and the armed catch-descent onset. A throw whose `pos_cmd` follows the planned decel ramp to `x3` reports `trunc=-`, `seeds=0` and `dip_below_x3 <= 0.100` rev, which is the FIXED shape — so the same command is both the pre-fix characterisation and the post-fix verdict. **The gated dip row is `dip_below_x3`, deliberately not `dip`**: `dip` is peak-minus-bottom and so is non-zero on any healthy capture that overshoots and settles (0.6 mm on the clean synthetic, 20.2 mm at a 10.60 rev coast — the size of the smallest pre-fix defect), whereas the pre-fix position loop yanks the hand **0.339–1.748 rev = 10.7–55.3 mm below `x3`** and a healthy stroke never goes under it at all. `pullback` is bounded (ABORT below −5.0 rev/s, pre-fix −17.9 to −42.4) rather than required non-negative, for the same reason: a healthy settle from the coasting peak reads −0.31 rev/s at 0.02 rev of overshoot and −10.03 at 10.60 rev. `catch_desc` likewise requires the commanded position to actually reach `x5 + 0.33` rev = 6.457 rev = `x3 − 3.502` rev (velocity-independent; the "0.5 rev below `x3`" premise this line carried died with Phase 4, when an honoured brake was shown to dive up to 3.126 rev), so a `makeSmoothMove` **brake** — which plan Phase 4 step 3 charters for the at-target-but-moving case, i.e. a shape only POST-fix captures have — is reported as `first_neg_cmd` instead of collapsing the dip/peak measurement window to ~130 ms of the ~600 ms it must cover. **The two release estimates are the point of the instrument**: `rel_ann` comes only from `/throw_announcements` (the host's *intended* release), `rel_fit` least-squares time-shifts the closed-form `Trajectory.h` stroke onto the observed `pos_cmd` ascent (the *physical* release), and their difference measures the dispatch-path latency — `teensy_bridge_node.py:3652` stamps the absolute event from the BRIDGE's `time.time()` plus a delay the caller computed from ITS own clock read, so every microsecond of service transit lands the stroke late. Worst measured **+23.4 ms** (7 self-tosses: +12.8 to +21.9 ms by bag clock, median +19.0; the two tosses also captured to a jsonl trace read 1.5–1.7 ms higher through that path, and neither source is calibrated better than the bias below — so the margin a host-side stroke-busy window has to cover is the larger, +23.4, which Phase 1's 40 ms is 1.7× of); the estimate is biased LATE by ~5 ms because `hand_telemetry` publishes a ~100 Hz snapshot of a 500 Hz frame stream (safe sign for sizing a window, wrong sign for quoting a calibrated latency — the docstring says so). The seed detector is the other half: `makeSmoothMove` starts at `start_rev = current_hand_position` exactly, so each seed is cross-checked against the live `pos_meas` of the preceding samples (`vs_meas` reads **0.0000 rev** on every seed measured) and the seed COUNT independently reproduces the `Arming hand catch` count from `/rosout` on **7/7** tosses that had a stroke (the eighth, `ABORTED_NO_RELEASE`, reads log 1 / seeds 0 — the dispatch was eaten before any stroke) — a source with no `/rosout` prints `arms=?`, never `0`, so "channel absent" and "genuinely none" never collapse into one glyph — and, equally, `/rosout` presence is DETECTED on both input paths rather than assumed from the format, so an available channel is never discarded (the three 2026-07-25 evidence bags happen to lack it; the runbook's § Recording command records it). `--gate` asserts 25 rows against `temp/logs/toss_trace_2026-07-25_15-24-25.jsonl` — 20 Context-table instants/positions (each reproduced to ≤0.4 ms against a 20 ms tolerance = two hand-telemetry new-value periods, measured median 10.0–10.2 ms) plus `x2_rev`/`x3_rev`/`shift_ms`/`n_seeds`/`dip_below_x3_rev`, which no instant row pins (a 5 % error in `x2` moves `x2_cross` by ~2.4 ms, and a broken release fit or seed detector moves no instant at all) — and then a SECOND branch against synthetic post-fix captures (`clean`, `overshoot`, `short-flight`, `braking-prelude`, and — added by `5369fc2` — `deep-brake`, the separator case at the excursion clamp's reach, which pins that a 0.5-rev-deep brake is still *reported* as `first_neg_cmd` rather than mistaken for the armed catch descent), because the fixed shape must be exercisable without a capture: the evidence base does now carry it (69 of 71 analysable throws read `trunc = -` after the 2026-07-27 sitting), but those recordings live under gitignored `temp/` and on a bag drive, and none of them carries the `deep-brake`, `late-trunc` or arm-hold variants the gate pins. Three more cases were added 2026-08-18 with the truncation-criterion fix — `arm-prelude+28ms` / `arm-prelude+62ms` (the gated catch arm's own prelude, at two holds either side of the retired 50 ms `_TRUNC_SCAN_MARGIN_S` wall) and `late-trunc` (a real clobber 0.30 rev short of `x3`, the blind spot a narrowed wall would have bought); `band-floor` followed on 2026-08-20. **The truncation scan is bounded on the commanded profile REACHING `x3`, not on a clock**: Phase 1's arm gate dispatches the catch arm the moment the stroke completes, so the gate WORKING lands a from-rest prelude just past `x3` seeded at a live position sagged 0.06–0.17 rev under it — which satisfied both halves of the old predicate whenever it fell inside 50 ms; on the 2026-07-27 sitting 6 tosses reported a truncation across its six bags and 3 across its three traces, and `2026-07-27_15-39-3x/5x`, recorded both ways, read 4 (bag) against 3 (trace) on the SAME physical tosses (counts are per RECORDING; two sessions appear twice in the evidence set) — all adjudicated PASS by hand. Measured over the whole evidence base (six 2026-07-27 bags + six traces, 71 throws): every arm prelude lands +30.6 to +128.3 ms after the command reached `x3`, both real pre-fix truncations at −208.1 / −299.0 ms — sign, not size. The prelude is now REPORTED as `post_stroke_cmd` rather than scored as a truncation, so nothing went invisible. **The OTHER half of the predicate went profile-relative on 2026-08-20, for the same root cause**: "collapse" was an absolute `_COLLAPSE_VEL_REV_S = 10` rev/s judging a ramp whose velocity scales with the throw, so at the slow end of the band the ramp fell under it while still short of `x3` and the probe reported a truncation on a clean stroke — 1.94 ms of exposure at the C-HAND-3 admission floor (2.440 m/s), **20 of 100 sampling phases firing**, 13 at 2.550, 6 at 2.6971, 2 at 2.800, 0 at 2.845 and above. (The envelope floor had just moved DOWN from 2.6971 to 2.440 m/s with C-HAND-3, tripling the exposure that had been characterised days earlier — a literal would have gone stale in the safe-looking direction.) The threshold is now `_collapse_floor_rps(model)` = the modelled stroke's own commanded velocity at `x3 − _X3_SHORT_REV`, closed form `sqrt(-2*throwD*delta)`, cross-checked against a bisection on `pos_rev` to 1e-3 rev/s over v in [0.3, 7.0]: 8.58 rev/s at the admission floor, 15.31 at the ceiling. Because the firmware's decel segment is constant deceleration the commanded velocity falls monotonically with commanded position, so an intact stroke short of `x3` is **never** under the floor — the self-trigger window is empty at every speed rather than narrow (0/100 phases at each speed above, 0 over 960 clean captures spanning 0.70–7.00 m/s), while detection is untouched (the two real truncations freeze at 0.010 rev/s against a 13.82 rev/s floor, a **1382×** margin, and the evidence base still reads 2 truncated / 69 clean). No constant was added — `delta` is the instrument's already-declared `_X3_SHORT_REV` read through the shipped stroke model. Pinned by the `--gate` `band-floor` case, which sweeps the SAMPLING PHASE (the free parameter the old defect turned on) and prints the C-HAND-3 admission speed it tested against. `--gate` also runs in the pytest suite (`tests/motion/test_hand_stroke_timeline_probe.py`) after it was found RED on the shipped tree — `headroom_to_limit_rev` was still keyed to the retired 11.1 rev anchor, and nothing tested it. That branch is the two-sided half of the acceptance criterion and it has now caught five instrument defects that would each have scored a working fix as FAILED at the bench: an unbounded truncation scan reporting the armed catch DESCENT as a mid-stroke truncation (while measuring `peak` at the descent onset instead of the coasting peak, so the end-stop row read PASS with the hand 0.5 rev from the limit); a `catch_desc` predicate that any downward `makeSmoothMove` brake satisfied (`braking-prelude` case — mutation-verified: the old predicate puts `catch_desc` 567 ms early, collapsing the window to ~130 ms so `peak` reads 10.1298 rev against a real 10.1588); a dip measure that was non-zero on healthy captures (`dip_below_x3` vs `dip` — mutation-verified on the `overshoot` and `braking-prelude` cases); a truncation criterion that fired on the gated catch arm's own prelude (`arm-prelude` cases — mutation-verified: reverting the stroke-completion bound fails the +28 ms case and passes the +62 ms one, which is exactly the luck-dependence the 2026-07-27 sitting hit); and an absolute collapse threshold that fired on the decel ramp itself at slow throws (`band-floor` case — mutation-verified five ways, including a floor that is computed but does not SCALE, which is the subtle form of the same defect). The `short-flight` case (a 0.55 s flight — the band floor when it was written; C-HAND-3 moved it to 0.4949 s, and `band-floor` tests the live value) is the only one in which the `catch_desc` cap on the dip window is load-bearing — without it a clean short-flight capture reports 3.83 rev of dip. The reference trace lives under gitignored `temp/`, so the gate falls back to the committed cut at `data/hand_stroke_timeline_gate_ref.jsonl`, which `--emit-gate-fixture <out> --trace <full>` regenerates byte-identically (25/25 either way, `arms=1` from the 5 `/rosout` rows the cut keeps). `--all` reports and skips an unreadable input rather than aborting the sweep — a session killed mid-write leaves a truncated `.mcap` whose reader raises `RecordLengthLimitExceeded`, and date-sorted discovery meant one such bag (`2026-06-08_22-25-46`) used to kill the run before it reached any 2026-07-25 session. `--preview` prints the raw per-sample window rather than opening a figure (headless Jetson; `tools/README.md`'s matplotlib `--preview` convention is for the operator-facing hardware harnesses). Strictly offline and read-only; needs NO ROS2 for either input path. `--trace` / `--bag` / `--all` (`--match <date>`) / `--json` (→ a **timestamped** file under `temp/probes/hand_stroke_timeline/`, like the two sibling bag probes, so successive `CHECK HAND-n` analyses in one sitting accumulate rather than overwrite) / `--gate` / `--emit-gate-fixture`. | `plans/active/hand-command-continuity.md` Phases 0–5 — any kind-0/1/2 HAND_TRAJ_CMD makes `Teensy_code_platform.ino:539` clear the packed queue and re-prelude from the live encoder position with a `v = a = 0` quintic, so a hand-catch arm landing inside the throw's 65 ms decel ramp replaces the stroke with a rest-to-rest ramp computed from a position the hand is passing at ~120 rev/s. Baseline over 7 self-tosses: dip **20.3–64.5 mm** (6.4–20.5 % of stroke; the plan's Context table quotes the 43.0 mm median case, not the worst), coasting peak **10.165–10.325 rev** i.e. as little as **0.475 rev = 15.0 mm** of headroom to the hard stop (read "0.775 rev = 24.5 mm to 11.1" until the 2026-08-18 correction to the measured **10.8 rev**), truncation position spread over **1.586 rev = 50 mm**. Consuming tests (Phases 1–2): `tests/motion/test_hand_stroke.py`, `tests/ros/test_catch_coordinator_node.py`. **The closed-form model is no longer local to this probe**: Phase 1 moved `StrokeModel`/`smooth_move_duration_s` into `jugglebot/motion/trajectory/hand_stroke.py` and the probe imports them back, so the instrument that scores the bench capture and the suppression window that ships are the same code. A pinned duplicate was rejected — a pin only catches the quantities it happens to assert, and a `t_dec` divergence would make Phase 5 score the fix against a different model than the one that shipped. |
| `hand_decel_authority.py` | Characterises the hand's POST-RELEASE deceleration from a `toss_trace_*.jsonl` and answers the one question that decides the shape of a fix: is the plant **AUTHORITY-limited** (achieved decel saturates ⇒ steepening the commanded ramp changes nothing) or **TRACKING-limited** (achieved follows commanded with a shortfall ⇒ more commanded braking torque buys margin directly)? Per self-toss it emits the commanded decel and `t_dec` re-derived from the SHIPPED firmware header (`Teensy_code_platform/hardware_config.h`, namespace `TeensyTraj` — parsed, not copied), the measured `peak`, `over_x3`, the implied tracking fraction `eta = d_dec/(d_dec + over_x3)`, max \|iq\| over the decel ramp (anchored on the COMMANDED profile, with the count of genuinely fresh `iq` samples in that window) and over the arrest, the residual velocity feedforward standing after the stroke, and the settled `pos_meas - pos_cmd`; per tier it adds the decel-side reflected-inertia LOWER BOUND `(tau_FF + tau_grav)/(2*pi*a_achieved)` and the authority table it implies at `hand_curr_limit_a`. On the 2026-07-27 sitting it measured `eta` = **0.982 / 0.985 / 0.921 / 0.799** at 2.74 / 3.44 / 3.97 / 4.86 m/s — i.e. the achieved deceleration keeps GROWING (911 → 2330 rev/s²) with no ceiling in sight, and the whole-session max \|iq\| is **25.67 A against a 50 A limit** — so the plant is tracking-limited, not current-saturated. But the same table shows the commanded decel at the shipped `FLIGHT_TIME_MAX_S = 1.10 s` is already **83-86 % of the axis's torque-limited ceiling**, which is why steepening was rejected and the feedforward corrected instead. **The caveat is load-bearing and is printed by the tool**: `hand_telemetry` is a ~100 Hz snapshot of a 500 Hz stream and `iq_meas` has a median repeat-run of **4 samples** (mean 7.8, max 918) ⇒ effective refresh ~13-25 Hz, so every current is a LOWER bound and **the gated bench rows must rest on the KINEMATIC columns, not the current ones** — which is why the per-tier inertia bound it prints uses no `iq` measurement at all (binding value **1.0126e-5 kg·m²**; the regression identification 1.050e-5 lives in the contract). It also refuted a hypothesis in passing: the settled `pos_meas - pos_cmd` offset (per tier: **+0.067** at 2.74 m/s, **+0.043…+0.066** at 3.44, **+0.019…+0.041** at 3.97, **−0.004…+0.007** at 4.86 — it SHRINKS with speed) is NOT the residual velocity feedforward, whose median through the same windows is 0.00-0.02 rev/s. Strictly offline and read-only. `--self-check` (two-sided synthetic gate: a PRE-fix capture must score FLAG and a POST-fix one ACCEPT, through the same `analyse()` the bench uses — bench row INST-6) / `--trace` (one or more) / `--robot` / `--curr-limit-a` / `--json` (→ timestamped file under `temp/probes/`). | `logbook/2026-07-28-anomaly-fixes-validation-sitting.md` (§ Discussion → *the hand's end-stop margin above 0.78 m*, and the 2026-07-28 amendment: the hand made **light physical contact with its end stop**). Underpins contract **C-HAND-2** (`ros_ws/docs/hand_decel_feedforward.md`) and its tests `tests/sim/test_hand_throw_decel_ff.py` + `tests/firmware/test_hand_throw_decel_xref.py`. Re-run by `tests/hardware/session_anomaly_fixes.md` § CHECK HAND-7 to score the post-flash capture against these pre-fix numbers. |
| `displaced_reach_frontier.py` | **How far a displaced self-toss can actually reach, per the production planner.** Sweeps the Tier-8b deferred A→B catch reach across 8 directions × 7 flight times × 8 radii by building the REAL chain (`toss_release.compute_release_state_tilted` for the swing-compensated pre-tilt seed at A and the aim, the receive tilt from the ARRIVAL velocity, the swing-compensated catch centroid at B, then `planner.build_catch` with the flight as the lead) and reporting accepted-direction counts against the closed-form quintic bound the FSM gates on. Also answers three sibling questions in one run: which off-centre throw sites can reach the cap inside the ±150 box, whether the DEGENERATE `B == A` case plans and is BITWISE the Tier-8a vertical toss (the operator's "8b subsumes 8a" expectation, never flown on hardware), and where `MAX_TILT_DEG` binds. Pure in-process planning — no MuJoCo, no bag, seconds not minutes; `--json` writes a timestamped result to `temp/probes/`. | `plans/active/single-ball-toss.md` Phase E and `the anomaly-fixes orchestration prompt (completed, deleted 2026-08-15)` § Follow-on programme item **E**. The old 70 mm displacement cap was justified as *the 80 mm reach envelope ∩ the Rung-2a clean box*; contract **C-REACH-1** (`ros_ws/docs/catch_reach_envelope.md`) removed the envelope half, so the cap needed its own evidence. Measured 2026-07-29: the real all-8-directions frontier is **125 mm at T = 0.55 s, 175 mm at 0.60, ~225 mm from 0.70 up**, so the closed-form bound is CONSERVATIVE below T ≈ 0.75 s and OPTIMISTIC above it — which is exactly why `toss_max_displacement_mm` (150.0) must not be relaxed to lean on it. Underpins `jugglebot_operational.toss_max_displacement_mm` and its YAML comment; `toss_sequencer.TOSS_MAX_DISPLACEMENT_MM` / `reach_displacement_limit_mm` docstrings; `tests/ros/test_toss_sequencer.py::test_displacement_rejected` and `::test_reach_displacement_limit_closed_form`; `ThrowTiltInfeasible`'s "contract, not a live constraint" claim; `tests/hardware/session_anomaly_fixes.md` § SECTION DISP. |
| `link_status_health_scan.py` | **"Did behaviour X change across a can-bridge flash?"** — the generic form. Reads every `/link_status` message out of one or more rosbag sessions and reports, per session, the span, the can-bridge `uptime_ms` span, and the **value duty cycle of each watched KeyValue row** (`bus1_health[OK=480(58%),WARN=354(42%)]`), plus which *marker* rows were present at all — a KeyValue that exists only in a newer firmware / ROS build is itself a deployment fingerprint (`hand_ball_sensor` is present in the 2026-07-29 22:37 bag and in no earlier one, which is what proves FW4 ran). `/link_status` is the bridge's whole observable state surface and almost every row on it is a low-cardinality enum, so a large class of "did the flash change something?" questions reduces to a duty-cycle delta on one row — the reason this is a committed harness rather than a one-off: **every** future can-bridge flash asks it again. The regression that motivated it is invisible in any single message: it is a 42.4 % OK↔WARN duty over a whole session. Default watchlist covers all three bus-naming eras (`bus*_health` / `can*_health` / `jugglebot_health`) and skips absent rows; `--keys` / `--markers` retarget it at any other row. Session discovery filters the rosbags root to **date-prefixed** dirs before sorting — a plain reverse-sort returns the `cone_test_*` and `Old naming scheme` dirs as "the newest sessions". Strictly offline and read-only (opens `.mcap` only; no node, no socket, no hardware) and needs NO ROS2 (`mcap_ros2` decodes the embedded schemas); needs the project venv. Degrades per-bag on an unreadable file and continues the sweep. `bags…` / `--root` / `--sessions N` / `--match` / `--keys` / `--markers` / `--top` / `--list-keys` / `--out` (timestamped JSON into `temp/probes/`). Exit 0 = all scanned, 1 = a bag was unreadable or carried no `/link_status`. | `logbook/2026-07-29-can3-bus-health-flap-hand-sensor-poller.md` — the operator flashed can-bridge FW_VERSION 4 (hand ball-sensor GPIO poller, `7dc347f` + `6cc38f7` + `73d70c6`) on 2026-07-29 and the sitting that followed flapped CAN3 bus health at a 42.4 % duty. Motivating bag: `/home/jetson/Desktop/rosbags/2026-07-29_22-37-06/`. Plan: `plans/archived/hand-ball-sensor.md` Phases 3–5. Runbook: `tests/hardware/session_hand_ball_sensor.md` § P5 (the on-board serial poller-ON/OFF A/B this is the offline counterpart to). Sibling: `archived/link_status_flash_control.py` |

### Live observers

| Probe | Purpose | Motivating logbook entry |
|-------|---------|--------------------------|
| `profile_session.py` | Records `tegrastats` (CPU/EMC/thermal/power), `pidstat` (per-process CPU / context switches / IO wait), `mpstat` (per-core utilisation), optional `py-spy` flame graphs per process name (`--py-spy-targets run_mpc.py,can_node`), optional `candump` CAN trace (`--candump can0`), plus a pre-flight snapshot (nvpmodel / governors / max freqs / ptrace_scope). Auto-analyses into a `report.md` that flags CPU saturation, DVFS throttling, thermal warnings, EMC saturation, memory pressure, top processes, CAN frame rate / top arbitration IDs, and cross-references can_node CPU% against CAN traffic to estimate per-frame dispatch cost. Subcommands: `start` / `stop` / `record` (Ctrl-C-able blocking mode) / `analyze`. | `logbook/2026-05-22-mpc-compute-bound-jetson-profiling.md` — the investigation that built this harness and used it to show the MPC is compute-bound on the Jetson Orin Nano |
| `traj_stream_probe.py` | READ-ONLY live observer for the MVP trajectory generator's 40 Hz :5557 knot stream. Opens a ZMQ SUB (connect, never bind; never sends on any wire → cannot command a motor; coexists with the live `trajectory_node`) on `mpccmd`, decodes each `make_mpc_command` frame, and prints the observed stream rate, per-leg `u0` (= `motor_rev`), and max per-knot `|Δu0|` — feeding each frame through a real `SetpointPump` so the printed pump-reject column mirrors what `teensy_bridge_node` would decide. Confirms during the Phase 1 arming bring-up that a steady ~40 Hz hold is flowing, `u0` sits at the activate revs (bumpless hold at the active pose), and the per-knot step stays well under the 0.3 rev gate. CSV to `temp/probes/`. | `plans/active/mvp-trajectory-bringup.md` Phase 1 (logbook `2026-07-07-mvp-phase1-streaming-foundation.md`; hardware session `tests/hardware/session_phase1_hold.md`) |
| `teensy_can3_telem.py` | Live observer for the can-bridge: brings up the UDP link RX-only (10 Hz J→T heartbeats with `flags=0` + a `TIME_OF_DAY` responder — it cannot command anything) and prints all 7 Jugglebot axes pos/vel + per-axis diagnostics (state, `active_errors`, bus voltage, FET temp) as decoded by the can-bridge off CAN3. Confirms link health, time-sync anchoring, and CAN3 ODrive decoding during bring-up. (The motor-commanding counterpart — encoder index search — lives in `tests/hardware/teensy_encoder_search_bench.py`, per the never-command-motors rule below.) | `plans/archived/teensy-can-offload.md` (Phase 5/6 CAN3 bring-up) |
| `gui_dom_probe.py` (+ `gui_synthetic_stack.py`) | Live behavioural GUI verification with NO hardware and NO real robot nodes: `gui_dom_probe.py` is the one-command entry point (`python3 tools/probes/gui_dom_probe.py --scenario scenario1`) — it spawns `gui_synthetic_stack.py` through the ROS env (an rclpy fake stack that REFUSES to start unless the ROS graph is EMPTY and :9090 is free, re-checks the graph at ~1 Hz and auto-kills its publishers + tears down if any foreign node appears mid-run, launches ONLY `rosbridge_websocket`+`rosapi` with `jugglebot_launch.py`'s ping params, publishes ONLY GUI-consumed telemetry topics — `profile`, `link_status`, `robot_state`, `leg_setpoint_echo`, `orchestrator_state`, `control_mode_topic`, `hand_telemetry`, `cone/heartbeat`, one-shot `cone/timing_result` — and serves `set_setpoint_output`/`clear_errors`/`home`/`trajectory/*` as fake-state recorders), then drives headless snap chromium via CDP (tornado websocket transport, no pip installs) against the live systemd GUI on :8081, steps scripted scenario stages, and polls DOM assertions. `scenario1` (17 assertions): connect dot, the KEYSTONE CAN wire-slot→physical-bus mapping (distinct can1_*/can2_* fixtures must land on the CAN3/CAN1 rows), health dots under `bridge_link=UP`, the `bridge_link=LOST` cached-republish deception (bridge keeps publishing frozen profile/healths at full rate — panel must still show `--`/STALE/UNKNOWN), >3 s dead-stream watchdogs (badge tooltip must name both message-watchdog causes), leg tracking-error live-then-cleared on echo stop, orchestrator IDLE badge. link_status fixtures always carry `bridge_link` — the real bridge always publishes it. `minimap` (33 assertions, Tier B interactive): trusted CDP pointer holds (`pointer_hold()`, 1000 ms > the 800 ms hold-to-confirm window) drive the orchestrator state-minimap against a REACTIVE fake orchestrator (stack `set` beats answer the GUI's recorded commands, so the sequencer sees states actually move) — pins the SAFETY-CRITICAL teardown order standby→go_home→disarm+VERIFY→deactivate with its NEGATIVE variant (mpc_active never confirms 0 ⇒ ABORT at the verify gate, deactivate NEVER published), Arm-button gating on the control_mode echo, the `bridge_link=LOST` G_BRIDGE greying (fresh-but-frozen link_status must not enable actuation), the worst-case armed-staleness rule (link_status stops with last-known mpc_active=1 ⇒ amber `ARMED?` badge + ENABLED Disarm escape hatch), HOMING command-discarding (holds on all 12 nodes, zero records), unreachable-reason tooltips (not-homed), mid-sequence FAULT abort naming the fault, and disconnect greying (stack `stop_rosbridge`). `cone` (11 assertions): catching-cone panel — `#cc-last-catch` heartbeat catch feedback (no-catch baseline with no flash, offline re-baseline to `—`, SILENT baseline when the first connected heartbeat already carries catch history — the phantom-flash guard, green 800 ms flash + text update on a genuine seq advance), `cone/timing_result` readouts (unmatched ⇒ Actual clock + em-dash delta + `unmatched catch` footer; matched ⇒ signed delta with threshold class + `last: <thrower>` footer), and the GUI-side websocket-disconnect reset (rosbridge dies while the line shows a real catch ⇒ wipes to `—`, so an outage-window catch can't phantom-flash on reconnect). PASS/FAIL report + per-stage DOM snapshots + console log to `temp/probes/gui_dom/<ts>/`; exit 0 = all pass + clean teardown (kills rosbridge/chromium, verifies :9090 free). Note: the GUI loads three.js from cdn.jsdelivr.net, so this probe needs internet (pre-checked, fail-fast). | `logbook/2026-07-11-gui-leg-setpoint-echo-poscmd.md` (pos_cmd datasource) + `logbook/2026-07-11-gui-can-traffic-per-bus-panel.md` (per-bus CAN panel) |
| `canhub_tier2_hw_validation.py` | READ-ONLY interactive operator runner for the Fable-5 Tier-2 hardware-validation checklist. Tails the Teensy 1 Hz USB-serial `[guard]`/`[diag]`/`[axes]`/`[canhealth]` block (never owns the UDP link → coexists with the live ROS2 bridge), walks the operator through the checks, auto-assesses from the parsed serial, and writes a per-check PASS/FAIL report + serial snapshots to `temp/logs/`. Drove checks 5/6/7 of the 2026-07-04 sitting (all PASS). Includes a `sendto`-only UDP junk-flood helper (check-3 mechanic — bumps `crc_err`, not `rx`) and enum-decode tables. The motor-commanding counterpart for checks 1/2/3 is `tests/hardware/teensy_guard_validation.py` (arms `mpc_active` → out of `tools/probes/`, per the never-command-motors rule). | `logbook/2026-07-02-canhub-hardening-tier2.md` (Tier-2 hardware validation) |

### Can-bridge Teensy (`teensy_link_profiling/`)

Offline validation + bench/profiling tools for the new can-bridge Teensy firmware.
See `teensy_link_profiling/README.md`.

| Tool | Purpose | Motivating plan |
|------|---------|-----------------|
| `gravity_ff_ab_extract.py` | A/B extraction for gravity-FF rosbag session pairs (FF off vs on): link_status flag chain (ramp/without_ff/clamp), identical-approach-history hold-current deltas (the sign-inversion check), pos-delta-matched move comparison (deviation/settle/iq), arm-edge zero-motion check. Outputs to `temp/probes/`. First use + results: `logbook/2026-07-16-gravity-ff-armed.md`; tests: the A/B criteria live in `tests/hardware/session_torque_ff.md`. | `leg-gain-tuning-methodology.md` (feedforward arc) |
| `teensy_link_profiling/hermite_xref/xref.py` | Offline cross-check: the firmware's 500 Hz interpolator (`leg_interp.cpp`, via the line-for-line Python port `teensy_interp.py`) vs. the real `motor_guard.py`. Proves 0.0 rev divergence over synthetic + recorded `mpc_*.csv`. | `plans/archived/teensy-can-offload.md` Phase 7 |
| `teensy_link_profiling/jetson/profile_monitor.py` | Live observer: ingests the 1 Hz PROFILE UDP frame (per-task CPU, CAN1/CAN2 util, UDP RTT/jitter, interp deadline-misses, heap) → CSV + matplotlib plots in `temp/probes/teensy_link_profiling/`. | `plans/archived/teensy-can-offload.md` (profiling) |
| `teensy_link_profiling/jetson/setpoint_stub.py` | Bench exerciser / Phase-4 stub client: sends a synthetic 40 Hz setpoint stream + heartbeats, answers the time-of-day RPC, prints uplink — exercises the firmware data path without the real MPC. | `plans/archived/teensy-can-offload.md` Phase 4 |
| `juggle_cup_bandlimit.py` | Lock-in Bode characterisation of the juggle demo's cup-tracking: the Stewart platform (lateral cup motion) and the hand slider (vertical cup stroke), with static-gain / workspace checks at the operating height. Grounds the online cup planner's per-axis jerk limits — slider is fast + accurate (do the throw stroke there), platform tracks only smooth lateral motion (-3 dB ~5 Hz) within a ~150 mm workspace. Headless MuJoCo plant, no hardware. | `logbook/2026-06-27-throw-aim-band-limit-and-closed-loop-catch.md` (and the online-replanning successor) |
| `juggle_online_debug.py` | Debug harness for the online re-planning juggle runner (`sim/juggle_online.py`): per-tick cup/ball trajectory log + a separation analysis (cup z-vel/accel vs ball z-vel through the first throw, flagging `a_cup < -g`). `--stiff-always` forces stiff contact for the A/B test that showed the throw blocker is **contact cohesion** (the cup drags the ball), not a bounded slider — stiff makes the ball fly free (catches 1), exposing the second issue (the **slam** to ~16.8 m/s from the carry pre-roll leaving the cup low). Headless MuJoCo plant, no hardware. | `logbook/2026-06-27-online-replanning-architecture-and-cup-bandlimit.md` (throw-separation re-diagnosis) |
| `juggle_tilt_bandlimit.py` | Platform-tilt (orientation rx/ry) tracking characterisation for the throw-aim re-architecture: static-hold accuracy (no droop), the lever arm (~1.66 mm/deg lateral cup shift + second-order vertical cross-coupling), leg headroom at high tilt (>45 mm margin even at 24 deg — tilt is not stroke-limited at z=170), and a low-frequency tilt Bode around the 1.6 Hz cycle freq (~0.99/3deg, better than lateral translation). Grounds the tilted-detach planner — tilt aims the throw, the slider supplies the speed. Headless MuJoCo plant, no hardware. | `logbook/2026-06-29-platform-tilt-tracking-characterisation.md` (Phase 0 of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_catch_offset.py` | Rung-1 single-catch characterisation sweep: drives `sim/juggle_catch.py::run_single_catch` (BB-reload catch = translate-to-reach + tilt-to-receive + lever-arm realisation, under the §3 noise) across a grid of nominal landing placements × seeds, reporting the clean-catch rate and the in-cup seat-offset distribution. Grounds the catch realisation constants and the test thresholds (`tests/sim/test_juggle_catch.py`). Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_catch_offset.csv`. | `logbook/2026-06-30-rung1-clean-single-catch.md` (Phase 1 / Rung 1 of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_throw_accuracy.py` | Rung-2a single-throw open-loop accuracy sweep: drives `sim/juggle_throw.py::run_single_throw` (tilt-aimed throw = carry up the tilted cup axis → detach along it → fly → measure landing, under §3 tracking noise) across a grid of target placements × cadences × seeds, reporting the clean-separation rate, the landing error, and the noisy "observed landing" the catch reaches for. The gate question (Rung-1 framing): is the throw scatter inside the catch's ~60-80 mm reliable reach? Found the column + 50 mm ring all land within reach (≤33 mm), the ±100 mm ring exposes a directional separation/aim asymmetry. Grounds `tests/sim/test_juggle_throw.py`. Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_throw_accuracy.csv`. | `logbook/2026-06-30-rung2a-single-ball-tilt-throw.md` (Phase 2 / Rung 2a of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_selfcatch_loopgain.py` | Rung-2b single-ball self-catch loop-gain sweep (the MAKE-OR-BREAK gate evidence): drives `sim/juggle_selfcatch.py::run_self_catch` (compose the Rung-2a tilt-aimed throw with the Rung-1 catch into a single-ball toss→catch→toss loop, re-planned each cycle under §3 tracking noise). **(1) COLUMN** (`oscillate=False`) — three recover/geometry variants (faithful plan_cup_cycle + stationary column, drift in-place, ad-hoc axial retract), reports the per-cycle **reach trend**. Finding: does NOT sustain — diverges within 0–3 cycles (reach amplifies ~8 → ~210 mm past the ~60–80 mm reliable reach). Tilt ~0 (degenerate). **(2) OSCILLATION** (`oscillate=True`, the operator's OPTION-1 re-plan) — shuttle a single ball A↔B so every throw is lateral and tilt ENGAGES (~1.4 deg); axes x/y/diagonal × separations 20–70 mm (x swept at 20/30/40/50/60/70 mm), reports the per-cycle **in-cup-offset trend + landing-error trend**. Finding: STILL a BREAK — diverges within 1–4 cycles (max sustained 4 of ≥ 10, at x-20) WITH tilt engaged (in-cup seat offset stays small, but the LANDING amplifies, e.g. x-40 seed 1: 3.7 → 89 → 728 mm; loop gain > 1). Root cause: the tilt-aimed throw is chaotically sensitive to the throw-ORIGIN pose (dLanding/dOrigin ~4), a contact-detach knife-edge tilt does not address. **(3) KINEMATIC RELEASE** (`release="kinematic"`, the Rung-2b FIX) — the same oscillation but the throw cuts the ball free at the planned take-off velocity (`Ball.ballistic_release`); x-40 (default) and x-60 sustain **12/12 on all 6 seeds** with a FLAT ~0.6 mm in-cup offset (the real seat offset, REJECTED — loop gain < 1, the MAKE); x-20 (near-column) + y-40 (Rung-2a asymmetry) are the residuals. Grounds `tests/sim/test_juggle_selfcatch.py`. Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_selfcatch_loopgain.csv`. | `logbook/2026-07-01-rung2b-selfcatch-column-divergence.md` + `2026-07-01-rung2b-oscillation-tilt-engaged-diverges.md` + `2026-07-01-rung2b-kinematic-release.md` (Phase 3 / Rung 2b of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_kinematic_release.py` | Rung-2b kinematic-release knife-edge gain recipe (open loop, the make-or-break FIX): measures the throw's `dLanding/dOrigin` (target (100,0) mm, origin swept ±10 mm, noise off) for the two throw regimes — **contact detach** (emergent `begin_physics_throw`): gain ~2.7–3.5 (BREAK, loop gain > 1, no catch scheme stabilises); **kinematic release** (`Ball.ballistic_release`, cut free at the planned take-off velocity): gain ~0.01–0.04 (MAKE, loop can converge). The isolated recipe that ruled out contact-parameter tuning and validated the fix before it was wired into the harness. Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_kinematic_release.csv`. | `logbook/2026-07-01-rung2b-kinematic-release.md` (Phase 3 / Rung 2b of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_allkinematic_loop.py` | Rung-2b ALL-KINEMATIC self-catch loop (the de-risk before the hybrid): a single ball shuttles A↔B with a kinematic velocity release AND a kinematic-capture catch (snap on contact), decoupled from the contact-carry seat hand-off. Confirms a clean release CLOSES the loop — sustains **12/12 on all 6 seeds** with a FLAT ~2.4 mm catch-reach-error trend (loop gain << 1) — which de-risked the make-or-break before the faithful hybrid (contact-carry catch + kinematic release, also 12/12) was built. The measured disturbance here is the catch REACH error (kinematic capture snaps to centre), not a physical seat offset. Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_allkinematic_loop.csv`. | `logbook/2026-07-01-rung2b-kinematic-release.md` (Phase 3 / Rung 2b of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_fastcatch.py` | Fast-catch FIDELITY recipe: seat a REAL fast Ball Butler throw (~4.9 m/s vz, ~15° from vertical at catch_z, from the demo BB placement) in the Rung-1 catch. Sweeps **contact** (SOFT = the old 50 ms overdamped catch contact vs FIRM = the shipped 10 ms `(0.01,1.0)`) × **arrival speed** (gentle synthetic lob ~2.5 m/s vs real BB ~4.9 m/s) at the shipped constants (ready_lift 0.15, ratio 0.55, phase-matched descent) under §3 noise, reports held/6 + in-cup offset. Finding: SOFT plunges through at BOTH speeds under the phase-matched seat (0/6, ball ends ~1 m below — so the firm contact is necessary even for the gentle lob once the seat co-moves); FIRM + phase-matched descent seats the gentle lob **6/6** and the real BB **5/6** tight (~5–12 mm), the lone real-BB miss being a REACH failure (the real BB's long throw × 2% §3 scatter puts the landing beyond the workspace, not a plunge-through). Grounds `tests/sim/test_juggle_catch.py::test_fast_real_ball_butler_throw_is_seated`. Headless MuJoCo plant, no hardware. Writes `temp/probes/juggle_fastcatch.csv`. | `logbook/2026-07-02-fast-catch-fidelity.md` (fast-catch fidelity fix for Phase 1 / Rung 1 of `plans/active/bb-online-juggle-tilt-rearchitecture.md`) |
| `juggle_motion_quality.py` | Motion-quality probe (Fable-5 review P0, 2026-07-03): instruments the cup (`hand_opening` site) vs the true ball each control tick and reports cup vertical path length/cycle, slider stroke span + ceiling/floor clamp-saturation ticks, disengaged-travel fraction (cup moving while not interacting with the ball), max per-tick z DISPLACEMENT, **command continuity (max \|Δcup vz\|/tick)**, and **cup vz at first ball contact (sign + ratio)** — the "receive" check (a smooth catch contacts with the cup moving DOWN). NOTE the per-tick z-displacement during a velocity-matched descent is `v·dt` by construction (~67 mm/tick at −2.7 m/s) — the co-move SIGNATURE, not a discontinuity; judge command smoothness by \|Δcup vz\|. Original finding (before the co-design catch): the self-catch MAKE cycle used **1026 mm/cycle**, slammed the stroke ceiling **93 ticks**, was **45% disengaged**, and received the ball on a **RISING** cup (+10 mm/s). After the co-design catch (`logbook/2026-07-04-...`): **322 mm/cycle, 0 clamp ticks, 26% disengaged, received DOWN at −669 mm/s** — the gate the fix was measured against. Grounds the P0 gate, the P2 re-unification, and the 2026-07-04 co-design catch. Headless MuJoCo, no hardware. Writes `temp/probes/juggle_motion_quality.csv`. | `logbook/2026-07-03-motion-quality-review.md`, `logbook/2026-07-04-codesign-catch-continuous-velocity-matched.md` |

## Archived probes

Probes whose motivating investigation is CLOSED move to `tools/probes/archived/`
(owner convention, started 2026-08-09). They stay committed and runnable — an
archived probe is a validated instrument you can resurrect for a regression
check — but the move keeps the live directory answering "what would I reach for
today". Each archived probe keeps a ledger row here; never delete the row.

| Probe | Purpose | Arc + closure |
|-------|---------|---------------|
| `archived/hand_dispatch_ladder.py` | Bench ladder for HAND_TRAJ_CMD dispatch reliability: N null smooth-moves through the real stack with per-dispatch `/link_status` counter snapshots (`hand_traj_acks`, `bridge_tx_diag`, `can3_errors`, `uptime_ms`) + CSV. The instrument behind both the 15/40-vs-0/40 discriminator and the 120/120 FW 10 validation. | ERR_TIMEOUT epidemic, CLOSED 2026-08-09 — `logbook/2026-08-02-err-timeout-attribution-instrumentation.md`; runbook `tests/hardware/session_err_timeout_bench.md` |
| `archived/link_status_flash_control.py` | **The control half of the same question: were the compared sessions in comparable states?** One line per session from `/link_status` alone — span, the non-OK duty of a chosen health row, and the counter RATES that say what the bus was actually doing (`setpoints_sent`/s = was the robot being commanded or idle, UDP `tx_frames`/s and `rx_frames`/s = was the link loaded), plus the `uptime_ms` span, the `mpc_active` and `platform_fw_version` values seen, and the deployment markers present. **A duty-cycle delta across a flash is not evidence until this table says the sessions were otherwise alike**: the 2026-07-29 post-flash session that flapped was *idle* (`setpt/s = 0.0`), so "the poller's added CAN3 traffic did it" only survives if some pre-flash session was ALSO idle and did NOT flap — otherwise "an idle bus flaps on its own" explains the data equally well and the two cannot be separated. Finding that control session is exactly a scan of `setpoints_sent`/s against the health duty, per session, which is what this prints. Same date-prefix session-discovery fix as its sibling. **Caveat printed in the header**: rates are `(last − first)/span` on monotonic counters, so a bridge reboot mid-session (which resets `uptime_ms` and the frame counters with it) reads as a spuriously low rate — check the `uptime` column first, per the standing uptime-logging rule. Strictly offline and read-only; needs NO ROS2, needs the project venv. `bags…` / `--root` / `--sessions N` / `--match` / `--health-key` / `--markers` / `--out` (timestamped CSV into `temp/probes/`). Exit 0 = all scanned, 1 = a bag was unreadable or carried no `/link_status`. | CAN3 drive-path arc, CLOSED 2026-07-31 — `logbook/2026-07-31-can3-drive-path-fault-jugglebot-to-can2.md` (promoted in `logbook/2026-07-29-can3-bus-health-flap-hand-sensor-poller.md`); archived 2026-08-09 |
| `archived/canbridge_relay_probe.py` | READ-ONLY validator for the Phase-1 Platform-Teensy relay: issues only `STATE_READ`/`TILT_READ` RPCs (never writes RobotState, never commands a motor) and measures (1) **SRX_DIS** — whether the bridge self-echoes its own `dlc-1` request as a `PLATFORM_FRAME` (would break the `(id,dlc=8)` reply discriminator) and (2) request→reply **latency** (sets `_RELAY_READ_TIMEOUT_S`). First run 2026-06-29: both PASS (0/24 self-echo; ≤14 ms). Re-run after any relay / FlexCAN3 firmware change. | can-bridge cold-start parity arc Phase 1, COMPLETE — `plans/archived/canbridge-foundation-coldstart-parity.md` (logbook `2026-06-29-canbridge-phase1-platform-relay-seam.md`); archived 2026-08-15 |
| `archived/canbridge_version_probe.py` | READ-ONLY validator for the Phase-3 firmware Get_Version handshake: pulls `GET_AXIS_VERSIONS` (a bridge-local cache read — no CAN3 round-trip, never commands a motor) and checks each Jugglebot axis's reported hw/fw against `EXPECTED_HW_VERSIONS` (inlined `validate_group` mirror, no ROS2 dep). Safe with motor power OFF (the ODrives need only logic power + CAN3 to heartbeat the sweep). First run 2026-06-29: PASS (all 7 axes hw 4.4.58 / fw 0.6.11). Re-run after a firmware version bump or an ODrive swap. | can-bridge cold-start parity arc Phase 3, COMPLETE — `plans/archived/canbridge-foundation-coldstart-parity.md` (logbook `2026-06-29-canbridge-phase3-version-validated.md`); archived 2026-08-15 |
| `archived/canbridge_reboot_latch_probe.py` | Reboot-latch gate bench validator: measures REBOOT_ODRIVES → first-fresh-leg-heartbeat latency via the CAN_BUS_DOWN clear edge in `HeartbeatT2J.fault_state` (PROBE 2 — the window `REBOOT_WATCHDOG_SUPPRESS_US` must cover), and sends CLEAR_ERRORS probes through the reboot-silence window to demonstrate whether the staleness gate refuses recovery with ERR_BUS_DOWN (PROBE 1 — the 2026-06-27 deadlock, before/after the reboot-latch firmware). UDP only, no serial console; requires the bench robot (12 V CAN + ODrives). | can-bridge cold-start parity arc Phase 6, COMPLETE — `plans/archived/canbridge-foundation-coldstart-parity.md` (logbook `2026-06-30-canbridge-phase6-reboot-latch.md`); archived 2026-08-15 |
| `archived/canhub_500hz_deadline_gate.py` | READ-ONLY automated PASS/ABORT soak gate for the can-bridge 500 Hz interp real-time path (Fable-5 Tier-2 plan item 19). Ingests the 1 Hz PROFILE UDP frame and asserts the `interp_deadline_misses` DELTA (== 0) and worst-window `interp_max_jitter_us` (≤ 500 us = the firmware `JITTER_MISS_US` boundary) over a soak window → exit `0` PASS / `1` FAIL / `2` ABORT(no-data), with a report to `temp/probes/`. Never sends setpoints/heartbeats (no motion). Binds the STREAM port (owns the UDP link → the ROS2 bridge must be DOWN; but it touches no firmware/flash so it MAY run alongside a CAN3-diagnosis session). `--self-test` verifies the PASS/FAIL/ABORT decision offline (no hardware). For the ISR/stow-re-arm soak, run a long `--duration` while the operator induces a CAN-loss→reconnect deferred stow (Tier-2 check-5 mechanic) — the cumulative miss counter persists across it. | can-hub Tier-2 hardening item 19, COMPLETE — `plans/archived/canhub-hardening.md` (logbook `2026-07-02-canhub-hardening-tier2.md`); archived 2026-08-15 |
