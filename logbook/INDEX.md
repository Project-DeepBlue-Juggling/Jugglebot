# Engineering Logbook

Structured record of investigations: symptom → diagnosis → fix → outcome.

Browse with `/logbook`, create entries with `/investigate` or `/logbook --new <title>`.

| Date | Status | Phase | Title | Entry |
|------|--------|-------|-------|-------|
| 2026-08-22 | resolved | operator-observability F2 | **The topics panel toggles to true per-type UDP message rates** — the ROS2 Topics panel counted messages *this browser* received, and spy subscriptions are throttled to 200 ms, so its Hz column saturates at 5 and says nothing about the wire; meanwhile the UDP link's 21 message types had per-type RX counters in `LinkStats` that nothing published. `teensy_link` gains `tx_count_by_type` (one increment in each of the three send paths) and **pre-seeds all three per-type dicts with every `MsgType`** — which both kills the unlocked `snapshot()` insertion race (rebinding a key is safe, inserting one mid-`dict()` raises) and makes a never-seen type an honest `0` row. New 1 Hz `/udp_diag` publishes `rx_<TYPE>`/`tx_<TYPE>` by enum NAME plus nonzero-only `gap_<TYPE>` and the lower-case aggregates — **counters, never rates**: a rate computed in the publisher throws its divisor away before the bag sees it, so the GUI differentiates instead. New ES module `udp-traffic.js` renders Δcount/Δt over 5/10/30/60 s windows (1 Hz cadence makes a 1 s window one sample) under a **`msg/s` header that is never "Hz"**, behind a `<button>` segmented control in the shared panel header. Gated on `bridge_link`: a dead uplink shows a red LINK DOWN banner and `--`, never a plausible table of zeros — but counter samples taken *during* the outage are deliberately KEPT (unlike can-traffic.js's frozen cache), because a total that stopped advancing is the truth, and dropping them would rewrite the outage out of the recovery window | [udp-rate-panel](2026-08-22-udp-rate-panel.md) |
| 2026-08-22 | resolved | operator-observability F1 | **Telemetry charts read in physical units, and the 3D hand stops being 2.2x too long** — all nine charts plotted raw motor revs, so a leg trace needed a per-leg factor carried in the operator's head and a BB-pitch trace said nothing about where the barrel pointed. Four generated constants (`HAND_MM_PER_REV`, `BB_HAND_MM_PER_REV`, `BB_PITCH_DEG_PER_REV/OFFSET`, all derived from the rev/m gains `compute_derived` already emits) feed one per-chart unit table, `axisUnitsFor(chartIdx)`: per-leg mm (the six legs differ by ~1.3 %), hand and BB-hand spool mm, and **absolute** barrel degrees `90 + 360·rev` for BB pitch so the chart and the BB panel's `pitch_deg` are the same number. Conversion is applied ONCE, at ingestion, to measured AND commanded alike — converting per-consumer is how one trace eventually misses a factor and the chart invents a constant tracking error; axes, callouts, Δ pills, y-range pad floors (the old floor was half a rev = ±35 mm on a leg) and CSV headers all inherit it. Fixed en route: the 3D hand divided its rev by the **leg** factor `MM_TO_REV[0]`, rendering ~2.2× too much travel | [chart-physical-units](2026-08-22-chart-physical-units.md) |
| 2026-08-22 | resolved | operator-observability Phase A | **Phase A audit fixes — the arc-span floor did not survive real marker noise** — six approved audit findings applied. The BLOCKING one: `MIN_ARC_DEG` measured span around the *fitted* circle centre, which on a truncated sweep is precisely the ill-conditioned quantity it exists to distrust — at QTM-realistic noise a real 5° arc fits a ~2 mm circle (true 110 mm) reading 56°–342° of span, so the floor would have fired on NOTHING on hardware, and the per-marker exclusion INVERTED (the noisiest stub read widest, so it was the one kept). Truncated sweeps instead died in the pre-existing `max_dev > 3.0` check under an occlusion message blaming the wrong subsystem. The primary gate is now BB's **reported yaw series** (`angular_span_deg` — encoder-derived, nothing fitted, reads 5.0° at every noise level), plus a `MIN_MARKER_RADIUS_MM` floor for the collapse signature; every sweep fixture parametrised over 0.0/0.1/0.5 mm noise. Also: the MAX_DEVIATION frozen-snapshot attribution made ADDITIVE (a sticky BB `disarm_reason` was suppressing the 2026-07-16 S4 leg attribution), the GUI Calibrate gate no longer re-enables on a disconnected BB via a stale `lastBBState`, the dispatch-vs-handler QTM race now SKIPS instead of FAULTing HOMING, and C2/C3 stop narrating a frozen `_latest_diag` after a link loss (C1 exempt by design). Found and fixed en route: `calculate_yaw_offset` measured a circular mean's spread with an UNWRAPPED difference, so a hold pose on the ±π branch cut read ±242.8° of uncertainty against a true ±0.053° and rejected good calibrations | [phase-a-audit-fixes](2026-08-22-phase-a-audit-fixes.md) |
| 2026-08-21 | resolved | operator-observability F4 | **BB calibration gated on QTM, and hardened against partial sweeps** — the calibrate COMMAND path (button/HOMING → `bb/calibrate` → BB sweeps) and the calibrate DATA path (QTM → `mocap_node` → the circle fit) shared no edge, and `is_receiving()` never left its process. New `mocap/status` (DiagnosticStatus, 5 Hz) is the missing signal: the bridge now REFUSES `bb/calibrate` with a named code and **no RPC dispatched** (`QTM_STALE` vs `BB_MARKERS_NOT_VISIBLE`), while the orchestrator SKIPS the HOMING step instead — `HomingHandler` turns `operation_result is False` straight into FAULT, so a refusal reaching HOMING would fault the robot over an optional subsystem. Hardening closes the garbage-pose path the gate alone leaves open: a `MIN_ARC_DEG = 20°` floor (branch-cut-safe `360° − largest gap` span) refuses a truncated sweep by name, a mid-sweep QTM dropout latches the window invalid, and a 60 s cap closes a CALIBRATING window that never ends. Predicate + thresholds extracted to a shared pure `mocap_status.py` so the two gates cannot drift. Salvaged from a dead agent hand-off; its `_check_calibration_health` ordering defect (dropout-then-wedge left `_calibrating` True forever, silently ignoring every later calibration) found and fixed. First-ever `mocap_node` tests — it was previously unimportable under the mocked-ROS conftest | [qtm-calibrate-gate](2026-08-21-qtm-calibrate-gate.md) |
| 2026-08-21 | resolved | operator-observability F3 | **ODrive errors decoded by name and axis — shell, `robot_state` and GUI event log** — the full `active_errors`/`disarm_reason` masks always reached the Jetson; detail was destroyed here and only here (booleans + a fixed string, a bare `(leg 3)` guard hint, and `odrive.ERROR_CODES` with **zero consumers in the launched node graph**). Restores `can_node`'s decode + 10 s per-(axis, code) throttled logging off the 10 Hz link-status timer, widens the guard hint to **all nine axes** (a hand/BB fault used to print nothing), appends decoded per-axis lines to `robot_state.error[]` **strictly inside the already-fatal branch** (the orchestrator force-FAULTs on any non-empty `error[]`, so observability must never declare a fault), logs `ctx.errors` once on FAULT entry, and adds a hand-mirrored GUI decoder driving per-axis event rows. **Both** masks everywhere — the 2026-08-10 leg-0 spinout had `active_errors == 0`. Drift pins over all four copies of the table caught a real bug: `supported_platform_test.py` mislabelled `0x1000000` as `ESTOP_REQUESTED` (it is `WATCHDOG_TIMER_EXPIRED`) and decoded `SPINOUT_DETECTED` as nothing at all. Parity rows 35/62 restated | [odrive-error-propagation](2026-08-21-odrive-error-propagation.md) |
| 2026-08-21 | in-progress | operator-observability planning | **Operator observability quartet — plan opened on worktree branch `2026-08_operator-observability`** (base `5de4a1c`, independent of the ILC session) — four owner-requested software-only features: charts in physical units, a true-UDP-rate toggle on the topics panel, ODrive errors decoded name+axis to shell and GUI event log, and a QTM-delivering gate (plus hardening) on BB calibrate. Survey headline: the full ODrive error bitmasks already reach `/robot_state` and `LinkStats` already counts RX per type — both unpublished today. Plan: `plans/active/operator-observability.md` | [operator-observability-plan](2026-08-21-operator-observability-plan.md) |
| 2026-08-21 | resolved | sim codegen — MJCF drift gate | **The MJCF model stopped being generated in March and nobody noticed for five months** — `sim/model/jugglebot.xml` diverged from `sim/model/generate_mjcf.py` at `1953943` (2026-03-22) and took **four substantive hand-edit commits** afterwards. Running the generator today produced a **38-line diff of which one line was a wanted fix and the rest was destruction**: it would have deleted the **second ball** outright (`ball2` body/joint/geom/site/material + its two sensors + the ball-ball `<exclude>`; `sim/ball/manager.py` scans `ball`, `ball2`, … *until one is absent*, so the loss would have been **silent**, not loud) and **cut the ball from 35 mm / 71 g to 20 mm / 43 g**. Four general properties had to hold simultaneously for this to survive: **no generated-file header**, not in a `generated/` directory, the generator asserted nothing, and **nothing tested that the two agreed** (`test_model.py` loads the XML and never mentions the generator) — the identical class `tests/firmware/test_config_drift.py` already closed for `config/generated/*`. Fixed by making the generator real again, not by retiring it: the XML is **derived data** (six leg quaternions, the inertia tensor) and it was **already biting** — the 2026-08-18 `hand_stroke_mm` 355.0 → 344.75 correction had never reached it, so the **simulated hand could be commanded 10.25 mm past where the real one stops** (the *second* place that correction missed). Ball spec moved to `config/hardware_config.yaml:physics` as **`juggling_ball_*`** — named to disambiguate, because `throw_envelope.BALL_MASS_KG` derives **0.0952 kg** from `INERTIA_RATIO = 0.747` against a **0.071 kg** physical ball, a **+34 % gap left deliberately unreconciled** (the ratio is a firmware-baked, hardware-validated throw constant, not a ball measurement). New `tests/sim/test_mjcf_drift.py` (6 tests) **proven to bite**: reverting the hand-stroke, deleting `ball2`, and stripping the header each go RED. Regenerating also surfaced a **third** place the 2026-08-18 hand-stroke correction never reached (`tests/sim/test_hand.py` asserted `approx(0.355)`) — fixed at the root, it now reads `hw.GEOM_HAND_STROKE_MM`. Net XML diff = the 6-line header + the two intended hand-stroke lines | [2026-08-21-mjcf-generator-reconciled](2026-08-21-mjcf-generator-reconciled.md) |
| 2026-08-21 | resolved | hand-command-continuity — archival remediation | **Two stale anchors that would have mis-scored a powered sitting, and a sweep that only checked the name** — `hand-command-continuity` reviewed NOT READY on bookkeeping (the engineering was complete, flashed at Platform FW 3 / can-bridge FW 15 and validated to the envelope's ceiling), remediated and **archived `completed`**. The two expensive items: runbook rows **FW-1** and **H4.0d** still gave PASS as `v2` and routed anything else to *"flashed from a different tree; git pull + re-flash"*, so they would have sent the operator to **re-flash a correctly flashed board**; and `sim/plant/mujoco_plant.py`'s `_hand_stroke_mm` was a hardcoded `355.0` used as the `command_hand` clip bound against a config that moved to **344.75**, so **the simulated hand could be commanded 10.25 mm further than the real one** — verbatim the failure the 2026-08-18 correction exists to prevent. Root cause of the second: **a rename-driven sweep verifies the NAME, not the VALUE.** The 2026-08-18 sweep took `hand_motor_max_position_revs` to zero and that claim is true; the **11.1 rev value** stayed live in two places a symbol grep cannot see — the plant's `355.0`, and ⚠ `experimenting/.../can_interface.py`'s `_HAND_MOTOR_MAX_POSITION = 11.1`, **a declared travel limit 0.3 rev = 9.5 mm PAST metal in a script that actuates the real motor**. Claim withdrawn in that entry with both survivors and a grep recipe. Also: C-HAND-1 obligation **F** re-statused LIVE (it read "NOT LIVE until flashed" four weeks after the flash); **seven** logbook entries closed out of `in-progress`, three of them re-pointed off a `related_plan:` deleted in `07f2361`; **three unowned Phase-0 catch findings re-homed** to `hand-trajectory-generator-overhaul.md` (whose own "keep `hand_stroke_mm` authoritative" instruction, now contradicting the deliberate 2026-08-18 split, was fixed in the same edit); ~40 stale live numbers swept (11.1 → 10.8 rev, band floor 0.55 → 0.4949 s and everything keyed to it, `smoothMoveMaxDuration()` 0.8005 → 0.78964 s, continuity band 20.3 → 19.96 rev/s with the binding bound flipped); every firmware line reference re-anchored and now citing the symbol; and **48 path-qualified inbound references across 28 files** re-pointed at archival. Platform firmware **byte-identical** (text/data/bss and `firmware.hex` md5 both unchanged) — comment-only, no reflash | [2026-08-21-hand-continuity-remediation-and-archival](2026-08-21-hand-continuity-remediation-and-archival.md) |
| 2026-08-21 | resolved | C-HAND-3 validation | **The derived envelope was flown to its ceiling and the extrapolation caveat is discharged — coast is FLAT, not quadratic** — one launch (`2026-08-21_10-11-42`) at **4.436 / 4.858 / 5.246 / 5.608 ×2 m/s**, the last **0.029 m/s under the 5.637 m/s `DECEL_FF_HEADROOM` bound**; every throw ACCEPTED, none truncated, and the only drops were the 1.6 m throws whose balls hit the ROOM's ceiling. C-HAND-3 shipped with a ⚠ that its ceiling sat **1.27× past the fastest speed ever measured here**; measured coast at the ceiling is **0.215/0.250 rev (18.7 mm headroom to the 10.8 rev stop)** against the p = 2.0 model's predicted 0.361 — the model is **conservative by ~40 %**. Across 3.142 → 5.608 m/s coast runs 0.134 → 0.250, ratio 1.87 on a speed ratio 1.78, i.e. **p ≈ 1.0** — flat, exactly what a tracking-limited plant with ~1.8× unused braking authority does. **p = 2.0 RETAINED deliberately** (now measured-conservative; lowering it buys ceiling `DECEL_FF_HEADROOM` forbids anyway). Also closed: **`H1.6`/`H4.7` scored PASS by measurement** — a full Platform Teensy serial capture across the session contains **ZERO** `Not enough time for smooth-move`, retiring the 2026-08-18 latency-margin argument; and the post-hard-stop-correction duration cap is confirmed non-truncating (longest prelude `dur=0.76 s` vs the 0.78964 s cap, 30 ms clear) | [2026-08-21-envelope-flown-to-ceiling](2026-08-21-envelope-flown-to-ceiling.md) |
| 2026-08-18 | resolved | single-ball-toss / hand-command-continuity follow-on | **Throw height is derived from the machine's limits, not two hand-picked constants — and the measured coast says the ceiling is torque, not the end stop.** `FLIGHT_TIME_MIN_S = 0.55` / `FLIGHT_TIME_MAX_S = 1.10` replaced by contract **C-HAND-3** (`ros_ws/docs/hand_throw_envelope.md`): one enforcement point (`throw_envelope.evaluate`), seven bounds, every input a config key, refusals naming the binding bound with numbers. **Two passes, and the second overturned the first.** The first had only the 2026-07-27 PRE-CLAMP-FIX coast ladder, derived a 0.887 s ceiling, and *refused the 1.0 m working point the machine flies every session* — saying explicitly that one capture would settle it. It did: a purpose-built session 2026-08-20 (`2026-08-20_21-51-39`, six throws at 0.5/0.7/1.0 m, all caught, `iq_meas` to −17.32 A) measured coast **0.1342 / 0.2119 / 0.2260 rev at 3.142 / 3.714 / 4.436 m/s** (n = 2/2/14) against the pre-fix model's extrapolated 0.700 — the working point sits **19.4 mm from metal, not 4.4**. **The clamp was the mechanism, proven by a within-session A/B**: bag `2026-08-18_18-42-19` changes the braking clamp mid-session (iq floor −8.87 A through 0–100 s, −17.4 A after) and its first throw coasted **+0.763 rev** against 0.18–0.23 for every other throw at the identical commanded speed — 3.8×, same firmware — which also answers C-HAND-2's open clamp question. **The extrapolation LAW had to change too**: the old "hold achieved decel constant" is the authority-saturated shape and fitted the pre-fix plant (η 0.982→0.799); post-fix η is flat at 0.951–0.968 with 1.8× unused authority, so that law would predict 3.30 rev of coast at the ceiling vs the fitted family's 0.39. Now `coast_top·(v/v_top)^p` at **p = 2.0**, above every fitted exponent (maxima 1.50, means 1.31, all points 1.12). Derived band **`[0.4949, 1.1485]` s** = apex **0.300–1.617 m**; binding bound is **`DECEL_FF_HEADROOM`** (5.637 m/s) — a bound the first pass never reached, added because at `DECEL_AUTHORITY`'s 5.816 the decel feedforward wants **45.2 A of 50**, breaking C-HAND-2's own 85 % rule (that 0.85 was a bare test literal; it is now a config key). `END_STOP` binds at 7.468 and no longer constrains anything. Regen corrected: bus **45 V** (was an inferred 48), rail **300 W steady-state** — fenced on the drive's **360 W burst** because a 50–90 ms ramp at a 3.5 s cadence is ~2 % duty; fencing instantaneously on 300 W would have wrongly bound at 5.478. ⚠ **The ceiling is 1.27× past the fastest speed ever flown here** — tolerable only because a torque limit, not the extrapolation, sets it, and the "`END_STOP` does not bind" claim survives the whole exponent family. Whole HAND-7 ladder re-admitted (R5 peak 10.294 rev vs the pre-fix 12.171). `./run_tests.sh --full`, run 2026-08-20: **5741 tests, RESULT: PASS, 532 s**. **NOT DEPLOYED** — `colcon build --packages-select jugglebot` + relaunch; no flash | [2026-08-18-derived-throw-envelope](2026-08-18-derived-throw-envelope.md) |
| 2026-08-18 | resolved | hand-command-continuity Phase 5 (criterion fixes) | **The truncation criterion goes profile-relative on both halves — the stroke end, then the collapse floor** — the same defect class twice in one detector: an absolute constant judging a profile-relative event. (1) The scan was bounded by a fixed 50 ms past the *modelled* stroke end, so Phase 1's catch-arm gate **working** — dispatching a from-rest prelude the moment the stroke completes — read as the clobber it prevents. The 2026-07-27 sitting straddled the wall: **6 tosses reported a truncation across its six bags and 3 across its three traces**, and one session recorded both ways read **4 (bag) vs 3 (trace) on the same physical tosses** — the verdict depended on the recording path, and all were adjudicated PASS by hand. Now bounded on the commanded profile REACHING `x3`; the prelude is reported as `post_stroke_cmd` instead of scored as a truncation. (2) `_COLLAPSE_VEL_REV_S = 10` rev/s localised the freeze with a fixed velocity against a ramp whose velocity scales with the throw, so at slow throws the **ramp itself** fired the detector — **1.94 ms / 20 of 100 sampling phases** at the C-HAND-3 admission floor (2.440 m/s), three times worse than when first characterised because the envelope floor had just moved *down*. Now `_collapse_floor_rps(model)`, the modelled stroke's own velocity at `x3 − _X3_SHORT_REV`; the floor is a velocity-independent **11.1 % of the commanded peak**, so monotonicity makes the self-trigger window **empty at every speed**, not narrow. Validated over 71 analysable throws (six bags + six traces): **2 truncated / 69 clean** unchanged, real truncations firing at **1382×**, 0 spurious fires over 960 clean captures spanning 0.70–7.00 m/s, min in-scan margin on real telemetry **+0.4288 rev/s**. Ten mutations, both directions; `xfail` removed not flipped. **Incidental**: `--gate` — the runbook's mandatory instrument self-check — had been **RED on the shipped tree** since the 11.1→10.8 rev hard-stop correction with nothing in the suite running it; now derived from the generated constant and gated by pytest. Two stale live safety numbers fixed (runbook headroom column → 0.475 rev = 15.0 mm; the decel-xref hold window) | [trunc-criterion-stroke-end](2026-08-18-trunc-criterion-stroke-end.md) |
| 2026-08-18 | resolved | — | **ODrive config-drift assertion is blocked on firmware — the SDO read path is one-way** — a launch-time check that live drive registers still match the committed snapshots cannot be built on `teensy_sdo_read`: `rpc.cpp:249-257` returns an empty blob and `can_buses.cpp:248-262` discards every TxSdo but the hand ball sensor's, and the hand is refused `SDO_READ` outright (`udp_protocol.h:593-608`). USB ruled out on evidence (drives are maintenance-plugged only). Design preserved in `plans/parked/odrive-config-drift-assertion.md`; parked on one owner decision | [odrive-config-drift-assertion-blocked](2026-08-18-odrive-config-drift-assertion-blocked.md) |
| 2026-08-18 | resolved | hand-command-continuity Phase 5 (anchor resolution) | **The hand's end stop is 10.8 rev, not 11.1 — the shipped guard sat 0.3 rev PAST metal and was never protective** — the operator measured the hard stop (metal contact) on the sensorised hand, which changed during ball-sensor integration without the limit being updated. **All four declared values were too high, one-sidedly**: the shipped `hand_motor_max_position_revs` 11.1 (0.3 rev / 9.5 mm past metal), its own comment's "true max ~11.4", geometry's 11.224 from `hand_stroke_mm` 355, and runbook H4.5's 11.124. Consequence: `clip_position` — in the host `can/odrive.py` **and** in the can-bridge firmware `odrive_protocol.h` — accepted commanded setpoints 9.5 mm into the stop. It also mis-scored history: the 2026-07-27 sitting's five ~1.2 m tosses at **10.8601–11.0621 rev**, recorded then as "1.2–7.6 mm from the declared limit", were **0.06–0.26 rev PAST the stop** — the contact the operator felt, and why `2026-07-29-hand-post-release-decel` § 2 could not close the question. **The naming was the defect as much as the number**: `hand_motor_max_position_revs` meant "the hard limit" to one reader and "the limit minus a margin" to the next — its comment asserted both at once — and the sibling `leg_motor_max_position_revs` means the OPPOSITE again (its value sits ABOVE its stroke-derived limit), so a quantity with two meanings survived three investigations that all quoted it. Renamed to **`hand_motor_hard_stop_revs`** (a measured fact; margins now subtracted explicitly at each site), leg key deliberately left alone with the asymmetry documented so nobody "fixes" it back. **The smooth-move ceiling is deliberately held at 10.60 rev** (owner's call): `smooth_move_excursion_margin_rev` 0.5 → 0.2 so `10.8 − 0.2` lands on the same 10.60, making the commanded smooth-move **ceiling** bit-identical (float32 0x41299a9a both ways) — though NOT the whole path: the duration cap moved with the base, so a prelude whose honoured duration lands in (0.78964, 0.80054] s, i.e. `|v0|` in (20.04, 20.32] rev/s, now takes the rest-to-rest fallback (conservative — that is pre-Phase-4 behaviour); the honest cost is stated in the YAML rather than buried — 0.5 rev was 2.7× the measured +0.186 rev tracking overshoot and 0.2 rev is 1.08×, so the arithmetic that believed it had 0.31 rev of clearance actually has **0.014 rev (0.44 mm)**, the "occasional light tap" regime the owner accepted (a 2.7× ratio would need a 10.3 rev ceiling, declined because Phase 4's max commanded prelude is 10.2259 rev). One derived quantity genuinely moves — `smoothMoveMaxDuration()` **0.80054 → 0.78964 s** (defined as the longest rest-to-rest move the stroke admits; conservative direction, and `_PRIME_INFLIGHT_S` was sized against the larger number), narrowing the velocity-continuous band mid-stroke 20.90 → 19.96 rev/s and duration-capped 20.32 → 20.04 (the 9.07 rev/s stroke-top figure is unchanged — it is sized off the unmoved ceiling). **Deliberately NOT changed: `hand_stroke_mm = 355.0`**, which now implies 11.224 rev, 0.42 rev above the stop — it functions as a THROW-PROFILE parameter feeding `total_stroke → x2/x3/x5`, all empirically validated, so re-deriving it would move the release and catch points and needs its own validation; recorded as a known bounded inconsistency (the same naming disease in a second place). Grep-before-refactor sweep: **~50 references enumerated, 13 source/test files patched**, artifacts regenerated, old-symbol live-code count verified **down to zero**. Eight assertions pinned to the old anchor were re-derived, not relaxed. **NOT fully live: needs TWO flashes** (Platform Teensy for `SMOOTH_MOVE_POS_CEIL_REV` + `smoothMoveMaxDuration`, can-bridge for `clip_position`); until then the host-side clamp is the upstream one and the tighter clamp binds, live at the next `colcon build` | [2026-08-18-hand-end-stop-corrected](2026-08-18-hand-end-stop-corrected.md) |
| 2026-08-18 | resolved | catch-robustness Phase 0 (clamp half) | **The hand ODrive's asymmetric −10.00 A torque clamp WAS live — removing it removed the post-throw dip** — the operator's bench ladder on `torque_soft_min` settled the pre-registered **H7.0c** question in the direction the 2026-08-10 diagnosis bet on: `axis0.config.torque_soft_min = −0.055133 N·m` (= **exactly −10.00 A** at the hand's Kt 0.0055133, against a `torque_soft_max` of +0.5 N·m) was truncating the FW-2 decel feedforward on the flashed drive. Set to a symmetric **±0.7 N·m (±127 A)** and locked into `config/ODrive config Files/odrive_pro_hand_config.json`, the post-throw dip at the stroke peak is **essentially gone by eye** — the symptom 54/54 throws showed on 2026-08-10. Both clamps now sit far outside `current_soft_max` = 50.0 A, so the **current limit is the fence on both sides**; regen does not become the new binding constraint (~50 W of peak braking mechanical power against the ~400 W that `dc_max_negative_current` = −8.0 A allows on the ~50 V bus). Likely origin of the bad value: `−0.055133` is the **legs'** `torque_constant` (8.27/Kv) and the hand's Kt is ten times smaller — the wrong constant pasted into a clamp field, which is why it landed on a suspiciously round −10.00 A. **A hypothesis this repo argued AGAINST was right, and the counter-argument is now recorded as wrong rather than deleted**: `tools/probes/hand_decel_authority.py` printed on every run that the clamp was "very probably NOT binding" because a hard clamp would force one single achieved decel per tier while `a_ach` grows 2.6× across the band — that inference does not survive, since the clamp bounds TOTAL commanded torque and the position loop still varies its contribution across tiers. **The expensive consequence is measurement-through-the-clamp**: every hand capture in the repo predates 2026-08-18, so all braking-direction `iq` in them describes the clamp, not the plant — the plan's conditional Open row has **FIRED** and C-HAND-2's decel-side bound `J ≥ 1.0126e-5 kg·m²` (hence `throw_decel_reflected_inertia_kgm2`) must be re-derived on the restored drive, discriminator unchanged (does achieved decel become tier-independent?). The catch-knob freeze **lifts** — the regen-direction degradation behind it is gone — but the 60 % baseline it protected is now meaningless as a reference and Phase 3 must re-measure, not compare. **Phase 0 is deliberately only half closed**: "dip gone by eye" unblocks Phase 1 (sensor-truth bench validation, which does not depend on braking authority) but is not the quantitative gate (`dip_below_x3 ≤ 0.10 rev`, `peak ≤ 10.060 rev`, braking `iq` tracking commanded), and the re-derivation needs the HAND-7 R0–R5 ladder's numbers; the whole-config dump/diff (`dc_max_negative_current`, `torque_constant`, `gpio2_mode`) is also still outstanding — this session changed the one register the ladder identified, not the drift class it belongs to. **Also closed this session, both by OWNER DECISION on operator attestation rather than scored captures**: Phase 0 (the owner declined the remaining characterisation, so the quantitative gate was never measured on the restored drive and the config-drift class is unswept; C-HAND-2's `J` re-derivation is downgraded to a watch-item, since `J_ff = 9.5e-6` under-brakes and both the over-brake and end-stop questions move the safe way) and Phase 1 (the hand sensor is accepted as the possession source of truth on extensive operator testing; § SECTION POSS / Stage 6 CAP-WORK declared complete in the PLAN, without a scored capture and with the runbook rows left unmarked, leaving `POSS-1.7` and `POSS-1.8` unexercised against their written criteria) | [2026-08-18-hand-torque-clamp-removed](2026-08-18-hand-torque-clamp-removed.md) |
| 2026-08-16 | resolved | plans-board restructure | **Plans board goes three-way (`active` / `parked` / `archived`), and archival stops renaming plans** — two owner-directed changes in one pass. (1) The six plans parked on 2026-08-15 moved out of the active board into `plans/parked/`, whose new INDEX carries the column that makes the directory useful: **what would unpark it**, derived from each plan's own text (one plan states no gate at all — its row says so rather than inventing one). (2) The `YYYY-MM-DD ` archival prefix is retired: `related_plan:` is filename-only by contract and resolves by searching the plan directories, so the rename broke every inbound reference at exactly the moment a plan stopped being editable — **twelve entries pointed at nothing** within a day of one archival, silently, because no code consumer validates the field. All 28 prefixed plans renamed back to bare names with an `archived:` frontmatter date; **a plan's filename now never changes for its life**. Sweep: 258 path references across 134 files for the un-prefixing, plus 248 across 146 files for the directory moves (102 of those pre-existing rot from earlier archivals). Both warned-of grep traps were live — `rg` is not installed here (silent zero), and **five paths were split across two source lines**, one of them in a generator that emits five artifacts. Firmware **byte-identical** (text 232768 / data 35520 / bss 107872, md5 unchanged) so no reflash. `tests/sim/test_plans_index.py` grew from one board to three, including the rule that would have caught this: **every archived plan filename is bare** | [2026-08-16-plans-board-three-way-split](2026-08-16-plans-board-three-way-split.md) |
| 2026-08-16 | resolved | record correction | **`levelling_complete` ownership resolved — the flag is a RAM global on the PLATFORM Teensy, on the ODrive rail, so a can-bridge power-cycle cannot clear it** — the record contradicted itself (standing rule 2 and pre-brief item 2 said Platform-held and can-bridge-immune; pre-brief item 1 and `levelling_frame.md` said a can-bridge cycle is what made it false every launch), and two prior sessions *flagged* the contradiction rather than resolving it. It went live when reboot-before-every-session was retired, because it decides whether an operator must re-`level` after a bridge reset. Settled from source: `Teensy_code_platform.ino:139-145` declares `RobotState state = { false, false, 0.0f, 0.0f }` — a file-scope global, and `grep EEPROM` over that sketch returns **nothing**, so "persisted" is RAM-under-power, never NVM; the can-bridge firmware holds **no copy** (`platform_relay.cpp:39-75` builds the `0x6E0` frames, `can_buses.h:132-135` forwards the reply verbatim); the two boards are on **different rails** (Platform on Jugglebot 12 V/ODrive — `teensy_bridge_node.py:3426-3428`; bridge on Jetson 5 V). **The operator's model is CONFIRMED.** Exactly four things move the reported value, and only three are storage events (Platform power loss/reset incl. a flash; `REBOOT_ODRIVES`; an explicit level/home write) — the fourth is the **read** failure: the boot read's conservative fallback covers `levelling_complete` on the *same line* as the known-benign `is_homed` transient (`:5183-5185`), and racing that synchronous boot read with a just-power-cycled bridge is the most plausible origin of the whole belief. Hardware corroborates: the persisted boot auto-push, gated on `ctx.levelling_complete`, scored **5/5** in the 2026-07-27 sitting (7/7 across all publishes), so the flag was true at five post-`level` relaunches; the one launch that read `levelling=0` followed a Platform **flash**. **LG-3's three-week-old analyst standoff is settled, with both sides partly wrong**: the "power-cycle the Platform Teensy" recipe is refuted (it zeroes the flag → LG-1's state), "unreachable by any power-cycle" is correct, but "so the closure is a unit test" is too strong — the state is deterministically reachable with no power-cycle at all, because the orchestrator's push is **one-shot per orchestrator boot** (`_startup_offset_sent`, never reset), so `level` → restart **`trajectory_node` alone** gives `levelling_complete: true ∧ gravity_correction_loaded: false` by construction. Corrected: the contract doc (resolution box replacing the "unreconciled" amendment), the runbook (pre-brief item 1 struck, the one-recipe ⚠ box split into LG-1's and LG-3's, LG-3 given Route A/B), `RobotState.msg` (comment-only — the un-scoped "since the last bootup" **is** the root ambiguity; no field change, no rebuild needed), and two one-line code comments. `Teensy_code_platform.ino` deliberately untouched: ~30 committed line-number citations point past line 145. | [2026-08-16-levelling-complete-ownership-resolved](2026-08-16-levelling-complete-ownership-resolved.md) |
| 2026-08-15 | resolved | bridge-temporal-trustworthiness closure cleanup | **Arc-closure cleanup — R7 re-keyed from bridge UPTIME onto bridge HEALTH, plus the stale-reference sweep** — with FW 14 validated and reboot-before-every-session retired, `toss_cal_grid.py`'s R7 (hard refusal above `UPTIME_ABORT_MS` = 30 min) was obsolete advice that would have blocked legitimate captures on a healthy warm bridge. Shared helper `temporal_health_verdict(link_kv, uptime_ms)` in `tilt_cal_grid.py` (with `bridge_fw_version_value`, `worse_latency_monitor`, `TemporalVerdict`), imported by `toss_cal_grid.py` like `wire_armed_verdict` — one implementation, one place for a threshold to drift. **Branch ORDER is the design**: a `latency_monitor` alarm (`RING_LEAK`/`CACHE_AGE`/`CLAMP_DUTY`) outranks everything **on any firmware** (direct evidence of a degraded plant *now*, which no uptime number can acquit — the old gate waved a 1-minute-old *leaking* bridge through); FW ≥ 14 + monitor OK passes with `uptime_ms` logged but never gated; everything else (pre-FW-14, unreadable `bridge_fw_version`, ROS build too old to publish the row) falls back to the 30-min ceiling verbatim, because each is a state where health cannot be *seen*. The non-obvious part: the uptime ceiling bounded the WHOLE 2–3 h capture (drift monotone in uptime) and a preflight *snapshot* does not — so the runner tracks the **worst token seen across the session**, R7 folds it in (recovered-just-now ≠ never-degraded), and `latency_monitor_first/last/worst/samples/bad_samples` land in `_meta.json` beside `uptime_ms_first/last` with an end-of-run verdict. Deliberately NOT done: a second `/ring_diag` subscription re-thresholding `leak_*` (the monitor's highest-precedence input already IS the ring leak, at 10 Hz inside the node that receives the frames); `tilt_cal_grid.py` stays a WARNING (static inclinometer reads are uptime-insensitive). Tests cover all six verdict branches, each alarm refusing on a 1-minute-old bridge, alarmed-earlier-in-session, pre-FW-14 warm/unknown, missing `/link_status`, every `bridge_fw_version` render shape, plus an **xref test** pinning the four token strings and their rank order against `teensy_bridge_node.py`'s source **text** (read, not imported — the harnesses must import without `rclpy`). Also: stale archived-plan references swept across seven files — the trap being the generator's copy **split across two Python source lines** (invisible to grep on either the full path or the bare filename) which regenerates five artifacts, so fixing only the five would have restored the stale path at the next codegen; firmware **byte-identical** after the comment-only edits (`pio run -e teensy41`, md5 `ea705b4bb4026047318c0361750c87ab` before and after, no reflash implied). Retired-rule sites the first docs pass missed now amended (incl. a fifth-sitting banner whose "reboot isolation still outstanding" claim was also false, and a cross-reference that named the wrong Teensy). Archive assessment: **nothing qualified** — all nine arc-added test files cover telemetry with an ongoing job, and the arc created no committed probe at all. | [2026-08-15-arc-closure-cleanup-r7-health-gate](2026-08-15-arc-closure-cleanup-r7-health-gate.md) |
| 2026-08-15 | resolved | bridge-temporal-trustworthiness closure | **FW 14 validated at 5.8 h and 15.2 h — the uptime lag is gone, both residuals reconciled, and the five-week bridge-temporal arc CLOSES** — acceptance criteria 1 and 2 **PASS**: `leak ≡ 0` on all three buses across **167/167** `/ring_diag` samples at 5.73–5.84 h (`true_depth_hwm_jb` **1** — the drain empties the ring every tick), `fifo_overflows`/`fifo_warns`/`rx_cap_hits` **0**, e2e echo→encoder lag **10 ms** median (15.9 ms sub-sample), lead-clamp duty **0.0000**, SDO RTT **872–931 µs** (S3: 46.9 ms), cache ages 1–10 ms; then **two full 11-move batteries at 15.17–15.19 h on the SAME un-rebooted boot** (continuity verified: 34,265,600 ms uptime delta against 9.516 h wall clock) at **20 ms**, clamp duty **0 % on every move**, `leak ≡ 0` — identical to FW 14 fresh (15 s). Criterion 3 **RECONCILED**: **(a)** the `lag_now` creep is a **FlexCAN capture-clock artefact** — `cap_span_us/window_us` predicts the slope to <1 % in all four bags, the rate is **load-dependent** (≈230 ppm idle, ≈580–670 ppm streaming) with a **load-INVARIANT** ~1950 RX-frame fold rate (so per-second and per-frame are the same normalisation — the first-pass “scales with FRAMES” inference is WITHDRAWN), proven non-physical three ways (it passed the 135 ms one-lap cap at `leak ≡ 0`; it reset to 5.6 ms on a reseed with nothing physical changing; the three real latency measures read sub-ms to 16 ms) — a continuously re-estimating normaliser ships as `lag_now_corrected_us`, itself a **growth channel since reseed**, never an absolute lag; **(b)** the SDO RTT floor is valid **by condition** (0.87–0.93 ms against a 20 ms poll ⇒ one reply in flight), limitation restated. Three findings that are NOT this defect: **leg-0 SPINOUT is hardware** (estimate-first, +50–63 mrev in one 10 ms sample, self-inconsistent velocity, peers undisturbed, also present pre-FW-14) and the operator's **encoder re-seat** cleared it (`iq_rms` 2.97–3.09 A → **1.41 A** = peer baseline); **Y is NOT rougher** (pooled HF tracking-error RMS **Z 2.58, TILT 2.30, Y 1.67, X 1.52** ×10⁻³ rev — roughness tracks commanded speed; Y− merely strokes four legs hard at once, and one genuine ax4 dropout landed on a Y move); and **per-axis leg-bus frame drops**, gated by the 500 Hz stream and **NOT uptime** (0/232 idle windows; 15.2 h reads mid-range), localised to frames that never reached the CAN peripheral (encoder vs `can1_rx` deficit r = 0.62), hypothesis = **ODrive-side TX suppression**, now `plans/active/leg-bus-frame-drops.md`. Parent `2026-07-18-teensy-uptime-tracking-degradation` **CLOSED** — both deliverables of its 2026-07-24 contract met (FW 14 **plus** the alarmed latency monitor: ring-leak / cache-age / clamp-duty alarms in causal precedence, logged beside `uptime_ms`, `latency_monitor` on `/link_status`); **reboot-before-every-session RETIRED**; guard-raise, chase-ceiling, retime-OFF, accel-FF and ILC **G-A** premises all re-derivable on a healthy plant. | [2026-08-15-fw14-validated-arc-closed](2026-08-15-fw14-validated-arc-closed.md) |
| 2026-08-15 | resolved | plans-board hygiene | **Plans-board cleanup — 5 archived, 6 parked, 2 prompts deleted, 4 probes archived** — owner-ratified triage of all 21 active-plan files (surveyed by content, not mtime) restores an honest board of 10 rows. Archived with notes + residue re-homing: fk-convergence-tolerance, hand-ball-sensor (Phase 7 steps 4–5 → the hand-sensor session runbook + a catch-robustness Phase 0 add-on), tilt-calibration-grid (leg-0 SPINOUT already homed), teensy-can-offload (residuals → the parity matrix), bb-led-two-ball-juggle-demo (superseded by the tilt-rearchitecture Rung 3). Parked with reasons-in-banner: accel-ff-inertia, learned-ff-residuals, catch-reach-degenerate-overshoot, hand-trajectory-generator-overhaul, levelling-frame-contract (int16 truncation defect carried in the banner), refactor-2026-07 (§ Standing coordination rules stays live). Both completed PROMPT files deleted per the 2026-08-09 convention (Item D narrative verified mirrored first); three stale one-shot workflow runners removed, `mvp-phase-runner.js` committed; four closed-arc probes → `tools/probes/archived/` with ledger rows (which also fixes two dangling `plans/active` citations in the README). | [2026-08-15-plans-board-cleanup](2026-08-15-plans-board-cleanup.md) |
| 2026-08-14 | resolved | single-ball-toss widening | **Toss gates follow the live session limits — operator authority over toss width** — `trajectory/status` now publishes the LIVE session leg vel/acc/jerk limits (the values `feasibility.validate` enforces, as ramped by `trajectory/set_limits`); the toss sequencer's closed-form reach bound judges against them (YAML-default fallback when absent — bit-for-bit the old behaviour), so a ramp-UP widens the pre-throw gate in lockstep with the planner and a ramp-DOWN refuses pre-throw instead of `TOO_FAST`-ing at `t_release` with the ball airborne. The hardcoded ±150 mm planning box became `toss_workspace_xy_mm` (default 160 = cap + 10), which also DISSOLVES the chaining-at-the-cap known limitation (the 2.07 % centroid-vs-cup divergence no longer binds; C-REACH-1 residual 7 updated in place); `toss_max_displacement_mm` made genuinely operator-adjustable (mechanism-pinned, not YAML==literal-pinned). `toss_tier` default → **8b** (owner, 2026-08-14). Deployment needs the two-package build (`jugglebot_interfaces` changed). Verification: scoped 472/472 (2026-08-14); `./run_tests.sh` PASS. | [2026-08-14-toss-live-limits-authority](2026-08-14-toss-live-limits-authority.md) |
| 2026-08-14 | resolved | bridge-temporal-trustworthiness P3 | **FW 14 — the ring-pop IRQ guard: THE FIX for the convicted FlexCAN_T4 `_available` one-way leak** — two more patches on the vendored library (`PROVENANCE.md` § Local patches), against the conviction number `/ring_diag` `leak_jb` **247–248** at 4.03 h. **P3**: the RX pop — payload `memmove`, head advance, `_available` decrement — now runs inside `NVIC_DISABLE_IRQ(nvicIrq)`/`NVIC_ENABLE_IRQ`, with `dsb; isb` after the mask write (ARMv7-M: an ICER write may not take effect before following instructions retire) **and** an audit-added compiler barrier before the re-enable, because head/tail/`_available` are `volatile` but the `_cabuf` payload copy is not — without it a future toolchain could legally sink those loads past the re-enable and silently re-open the ring-full frame tear while `leak ≡ 0` still passed, so the barrier makes the tear-closure a **source guarantee**, not a toolchain-empirical fact. Masking the *whole* pop is deliberate: one guard closes the leak and the tear together, and the narrower Circular_Buffer-level alternative was rejected because that buffer is shared with every other ring instance and knows no NVIC line — the narrower-looking guard is the wider blast radius. Verified by exhaustive enumeration of the compiled ARM assembly (every head/`_available` store inside the window, only benign volatile loads outside, ICER/ISER literals and `dsb; isb` confirmed in the object) and independently re-verified by the audit at source and object level. **P4**: the missing `break` in the TX-deferral `mb == -1` refill loop — the library's first *confirmed* defect, dormant at `tx_deferred = 0` on every bus, blast radius doubled by FW 10's mailbox raise; one line, cannot change live behaviour, discharges `can_buses.cpp`'s standing "must fix the vendored loop" warning. **IRQ-off cost bounded**: <0.1 µs per window, ≤32 disjoint windows per 1 kHz drain tick, against the 6-deep hardware FIFO's ~690 µs tolerance — four orders of margin, with P2's FIFO-overflow counters reporting if the reasoning is ever wrong. `FW_VERSION` 13→14, `EXPECTED_BRIDGE_FW_VERSION` 14, **no wire change** (diag frames byte-identical, `PROTOCOL_VERSION` stays **5**); `RING_DIAG` retained unchanged as the fix's own proof, leak-criterion acceptance being `leak ≡ 0` at any uptime, while full FW 14 acceptance (plan § S3 RESULTS) adds the aged-soak e2e lag ≤ 20 ms and the two S3 residual reconciliations. Comment amendments in `can_buses.cpp`: the REFUTED drain comment now carries CONVICTED-then-FIXED, the `setMaxMB` deferral warning marked DISCHARGED. **NOT FLASHED** — the operator flashes to start the validation soak. | [2026-08-14-fw14-ring-leak-fix](2026-08-14-fw14-ring-leak-fix.md) |
| 2026-08-14 | resolved | bridge-temporal-trustworthiness S3 | **S3 — the ring leak MEASURED, at 97 % of a lap, and the stale-install near-miss that nearly cost the conviction** — FW 13's `RING_DIAG` on a bridge aged **4.01–4.04 h** (bag `2026-08-14_18-18-59`, **92 samples**) reports `leak_jb` **247–248** (`true_depth_jb` 247–248 against `avail_reported_jb` **0**, hwm **249** ≈ **97 % of the 256-slot lap**), `leak_bb` **1**, `leak_cone` **0** — the traffic-scaling the collision-rate prediction requires — with `fifo_overflows` **0** everywhere, i.e. **pure software-ring stranding, no peripheral loss**. Two independent cross-checks agree: echo→exec aged−fresh **115.4 ms** lands inside the predicted **114–135 ms** one-lap band, and 256 ÷ ~90 slots/h ≈ **2.84 h to saturate** retro-explains the July plateau (3.8 h → 252 ms, 28 h → 283, 63 h → 290–340 = **saturated by hour three**, never still climbing). **Root cause of the whole 2026-07-18 arc established: one missing IRQ guard around the vendored FlexCAN_T4 ring pop.** The conviction was nearly lost first pass — the node ran a **stale `install/` dated 2026-08-12** (zero `ring_diag` references) while `BRIDGE_FW_CHECK` printed **OK**, being structurally blind to install skew; recovered same-day without re-aging (`colcon build` + relaunch does not reboot the Teensy). Code product: the **install-skew self-check** (own-`__file__` hash vs the repo source, three verdicts, `/link_status` `install_skew` row, advisory-never-gating). Two residuals recorded honestly for the FW 14 pass: the lag integral's absolute value (151→183 ms, ~0.35 ms/s creep) and the SDO RTT floor's mispairing under pipelining. Next: FW 14 (acceptance `leak ≡ 0` + aged-soak lag ≤ 20 ms) → the alarmed monitor → the 2026-07-24 contract closes. | [2026-08-14-s3-conviction-ring-leak-measured](2026-08-14-s3-conviction-ring-leak-measured.md) |
| 2026-08-14 | resolved | bridge-temporal-trustworthiness S3 prep | **FW 13 — the additive `RING_DIAG` uplink: true ring depth against the leaking `_available`, plus the `/robot_state` staleness gate** — `MsgType` **0x92**, 103 B, `PROTOCOL_VERSION` stays **5**, written and compiled (`pio run -e teensy41`, 2026-08-14, SUCCESS) but **NOT flashed**: the operator's flash *starts* the ~3–4 h powered-idle S3 conviction soak (protocol + decision rule in the plan § S3). Two patches on the verbatim FlexCAN_T4 copy (vendor `fef2df5`, `PROVENANCE.md` § Local patches) — **P1** a read-only ring probe reading head/tail first and `_available` last, so a concurrent push can only *under*-report the leak, exposed as `rxRingProbe`; **P2** exact FIFO overflow/warning counters at the ISR IFLAG bit-6/7 clear site — while the `_available` defect itself is deliberately left unfixed so FW 14 fixes against a number. The frame carries, at 1 Hz from `task_telem`: per-bus `true_depth` + `avail_reported` probed **immediately** after the drain loop, where `_available` is 0 by definition so `true_depth` *is* the stranded leak and **`true_depth − avail_reported` is the conviction number**; `leak_hwm`/`true_depth_hwm` maximised per 1 kHz service tick over a `probe_ticks` denominator; FIFO overflow/warn counts; a wrap-aware delivery-lag integral (16-bit capture stamps into a 64-bit arrival clock, reseed + counter + flag on ≥50 ms decode gaps against the 65.536 ms wrap, plus an audit-added physical-cap reseed at \|lag\| > 200 ms — one ring lap is ~135 ms, so beyond that is unwrap corruption, not physics); and SDO round-trip min/max/last/count around the existing 50 Hz `gpio_poll`, the causal cross-check where aged-minus-fresh RTT *is* the ring delay. Jetson: `/ring_diag` `DiagnosticStatus` (queue → 1 Hz drain), WARN at leak ≥ 2, bagged; `EXPECTED_BRIDGE_FW_VERSION` 12→13. Shipped alongside, Jetson-only and needing no flash, the `/robot_state` **honesty fix**: the 100 Hz timer republished a latest-wins telemetry latch with no staleness gate — the source of the **~97 % freeze artifact** in the S2 bags — and now skips when the telemetry generation hasn't advanced *and* the link is up, **bounded at 50 consecutive skips ≈ 0.5 s** because telemetry and heartbeats come from separate firmware tasks, so "link up" does not prove telemetry flows; skips counted on `/link_status` (`robot_state_stale_skips`), with the consumer sweep finding nothing cadence-sensitive (`trajectory_node`'s 500 ms arrival gate fails safe; GUI quiescence counts `/leg_setpoint_echo`, which publishes *before* the gate, pinned by test). Audit fixes applied pre-commit: the suppression bound + its double-failure test, the WARN-threshold skew direction corrected (the probe under-reports), the refuted ±1-self-corrects drain comment annotated in place in `can_buses.cpp`, the lag physical cap, anchor-text doc cites, the 0x92 wire-id collision note on the lead-clamp draft, and the honesty fix de-double-booked from FW 14 in four documents. Instrumentation only — nothing in the bridge reads the new accumulators. | [2026-08-14-fw13-ring-diag-conviction-instrument](2026-08-14-fw13-ring-diag-conviction-instrument.md) |
| 2026-08-14 | resolved | bridge-temporal-trustworthiness S2/audit | **The S2 endgame, the freeze-structure forensics and a targeted concurrency audit converge — FlexCAN_T4's one-way `_available` leak turns the CAN RX ring into an uptime-ratcheting delay line** — the S2 decision rule answered **NO**: per-axis cache age does not grow with uptime (p95 **11.05 ms at 27.96 h**, *below* the 1.05–1.30 h bags at 18.05–18.07 and indistinguishable from a 9.4 s fresh boot) while the same bag carries **282.9 ms** e2e lag against **15.9 ms** fresh, `enc_frames` reads exactly **100.0 fps/axis everywhere**, and `/clock_diag` shows recover-slew/extrap ≈ 0 with interp at exactly **500.0 Hz** at 28 h — **FW 12's own instrument refuted the S1 cache-staleness mechanism** (and the identical-firmware reflash that cleared the degradation closes S1's Arm C flash-vs-reboot confound). Onset forensics: **no command step** (median \|cmd−enc\| 0.0034 rev), the encoder follows at **7 %** of commanded slope aged vs **88 %** fresh at **identical current** (2.71 vs 2.65 A), and the leg-0 trace is the mechanism in miniature — `pos_estimate` bit-identical for 11 frames (~100 ms) while the executed command walks to `frozen − 0.100` and **stops**, i.e. the clamp converts an anchor fault into a **commanded stop per move onset**. Freeze structure: **no** duration quantisation, **no** node-id clustering, and S1's **per-axis independence REFUTED** — ≥3-sample freezes are multi-axis at **193×** the independent expectation, with `enc_frames` window deltas 99–101 straight through ≥100 ms aged stalls (fresh's rare long stalls show the **opposite** signature, −47 frames at 63 ms age). **The audit (compiled-assembly verification on the project toolchain, not argued from source) convicts a named defect:** `FlexCAN_T4::events()` pops the RX ring **before** `NVIC_DISABLE_IRQ`, so the consumer's non-atomic `_available--`/head RMWs race the CAN ISR's `_available++` **one-directionally** ⇒ `_available` monotonically under-counts ⇒ the drain exits with true occupancy `D > 0` ⇒ every delivery is `D` frames late; `D` **ratchets** (~1e-5 collisions/pop × ~1.6e8 pushes/day ≈ 90 slots/h ≈ **40 ms/h**, matching the July curve's early ramp) and **caps at one ring lap — 256 slots ≈ 114–135 ms**, the mod-512 `head ^ 256` full test **proving** stale-lap re-reads, duplicates and replays impossible: **frames arrive exactly once, in order, late.** The mechanism **predicts every counter's blindness** rather than contradicting it — `depth_hwm`/`cap_hits` derive from `_available` itself, cache age is stamped **at decode** (downstream of the delay), `enc_frames` counts deliveries which a delay **conserves** — leaving only the lead clamp and `MAX_DEVIATION`, the two consumers that read delayed *content*, able to see it. Also: the `can_buses.cpp` drain comment (above `CAN_RX_DRAIN_BUDGET`) is **refuted** (the ±1 is a **monotone** leak; its near-empty-ring evidence was circular), four secondary finds (ring-full pop `memmove` can **tear** a frame past both decode guards; FIFO overflow `IFLAG`s cleared uncounted; `writeIFLAGBit` W1C footgun; no NXP errata workarounds; `isEventsUsed` never flips back), and a **large Jetson artifact** — ~**97 %** of the S2 "bit-identical freezes" are manufactured by the aged uplink arriving in ~20 ms **pairs** (31 % paired drain ticks aged vs 2.6 % fresh, 12× uptime-dependent) hitting `/robot_state`'s latest-wins latch behind a 100 Hz ROS-clock timer with **no staleness gate**, which is inherently multi-axis and explains the 193× co-freeze; **S1's "arrival cadence is clean" exoneration was measuring that ROS timer, not the data** (method correction #4 of the arc; the transport exoneration itself stands). Residual ~19 long runs (95–739 ms) are a **physically stalled plant** (clamp-commanded stop + the S1 binding signature, leg 0 at 20.53 A) — a consequence, not a cause. **Honest residual recorded:** aged 283–340 ms **exceeds** the 135 ms cap; clamp-drag amplification is an argument, not a measurement. **D3 gate for the lead-clamp proposal: no viable `T_freeze`** (margin 0.49× at 30 ms, 0.82× at 50 ms against a 60.7 ms conservative healthy hold at speed; quantum 2⁻¹⁹ rev rules out quantisation) — "detects but does not protect"; salvage = the **velocity-extrapolated anchor** (over-budget freezes 0.160 → **0.000** fresh) and a **T=100 ms/0.67** *reporting* threshold (0.28/min healthy vs 4.75/min aged, 17×). Closure: **FW 13 `RING_DIAG` 0x92** on the vendored library (`fef2df5`, verbatim, `PROVENANCE.md`) — true ring occupancy, so `true_depth − avail_reported` **IS** the leak — convicted by **one ~3–4 h motionless soak**, then FW 14 (fix the pop's bookkeeping) closes the 2026-07-18 contract (the Jetson honesty fix — the `/robot_state` staleness gate — landed with the FW 13 change-set). **`status: resolved` scopes the LOCALIZATION;** the parent 2026-07-18 entry **stays open** (addendum added), and the S1 entry carries a correcting addendum. | [2026-08-14-ring-audit-available-leak-delay-line](2026-08-14-ring-audit-available-leak-delay-line.md) |
| 2026-08-12 | resolved |  | **Fleet-wide leg-ODrive config reset tool — batch erase + reflash that preserves CAN node IDs** — `tools/odrive_fleet_reflash.py` does in one unattended pass what has been a per-drive `odrivetool` ritual (erase in the shell, `restore-config`, re-set `axis0.config.can.node_id`, save), for the standing quirk that this machine's ODrives occasionally need a config reset — one cured the 2026-08-10 leg-0 `SPINOUT_DETECTED` — and a bare reset reverts every tuned value. Records USB-serial → node-ID to `fleet_map.json` **before** anything is erased (an erased drive reports the firmware default, so the association is otherwise unrecoverable from hardware), backs each drive up in `odrivetool backup-config` form, erases + reboots, restores the leg snapshot **with that drive's own node ID already patched into the restore dict** — not fixed up in a second save, which would otherwise commit node 0 to NVM on all six — saves, reboots again, then verifies every property, the reboot being what makes the verify evidence about NVM rather than about the RAM the restore just wrote (an armed axis refuses `save_configuration()` silently). Node 6 is recorded and never written; leg/protected sets come from `protocol_config.yaml`, not a literal. Per-motor calibration (`index_offset` 0.47440463 `_valid: true`, measured phase R/L) is flashed **verbatim** — owner's call: the next index search regenerates it, so **home every leg before arming**. Refuses rather than guesses on duplicate node IDs (the post-interrupt state; `--map` resolves by serial instead), unrecognised node IDs (a BallButler drive at 7/8 must not take a leg config), a target whose `commutation_encoder` ≠ INC_ENCODER0 (that is the hand snapshot), and a live `teensy_bridge_node`/`odrivetool`/GUI holding the drives. 41 unit tests on the pure decision logic with both real snapshots as fixtures; **no hardware path exercised** — nothing was on USB, so first use is `--dry-run`. **Addendum, same day:** owner asked for `FULL_CALIBRATION_SEQUENCE` + a final save per leg, which closes the loop on the verbatim-flash decision (each drive then measures its own values); the config verify deliberately stays *before* the motor turns, since the calibration spins using the values just flashed. Finding that changed the design: the snapshot's default `calib_scan_distance` **8.0 rev** is **~564 mm of leg travel** through `GEOM_MM_TO_REV` against a `GEOM_LEG_STROKE_MM` of **280 mm** — **2.0× the stroke** at ~141 mm/s — and the offset scan is **open-loop**, so nothing in the ODrive stops it at end of travel; the tool refuses to actuate unless the scan fits, naming `--calib-scan-distance` (≤3.95 rev) or `--force`, with the guard *ahead of* discovery so the refusal is instant. A calibration failure aborts the run and states the hazard: NVM then holds the snapshot's commutation offset **marked valid** while that drive's own calibration never ran. Also fixed a **red gate not of this tool's making**: a mid-session re-capture of the snapshot (odrivetool 0.6.10 writes `backup-config` **flat**, the committed one was nested, 16 values moved) broke two `test_leg_torque_ff.py` pins that indexed `cfg['axis0'][...]` — `KeyError`, i.e. a failure saying nothing about the `torque_constant` / `input_torque_scale` values being pinned; fixed at altitude with a path-keyed `_flashed_odrive_value()` helper, verified against both shapes. 57 tool tests + 54 torque-FF tests. | [2026-08-12-odrive-fleet-reflash-tool](2026-08-12-odrive-fleet-reflash-tool.md) |
| 2026-08-12 | resolved | bridge-temporal-trustworthiness S2 prep | **FW 12 — the additive `CACHE_DIAG` uplink: per-axis cache-age floor/peak, per-axis encoder-frame counters, and the RX-ring occupancy that has been on-chip since 2026-06-04** — `MsgType` **0x91 (145)**, the first id **above** `RPC_RESPONSE`, opening the new uplink block per the FW 11 generator note; `PROTOCOL_VERSION` stays **5**; 129 bytes. Written and compiled, **NOT flashed by this commit** — the operator flashes it to *start* the S2 soak, and the flash resets the aged state, which is now the intended `t=0`. Payload: `t_local_us` (`micros64` — bridge uptime as the independent variable, never the steppable wall), per-axis `age_min`/`age_max` `u32[7]` sampled task-side at the telemetry rate through the existing `snapshot_pos_vel` seqlock (zero ISR work, no IRQ masking — which makes "instrumentation only" *structural*; ages saturate at `u32` max, never wrap), per-axis `enc_frames` `u32[7]` cumulative counters incremented **after** `write_pos_vel` in `decode_into_cache` (invariant: counter advanced ⇒ cache write completed, so a stalled value with an advancing counter puts the fault at/above the wire and a paused counter puts it upstream — the ODrive-silent-per-axis vs cache-stall split S1 could not make), rx `cap_hits` `u32×3` + `depth_hwm` `u16×3` (the 2026-06-04 drain fix's on-chip observability, never uplinked until now), `decode_short`/`decode_bad_axis`, `seq`/`window_us`/`samples`/`seen_mask`. Emitted 1 Hz from `task_telem`. Jetson: `teensy_link` re-exports, bridge node publishes `/cache_diag` as a `DiagnosticStatus` on the `clock_diag` queue/drain pattern (level keys on the age **floor**, `CACHE_AGE_FLOOR_WARN_US` = 20 ms; ages `n/a`-blanked for unseen axes but `enc_frames` deliberately raw — **zero IS the measurement**), `/cache_diag` bagged, choreography regenerated, `EXPECTED_BRIDGE_FW_VERSION` 11→12 (advisory skew string `11 (SKEW — expected v12)` until the flash). Two audit findings fixed pre-commit: a preemption window between the `micros64()` capture and the seqlock snapshot could return `ts > now` and wrap the age to ~2^64, painting the saturation rail into `age_max` — a spurious stale spike in the exact instrument the soak reads (fixed by clamping fresher-than-now to 0); and an axis whose first frame landed mid-window could emit `seen_mask` set with rail-valued extrema from earlier `ts == 0` folds (fixed by per-axis seeding). Recompiled after both. S2 soak protocol + decision rule live in the plan § S2. | [2026-08-12-fw12-cache-diag-instrumentation](2026-08-12-fw12-cache-diag-instrumentation.md) |
| 2026-08-12 | resolved |  | **The `MOTOR_FB_STALE` guard read a naked `u64` timestamp — tearable against the higher-priority CAN-RX writer, and unordered against it too** — `fault_machine.cpp` read `axes[i].pos_timestamp_us` with a bare `u64` load: two 32-bit reads on Cortex-M7, tearable when `task_can_rx` (priority 5, the writer) preempts `task_fault` (priority 3) mid-load, while **every sibling guard read in the same file already used `atomic_read_u64`**. A torn timestamp flaps the guard in **both** directions — false `MOTOR_FB_STALE` = ~100 ms of output suppression mid-motion; false-fresh = a real feedback freeze unnoticed for one 10 Hz tick. Fix: the read goes through `atomic_read_u64`, **and** the comparison gains a `now > ts` ordering guard — the audit caught that the atomic read alone leaves the other half of the hazard, since the writer can stamp *after* the loop's `now` capture and the unsigned subtraction then wraps past the threshold (a false one-tick trip). Same class as, and found by the same audit as, the `CACHE_DIAG` age-underflow fix. Pre-existing since the guard landed; surfaced by the FW 12 implementation agent while porting the read pattern. The heartbeat guards share the ordering half and are **deliberately deferred** to a follow-up (flagged, not fixed here). Ships in the FW 12 image — same compile, same flash. | [2026-08-12-fault-machine-staleness-guard-atomic-read](2026-08-12-fault-machine-staleness-guard-atomic-read.md) |
| 2026-08-12 | resolved | bridge-temporal-trustworthiness S1 | **S1 — the aged-bridge four-arm isolation: the uptime latency drift is Teensy-internal, and the surviving mechanism is a per-axis encoder-cache refresh-stall tail** — run by the operator at **62.9–63.1 h** of bridge uptime. **Only Arm C (Teensy reboot) restored the ≤20 ms class (15.8 ms)**; Arm 0 310 ms, Arm A (ODrive reboot) 340 ms, Arm B (`ip link` bounce) no fix, and the decisive control — link bounced, ODrives fresh, Teensy aged, zero ODrive errors — still **290 ms**. The 63 h points **extend** the July curve (no saturation at ~240 ms). Two hypotheses overturned: 2026-07-18's ranked **candidate 1 (Jetson-side transport) is dead**, killed by Arm B *and* by `udp_rtt_us` flat at 1–3 ms, the fresh bag as high as any aged one; and the mid-analysis **FlexCAN RX-ring backlog hypothesis was predeceased** — that exact defect was fixed 2026-06-04 (`e2b4cfb`, drain-to-empty, `CAN_RX_DRAIN_BUDGET=32`), so the whole July curve was measured *with* the fix active (lesson: read the drain code before hypothesizing about it). Exonerated with data: Jetson transport, Teensy→ODrive TX (`defer_jb`/`txq_jb` **zero in absolute terms** at 63 h in bags 1–3; bag 4 opens with a frozen 519/64 accrued during the ODrive power-cycle's bus-down window, in-bag delta 0), CAN wire errors (zero in-battery deltas), heap, interp (0 deadline misses, ≤3 µs jitter), CAN throughput, ODrive-internal accumulator, and stream-vs-RPC socket asymmetry (one `task_net` loop drains stream *before* rpc, `udp_link.cpp:144-162`). **Surviving signature:** median cache refresh 10.0–10.2 ms in *every* bag — only the tail degrades (frac >30 ms 9.15/17.75/16.43 % aged vs **4.29 %** fresh; p95 80–130 vs 30 ms; max ~500 ms), per-leg independent, no uniform shift recovers the vel-vs-dpos/dt decorrelation, and messages arrive on time (p99 ≤17.8 ms) — so the **value** stalls, not the transport. At 2.5 rev/s a 130 ms stale anchor hides 0.325 rev = **3×** the 0.100 rev lead-clamp budget, and the clamp pins from move **onset** (Arm 0: 30/49 moves clamped before motion; Arm C: 0/24). Method corrections recorded: exec→enc "140–147 ms" is **not** a latency (bimodal curves; clamp-pinned windows peak at ~0 ms), the "27–58 % identical samples" figure was a low-speed pooling artefact, and P0's `/leg_cmd_executed` **saturates as a discriminator exactly when the clamp pins** — so the P3 monitor must watch clamp duty + cache age, not lag alone. Remaining split (cache stall vs ODrive-silent-per-axis) needs FW 12's per-axis frame counters. The underlying issue **stays open** in the 2026-07-18 entry (addendum added). | [2026-08-12-s1-aged-bridge-isolation-teensy-internal](2026-08-12-s1-aged-bridge-isolation-teensy-internal.md) |
| 2026-08-11 | resolved | bridge-temporal-trustworthiness P1 | **P1 — FW 11 adds the `CLOCK_DIAG` uplink and the interp-occupancy census: written, compiled, NOT flashed** — an additive `CLOCK_DIAG` uplink, `MsgType` **0x8F** (the last free id below `RPC_RESPONSE` = 144), 49 bytes, `PROTOCOL_VERSION` **stays 5** (0x8D/0x8E precedent). Per anchor: `t_local_us` (pure-crystal x-axis), `jetson_wall_us` (the applied anchor, raw — the offset stays consumer-derived), `dt_local_us`, `rtt_us` (stored *before* `set_wall_anchor` so happens-before holds), `err_us` (the pre-slew diff, previously computed and discarded), `freq_ppb` (consecutive measurements, open-loop w.r.t. the IIR), `anchor_seq` (loss-visible), `flags` (STEPPED / FIRST_ANCHOR / FREQ_VALID) — plus the interp-occupancy census `interp_ticks` (denominator), `recover_slew_ticks`, `extrap_ticks` (Mode 2), cumulative ISR counters differenced at emit and never reader-cleared. **Where it runs:** `task_time_sync`, once per anchor (~30 s; 500 ms fast-retry), deliberately **off** the RX drain (the net task carries every SETPOINT; the handler contract is fast/non-blocking); the 64-bit divide is outside the servo critical section, `ClockAnchorSample` publish/read is whole-struct under its own PRIMASK window so a torn sample is impossible, `now_wall_us` untouched, and the 500 Hz ISR gains exactly three aligned-`u32` increments. **Jetson decode ships in the same commit:** `teensy_link` re-exports `ClockDiag`; the bridge node queues and drains at 1 Hz onto **`/clock_diag`** (`DiagnosticStatus`), added to the launch record list — a topic rather than `/link_status` KeyValues because these are independent per-anchor measurements for a multi-hour fit, not cumulative counters (the `/profile` vs `/bb/axis_estimates` split); an FW 10 board never sends 0x8F, so the topic records empty, pinned by test. **Not flashed, deliberately:** the board stays on FW 10 until after the S1 aged-bridge sitting, because a flash is a reboot and would reset the aged state. `FW_VERSION` 10→11 forces `EXPECTED_BRIDGE_FW_VERSION` 10→11 (pinned by `test_bridge_fw_version_xref`), so `/link_status` `bridge_fw_version` reads `10 (SKEW — expected v11, proto 5)` for the entire pre-flash window **including S1** — audit-verified ADVISORY everywhere (log + string surfaces only; no launch gate, arming precondition, RPC refusal or harness hard-fail consumes it; toss libs partition by it, never refuse). **Audit — one NOTE, fixed:** census reads were denominator-first, so an interleaving ISR tick could report `extrap_ticks > interp_ticks` (>100 % duty); numerators are now read first and the denominator last (the ratio can only round down), mirrored in the init baseline, recompiled after the fix. **Deviations:** no Mode-3 (velocity-decay) counter (the plan named only recover-slew + Mode 2; 4 bytes to add later if S1 analysis wants it), and `interp_ticks` deliberately counts early-return ticks — a true tick census, hence a starvation detector. | [2026-08-11-p1-fw11-clock-diag-written-not-flashed](2026-08-11-p1-fw11-clock-diag-written-not-flashed.md) |
| 2026-08-11 | resolved | bridge-temporal-trustworthiness P2 | **P2 — the TOD responder returns the MIDPOINT of the kernel RX stamp and a just-before-send stamp** — the Jetson-side responder now puts `(t2 + t3) // 2` in the existing `jetson_wall_us` field: `t2` the **kernel** RX timestamp (`SO_TIMESTAMPNS` ancillary data, numeric constant 35 with a runtime probe), `t3` a userspace stamp taken just before send. Wire-compatible — no firmware change, no `PROTOCOL_VERSION` bump. **Why the midpoint and not kernel-RX alone:** it cancels the Jetson scheduling-jitter term `p` *exactly*, where kernel-RX-only stamping — the clock plan's original sketch — would merely have flipped its sign (`+p/2` → `−p/2`), trading one bias for another; what remains is pure path asymmetry `(df − dr)/2`, addressed later by min-RTT gating (arc P4). **Transport:** `teensy_link/client.py` arms the **RPC socket only** (stream path untouched), `recvmsg` with a 32-byte control buffer, three-state `rx_stamp_mode` `'probing'` → `'kernel'` | `'userspace'` — the mode is confirmed by a real packet carrying a parseable stamp, **not** by `setsockopt` succeeding, and the degrade latches behind a one-shot WARNING; plumbing is opt-in (`subscribe(..., with_rx_stamp=True)` / `register(..., wants_rx_stamp=True)`) so all ~40 existing 4-arg callers are unchanged (audited). **Observable fallback:** `tod_server.py` carries a per-query `stamp_mode` (`'unknown'` | `'kernel-midpoint'` | `'userspace'`) that recovers on the next good stamp — deliberately *not* the client-side latch, so a transient NTP step doesn't condemn the session — plus a plausibility guard (`0 < t2 <= t3`, delta ≤ 1 s) with an `implausible_rx_stamps` counter, and the bagged `/link_status` carries both as `tod_stamp_mode` and `tod_implausible_rx_stamps`: a silent fallback would reinstate `+p/2` invisibly, the exact "invisible until degraded" class this arc closes. **Audit findings fixed pre-commit:** `t3` was read at handler entry rather than just-before-send (WARNING — restructured so the guard runs on a provisional read and the final `t3` is re-read after all bookkeeping and logging, leaving only pack + encode + `sendto`, ~tens of µs, uncancelled); the userspace→kernel *recovery* direction was implemented but unpinned (the degrade test now drives it); the rejection counter was not bag-visible (KeyValue added). `SO_TIMESTAMPNS` was probed live on this box 2026-08-11 (kernel 5.10.104-tegra, system 3.8.10 and venv): cmsg (level=1, type=35, 16-byte timespec) tracking `time.time()` to ~100 µs. `teensy_link/` is live at the next relaunch (repo-root `PYTHONPATH`); the `/link_status` keys need `colcon build --packages-select jugglebot` + relaunch — both must be live before the S1 aging window. | [2026-08-11-p2-tod-midpoint-kernel-timestamp](2026-08-11-p2-tod-midpoint-kernel-timestamp.md) |
| 2026-08-11 | resolved | bridge-temporal-trustworthiness P0 | **P0 — the Teensy's executed leg command is echoed to ROS and bagged alongside `/profile`** — `teensy_bridge_node` subscribes the existing 100 Hz LEG_CMD uplink (MsgType 0x88; the post-lead/stroke-clamp *executed* leg command, stamped with the bridge wall clock) and republishes it as `sensor_msgs/JointState` on **`/leg_cmd_executed`** — RX-thread callback queues under the node lock (bounded 4000), executor-side 100 Hz timer drains and publishes, mirroring the `bb/axis_estimates` pattern (determinism rule: no DDS work on the socket thread). The launch rosbag record list gains `/leg_cmd_executed` **and** `/profile` (1 Hz firmware profile with `udp_rtt_us`/`udp_jitter_us`), closing two of the three telemetry gaps named by `2026-07-18-teensy-uptime-tracking-degradation` — the third (recover-slew/extrapolation occupancy) is FW 11, phase P1. A single degraded bag can now split transport vs interp vs ODrive, and RTT-vs-uptime is bagged instead of watched live. Landing this does **not** reset the bridge's aging (`colcon build` + relaunch only), which is why P0 precedes the S1 aged-bridge sitting. **Two discoveries:** `tests/ros/conftest.py` claimed `sensor_msgs` was mocked and never was — with no `Header` on the mocked `std_msgs`, a real `JointState` raised `ImportError`, so `_publish_bb_axis_estimates` was never executable under the harness until now (`MockHeader` added, docstring corrected); and `/leg_setpoint_echo` carries no header stamp (bare `Float64MultiArray`), so bag joins against it lean on rosbag receive time — exactly what a transport drift corrupts — flagged for P3's monitor design, deliberately not fixed here. Needs `colcon build --packages-select jugglebot` + relaunch, live before the S1 aging window opens. | [2026-08-11-p0-leg-cmd-echo-profile-bagged](2026-08-11-p0-leg-cmd-echo-profile-bagged.md) |
| 2026-08-11 | in-progress | bridge-temporal-trustworthiness — plan kickoff (pre-P0) | **Bridge temporal trustworthiness arc opened — the uptime latency drift finally gets an owner, and the clock plan gets sequenced behind it** — planning only, no code: `plans/archived/bridge-temporal-trustworthiness.md` created, taking ownership of the can-bridge uptime command-latency drift (until now owned solely by the open `2026-07-18-teensy-uptime-tracking-degradation`) and sequencing the dormant `plans/active/bridge-clock-frequency-discipline.md` clock-precision phases behind it; `plans/active/INDEX.md` row + a sequencing status note on the clock plan land in the same change-set. **Why now:** the drift has spread past leg tracking — 2026-07-28 measured it in the throw dispatch shift (+14.5 ms at 0.24 h uptime → +54–63 ms at ~1.6 h) and 2026-08-10 sized the possession ARRIVAL windows *around* it rather than around a fresh-boot plant — the operator wants the Teensy able to stay up indefinitely with zero drift, and the four-arm isolation experiment pre-registered 2026-07-18 has still never run. **Owner decisions (harrison, 2026-08-10/11):** route **B then A** (telemetry before experiment), both halves in one arc, and **S1 (the aged-bridge sitting) runs BEFORE the FW 11 flash** because a flash reboots the Teensy and resets the aged state — the reboot-before-every-session rule is suspended for that one sitting. **New finding recorded in the plan:** the TOD anchor's `rtt/2` compensation assumes path symmetry, so a transport-side latency drift can bias the CLOCK by up to half the one-way delay — the two "complementary halves" are coupled, hence clock servo (P4) after latency fix (P3); and kernel-RX stamping alone flips the sign of the server-processing jitter term rather than removing it, so P2 returns the midpoint `(t2+t3)/2`. Phases: P0 LEG_CMD echo + `/profile` bagged (Jetson-only, preserves bridge aging) → P1 FW 11 `CLOCK_DIAG` + interp-occupancy counters, written NOT flashed → P2 midpoint TOD stamp → S1 aged sitting + four arms → P3 fix + the alarmed latency monitor from the 2026-07-24 closure contract → P4 clock plan Phases 3–5. | [2026-08-11-bridge-temporal-trustworthiness-kickoff](2026-08-11-bridge-temporal-trustworthiness-kickoff.md) |
| 2026-08-10 | in-progress | toss-selftuning build (Phases 2a-2f) | **Toss self-tuning loop — the build** — one shared entry for the six-phase build of `plans/active/toss-selftuning.md`; a section per phase. **2a — the instrument, zero new control authority**: pure `jugglebot/toss_record.py` (`FIELDS` + drift guard, `encode`/`decode`/`validate`, `label_from_sensor`, `latch_announced_ball` extracted verbatim from the coordinator, `join`), a `/toss/record` JSON declaration published at the single authoritative outcome line plus a best-effort JSONL belt, `tools/probes/toss_record_miner.py` (two-pass bag read, `--self-check`, `--emit-fixture`), and **THE ONE bag-record list** (D18 — the launch list becomes the union and the runbook says `record:=true`, retiring eight per-section append instructions). Acceptance met: the miner reproduces the hand-mined census of `2026-08-10_16-30-44` — **39 departures / 38 catches / 3 quick-drops** — from a bag that predates every line of the phase, every row `mined-only`. **Three design assumptions failed against the bag**: the plan's catch-search window (`CATCH_CONFIRM_WINDOW_S` 0.70 s) relabels the +798 ms arrival — the population maximum that SIZED `JB_BD_ARRIVAL_WINDOW_S` — so the shipped windows are used and injected; the debounce lag is **asymmetric and 241 ms** on the departure edge (0 ms on the catch edge) against the plan's ~100 ms estimate, i.e. larger than the uptime dispatch shift the fresh-boot discipline exists to control; and the sensor poll runs at **p50 71 ms** against a configured 20 ms, which makes § 3.7's timing-fit gate unsatisfiable (OPEN, 2c owns it). Fourth finding: the reference sitting carries **no unlabelled ball marker** near the cup, so it cannot support an aim fit at any record-list completeness — a capture precondition the plan does not yet check. **2b (2026-08-11) — the map, APPLIED AT ZERO**: pure `motion/toss_cal.py` in the C-LEVEL-2 loader shape (marker-search candidates, authoritative env override, source tree before ament share, all-or-nothing validation, hull-clamped lookup, `map_version` over float-normalised APPLIED numbers only), the `toss_release.aim_target_offset_mm` aim-angle → virtual-target conversion, ONE lookup per goal in `_build_toss_cycle`, `toss/reload_calibration` + a latched `toss/calibration_status`, and the three `tier == TIER_8B` branches re-keyed on *the commanded release carries a non-zero tilt* — so `catch/pretilt_hold` now rises for ANY aim, tier-independent. Three decisions worth the Discussion: `aim_rad` stores the **commanded** tilt, not the measured bias, so the runtime path contains **no sign flip at all** and every sign question collapses into 2c/SC-0; **two release states**, because D4's uncorrected announcement cannot be faked by overriding one field (the velocities would still carry the aim and the receive tilt would move) — and at zero aim they are the SAME OBJECT, so the disabled path costs not one float op; and the status fields ship on a latched JSON topic rather than `TrajectoryStatus`, whose publisher does not own this map. Probe-first: the virtual-target round trip reproduces a commanded aim to **8.35e-13 rad** and the production gain is **54.578 mm/deg at h = 0.78** vs the design's idealised 4h = 54.454. **2c — the fit, the analyser and the offline closed loop**: `tests/hardware/toss_fit_lib.py` (pure core: the partition rule that refuses to pool two plants, the fixed-point reduction, the D15 thin-node rule, the flat-field and ball-actually-flew guards, the document validated through the production loader), its thin CLI `tests/hardware/toss_cal_fit.py`, and `tools/toss_cal_analyse.py`. **The design's reduction had a sign error and this phase caught it offline**: `b = A + S⁻¹·land_err/4h` evaluates to `2A + ψ` and would have commanded the machine to tilt INTO its own error; the fixed point is the minus. The sign is now never written down — the Jacobian is finite-differenced out of the production apply path, which immediately showed `S = [[0,1],[−1,0]]`, a **90° rotation, not the identity everybody's first intuition writes down**. Acceptance is a true closed loop (inject a known bias → real fit → replay through `toss_cal.lookup` → 8+ mm becomes < 1 mm; a sign flip is pinned to fail by >1.8× the uncorrected error). Also re-derives the timing gate the design handed to this phase: the measured departure-shift sd (20.51 ms) **is** the poll quantisation Δ/√12 (20.50 ms). **2d — auto-reload, the reopened retry, and a covariate that does not fit**: the first phase of this build that commands motion. `TossContinuous` gains `on_empty_cup` (IDL default `"STOP"`, re-applied by a WHITELIST — anything not exactly `RELOAD` is STOP, because the failure being prevented is a real ball thrown by a field nobody meant to set) and `max_reloads` (0 ⇒ config 3, negative ⇒ refused BY NAME), and one motion-bearing interlude entered from `REJECTED_NO_BALL` ONLY — the one toss terminal where the FSM provably commanded nothing. Every rung is an existing validated mechanism (`go_home`, then the shipping `ReloadSequencer` driven through the node's own `_step_sequence`) and every refusal names itself: `STOPPED_BALL_EVIDENCE_DISABLED` / `_BB_NOT_READY` / `_BB_UNVERIFIED` / `_SENSOR_UNKNOWN` / `_CUP_NOT_EMPTY` / `_RECENTRE_FAILED` / `_RELOAD_BUDGET` / `_FLOOR_CLEAR_REQUIRED`. **Three findings the design did not have.** (1) **`THROW_ABORTED_NOT_SETTLED` was identifiable but not OBSERVABLE** — `bb/throw_at_target` is fire-and-forget, so the firmware's terminal `CMD_RESULT` only ever reached a log line and a BB-not-positioned abort was indistinguishable from an ordinary MISSED; § 3.9's either/or therefore had a third option, and `ball_butler_node` now relays the outcome on `bb/throw_outcome` so the retry keys on a NAMED firmware code (the rejected proxy — 'no ball was ever tracked' — also fires on every tracker failure, which is a different fault with a different remedy). (2) **§ 3.10's Layer-1.5 read budget does not fit**: the QUIESCENT dwell is `dwell − throw_delay − verdict latency` ≈ **0.7 s at the shipped 6.0 s cadence**, not 6.0 s, because everything after `landing + dwell − throw_delay` is the next cycle's own countdown — so 8 reads at 0.15 s become 1–2 and `dwell_tilt_degraded` says so. Reading inside the cycle was rejected: it would convert 'reads never overlap PREPARE→THROW' from a STRUCTURAL guarantee into a conditional one, on a service that blocks the Platform-Teensy loop streaming hand moves. D17 cannot be answered from a 6.0 s-cadence corpus. (3) the session completion test moved from `cycle_index` to `throws` — **behaviour-identical for every pre-2026-08-11 session**, and it is what makes a drop cost a reload rather than one of the operator's data points. Also: the recentre pad is MEASURED (15,409 `/trajectory/status` gaps across five 2026-08-10 bags — median 0.200 s, p99 ≤ 0.252 s, worst 0.643 s ⇒ 1.5 s pad), the sensor rung ships TWO codes because SEATED at that gate is a contradiction rather than blindness, and `_execute_reload` was deliberately not refactored (the mechanism is shared through `_step_sequence`; only the goal-handle loop shell is not). **2e — the session trim, and the measurement it does not have**: `jugglebot/toss_trim.py` lands layer 2 — the RAM-only, per-goal COMMON MODE the home-referenced map deliberately leaves out — with the shrinkage mean (`n₀ = 4`), per-axis significance + deadband gates, magnitude `STEP_MAX`/`TRIM_MAX` clamps, admission guards G1–G11, a two-sided CUSUM freeze, CONVERGED/STALLED and **freeze-never-zero on every guard path**; the affine model is structurally REFUSED on a rank-deficient geometry rather than regularised. The reduction, the Jacobian and the three `admit_for_*` filters **MOVED out of `tests/hardware/toss_fit_lib.py` into the production package** and are imported back — D11's argument one layer up, because an online trim and an offline fit that disagree about what a toss reduces to would fit the promoted map on a different population from the one the machine learnt on and nothing would surface it. **The phase's central finding is why `toss_trim_enabled` defaults FALSE**: § 3.6's measurand `land_err_mm` is a MINED field, the node has no producer of it, and both live candidates (`TossResult.catch_error_mm`, `BallState.landing_position`) are D5's forbidden dead-reckoned lineage — so the seam is built and the measurement is refused BY NAME (`no_mocap_fit`), the trim stays WARMUP and commands exactly zero, and `toss_trim.replay()` closes the loop offline. Four design constants amended against probes: the `2·se` gate commanded a trim in **45.7 % of NOISE-ONLY sessions** (a sequential multiple-comparison problem the one-shot `se = σ/√(n₀+n)` arithmetic cannot see) so it is 2.5·se confirmed over three updates at n ≥ 6, chosen on the measured expected residual error in mm; G8's `3·se` is unreachable (0.75σ at n = 16) and is re-derived as a tabular CUSUM `k = 0.5, h = 8.0` — 2 % false alarm, 99.7 % detection of a 2σ shift within six tosses. Four bugs the property tests found, each a working program that silently does the wrong thing: the deadband belongs on the ESTIMATE not the step (on the step it opens a 0.10–0.15° dead zone in which **no correction can ever be commanded** — a real 0.12° bias produced nothing), CONVERGED must not fire while SATURATED, must not LATCH (the shrinkage estimand moves as the prior decays, so a session warm-started from a disagreeing anchor freezes at zero passing through it), and a guard freeze must override it. And a finding for the first sitting: at the `level` common mode the trim was SIZED on (0.069–0.097°) it is a **WASH** at σ = 20 mm, and at zero bias it COSTS ~1 mm — the value proposition turns on an arrival scatter that has never been measured on this machine. **2f — the acquisition tool, and three gates that could not be met**: `tests/hardware/toss_cal_grid.py` drives § 3.8's four rungs by SENDING TOSS GOALS AND OBSERVING — no arming, no mode change, no hand command, zero publishers, pinned by a set-equality manifest over every `create_client` / `create_subscription` / `ActionClient` site. All nine preflight refusals R1–R9 are hoisted into ONE pure function over an observation dict, so each is unit-tested without a robot; node exhaustion (`STOPPED_RELOAD_BUDGET`) marks the node thin/stale and SKIPS; the wire-DISARMED check is re-run between goals OUTSIDE the per-goal `try`; the probe map is restored BEFORE the return-to-centre, both under a `BaseException` guard. **The phase's central finding is that three of § 3.8's four gates, taken literally, refuse a HEALTHY machine most of the time** — measured over 20 000 Monte-Carlo trials per cell at the design's own working σ = 20 mm/axis: the literal SC-0 gate passes a perfect plant **32.6 %** of the time (its ±25 % band is 1.08 se wide at n = 5, δ = 0.5°) and the literal SC-3 gate passes a perfect map **8.0 %** (per-pose se 8.94 mm against a 10 mm bound). Raising n costs 186 tosses against a 129-toss capture; raising δ to 1.0° displaces the landing 54.6 mm against an 80 mm reach envelope and the rung eats its own reload budget. So every gate is now THREE-VALUED and refuses on EVIDENCE (FAIL only when the 2·se interval EXCLUDES the bound, PASS only when it is wholly inside, INCONCLUSIVE reported with the resolving n) — and nothing they exist to catch is weakened: a sign flip leaks **0** trials under both forms. Two more the design did not have: **SC-1 cannot decide the map's UNITS at any sample size that fits a sitting** (the exponent CI's median width is 4.47 against a 0.6-wide rad band and lands inside a branch 0.1 % of the time), so its real product is the first honest σ_L; and **SC-0's accept test had to move into the PREDICTED Jacobian basis**, because 2c measured S as a 90° rotation and the design's literal "diagonal"/"off-diagonal" wording is inverted on this plant. Capture and scoring are two invocations with a rung LEDGER between them — `land_err_mm` is mined, so a capture cannot score itself, and the ledger is what turns "SC-0 BLOCKS everything" from a sentence in a plan into a refusal in code. **INDEPENDENTLY VALIDATED 2026-08-11** — every acceptance instrument re-derived rather than trusted: the miner reproduces the reference bag's hand-mined census (**39 departures / 38 catches / 3 quick-drops**) and passes 22/22 bag-free self-checks; the zero-bias bitwise identity and the injected-bias sign recovery both hold; all four rung dry-runs construct **zero ROS objects under a sabotaged `rclpy`**; both packages build and the install tree matches source for all nine touched modules; and the eight safety invariants were traced IN CODE (`toss_sequencer.py` is byte-untouched across the whole build, so the 3.5 s floor stands; `toss_trim.py` contains no file I/O at all, so the trim structurally cannot persist). **One defect, test-side**: three test modules drove a goal terminal without patching `_RECORD_BELT_DIR`, so ordinary suite runs had appended **468 `toss_records_*.jsonl` + 175 `*_trim_proposal.yaml`** into the operator's `temp/logs` — a fixed shared path under a four-worker gate, and worse, artefacts byte-indistinguishable from a real session's sitting in the exact directory the 2f capture workflow mines. Isolated by an autouse fixture whose companion `real_record_belt_dir` fixture keeps the marker test asserting the PRODUCTION-resolved value instead of passing vacuously against the redirect. **AUDITED 2026-08-11 — NOT CLEAN: eleven findings (2 HIGH / 6 MEDIUM / 3 LOW), of which SEVEN landed as fixes the same day**, each with a named regression test where there was behaviour to pin. The HIGH that mattered most was a *scheduling* bug hiding inside an *accounting* branch: the `ABORTED_NO_RELEASE` retry returned from `note_cycle_result` without touching `_next_cycle_at`, so it started on the very next FSM tick — immediately after a SAFE_ABORT ladder that had merely DISPATCHED the retract and the go_home on service acks. Fixing it reframed the constant it borrows: `DEFAULT_SESSION_MISS_CLEANUP_S` is named after the MISS but derived entirely from the LADDER (`CATCH_CONFIRM_WINDOW_S + GO_HOME_DURATION_S + 2 ticks`), so the invariant is *any continuation past a SAFE_ABORT ladder waits for the ladder* — a closed set of three (continued MISS, NO_RELEASE retry, reload interlude rung 4), now sharing one constant so an edit to `go_home_duration_s` cannot fix two and leave the third. **The third consequence is what made it HIGH**: the retry would normally have died `REJECTED_HAND_NOT_PARKED`, which is not `ABORTED_NO_RELEASE`, so the *two consecutive non-releases stop the session* epidemic gauge could never fire and the operator would be routed to the hand park band for a release fault. Also fixed: a **DORMANT** map was seeding the session trim's prior (`anchor.aim_rad` + `speed.k_v`) at n0 = 4 — D3's double-count walking in behind layer 1's own fence, because *layer 2 does not depend on layer 1's dormancy* is about the trim's MEASUREMENT, not about a borrowed number; the miner only PRINTED a declared-plane mismatch where plan § 7 R1 says it REFUSES (a printed warning protects nothing — the fit selects on `usable_for_aim_fit`, and R1's whole point is that the error is invisible in the residuals until a displaced throw); the `TossContinuous` IDL claimed RELOAD needs `stop_on_miss` false, which the feature's own primary route (CAUGHT, bounce-out during the dwell, then `REJECTED_NO_BALL`) contradicts; an aim-corrected **8a** tilt-clamp raise surfaced as `REJECTED_EVENT_VEL` because the gate sat inside the `TIER_8B` block; `speed_gain()` / `release_latency_ms()` read as if applied when nothing consumes them; and § 3.5 called `/trajectory/commanded_position` a 40 Hz `Point` (it is 5 Hz — `create_timer(0.2, _publish_status)`) and built a bag-size caveat on it. **ONE HIGH LEFT UNFIXED, deliberately**: a session cancel during the auto-reload interlude is honoured in ANY reload phase — BB ball airborne included — and runs `_safe_abort()` under it. The reload FSM has no `_toss_cancel_deferred` equivalent, so deciding which phases defer is a design call on a path four sittings of hardware evidence sit behind, not a finalisation edit. **Until it is made: do not cancel a `TossContinuous` session during a reload interlude — cancel in the dwell** | [2026-08-10-toss-selftuning-build](2026-08-10-toss-selftuning-build.md) |
| 2026-08-10 | resolved | catch-robustness Phase 1 | **The hand ball sensor becomes the possession source of truth — tri-state ARRIVAL, a source merge, and a live ball-evidence gate** — `HandBallSensorSource` lands as the **PRIMARY** source behind the C-POSSESS-1 seam and `TrackerArrivalSource` is demoted to the arrival corroborator (it stays the only supplier of `arrival_err_mm`); `merge_possession` is the one merge point, with three rules: retention is ALWAYS the sensor's, arrival is the sensor's whenever it observed it and the tracker's only when the sensor is UNKNOWN (so a blind sensor degrades to exactly the pre-sensor behaviour rather than refusing everything), and the tracker's accuracy numbers always survive. `toss_require_ball_evidence` flips to **true** on the operator's in-situ validation (the hand-ball-sensor plan's *"forbidden until Phase 7"* gate is SUPERSEDED, recorded at that plan's own statement of it), and CHECKING now reads the cup **live** — `REJECTED_NO_BALL` on a valid-empty cup, `REJECTED_BALL_UNKNOWN` on a sensor that cannot answer (it **refuses**: BallButler's fail-open `ball_in_hand_ = true` boot default is a recorded live bug, deliberately not copied), config `false` as the operator's total-bypass escape hatch. **Windows are measured, not chosen**: over three 2026-08-10 bags (203,922 samples, **100 % `ball_held_valid`**, 81 announcements) the arrival edge lands **+137…+798 ms** after the announced landing (median +399) and never before it, against a nearest non-catch edge at **+3194 ms** ⇒ `[-0.20, +1.50] s` (1.9x / 2.1x two-sided), deliberately generous because the release lag is can-bridge-**uptime**-dependent (+54–63 ms fresh vs +118–133 ms at ~16 h); retention 1.50 s = 1.5x the longest resolved seat-then-leave (0.999 s) and 2.3x under `MIN_TOSS_THROW_DELAY_S`. Contract extended FIRST (§ 2.1 ARRIVAL is tri-state — `arrival_ok` becomes a derived **property** so a source cannot claim an arrival it was blind for, which was § 1's own defect one level down; § 3.2 the tick-driven source kind + merge rules; § 3.3 the live precondition), closing § 7's accepted bounce-out trap (actuation half) and both of § 7.1's named coordinator edits. New probe `tools/probes/hand_sensor_verdict_replay.py` (10/10 self-check; **35 CAUGHT / 46 MISSED / 0 UNKNOWN**, ledger reconciles EXACTLY with an independent hand count on all three bags) + a committed fixture cut. Named residuals: every UNKNOWN path is test-only (no real invalid sample exists), the retention rule has never seen a real post-catch bounce-out, the reload's `CAUGHT` terminal is newly live, and the one-tick CAUGHT-over-a-just-bounced-ball **reporting** error survives by design (retention cannot answer before the goal finishes). **Audit fixes, same day** (§ Audit fixes): the operator scoring rows this phase INVERTED are rewritten (runbook `POSS-1` / `POSS-1.3` listed a reload `CAUGHT` — the headline capability — in their ABORT column), `describe()`'s REFUSED branch stops attributing a SENSOR veto to the tracker (it printed `arrival 3 mm > 70 mm` on the exact path the phase enabled), the retention window gains the absolute widen guard it shipped without, and three findings are carried unfixed as operator/design decisions. | [2026-08-10-sensor-truth-possession](2026-08-10-sensor-truth-possession.md) |
| 2026-08-10 | in-progress | catch-robustness Phase 0 (diagnosis) | **Post-throw dip recurs at 1.0 m — the hand drive under-delivers braking current, not a Phase 1–4 regression** — three retest bags at ~16 h can-bridge uptime, 81 announcements / 57 jugglebot strokes. At 4.436 m/s (n=54) `dip_below_x3` 0.268/0.899/1.672 rev with 54/54 OVER the 0.10 rev band and `peak` to 10.766 rev, but **0 truncations and 0 from-rest seeds** — the Phase-1 arm gate holds, so the 2026-07-25 mid-stroke-clobber hypothesis (uptime-driven) is **WITHDRAWN**. FW 2 is aboard and commanding: `tor_ff_cmd` = −0.1500 N·m on 54/54 (the FW-1 spool model would compute −0.11). The plant does not *deliver* it: braking `iq_meas` reads −10.52/−7.60/−3.85 A over the ramps against the **−27.2 A** that −0.1500 N·m asks for at Kt 0.0055133, with a hard whole-bag negative floor of −11.8 A while the positive side runs to +49.6 A — one-sided, and kinematics say the two sides should be within 0.5 % of each other ((a_dec−g)/(a_acc+g) = 0.995), which kills the "heavier/draggier plant" reading and the friction rescue (it needs ≈3.4 g of friction, which would then demand far more motoring current than the +22.8 A measured). Lands on a **pre-registered** risk: `axis0.config.torque_soft_min = −0.055133 N·m = exactly −10.00 A` (asymmetric vs `torque_soft_max` +0.5 N·m), i.e. C-HAND-2 § *The negative torque clamp* / bench pre-flight **H7.0c** — whose 2026-07-27 "probably not binding" counter-evidence was measured on the **pre-swap** drive (hand ODrive swapped 2026-07-31 during the CAN3 elimination). Dip mechanism: coast 0.51–0.81 rev past `x3`, pos-loop pullback −12.9…−19.3 rev/s, then the gated arm lands +31.9/**+44.8**/+57.7 ms after the commanded stroke end at ≈−11.7 rev/s (above `SMOOTH_MOVE_V0_DEADBAND_RPS = 6.0`) so the FW-2 velocity-continuous prelude fires with a large `v0`. Collateral: dispatch shift grown to +118…+133 ms at ~16 h (was +54–63 ms on 2026-07-27); `iq_meas` **frozen at −10.13 A for the first 171 s of bag 16-04-26** (17 138 samples, one contiguous run — exclude that window, and do not mistake it for the −10.00 A clamp); sensor ledger **100 % valid in all three bags** with 3 quick-drops (<1.5 s) in 16-30-44 = bounce-out labels mined with no wiring in place. **No fix landed** — diagnosis + hand-off; recorded tension the bench must resolve: a −10.00 A total-torque clamp cannot reproduce the 3.44 m/s tier's near-perfect tracking, so either the clamp truncates the FF term only, or C-HAND-2's decel-side bound `J ≥ 1.0126e-5` was measured *through* the clamp and must be re-derived on a restored drive. No catch-knob tuning until Phase 0 lands (the catch seat brakes in the same regen direction) | [2026-08-10-hand-drive-braking-clamp-diagnosis](2026-08-10-hand-drive-braking-clamp-diagnosis.md) |
| 2026-08-10 | resolved | tilt-cal Phase 4 / rungs C0–C2a | **Tilt-cal C0 blockers — home gate mistuned against level-path physics (map goes home-referenced) + first-of-class leg-0 SPINOUT collapse** — six attempts on disk not three; the one real gate abort missed its tolerance by 8.3% while the level reference's own single-sample scatter is 1.2–1.7 mrad/axis (every recorded offset decomposes exactly as int16 code × LSB — gate structurally untunable, false-abort 40–60%/attempt); owner decision: map goes HOME-REFERENCED (C_cap cancels exactly; staleness gate retired; end-of-sweep drift gate + mid-sweep /gravity_offset abort + WARN 0.010/abort 0.05 replace it); leg-0 SPINOUT_DETECTED (67108864) latched 2.5 s into the slow move to (−150,−150) with kinematics exonerated three layers deep (most-retracted leg, ≥85 mm margin, pose held 4× on 2026-07-27) — 30 reads ran on a collapsed platform, so the tool gains per-read timestamps, cached-kv fault checks at arrival + after every read, decoded ODrive forensics on every abort, centre-out ordering (corners LAST), lean deferred to config 0.6, IK stroke preflight; spinout root cause OPEN for next sitting, forensics-before-relaunch rule in the runbook — **ARC WRAP-UP (this entry now covers the whole Phase-4 arc, owner's one-file rule)**: the replacement drift gate then discarded *both* clean C0 re-runs over a reproducible +1.81 / +1.59 mrad ty home offset with `/gravity_offset` silent and forensics clean, and the owner reframed the mechanism as platform **ARRIVAL REPEATABILITY** (hand-built FDM structure, path-dependent hysteresis) rather than sensor drift — same-sign reproducibility cannot discriminate, both sweeps share one node sequence ⇒ **anchor-mean referencing** (7 interleaved home anchors on a 5×5, reference = their MEAN — robust under both open mechanisms where a time interpolation presupposes one; WARN 2 mrad p-p, ABORT 8.7 mrad p-p (owner's 0.5° tolerance) or 5 mrad consecutive step; commit `7cbfd9d`, **no Logbook-Entry trailer**, owner's no-logbook speed directive); C0 pins `--dwell-s 2.0` (median per-read sd 0.38 vs 0.56 mrad) and sizes orientation-dependence at ~+0.10° per 6° commanded rx (1.7%, tilt-axis sweep stays a follow-on); **C1 PASSED** (5×5 over ±150 at z=170, 7 anchors p-p 0.0398°/0.0797° with one excursion then a plateau, field tx −0.208…+0.289° / ty −0.204…+0.134° matching the C0 shape and the 07-28 worst quadrant, map auto-applied and confirmed as **2026-08-10-3bf7964f**, all 6 off-node checks PASS home-referenced at residual magnitudes 0.043–0.137° vs 0.15° — can-bridge uptime ~15.5 h, not fresh, accepted because these are static reads); **C2a PASSED** with the base deliberately tilted ~6° about ~x and re-`level` ONLY, no recapture (6/6, magnitudes 0.018–0.146°) — **the layered design is HARDWARE-VALIDATED**, base tilt absorbed as common-mode while the flat-floor map stays valid; **map committed `3df256b`** (data-only, no trailer, loads at boot, dormant until level); owner-run toss retest at multiple flat poses caught most throws after `levelling_settle_s` 0.5→1.0 and `catch_vel_scale_default` 0.8→0.9, closing the clipped-throw symptom empirically ⇒ **C3 SUPERSEDED** (no formal map-off/on A/B, still available); **owner decision: `toss_tier` stays "8a"** as the shipped default, superseding the 2026-07-28 8b decision, with dated corrections added to the four live docs that asserted 8b; still open — leg-0 SPINOUT root cause (mechanical check next sitting), optional C2b, optional tilt-axis sweep | [2026-08-10-tilt-cal-c0-blockers-level-noise-and-leg0-spinout](2026-08-10-tilt-cal-c0-blockers-level-noise-and-leg0-spinout.md) |
| 2026-08-09 | resolved | housekeeping (post ERR_TIMEOUT closure) | **Probe archival convention started + completed session prompt deleted** — `tools/probes/archived/` created (probes with a CLOSED motivating investigation move there, committed and runnable, ledger row in the probes README; first occupants `hand_dispatch_ladder.py` — the instrument behind the 15/40→0/40 discriminator and the 120/120 FW 10 validation — and `link_status_flash_control.py` from the CLOSED CAN3 drive-path arc; `link_status_health_scan.py` stays live, archival is by investigation status not age); `plans/active/PROMPT-err-timeout-hand-path.md` DELETED (owner convention, first instance — a completed prompt's arc lives in the logbook, and a stale prompt with a live-looking fence is the "stale plan documents actively mislead" class). Live references updated, 0 stale pointers; historical logbook text deliberately untouched. | [2026-08-09-probe-archival-and-prompt-cleanup](2026-08-09-probe-archival-and-prompt-cleanup.md) |
| 2026-08-04 | resolved | Tilt calibration grid — review completion | **The adversarial review's cross-doc and conventions dimensions, lost to the 2026-08-03 usage-limit boundary, re-ran against `00c7818`: 5 confirmed doc-drift WARNINGs (0 refuted by verifiers) and 3 NOTEs — every one documentation or comment drift from the dormancy-semantics change, no runtime defects.** Loaded-vs-applied wording fixed in four places (`TrajectoryStatus.msg`, the `_publish_status` comment, a test assertion message, the mock conftest) that still said the status pair means "loaded AND applied" — the contract's normative semantics are LOADED, and a loaded map is dormant until `level` runs. The contract's own "adding a new pose surface" recipe named `self._tilt_map`, which would bypass the dormancy gate at a future seventh ingest surface with no test able to catch it (the manifest keys on the call, not its arguments) — it now names `self._active_tilt_map()` with the trap spelled out. `CURVATURE_*` pinning re-attributed C0 → C1 (C1 is correct — the constants need a real field, not C0's four probe poses); the runbook's partial-rebuild signature corrected (AttributeError in the 5 Hz status timer, not module-scope ImportError); one 18.86 s vs 18.95 s verification-triple mismatch in the plan; `--force-uninstall` help text and an analyser docstring flag name still describing pre-`00c7818` semantics. | [2026-08-04-tilt-cal-review-completion-doc-fixes](2026-08-04-tilt-cal-review-completion-doc-fixes.md) |
| 2026-08-04 | resolved | tilt-calibration-grid Phase 3 (post-review) | **Adversarial review of the three tilt-calibration commits: ten findings, all reproduced at HEAD, all fixed. The BLOCKING one let a capture complete on a DISARMED wire.** `trajectory/go_to_pose` is *accepted* while `mpc_active=0` — streaming-while-disarmed is the legal pre-arm phase, so the planning verdict genuinely is "accepted" — and the only marker is a suffix on `response.message` that `tilt_cal_grid.py` discarded. The platform never moved, every node re-measured one stationary pose, and the tool wrote, applied and **verified** a plausible all-zeros calibration that verified *because* nothing moved. Same class as the **2026-07-15** silent-no-op ramp battery, which is why the suffix exists at all. `/link_status` was already subscribed and cached for `uptime_ms` and never read. Guarded in three layers (preflight `mpc_active`, the `DISARMED` marker on every accepted response re-checked between nodes, a flat-field WARN before the write), and the docstring's claim that a bypassed preflight was harmless — true for the mode, false for the wire — is corrected. **`--force-uninstall` could not uninstall**: it moved only the source-tree file while `setup.py`'s ament share copy shadowed it, so the old map stayed loaded, rung C2b was unrunnable after any post-C1 build and C3 arm A was silently confounded. **Owner decision on unlevelled maps**: a residual is defined against a fresh `level` reference, so with no `/gravity_offset` the map is now **DORMANT** — loaded and observable, but ingest behaves exactly as C-LEVEL-1 unlevelled — rather than composing onto the `(0.0, 0.0)` placeholder, which is the state at *every* boot once the YAML is committed. **Rung C2b's close-out could not work as written** — "Restore the C1 map" had no `cp` and no reload, so the shim-contaminated recapture stayed applied and the `grep` "confirmation" could never have shown the C1 version. Plus a 5 Hz status race that could abort a *completed* capture, `map_version` hashing ndarrays via `default=str` (>1000-node grids could collide through numpy's repr truncation), the analyser's half-cell heat-map skew, a silently-typo'd `$JUGGLEBOT_TILT_CAL`, preflight-only preconditions, and no warning for an off-plane capture height. A follow-up audit of the fixes caught a contract self-contradiction (the observability section still said the status pair "can never report a version for a calibration that is not being applied", which dormancy makes false), a stale-`/link_status` hole where both wire layers freeze together, and a C2b step that destroyed the C2 map it told the operator to retain. No hardware ran. | [2026-08-04-tilt-cal-review-fixes](2026-08-04-tilt-cal-review-fixes.md) |
| 2026-08-03 | resolved | tilt-calibration-grid Phase 3 | **The software half of C-LEVEL-2 is complete: a grid acquisition tool, an offline analyser and the C0–C3 runbook. No hardware ran.** `tests/hardware/tilt_cal_grid.py` drives an (x, y) grid (5×5 over ±150 mm at z = 170, **home node first** as the stale-level gate), reads the inclinometer N times per node, computes the exact `LevellingHandler` residual, writes `config/tilt_calibration.yaml`, reloads it and **confirms the node's published `tilt_map_version` matches what it wrote** before verifying against off-node check poses. Request-only (`trajectory/go_to_pose`; never arms, never changes mode, never touches the hand) and every exit path returns to centre first. **Three flags exist because a Phase-4 rung is otherwise unrunnable**: `--no-apply` (C0 — a read-noise probe must not leave a calibration behind), `--verify-only` (C2a is *defined* as re-`level`-only with NO recapture), and `--read-gap-s` — the SCL3300's **10 Hz internal LPF** means back-to-back reads resample the same filtered state, so the spread collapses and the tool would have reported a noise floor the sensor does not have, which C0 would then have "pinned". **The outlier detector was wrong first**: a departure-from-neighbours test flagged six of twenty-five nodes, all on the boundary, because the field is genuinely curved and an edge node's neighbours all lie on its interior side; it is now a **second difference** (a pinned leg kinks the field rather than bending it, so a pure gradient is ignored by construction). **No threshold on it survived scrutiny** — `4×median` missed a real 0.5° pin, `2×median` flagged ten good nodes — so the flag is a coarse net marked PROVISIONAL and the report leads with the scale-free **top/median curvature ratio** (probe: 1.01 clean, 2.43 at 0.2°, 6.07 at 0.5°); rung C1 pins the constants. Failed nodes are **refused, never interpolated**; the home gate is two-sided (`max(3×sd, floor)` *and* an absolute ceiling) because one-sided fails on either a quiet or a noisy sensor. No `__file__` walk in path resolution (the Phase-2 audit blocker) — the write target is `tilt_map.tilt_map_candidates()[0]`. **The audit caught a blocking safety defect**: the return-to-centre guard caught `Exception`, which does not catch `KeyboardInterrupt`, so a *second* Ctrl-C during the ~8 s return propagated out of the `finally` and skipped the artefact writes, the rclpy shutdown and the `RETURN TO CENTRE FAILED` message — leaving the platform parked displaced **in silence**, the one thing the docstring and the runbook banner both promise cannot happen. Now `except BaseException`, pinned structurally. It also found `levelling_frame.md` claiming the tool "refuses to start" on all four capture preconditions when only *no map loaded* is machine-checkable; the contract now carries a per-precondition enforcement table. | [2026-08-03-tilt-cal-phase3-acquisition-tool.md](2026-08-03-tilt-cal-phase3-acquisition-tool.md) |
| 2026-08-02 | resolved | tilt-calibration-grid Phase 2 | **C-LEVEL-2 is live in the node that applies it: loader, reload service, status fields and per-pose ingest keying.** `trajectory_node` loads `config/tilt_calibration.yaml` at construction (`$JUGGLEBOT_TILT_CAL` → repo source tree → ament share — the **inverse** of `friction_ff_params.py`, because the Phase-3 tool rewrites the source file at runtime and share-first would serve the last build's stale calibration under a green status), re-reads it on the new `trajectory/reload_tilt_map` (`std_srvs/Trigger`), and publishes `tilt_map_loaded` / `tilt_map_version` at 5 Hz. All six ingest sites now build their correction with `levelling.correction_for_pose(offset, tilt_map, intent)` keyed on the **uncorrected intent** pose, so `timed_target(hold_after=False)` gives its target the corner residual and its neutral return the home-node one. Three decisions Phase 3 depends on: the env override is **authoritative** (a typo loads nothing rather than a *different* calibration reported as loaded); reload with the file **absent unloads and succeeds** (`--force-uninstall` needs `tilt_map_loaded == false` before a capture — only an *invalid* file keeps the previous map); and the structural guard grew a **third kind** (`store` / `build:` / `apply:`) rather than mislabelling `correction_for_pose` as a store, plus a new `test_every_apply_has_a_build_in_the_same_scope` — an ingest that applies the correction without building it for its pose is a *working* C-LEVEL-1 application that silently ignores the map. **The specified keying regression is unwritable, which is the stronger result**: C-LEVEL-1 is rotation-only, so corrected and uncorrected poses share their x/y and key the same cell; the property is pinned instead. Non-gating throughout — absent map silent, invalid map loud with the previous kept, a raising lookup degrades that one pose and warns once per map. **Needs `colcon build --packages-select jugglebot_interfaces jugglebot` — both packages.** | [2026-08-02-tilt-cal-phase2-trajectory-node-integration.md](2026-08-02-tilt-cal-phase2-trajectory-node-integration.md) |
| 2026-08-02 | resolved | tilt-calibration-grid Phase 1 | **C-LEVEL-2 landed as contract + pure map core; nothing is wired into a node yet (Phase 2).** `ros_ws/docs/levelling_frame.md` gains **C-LEVEL-2**, which *composes with and never replaces* C-LEVEL-1: residuals are measured at the commanded-level orientation against a **fresh** `level` reference (home node ≈ 0 by construction), composed **additively in rotation-vector space** — `combined = level_offset + bilinear(map, intent_xy)` through the **existing single Rodrigues** — and keyed on the **uncorrected intent** pose so there is no fixed-point iteration. **The pre-commit audit killed this section's headline number**: the draft claimed the additive-vs-two-rotation error is "< 1e-4 rad for sub-degree tilts" and pinned it with one worked point 7× inside the bound, but the exact second-order term is `\|level_offset × residual\| / 2` — **7.19e-5 rad** at the measured envelope (0.78185° × 0.604°, orthogonal — holds), **1.523e-4 rad** at 1° × 1° (does **not** hold), **1.25e-3 rad** at what `MAX_ABS_RESIDUAL_RAD` alone permits. The contract now states a **regime table plus the closed form** instead of a bare bound, and the test sweeps the azimuth ring at both magnitudes rather than sampling one point (the error is a cross product, so a fixed direction measures whatever the author happened to pick). Physical impact today nil — 1.5e-4 rad is 0.36 mm at 41.9 mm/° — the defect was the false normative claim. Evaluation is **at ingest, per target, only**: a per-knot/emitter lookup is forbidden with three independently sufficient disqualifiers (it desyncs `u0/u1/u2` from the declared velocities the 500 Hz firmware Hermite interpolates over; it escapes `feasibility.validate` so the gate measures an object that no longer ships; and the per-knot delta sits far below both `MAX_POSITION_STEP_REV = 0.3` and `MAX_DEVIATION_REV = 1.0`, making a correct lookup and a buggy one produce identical telemetry — nothing). Also specified: clamp-to-hull (never extrapolate — a wrong-signed edge extrapolation aims worse than no map), **all-or-nothing** load validation (known schema version, strictly monotonic axes ≥ 2 nodes, `[iy][ix]` shape match, every node finite, `\|residual\| ≤ 0.05 rad`) with a loud refusal and **no** partial load, the `tilt_map_loaded` / `tilt_map_version` observability fields (Phase 2 lands them), **non-gating** semantics (absent/invalid ⇒ exactly C-LEVEL-1; the toss gate is untouched — resist adding a rejection code), the four capture preconditions, the inherited in-flight rule, the committed machine-written `config/tilt_calibration.yaml` schema v1, and a **B1 revival-obligation row** (dormant `mpc_bridge_node` must gain the map *and* validation before MPC revival, or the two pose paths re-create the original C-LEVEL-1 disagreement between nodes instead of within one). Code: pure `motion/tilt_map.py` (`TiltMapError`, `TiltMap`, `parse_tilt_map`, `load_tilt_map`, `lookup`, `map_version` = `<captured.date>-<sha256(applied numbers)[:8]>` over the schema version + the two axes + the residual grids, **float-normalised** so neither a provenance edit nor YAML's `170` vs `170.0` churns a version the Phase-3 tool re-emits every run — the first draft hashed the whole `grid` block, `z_mm` and `orientation` included, contradicting its own docstring) plus **one** new entry point `levelling.correction_for_pose(offset, tilt_map, pose)` — bit-identical to `correction_from_offset` when `tilt_map is None`, so the non-gating degradation is a property of the function rather than of a caller's `if`. No existing signature or behaviour changed. Docs hygiene: the stale `teensy_bridge_node.py:1430` citation now names the verified sites (decode `:349-372`, publish `:1747-1755`) in **both** runbooks that carried it — `session_phase8_toss_hardware.md:81` and `session_anomaly_fixes.md:93`, the second found by the audit, since a half-fixed citation leaves two runbooks disagreeing about one fact. Verification — `pytest tests/motion/ tests/ros/test_levelling_frame.py -q` (run 2026-08-02): **967 passed in 281.08 s**; `./run_tests.sh --full` (run 2026-08-02, post-audit-fix tree): **parallel 4501 passed + 3 xfailed in 451.44 s, serial 9 passed in 40.00 s, total 497 s — RESULT: PASS**. | [2026-08-02-tilt-cal-phase1-contract-and-map-core](2026-08-02-tilt-cal-phase1-contract-and-map-core.md) |
| 2026-08-02 | resolved | Tilt calibration grid — kickoff | **Tilt-calibration-grid kickoff: plan created for a pose-dependent residual tilt map layered on the C-LEVEL-1 single-offset `level` routine.** A 9-agent architecture scan (2026-08-02) mapped the levelling chain end to end and verified the design seam; the recorded seed evidence is the 07-28 extremity-tilt table (`2026-07-28-anomaly-fixes-validation-sitting.md`): 0.041° at (60, 0) → 0.604° at (150, −150), non-linear in (x, y), revisit repeatability 15–40× smaller than the effect. Plan (`plans/archived/tilt-calibration-grid.md`): capture via a driven `go_to_pose` grid reading the SCL3300 through the ungated `get_platform_tilt` service, persist as committed machine-written `config/tilt_calibration.yaml`, auto-apply via a reload service, evaluate once per external-pose ingest keyed on the uncorrected intent pose, clamp to the calibrated hull — **non-gating, and no firmware change anywhere in the plan**. Validation includes a deliberately tilted base (rung C2) proving base tilt is absorbed as common-mode by `level` while the map stays invariant; rung C3 (displaced-toss A/B, map off/on) captures the motivating clipped-throw symptom quantitatively. | [2026-08-02-tilt-calibration-grid-kickoff](2026-08-02-tilt-calibration-grid-kickoff.md) |
| 2026-08-02 | resolved | refactor-2026-07 Phase 7 / PROMPT-err-timeout-hand-path Steps 1-2 | **ERR_TIMEOUT hand-path: sequence verdict, mechanism reframe, attribution instrumentation. Three results. (1) The 399 recorded hand outcomes are RANDOM, not structured** — ordered per-dispatch sequences reconstructed for all 16 sessions with hand dispatches (2026-07-23→07-31) from `~/.ros/log/*/launch.log`, validated cell-for-cell against the recount (prime 133/63 fail, arm 266/139 fail, 202/399 = 50.6 %) and re-derived independently. No alternation (arm z +0.54, prime z +0.19), no parity effect, no lag-1 state signature (P(F\|F) vs P(F\|O) OR 0.82 arm / 0.76 prime, p ≈ 0.45), no genuine time-clustering once within-retry-chain pairs are removed, session rates homogeneous (X² 11.03/15 df) across fresh-to-30 h bridge uptimes ⇒ **the stale-pending-slot / state-bug class is EXCLUDED** at the effect sizes that matter (caveat: n=84 short-lag attempts cannot exclude OR ≤ 1.7). Random ⇒ congestion-consistent. Two secondary signals recorded honestly: an unexplained within-session drift on the PRIME path only (+0.62–0.77 log-odds/100 s, p 0.006–0.015, wall-clock-shaped, arm path flat) and a boundary-significant same-reload-cycle prime↔arm association (MH OR 2.79). Per-session O/F sequences are tabulated in the entry so re-analysis never needs the extraction again. **(2) `bus.write() <= 0` is DEFERRAL, not drop [VERIFIED IN SOURCE]** — the vendored `FlexCAN_T4::write()` returns only 1 or **-1 = pushed into the 64-slot software txBuffer** (`struct2queueTx`), never a drop at the call site, and the TX-complete ISR drains it one frame per freed mailbox in FIFO order, so a deferred frame goes out ~0.1–1 ms later. **The recount's "frame never enqueued ⇒ hand NOT armed" inference is withdrawn** (addendum added there): the lying-ack premise behind `_MAX_ARM_DISPATCHES` and the write-rejection narrowing are **compatible**, and the epidemic's likely cost shifts from "lost stroke" to the FSM's reaction to a false-failure ack. Source reading, not measurement — **the latch fence stays up**, no re-dispatch behaviour touched. Two genuine loss paths do remain and are narrower: ring overflow silently overwrites the oldest entry, and the `events()` TX drain has a real defect (peeks one frame, writes it to EVERY free mailbox, pops one entry per write). **(3) The attribution instrumentation landed** across three commits: host-side `hand_traj_acks` (calls/ok/fail_teensy/fail_host at the `_call_rpc` choke point, splitting Teensy-returned from host-synthesised ERR_TIMEOUT — the pooling the recount unpicked by hand, and the 20d01e9 forensics blind spot, closed against the CURRENT bridge); a pio staleness guard (sha256 of `udp_protocol.h` vs a BUILD_DIR marker, wiping project objects + link products — measured: a full BUILD_DIR rmtree breaks the run because library Dir nodes are already materialised) closing the 24608bb class for ADDITIVE changes where it is **silent**; and the wire half — **BridgeTxDiag 0x8D (42 B) + BridgeIdentity 0x8E (3 B), FW_VERSION 8→9, PROTOCOL_VERSION deliberately still 5**: per-bus `tx_deferred` + `tx_q_hwm` (sampled at SEND instants inside the existing PRIMASK region, not on the 1 kHz tick that would read ~0 through the very bursts that cause deferral), per-stage hand counters (calls/rej_homing/bus_down/pre1/pre2/traj, with OK derived so the row is arithmetically self-checking), and wire-visible bridge firmware identity with a warn-never-refuse `BRIDGE_FW_CHECK`. **Named `tx_deferred`, not the planned `tx_write_fail`** — a wire-format field name outlives its investigation, and the old name encodes the model this session refuted. **Per-stage counters, not new RpcStatus codes**: distinct codes would give per-call attribution in the log but mutate an existing wire field consumed by live catch logic (0x8F stays free for a per-call echo). `/link_status` 37→38→40 keys. Independently audited pre-commit: COMMIT-READY, zero blocking findings. **Open**: the prime-path drift; the ~50 % observed vs naive-burst-math gap (8 TX mailboxes predicts well under 50 % — do not paper over it) **[RESOLVED 2026-08-09 — see the addendum]**; `tx_q_hwm` is boot-monotone with no reset RPC, so bench arms must run low-load-first or reboot between arms. **Verification** — `pio run -e teensy41` + `-e teensy41_bench_sysid`, run 2026-08-02: both SUCCESS; `pytest tests/firmware -q`, re-run 2026-08-02 (warm native cache): **399 passed in 19.36 s**; `./run_tests.sh`, run 2026-08-02: **4009 passed, RESULT PASS**.  **Addendum 2026-08-09 — the bench discriminator ran and congestion is CONFIRMED**: N=40/arm A-B-A ladder on FW 9 gave idle **0/40**, 500 Hz leg stream **15/40 (37.5 %)**, idle again 0/40 at *higher* uptime (Fisher p = 8.5e-09; `can3_errors` zero throughout; host `fail_teensy`/`fail_host` = 15/0 matching the firmware exactly). **`pre1 = 0` is the signature** — the per-stage split (0 / 7 / 8) reads out the pending-mailbox count against the source-verified 8 TX mailboxes and the 6-frame leg burst, rejecting iid-per-frame deferral (exact p = 0.0011). **Open question 2 RESOLVED**: the ~50 %-vs-naive-burst-math gap is phase-locked dispatch quantisation (`task_net` wakes only on 1 kHz FreeRTOS ticks, commensurate with the 500 Hz interp PIT off one crystal ⇒ two phases mod 2 ms, 50 % ceiling — one link, the PERCLK clock source, is INFERENCE, not read from source; the phase-stamp probe is its falsifiable test), with ~62 % bus utilisation second-order and bus load alone refuted ~50×. Operationally the lying ack is ~50/50 by stage (pre2 fails abort before 0x6D0; traj fails queue and usually transmit it) and the latch fence stays up until a fix lands. **Step 4 fix DECIDED 2026-08-09 and IMPLEMENTED the same day as FW 10 (addendum 2), flash + bench validation PENDING**: `setMaxMB(16→24)` (8→16 TX mailboxes, jugglebot bus only) + a console-only `[handphase]` phase-stamp diagnostic — the only candidate with no 0x6D0 duplicate/invert hazard, and the one that also makes the `events()` residual unreachable. Wire-invisible (no MsgType, no payload, PROTOCOL_VERSION stays 5), so an FW 9 board decodes identically and a healthy link is NOT evidence the fix is aboard; the latch fence stays up until the flash lands AND a reload sitting validates it. Side-finding: the vendored FlexCAN_T4 `events()` `mb == -1` refill loop has no `break`, so "deferral is not a drop" is only conditionally true.  **Addendum 2026-08-09 (3) — VALIDATED ON HARDWARE, THE EPIDEMIC IS CLOSED.** FW 10 flashed; the identical three-arm ladder gave **0/40 in every arm = 120/120 clean** (armB was 15/40 pre-fix, Fisher p = 1.21e-05; `setpoints_sent` 199→3327 proves the 500 Hz stream was live), with `defer jb`/`bb`/`cone` and all `txq` high-waters at **0** — the software TX ring was never entered — `can3_errors` all-zero and `bridge_fw_version` `10 (proto 5)` in all 120 rows, host `fail_teensy` = `fail_host` = 0, and armA2 clean at 393 s uptime vs armA's 123 s. **Both pre-registered falsification rows held**: `[handphase]` showed **two tight clusters at ~60 µs and ~1060 µs (±3 µs)** — exactly 1000 µs apart = the two 1 kHz FreeRTOS tick parities in the 2 ms interp cycle — **empirically confirming the two-phase quantisation model and RETIRING the PERCLK inference flag**; and `can1_tx` read **~3150 fps unchanged**, **refuting the one RM-unverified arbitration-scan hazard at MAXMB = 24**. `interp_max_jitter_us ≤ 2` / `interp_deadline_misses = 0` — the ISR is untouched, as designed. A separate ordinary reload sitting had the hand responsive on **every** reload attempt (the single abort was BB-side yaw `NOT_SETTLED`, a known separate item), satisfying the sitting half. So the ~50 % epidemic running since at least 2026-07-23 was **TX-mailbox exhaustion sampled through a tick-quantised dispatch phase**, fixed by 16 TX mailboxes. **The latch fence is DOWN**: the `_MAX_ARM_DISPATCHES` comment block in `catch_coordinator_node.py` is corrected, along with the `_on_hand_traj_done` docstring (comments + docstrings only; mechanically verified ZERO EXECUTABLE CHANGE — identical AST and identical bytecode once docstrings are stripped) and the cap plus the telemetry-verified ladders are **retained deliberately** as defense-in-depth — a host-synthesised `RpcTimeout` on UDP loss can still fail an ack, and the kind-1 wall-time invariant is a property of the stroke encoding, not the transport. One live residual: the `events()` missing-`break`, dormant at `defer jb = 0` but with a blast radius the fix **doubled** (16 mailboxes, not 8), reachable only if a future TX producer re-opens the deferral path. | [2026-08-02-err-timeout-attribution-instrumentation](2026-08-02-err-timeout-attribution-instrumentation.md) |
| 2026-08-02 | resolved | Developer workflow — test-suite hygiene | **`tests/ros/conftest.py` claimed it made `import jugglebot.*` resolve without ROS2, and for two packages it did not.** Its docstring says it injects fake ROS2 modules "so that `import jugglebot.*` resolves on Windows without a ROS2 installation"; it mocked `rclpy`, `jugglebot_interfaces`, `geometry_msgs`, `std_msgs`, `std_srvs` and `sensor_msgs` — but **not** `diagnostic_msgs` or `ament_index_python`, which five modules import (`teensy_bridge_node`, `trajectory_node`, `orchestrator_node`, `motion_bridge_node`, `ball_butler_node`), so the claim had been false for those. Both are now stubbed as a **fallback** — registered only when the real package is absent — not unconditionally, and that was an evidence-based choice rather than taste: `tests/ros/test_teensy_bridge_node_read.py`'s docstring states outright that "`diagnostic_msgs` is the real package (installed on the Jetson)", a deliberate documented decision that unconditional injection would silently override, lowering fidelity on the box that is authoritative for ROS behaviour. And `ament_index_python`'s absence is **already** handled where it matters — `motion/friction_ff_params.py` wraps its import in `try/except` and `tests/motion/test_friction_ff_params.py` pins that fallback by simulating the `ImportError`; only `ball_butler_node.py`'s top-level import was unguarded (its call site at `:317` is already inside `try/except`). **The trap, worth stating**: Foxy declares `DiagnosticStatus.level` as `byte`, so the severity constants are `b'\x00'`/`b'\x01'`/`b'\x02'`/`b'\x03'` — **not** ints — and the real class asserts bytes of length 1 (`DiagnosticStatus(level=1)` raises). A natural `OK = 0` stub is self-consistent and passes in a stub-only world; the divergence appears only when a value crosses between stub and real package, i.e. exactly the cross-platform case the stub exists for — caught by reading the installed package rather than assuming what the constants "should" be. The ament stub **raises `PackageNotFoundError`** rather than returning a plausible path: without an ament index there genuinely is no share directory, every call site already falls back to the source tree, and a fabricated path would turn a correctly handled absence into a silent mislookup; its exception subclasses `KeyError`, matching the real `PackageNotFoundError -> KeyError -> LookupError` chain. Also corrects CLAUDE.md's stale "`--full` is 1259 s at ci-deep" (measured 2026-08-01), which disagreed with the 2026-08-02 nightly's 25m 52s — rather than swap one rotting number for another it now cites ~26 min with its date **and** points at `temp/reports/nightly/latest.md` as the living source, re-measured nightly. Verification: `/tmp/probe_ros_stubs.py` (run 2026-08-02) blocks both packages via a `sys.meta_path` finder, proves the block took, imports the conftest and asserts the stubs — **ALL STUB CHECKS PASSED**, needed because on the Jetson the real packages win, so the `except ImportError` branches never execute and a broken stub would be invisible; `diagnostic_msgs` and `ament_index_python.packages` both still resolve under `/opt/ros/foxy/` here (run 2026-08-02); `pytest tests/ros/ -q` (run 2026-08-02): **1613 passed in 93.19 s**. | [2026-08-02-ros-conftest-fallback-stubs](2026-08-02-ros-conftest-fallback-stubs.md) |
| 2026-08-02 | resolved | Developer workflow — test-suite runtime | **The hypothesis profile docstring still promised a 5-minute suite from when it had 1200 tests.** `tests/conftest_hypothesis.py` documented `ci-fast` as targeting "< 5 minutes wall-clock" and `ci-deep` as "~10 min"; both dated from a ~1200-test suite that is now ~4400, so both had been wrong for months. It also named the wrong invocation — `pytest tests/ -q --hypothesis-profile=ci-deep` predates `run_tests.sh`'s tiering and would run the suite serially without the `--full` marker composition. Replaced with measured, dated wall-clock: `./run_tests.sh` per-commit ci-fast **206 s** (2026-08-01); `./run_tests.sh --full` ci-fast **485 s** (2026-08-02); `./run_tests.sh --full` ci-deep **25m 52s** (2026-08-02). The load-bearing number is the ratio: ci-deep is **~3.2x** the ci-fast `--full` run, not the ~20x the 50→1000 `max_examples` ratio implies, because only the hypothesis properties scale and they are a minority of the suite's cost — exactly what the old "5 min vs 10 min" pair got wrong in both directions, implying a 2x cost for a 20x example count. The ci-deep figure is not bespoke: it is the 2026-08-02 nightly (`temp/reports/nightly/`, GREEN, 4415/4418), which re-measures it daily for free. Verification: `pytest tests/ -q --collect-only` (run 2026-08-02) **4418 collected** — the module still imports and its `settings.register_profile` side effects still run, the only thing a docstring edit here could plausibly break; `pytest tests/sim/test_ballistics.py -q --hypothesis-profile=ci-deep --collect-only` (run 2026-08-02) **15 collected**, i.e. the profile name still resolves — an unregistered profile is a hard error at startup. | [2026-08-02-hypothesis-profile-timings](2026-08-02-hypothesis-profile-timings.md) |
| 2026-08-02 | resolved | refactor-2026-07 Phase 2 follow-up — nightly runner | **First nightly RED was env, not code**: cron lacks the ROS PYTHONPATH interactive shells ride (297 ModuleNotFoundError on pure-Python ROS pkgs); runner now sources foxy + workspace overlays before the venv; worst file 30/30 under the fixed bare-env recipe. Conftest fallback-mock hardening named as follow-up. | [2026-08-02-nightly-env-parity](2026-08-02-nightly-env-parity.md) |
| 2026-08-01 | resolved | refactor-2026-07 — post-programme stragglers | **Straggler sweep**: `safety.md` rewritten live-first (four live layers, thresholds by pointer, parked chain as dormant record); `controller/demo` path refresh in 3 active plans (36 refs); 4 runner scripts rippled for the SHA-backfill retirement; docker sim stack deleted (owner call, pre-existing broken); `PROMPT-err-timeout-hand-path.md` authored for the epidemic investigation. | [2026-08-01-refactor-straggler-sweep](2026-08-01-refactor-straggler-sweep.md) |
| 2026-08-01 | resolved | Developer workflow — test-suite hygiene | **Nine tests computed a verdict and returned it — pytest discards a test's return value, so they could not fail.** 51 tests across `test_motor_guard.py` and `test_kinematics.py` ended in `return passed` / `return True`; for **nine** of them, which had no other enforcement, the computed verdict went nowhere. The other 42 do enforce — 33 via plain `assert` statements, 9 via `np.testing.assert_*` calls only, so their returns were only noise (and a hard error under pytest 8). The nine include **every test in `test_kinematics.py` bar the component-cross regression test** — the IK/FK round-trips, the analytical Jacobian against finite differences, and the `Jdot @ twist` bias term. Root cause: both files began as standalone scripts whose `main()` aggregates `results.append(test_fn())`, and when pytest later adopted them by filename the pass/fail contract was never converted from "return a bool" to "assert". **Two tests gained checks their originals lacked**: `test_loop_timing` now asserts `n_cycles > 0` *first* — with zero cycles the jitter statistic is vacuously small, so a loop that never ticked would have *satisfied* the gate — and `test_singularity_map`, previously `return True  # informational only`, now asserts the invariant it was already computing and printing (zero poses with Jacobian condition number > 100). `main()` becomes `test_fn(); passed = True`, which is load-bearing: a pure-assert conversion makes a *passing* test return `None`, and `all([None, ...])` is `False`, so script mode would have reported total failure. The 42 bare returns were stripped by an AST pass restricted to each test function's own top-level returns, leaving returns inside nested helper closures alone. Carries the refactor programme's deferred motor_guard `nightly` demotion (3 tests), which empties the default gate's serial phase. **The first classification said 18 vacuous tests and was wrong**, and the way it was wrong is the lesson: it looked for `ast.Assert` nodes and missed `np.testing.assert_allclose(...)`, which is a *function call*, so the 9 tests enforced *only* by an `assert_*` call scored as unprotected, inflating the count from 9 to 18 — reading the accused bodies caught it, and re-running with "any construct that can raise" gave the real 9. A structural query answers the question you encoded, not the question you asked. Verification: `pytest tests/motion/test_kinematics.py tests/motion/test_motor_guard.py -q`, run 2026-08-01: **57 passed**, zero `ReturnNotNone` warnings; script mode still aggregates correctly (**ALL 45 TESTS PASSED** / **ALL 6 TESTS PASSED**, both exit 0); `--collect-only -m "serial and not nightly"` collects **0 of 4418** while `-m serial` still collects **9**. | [2026-08-01-vacuous-tests-return-not-assert](2026-08-01-vacuous-tests-return-not-assert.md) |
| 2026-08-01 | resolved | Refactor programme Phase 4 — teensy_link to the repo root | **`git mv controller/teensy_link teensy_link`; 327 refs across 134 files, 89 files rewritten.** Owner-decided destination: the repo root keeps the bridge running the LIVE tree (both launch files already inject the repo root on `PYTHONPATH`), so a wire-format edit is live at the next relaunch instead of sitting behind a `colcon build` whose omission is silent. Two things the mechanical rewrite would have missed: `protocol.py` derived the repo root as `../..` from its own file (now `..`, and it has a `sys.path` fallback so the break would have surfaced late, not loudly), and the bridge **stopped importing CasADi** — `from controller.teensy_link import …` executed `controller/__init__.py` → `mpc`, and the ROS package now has zero `controller.*` imports. The compat shim `controller/teensy_link.py` is `sys.modules` **aliasing**, never a re-export, and aliases every submodule explicitly: two identities for `RpcError`/`RpcTimeout` would let a `raise` sail through an `except` written against the other path, silently. TEMPORARY — delete after 2026-09. The move also **claims the top-level name `teensy_link`, which `tests/teensy_link/` already claims** — at `sys.path = [tests, repo]` the import resolves to the test directory (reproduced live), and it is green today only by pytest's prepend-mode insertion order; now pinned in `test_compat_shim.py` (the named diagnosis in front of an error that otherwise points at a submodule), the same guard `test_sim_import_style.py` installs for `sim`, and `teensy_link` joined that file's `_SCAN_DIRS` so relocating a package out of `controller/` cannot silently drop it out of the bare-import scan. Wire bytes unmoved (xlang digest `1383b3fc18dc085c51eba58979ef60a3898b2ff74dc99931926d04c0bc7ceccb` identical before and after). Gate `./run_tests.sh --full` (2026-08-01): **parallel 4406 passed, 3 xfailed in 448.43 s; serial 9 passed, 4409 deselected in 40.52 s; total 494 s — RESULT: PASS** (exit 0). Hardware smoke **CONFIRMED 2026-08-01 23:17** (operator rebuilt, relaunched, ran LEVELLING end-to-end; log reviewed clean — see the entry's CONFIRMED note). | [2026-08-01-teensy-link-repo-root](2026-08-01-teensy-link-repo-root.md) |
| 2026-08-01 | resolved | Refactor programme Phase 6 (slice 2) — structure cleanups | **Four structural cleanups in the plan’s mandated order; no control-path behaviour changed.** **(1) One import root for sim/**: 179 bare-style imports across 71 files became `sim.*`, and ~30 hand-rolled `sys.path` blocks became one `sim/_paths.bootstrap_paths()` used only by runnable entry scripts — library modules under `sim/` now never touch `sys.path`. The bug closed is *two module objects for one file*, which makes a `patch()` against one identity silently patch nothing. `tests/sim/test_sim_import_style.py` is the enforcement point (AST grep, fails on any new bare import or hand-rolled path block). **Deliberate deviation**: the bootstrap installs three roots, not the plan’s four — `sim/` is retired, because keeping it keeps the dual-identity mechanism live in every bootstrapped process while providing nothing after the conversion (verified live: with `sim/` installed, a bare import still mints a twin, so “four roots” and “one module object” cannot both hold). **(2) `controller/demo/` → `sim/juggle_planner/`** (49 refs, 21 files): every consumer is a sim script/test/probe, while `controller/` is imported by the hardware MPC loop; the move dissolved the boundary that forced seven hand-copied hand-stroke constants into `juggle_optimizer.py`, now real imports from `sim.hand.*`, printed bit-identical before/after. **(3) Clock-offset dedup**: the character-identical ROS-clock→`perf_counter` estimator in `trajectory_node` and `catch_coordinator_node` is now `jugglebot/clock_offset.py` (pure, clock injected); `tests/ros/test_clock_offset.py` drives new code and a verbatim transcription of the old through one scripted clock and asserts **exact float equality**, 30 refreshes past the 20-deep trim. `reload_coordinator_node`’s single-read variant is left alone, with a comment saying the reconciliation is an open decision. **(4) Bridge test harness**: 21 files imported `_build_paired_node` from a *test module* and `_teardown` was copy-pasted 19 times → non-collected `tests/ros/_bridge_harness.py`. Collect-only gates: 4398 → 4402 (+4 guard tests) → 4402 (move) → 4410 (+8 clock tests) → 4410 (harness extraction adds/removes none). **Five real breaks that the green suite passed over** were found by review and fixed before the commit, all one shape — *code the suite never executes*: `controller/generate_solver.py` (the AOT solver compile, the one documented hardware-path build command) lost the roots `mujoco_plant` used to self-install; `sim/viz/reference_plot.py`'s lazy `motion.geometry` import survived the drop of its wrong fifth root; and three probes plus `tools/motion_onset_cogging_study.py` carried module-level renames leaving dangling `_REPO_ROOT`/`_REPO`/`_root` uses inside `main()` — three of them in the *result-writing* path, reached only after the whole sweep runs. An AST undefined-global-name scan over the 135 `.py` files in this slice's three commits now returns **0 hits**; `--help` exits 0 for the four CLIs; `generate_solver`'s import chain is checked **by import, never by running it** (it unlinks `mpc_gen.so` first). Also fixed: `REFRESH_PERIOD_S` was documented as the nodes' timer period while both nodes still hardcoded `30.0` — a shared constant with **no reader**, the same duplicated-timing-constant shape the unit set out to kill, now wired into both `create_timer` sites and AST-pinned against reverting to a literal; 19 entry scripts named the retired `sim/` root in their bootstrap comment; `juggle_planner` joined `_SIM_TOP_LEVEL` (the newest package was the one hole the dual-identity class could re-open through); and `sim.__file__` is now pinned, because `tests/sim/__init__.py` makes tests/sim a regular package *also named* `sim`. `./run_tests.sh --full`, run 2026-08-01 after the fixes: **parallel 4398 passed, 3 xfailed in 444.94 s; serial 9 passed, 4401 deselected in 40.23 s; total 491 s — RESULT: PASS** (exit 0). | [2026-08-01-structure-cleanups](2026-08-01-structure-cleanups.md) |
| 2026-08-01 | resolved | Refactor programme Phase 6 (slice 1) — analysis safety nets | **Four nets over things that rot without anything going red; no control-path code.** `logbook_search` stopped dropping malformed entries in silence — `scan_entries()` returns `(entries, warnings)`, a titled-but-incomplete entry still loads (it is real prior art) but warns, and `tests/sim/test_logbook_front_matter.py` fails if any committed entry would warn; all 180 entry files already passed. The contract got its normative half too: `logbook/README.md` now names title/type/date/status as REQUIRED with an ISO `date`, and warns that the no-dependency parser keeps a trailing `# comment` verbatim. New `tools/gen_choreography_map.py` renders `ros_ws/docs/choreography.md` — **75 distinct wires across 140 endpoints** — by pure `ast`: names resolve only through exact paths (literal, module/class constant, ROS-parameter default tagged `param:<key>`) and anything else prints `UNRESOLVED(<expr>)`, because a map that guesses is worse than none. Review found the resolver breaking its own rule in **three latent places**, all closed here: `self.X` was answered from a same-named *module* constant (the `ast.Attribute` path is deleted outright — keying constants by bare name cannot tell an instance attribute from a module constant, and no endpoint uses the shape), a nested-`def` binding leaked upward into the enclosing function's scope with provenance `literal`, and the doc's closing line claimed "every name resolved to a literal" while the body tagged one as `param:`. The drift test could not have caught any of them — it pins doc == generator, not generator == source truth. The map also presented Phase 3's two parked nodes (`motion_bridge_node`, `mpc_bridge_node`) as live consumers across 8 wires; they now render `(not launched)`, cross-checked against `jugglebot_launch.py` **in both directions** by a test. `stewart-fk.js` (the repo's only duplicate kinematics, previously pinned by a manual browser page) now has 25 Python-generated golden poses spanning 11.6–246 mm of the 280 mm stroke, replayed through the real JS under node with **no shim**: IK agrees to 1.1e-13 mm, FK pose to 4.6e-7 mm, a mutated `revs * MM_TO_REV` moves the answer **261.9 mm** against a 5e-6 mm gate, and `FK_POS_TOL_MM` is now *derived* — sweeping the JS solver's residual tolerance shows pose error is bounded by ~0.53x it, so the gate is 10x the algorithmic ceiling, not 10x a lucky sample. Plus the stale-doc sweep, whose every fix the entry enumerates: `git log --diff-filter=D` proved all nine "missing" `tools/archived/` harnesses were deleted deliberately in 67889a6, which closed that rot pocket instead of deferring it. `ros_ws/docs/safety.md` §3 (a step limit in the deleted `can_node.py`; the clamp lives on in `controller/teensy_link/setpoint_pump.py`) is named in Phase 3's existing STALE banner rather than spot-patched — the file is owed one re-framing pass. | [2026-08-01-analysis-safety-nets](2026-08-01-analysis-safety-nets.md) |
| 2026-08-01 | resolved | Refactor programme Phase 5 — config freshness contract | **Production stays build-frozen; the confusion dies by observability instead.** `generate_config.py --check` renders all 16 planned artifacts (5 generated + 11 delivered copies) in memory and diffs them against disk — check and write consume the SAME `build_artifacts()` plan, so an artifact cannot be added to one path and forgotten in the other, and check mode performs **no writes at all** (`mtime_ns` + size + sha256 identical on 16/16 across a run, pinned as a test). Three deliberate properties, each closing a way the gate could have become useless: the **exit code covers the 14 in-repo destinations only** (the other two live in `../BallButler`, a separate repo on its own branch — letting it fail this repo's pre-commit gate, or raise this repo's bring-up banner over two Teensy headers no node here reads, would spend the banner's only asset; its drift still prints as `EXTERNAL DRIFT:`); **anything not checked is announced** (`SKIPPED:` / `NOT CHECKED:` — with the hardware YAML absent, 9 of 16 artifacts silently dropped out behind a green `CONFIG FRESH: 7`, exactly the false-green the design forbids); and it **compares text, not bytes** (the write path's `newline=None` emits CRLF on Windows, where a byte compare would report permanent 16/16 drift on a clean checkout — and this repo is run from a Win10 clone). `jugglebot_launch.py` reports two links separately because the fixes differ — (A) source YAML → repo artifacts via `--check`, (B) repo artifacts → **installed** copies via byte compare off `AMENT_PREFIX_PATH`, the link `--check` structurally cannot see and the one that actually bites; drift prints a `!`-bordered banner naming both commands + RELAUNCH, and a check that could not RUN — including a module missing from either side of link B — reports `PARTIAL` rather than a green OK. It **never blocks**: 10 s timeout (30× the measured 0.33 s; `ros2 launch` prints nothing until the description returns, so the bound is the length of an unexplained frozen terminal), every failure path degrades to one line. The repo root is walked up from the launch file itself so a `git worktree` build compares against its own tree. `teensy_bridge_node` logs the resolved path + sha256 + mtime (`%z` offset, so it is reconcilable across a DST change) of the `hardware_config` module it **actually imported** (off `hw.__file__`) once at boot. **Phase 5 is partially delivered**: plan item 1 asked every node to log its effective tuning values + a config hash — delivered is one node, identity only; item 3 (`friction_ff_params.py` resolution order) is untouched pending the owner decision. Honest scope, per the plan: this would NOT have caught `24608bb`'s stale binary — that is Phase 7's pio-clean forcing. | [2026-08-01-config-drift-gate](2026-08-01-config-drift-gate.md) |
| 2026-08-01 | resolved | Refactor programme Phase 7a — ERR_TIMEOUT recount | **The hand-ack epidemic survived the bus-role swap at the same rate, on a bus with literally zero recorded wire errors — it is not the CAN3 drive-path fault.** The RPC code is recoverable offline from `~/.ros/log/*/launch.log`, **not** from the bags (no recorded topic carries an RPC status; `/rosout` is not in the record list) — the bags supply the context and the date. Pre-swap pooled (15 sessions, 07-23→07-31 13:56): prime **59/125 = 47.2 %**, arm **135/258 = 52.3 %**; post-swap (07-31 14:51, the only post-swap session with hand dispatches): prime **4/8**, arm **4/8**. **Every recoverable HAND_TRAJ_CMD failure is `ERR_TIMEOUT`, none `ERR_BUS_DOWN`** (distinct firmware paths: `hand_ops.cpp:34` gate vs `:42/:45/:56` sends), and every one is a Teensy-returned code — `rpc.py:86` synthesises host-side `RpcTimeout` with the same `METHOD: ERR_TIMEOUT` prefix, but `grep "ERR_TIMEOUT after"` finds it only on HOME/STATE_READ/STATE_WRITE/GET_AXIS_VERSIONS, never on the hand path. In the four codes-visible sessions `prime_fail + arm_fail == ERR_TIMEOUT lines` **exactly**, 93/93, validating the derived arm counter (needed because `20d01e9`, 07-24, demoted the arm-ack failure WARN→DEBUG). **Decisive cell**: post-swap bag `2026-07-31_14-51-38` (n=10513) reads `bus1/2/3_health` OK 100 %, `fault_state` NONE, `can3_errors` `tecInc=0 … bit1=0 gated=0` on **every** sample — zero wire errors, zero TX-gate refusals, still 4/8 acks failing ⇒ every failure was `bus.write() <= 0`, a FlexCAN write rejection on an error-free bus. **The epidemic era has no comparable cell and never will**: `can3_errors` is absent from the 07-27 and 07-29 bags entirely (33 keys) and reads `unknown (never seen)` in the pre-swap 07-31 13:56 bag, so the pre-swap half rests on the coarse 10 Hz health classifier alone — a counter-level pre/post comparison is impossible retroactively. **Open tension, deliberately left open**: a write rejection means the trajectory frame was never enqueued, but `catch_coordinator_node.py:159-166` encodes "the ack lies — frames were observed transmitted after a failed ack" as the reason `_MAX_ARM_DISPATCHES` KEEPS the latch; both cannot be true, the `tx_write_fail` counter settles it, and **the latch behaviour stays as it is until then**. A tempting `mpc_active` correlation (p≈9e-4) killed as pure selection. Swap dated 14:17–14:51 on 07-31 from the `bus3_health` marker row (keys 36→37), not the 15:29 commit stamp. Needed to close: `tx_write_fail` split from `tx_gated`, per-send attribution, a TX-queue high-water mark, a `link_status` hand-ack-failure counter, and one ordinary post-swap sitting (the CI on 4/8 is enormous). Read-only throughout; closes open item 4 of the 07-31 CAN3 entry, refuting its hypothesis. | [2026-08-01-err-timeout-recount](2026-08-01-err-timeout-recount.md) |
| 2026-08-01 | resolved | Refactor programme Phases 2 & 3 — nightly tier + MPC dormancy | **A test tier is only honest if something runs it, so the runner and the first demotion landed in one commit.** `tools/nightly_suite.sh` (armed as a `systemctl --user` timer, 04:00, `Persistent=true`, `Linger=yes`) runs `./run_tests.sh --full --hypothesis-profile=ci-deep` and writes `temp/reports/nightly/{report,junit,latest.md,status}`; a CLAUDE.md session-start rule makes `status` the delivery channel. `run_tests.sh` gained `--full` and per-phase marker composition — `not serial and not nightly` / `serial and not nightly` by default, deliberately AND-ed because a bare `not nightly` drops serial+nightly tests out of BOTH phases while the gate still prints PASS. Demoted on CONTENT, never runtime: `test_juggle_selfcatch.py` split (MAKE capability + composition smoke stay per-commit at 61 s; the two BREAK characterizations and three 6-seed sweeps go nightly), the `test_demo_juggle_*` e2e, and the 15-file MPC battery — because Phase 3 made the MPC operationally dormant (`jugglebot_launch.py` no longer starts `motor_guard` or `motion_bridge_node`; both already drove nothing since the `can_node` deletion). Docs re-framed: the **Teensy-side `MAX_DEVIATION` guard** is the leg-path safety authority, not `motor_guard`. Marker vocabulary is now `serial` + `nightly` only (`slow` deleted — one use, on a 9 s module). Collect-only equality gate recorded: 3890 + 3 + 432 = 4325 = 4316 + 9, all 9 serial tests accounted for. `test_retime.py` bisection 60 → 25 iterations, verified by a 54-case corpus diff with **zero** verdict changes. | [2026-08-01-nightly-tier-and-mpc-dormancy](2026-08-01-nightly-tier-and-mpc-dormancy.md) |
| 2026-08-01 | resolved | Refactor programme Phase 1 — process rules | **Process ceremony trimmed, and the retirement made to reach the tools that generate entries.** SHA-backfill retired (the `Logbook-Entry:` trailer is the canonical *bidirectional* link; verified first — of 175 entries dated 2026, 156 reachable by `git log --grep`, 16 more by recorded SHAs, 3 by neither, all pre-convention); short-form entries (10–30 lines + the verification triple) are the default, with the full form MANDATORY under the three Discussion triggers; `/audit` scoped to ≥2 narrative files or any normative doc; one `./run_tests.sh` satisfies both suite obligations when no edit intervenes; CLAUDE.md 25.4 → 22.1 KB with every rule retained (worked examples relocated, not deleted); five done plans archived + a test-pinned `plans/active/INDEX.md`; memory 53 → 18 topic files. Review caught the failure that matters: the first pass retired the convention in the docs that *describe* it but not in `/log`, `/next-phase-prompt` or `bb-tilt-phases.js`, which *generate* entries — an un-rippled generator reverts a retirement on its next run. | [2026-08-01-process-rules-phase1](2026-08-01-process-rules-phase1.md) |
| 2026-07-31 | resolved | Developer workflow — test-suite hygiene | **The 4→5 protocol bump was done correctly and still left the branch red: `PROTOCOL_VERSION` was pinned in two places and only one of them is named in the procedure that maintains it.** `tests/teensy_link/test_protocol_codec.py::test_constants_match_firmware_spec` failed `assert 5 == 4` after `bf1e9a5` ("cone PROFILE slot, UDP v5, FW8") — a bump that had updated every artefact the checklist names: the generator spec, the generated Python module, the committed C++ header, and the canonical pin. That canonical pin, `tests/firmware/test_udp_protocol_xlang.py::test_protocol_version_frozen`, is the designed tripwire: it pins the spec with a full bump-history comment, cross-checks spec == generated Python == committed C++ header, and prints the fleet-reflash procedure on failure. The second pin was a bare literal in `test_protocol_codec.py` that is **not** named in that procedure, so following the checklist to the letter still ships a red suite. **The fix deletes the duplicate rather than bumping it** — a 4→5 edit is the thirty-second fix the failure message invites, and it rebuilds the trap for the next bump. Pin 2 was fully redundant (`p.PROTOCOL_VERSION` is imported from the generated module pin 1 already checks), so it carried no independent signal and full maintenance cost; the assertions pin 1 does *not* cover are kept — `PORT_STREAM`, `PORT_RPC`, `NUM_LEGS`, `NUM_AXES`, `HEARTBEAT_HZ`. Also de-stamped a stale `PROTOCOL_VERSION 4` comment at `teensy_bridge_node.py:216`. Class of failure: **one fact pinned in two places with only one of them named in the procedure that maintains it** — a duplicated pin is not twice the protection, it is one real check plus one time bomb. Verification: scoped `pytest tests/teensy_link/ tests/firmware/test_udp_protocol_xlang.py -q`, run 2026-07-31: **231 passed**; full gate `./run_tests.sh`, run 2026-07-31: **parallel 446 s (4287 passed, 3 xfailed) \| serial 32 s (7 passed) \| total 478 s** — `RESULT: PASS`. | [2026-07-31-protocol-version-duplicate-pin](2026-07-31-protocol-version-duplicate-pin.md) |
| 2026-07-31 | resolved | Refactor programme Phase 0 — review + dead-weight sweep | **Refactor programme kickoff**: 13-agent review + red team — core architecture healthy, drag = boundaries/dead weight/god-files/process ceremony; owner decisions recorded (SHA-backfill retired, short-form logbook default, 4am nightly runner, teensy_link → repo root, MPC operationally removed with code parked, build-frozen config + drift warning); CAN3 hardware verdict stress-tested and upheld. Phase 0 sweep: `jugglebot/archived/` (16,311 lines) → `attic/`, sim ghosts + dead `yasmin` removed. Programme: `plans/parked/refactor-2026-07.md`. | [2026-07-31-refactor-programme-kickoff](2026-07-31-refactor-programme-kickoff.md) |
| 2026-07-31 | resolved | Refactor programme Phase 0 — analysis-layer bugfix | **Telemetry CSV loaders**: compare.py's duplicate loader crashed on current-schema CSVs (`float('')` on `throw_phase`) — every /diagnose entry point dead on the next fresh CSV; TelemetryLogger.load silently mistyped int fields as str. One typed canonical `load_records()` now serves both; round-trip test pins types. Load-side only — the 40 Hz hot-loop append path is byte-unchanged. | [2026-07-31-telemetry-csv-loader-typed](2026-07-31-telemetry-csv-loader-typed.md) |
| 2026-07-31 | resolved | Developer workflow — test-suite runtime | **The pre-commit gate was 27:40 and had quietly stopped being run — 4 xdist workers on `--dist loadfile` cut it to ~8 minutes, and the ROS plugin suppression turned out never to have worked.** Serial baseline (`pytest tests/ -q`, run 2026-07-31 on the Jetson, 6 cores / 7.2 GB, isolated worktree at `a7da5c5`): **4297 tests / 1660.65 s (27:40)**. The suite now runs as a parallel phase (`-n 4 --dist loadfile`) plus a 7-test serial phase through `./run_tests.sh` in **~8 min — a 3.5× cut, no test rewritten and no coverage dropped** (bare `pytest tests/ -q` stays serial, unmodified and still equivalent, so the parallel setup is never load-bearing for correctness). **`--dist loadfile` is the load-bearing choice**: keeping every test in a file on a single worker is what made this adoptable without auditing 4300 tests individually — the 49 module-/session-scoped fixtures build once per file exactly as they do serially, and the two files that bind **fixed** localhost ports (`test_motor_guard.py` :15555/:15556, `test_teensy_bridge_node_setpoint.py` :5599) cannot collide; the default `--dist load` scatters a file across workers and both hazards return nondeterministically. **Second, unrelated defect found and fixed**: the `-p no:launch_testing` / `-p no:launch_testing_ros` entries in `pyproject.toml` had **never disabled anything** — those are module names and pytest's `-p no:` takes pytest11 *entry-point* names (`launch`, `launch_ros`), and a `-p no:` that matches no plugin is a silent no-op, so both ROS launch plugins had been active on every run since the addopts block was written. The consequence is real: the launch plugin imports every collected module at collection time, so one unimportable test file aborts all 4297 tests as a single collection error — exactly how this investigation's first benchmark attempt died. **Worker count pinned at 4, not `-n auto`** (which selects 6 here), from a measured 2×2 of workers × BLAS thread pinning, all 2026-07-31: (default, 4) 444.37 s; (default, 5) 429.14 s / 237 MB avail-mem floor / peak load 8.13; (pinned, 4) **444.69 s / 417 MB / 6.32** ← shipped; (pinned, 5) 403.55 s / 337 MB / 6.41. **Pinning is a contention knob, not a speed knob** — worth 0.07 % of wall-clock at 4 workers, but it stops every worker sizing its BLAS pool from the core count, dropping peak load 8.13 → 6.41 and raising the memory floor, and it is what makes more workers actually pay (4→5 buys 3.4 % unpinned, 9.2 % pinned). 5 workers therefore stays one env var away (`JB_TEST_WORKERS=5`) rather than default: this Jetson also runs the robot, and its swap is **zram** and already ~50 % used, so over-commit degrades hard and an OOM-killed worker surfaces as a confusing "node down". The **`serial` marker set was re-derived from the contract rather than from observed failures**, after an audit caught the lazy first pass — grepping for process-global measurements yields 5 tracemalloc/GC tests plus 2 CasADi wall-clock solve-time budgets (~28 s tail), the last two of which no failure had nominated. Also adds `--strict-markers`, closing the sibling silent-no-op so a typo'd marker fails loudly instead of selecting nothing. | [2026-07-31-test-suite-parallelisation](2026-07-31-test-suite-parallelisation.md) |
| 2026-07-31 | resolved | CAN3 instability root-cause hunt (continuation of the 07-29 flap) | **The CAN3 wire errors were the bridge's own analog drive path failing under load — software exonerated by exhaustion; Jugglebot now runs on the CAN2 controller.** Exclusion ladder on deployed-state deltas, one element per rung: poller boots-OFF v6 (errors with ZERO poller frames on the ≤7-frame boot version sweep), USB unplugged, bridge **FW3 rollback** (clean-era binary reproduces within seconds), Platform Teensy **V1 rollback** flashed via the Arduino IDE (toolchain-clean A/B per the platformio.ini compile-gate discipline), a different hand ODrive, the Platform Teensy removed entirely (termination kept ~60 Ω), toolchain pinned (teensy@5.1.0, package mtimes May–June) — the fault survived every rung. **Breaking asymmetry**: RX at the bridge perfect throughout (7/7 heartbeats fresh, Platform-Teensy 2 Hz report clean end-to-end, REC ≈ 0, ACK pulses working) while ONLY the bridge's own full-frame CAN3 TX died bit1-dominant — a weak transmitter, not a sick cable; worst boot: `tec_inc_sum` 269 365, one BUS_OFF latch, FlexCAN's infinite hardware retransmit = the observed "once started, never stops". **Conviction by role swap** (2-line FlexCAN instance change + ESR1 base addresses + physical plugs): Jugglebot loom on CAN2 = flawless including a full reload session; cone on CAN3 = clean under sustained 100 Hz 0x7DD (light load); the same CAN3 path against the 8-node chain minutes earlier = catastrophic ⇒ **load-dependent drive-path fault** (transceiver output stage or resistive joint, hub-connector↔Teensy stretch), progressively worsening 07-29→31. **Landed (FW 6→7)**: the swap becomes the operating config (wire names stay role-keyed); poller boots ON again (Phase 7 runbook premise restored; FIX C dismissed — rate was never the problem); cone BusHealth added to `HeartbeatT2J.flags` bits 4-5 (no payload change, no PROTOCOL_VERSION bump, UNKNOWN=0 self-describing) → `/link_status` `bus3_health` → GUI health dot live with the row-grey/health-dot rendering split fixed; GUI relabeled to the physical truth (CAN2 = Jugglebot core with the role-keyed charts, CAN3 = cone, health-only); Platform Teensy gains a deterministic two-Teensy pio flash path (`-w`, no `-s`, `--mcu=TEENSY40`, button-press selects) and is reflashed V2 (decel FF) via pio. Verification: `pytest tests/firmware tests/ros -q`, run 2026-07-31: **1931 passed in 277.30 s**; xlang wire digest unchanged (**40 passed in 0.18 s**); `bus3_health='OK'` probed end-to-end on `/link_status`. OPEN: repair the CAN3 path then revert the swap; the substitute hand ODrive needs `gpio2_mode=1` + fw 0.6.11 if it stays; cone PROFILE slot (traffic charts); re-check the ERR_TIMEOUT epidemic's incidence on CAN2 — plausibly the same dying path all along. **Same-evening postscripts**: the v5 bump's first flash shipped a stale-header v4 binary (incremental build) → silent-and-total link darkness; clean-rebuild + ON-WIRE version-byte verify is now standing practice after any PROTOCOL_VERSION bump. **CORRECTION — the "ODrives went silent as a group" observations were a display misread**: a temporary per-cmd histogram proved heartbeats (7×10 Hz) + full telemetry flowing continuously throughout; `[axes]` `*` = active-error mark (uniform 48-V-off undervoltage, documented benign) and the number is heartbeat age in **ms**, not a count — the drive-path conviction rests on the bridge's TX evidence and stands. **Platform-Teensy pio image is CAN-MUTE** (board enumerates as "Teensy 4.1" despite being a 4.0 — 4.1-flavoured build leakage suspected): pio uploads SUSPENDED in platformio.ini (buttonless machinery retained, disabled); an Arduino-IDE V2 reflash restored the relay conduit (`platform_fw_version: 2`). And a **Foxy logger landmine** (one line logging two severities in `_set_mpc_active`) crashed the node on the first STANDBY arm — the safe-shutdown stow behaved exactly as designed — fixed with distinct call sites + the one sibling (`ball_butler_node.py:575`). | [2026-07-31-can3-drive-path-fault-jugglebot-to-can2](2026-07-31-can3-drive-path-fault-jugglebot-to-can2.md) |
| 2026-07-29 | resolved | Hand ball-present sensor — post-flash CAN3 regression (FW_VERSION 4) | **CAN3 sat error-passive 42.4 % of a session in which the robot was never commanded — and the gate meant to protect the bus was the amplifier.** Bag `2026-07-29_22-37-06`, robot idle all 83.5 s: `bus1_health` WARN 354/834 samples, **426 transitions / 213 full cycles**, WARN runs median 100 ms (max 900), **zero BUS_OFF**, BB CAN1 OK ×834, UDP `crc_errors`/`decode_errors` 0. **Key correction that reframed severity**: WARN does **not** key on the error-*warning* level — `can_buses.h:196-198` keys on **error-PASSIVE (TEC or REC ≥ 128)** and documents that 96 was deliberately rejected (a normal 12 V ramp peaks at REC ≈ 121), so 128 costs ~16 failed transmissions at +8 each. Staleness excluded structurally (480 OK samples *require* RX fresher than the 2 s `CAN_HEARTBEAT_TIMEOUT_US`, so every WARN is a live ESR1 `flt_live`); Teensy reboot excluded (`uptime_ms` monotonic, steps `{100: 832, 300: 1}`); Jetson excluded (first OK→WARN at **t = 0.089 s**, the first sample, bridge already up 755 s). **Controls are the spine**: 11 pre-flash sessions all **0.0 % WARN**, including **six in the identical idle state** (kills "idle ⇒ weak decay pump ⇒ flap" as sufficient) and one at **759 s uptime** vs tonight's 755 (kills fresh-boot transient); platform-Teensy fw 2 and the hand `gpio2_mode` NVM change both sit on the clean side. `git diff b3c82f7..HEAD` over the bridge = 15 files, every one traced: **the only new CAN3 frames are `0x0C4`/`0x0C5`, the FW4 50 Hz hand-sensor poll**. Poll grid measures exactly **49.999 slots/s**, but only **1205/4164 slots (28.94 %) get a reply**, 89 gaps ≥ 240 ms, max 980 ms. **Layer 1 ESTABLISHED**: the poll drives sustained error-passive and `jugglebot_commands_allowed` (`can_buses.cpp:760-767`) converts it into a system-wide ~42 %-duty outage — `platform_relay`, `hand_ops` (the catch stroke), leg/config RPCs, homing, version-check, and the poller itself (`gpio_poll.cpp:247`), which is the **positive-feedback limit cycle**: refusing TX removes the 0x7DD decay pump that `can_buses.h:200-202` names outright. Coupling measured: P(stale ⏐ WARN) 0.223 vs 0.052 OK, **RR 4.29×**, φ **+0.256** confined to L = 0/+1 (a shared *fast* mechanism), mean reply age +80.0 ms in WARN. **φ ≈ 0.26, not 1** — half the OK-window slots also miss, so the transaction fails beyond the gate. **Layer 2 OPEN by construction**: which wire-error class raises TEC/REC is unanswerable from a 90 s bag because the per-class counters are computed at 1 kHz and **discarded to a serial console** (`can_buses.h:269-270`). **The prime suspect died**: H-A concurrent FlexCAN TX killed twice — every write already runs in a PRIMASK critical section atomic against tasks *and* ISRs (`can_buses.cpp:659-732`), and `setpoints_sent = 0` means the 500 Hz interp writer, the only concurrent writer that exists, was **silent all session**. Also killed: malformed frame (wire form byte-identical to BB's production `requestArbitraryParameter`; endpoint 726 confirmed empirically, raw word tracked the physical ball 622 held / 212 empty; ACK precedes acceptance filtering anyway), FIFO overflow (does not touch TEC/REC/FLTCONF), bench-sysid build (50 Hz grid + 99.62 Hz telem, no 250 Hz topic anywhere), `0x0C4` duplicate-ID (platform analyser's `timingID` is `0x004`, RX-filter only), and "poller isn't flashed" — that last came from a **stale plan cell** and misled a sub-investigation. **Good news**: `leg_interp`'s 500 Hz setpoint TX calls `can_jugglebot_send` **directly** and bypasses the gate, so the safety-critical path is untouched in both directions. **Latent chain found, did not fire**: every WARN→OK edge dispatches the Jetson's conservative cold-start `STATE_READ` (`teensy_bridge_node.py:1765-1776`), itself gate-refusable, whose fallback sets `is_homed = False` (`:3097-3116`) — **a flapping CAN3 can make the robot forget it is homed** (bag: `cold_start_is_homed = '1'` ×834, but 213 edges in 83 s is thin margin). **FIX 0** (zero code, zero flash): `gpio_poll off` restores exactly FW3 CAN3 behaviour — **trap: `gpio_poll_init` boots ON (`gpio_poll.cpp:190`), the toggle does not survive a reboot**. **FIX A** is the class-closing contract: a **second** classifier `classify_command_gate()` gating on **sustained** confinement (`CAN_PASSIVE_SUSTAIN_US = 1.0 s`, BUS_OFF still instant, FW 4→5), with `classify_bus_health` left **byte-unchanged on purpose** — adding hysteresis there would hide the flap from `/link_status` in the same commit that stops it hurting, while layer 2 is still open. Threshold sized by **physics not the observed distribution** (at the 100 Hz floor, TEC 160→127 takes ~330 ms, so still-passive-at-1 s is not self-healing; and 1 s stays under the 2 s staleness term). **Tradeoff named**: FIX A widens the command window on a degrading bus — accepted because error-passive nodes still transmit, FlexCAN retransmits, callers have ack/timeout paths, `partner_recent` still refuses a partner-less bus, and leg setpoints were **already** ungated there, so it is strictly less exposure than the status quo. Control walk: **no TX timing changes**, one MPC cycle is bit-identical. **FIX B** uplinks the counters (additive MsgType `0x8C`, no PROTOCOL_VERSION bump). **FIX C deliberately deferred** behind the bench A/B discriminator table (`tecInc` vs `recInc` vs `ack=`/`bit1=`/`stuff=`), which **must run on the current FW4 before the fix is flashed**. Untested-but-recorded, with an earlier draft's error corrected: gate refusals return **`ERR_BUS_DOWN`** (`rpc.cpp:117`), **not** `ERR_TIMEOUT` (`rpc.cpp:118`, a `can_jugglebot_send` failure FIX A does not touch and **may raise**, since more commands now reach the send path) — so the open epidemic must first be attributed to a code before FIX A can be tested against it; 10 Hz sampling being blind to sub-100 ms excursions is what lets 0.0 % sampled WARN coexist with a real epidemic. **Safety verdict — with the poller ON do NOT run a catching or throwing session**: not dangerous (refusal, not misbehaviour; no faults, no clamps, no BUS_OFF) but operationally broken, since catching depends on `hand_ops`. With `gpio_poll off`: safe, FW3-equivalent. **Verification** — `pio run -e teensy41` + `-e teensy41_bench_sysid`, run 2026-07-29: both SUCCESS; `pytest tests/firmware -q`, run 2026-07-29: **368 passed in 190.94 s**; `pytest tests/ros/ -q`, run 2026-07-29: **1563 passed in 85.63 s** (1552 before); `pytest tests/ -q`, run 2026-07-29/30 overnight: **4294 passed, 3 xfailed in 1410.34 s**. **Addendum 2026-07-30/31**: the pre-registered A/B ran **NULL** (all counters frozen, all three arms — the condition is intermittent on an hours scale); same-arbitration-ID contention **exhaustively excluded** (single-transmitter proofs for 0x7DD/0x0C4 incl. the Platform Teensy's full git history; the three genuine shared-ID pairs rate/window-killed; `bad_axis` +2/s = **exactly** the Platform Teensy's 2 Hz 0x7DF report = wire-proof no foreign 0x7DD under SRX_DIS; foreign-0x0C4 blind spot named); operator physical inspection clean (CANH/CANL 66 Ω ≈ nominal 60); **v6 `cae3e6e` TEMP boots the poller OFF** (FW 5→6, runtime default only — serial `gpio_poll on|off` survives for the live-toggle arm; Phase 7 runbook premise suspended until the 6→7 revert); and the first poller-silent boot **reproduced the fault with zero poller frames**: the ≤7-frame Get_Version sweep (the boot's entire TX census — 0x7DD self-gates unanchored, no launch) collected ~18 failed attempts, bit1-dominant TX-context, TEC 146 error-passive, all counters frozen once the sustained gate closed. Layer-1 *limit-cycle* attribution stands; "poll traffic is the error *source*" **withdrawn** — the physical layer stands by elimination (prime suspect: the hand ODrive CAN stub, disturbed by the 2026-07-28 sensor install). Same boot, needs bench-state confirmation: all 7 ODrives heartbeated (counts 3–98) then went silent (`fresh=0/7`) while the Platform Teensy stayed clean at 2 Hz. Verification: `pio run -e teensy41 -t upload`, run 2026-07-31: SUCCESS (13.77 s), on-wire status `enabled=0`; `pytest tests/firmware -q`, run 2026-07-31: **368 passed in 191.34 s**. **CLOSED 2026-07-31**: root cause = the bridge's CAN3 analog drive path, load-dependent — see the 2026-07-31 entry; FIX C dismissed, poller boots ON again (FW7), Jugglebot runs on the CAN2 controller. | [2026-07-29-can3-bus-health-flap-hand-sensor-poller](2026-07-29-can3-bus-health-flap-hand-sensor-poller.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 6 (GUI pill + session runbook) | **The final software phase: the sensor reaches a human, twice.** A tri-state **ball-in-hand pill** in the System panel flags-grid (`flag-level-pill` precedent, driven from the existing `onHandTelemetry`): **filled** `HELD`, **hollow** `EMPTY`, greyed **DASHED** `UNKNOWN` — a *third look*, deliberately not a dimmed EMPTY, because fill/hollow is one visual axis and a faded hollow pill reads as "no ball, probably", collapsing at the last hop the distinction every layer below fought to keep. Flicker marker is a trailing `" ~"` in the **text** (single-encoded after review — the plan says "a flicker marker", singular; colourblind-safe and assertable from a DOM string; the first draft's amber ring deleted). The **latch semantics are the review-driven improvement**: set only on a valid sample whose raw bit disagrees with an *unchanged* verdict, cleared on any verdict change and any invalid sample — so the ~100 ms disagreement accompanying **every** ordinary edge (Phase 3's 5-sample debounce doing its job) never wears the marker, and a persisting `~` is unambiguous mid-carry spotty contact. A diagnostic that fires on the healthy path is worse than none: it trains the reader to discard the channel. 1000 ms hold (50 Hz sensor vs 100 ms GUI throttle — instantaneous would be unseeable). **Two blindness paths, two mechanisms**: `ball_held_valid` covers a dead **bridge** (node still publishing, vouching honestly), a **1 s message-stop watchdog** (10× throttle, `setMocapConnected` idiom) covers a dead **publisher** — the disconnect-branch reset deleted as redundant. Initial DOM state is **born UNKNOWN** in `initFlagsGrid`, forced by Phase 5 open question #4 (`/hand_telemetry` publishes nothing until the bridge's first `Telemetry` frame). New **7-test tripwire** `TestBallPillFieldNameContract` pins `ball_held`/`_raw`/`_valid` in **both** the `.msg` and `panels.js`, incl. a `\b(?!_)` lookahead so the bare verdict can't pass on its own prefix — rosbridge JSON means a rename yields `undefined` → false → **UNKNOWN forever, silently**, indistinguishable from a dead sensor during the one sitting that reads this pill; mutation-verified. Plus `tests/hardware/session_hand_ball_sensor.md` (**952 lines**, `session_phase7_reload.md` pattern) running the plan’s five Phase 7 steps as **six runbook steps** (bring-up inserted as step 3, so plan 3/4/5 = runbook 4/5/6). **Both BLOCKING findings came from the operator-safety lens** and neither was reachable from correctness/conformance: a motion-framing contradiction (fixed by inserting a bring-up step — launch → home → activate → confirm hold) and step 6 needing an explicit safe state before **every** hand-in-cup action, not once per step (hand axis is energised whenever ACTIVE; deactivate → seat → re-activate preferred). **Tooling honesty** as a runbook principle: step 4 states outright that true request→reply RTT is measurable by **nothing shipped** (the send time is recorded nowhere) — what IS measurable is inter-reply intervals from distinct `ball_held_stamp` values (mode ≈20 ms) + valid/stale/miss fractions, which sizes the provisional 240 ms `REPLY_STALE_US`; true RTT is an operator decision (firmware counter vs CAN analyser). Never let a step imply a measurement the system cannot make. Also baked in: `ros2 topic echo --once` **does not exist** on this Foxy box (older runbooks are wrong here), the bridge USB cable was **not connected** at authoring time (blocking precondition for steps 1/2/5), no soft-reboot exists so the banner recipe is `pio run -e teensy41 -t upload -t monitor` (**`-e` is load-bearing** — two envs, no `default_envs`, so an `-e`-less flash lands the NEVER-flash bench-sysid binary LAST and every v4 gate passes identically on it; `default_envs = teensy41` now added too), and ABORT-park leaves the bridge v4-with-poller-silent (v3-identical CAN3 load) but **not persistently** — re-apply after every reboot. **END-OF-PLAN FULL-SUITE GATE green**: `python -m pytest tests/ -q` **4288 passed, 3 xfailed in 1412.22 s**, reconciling 4271 + 10 + 7. Live as served (static files, no build step, hard-refresh only); browser confirmation of the four states is the runbook's pre-flight P3, **deferred to the operator — a real deviation from Phase 6's Done-when**, because no headless check in this repo can render `panels.js`. | [2026-07-29-hand-sensor-gui-pill-runbook](2026-07-29-hand-sensor-gui-pill-runbook.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 5 (ROS surface) | **The sensor reaches ROS as a tri-state.** `HandTelemetryMessage` gains exactly four appended fields (`ball_held` debounced, `ball_held_raw`, `ball_held_valid`, `builtin_interfaces/Time ball_held_stamp`); `teensy_bridge_node` subscribes to `MsgType.HAND_SENSOR` and caches the frame + a **Jetson-monotonic** RX stamp under `self._lock`. The load-bearing part is the **two-hop validity gate**: `ball_held_valid` requires the frame's own `VALID` **and** `TIME_SYNCED` **and** an RX age ≤ **3.0 s** on the host monotonic clock (≈3 keepalives) — never against `t_bridge_us`, a foreign wall clock that can step. Neither hop suffices alone: flags say "my cache is good" but can't see a dead link (the publisher is a free-running 100 Hz timer over cache, so flags-only republishes `held, valid` forever unplugged); RX-age says "I heard from the bridge" but a failed poller keepalives punctually with `¬VALID`. Never-seen ⇒ all-False + zeroed stamp; seen-but-invalid **retains** its bits and stamp (the "what was it, how old?" diagnostic — the flag is the protection, wiping only changes which wrong answer a broken consumer gets). `/link_status` gains `hand_ball_sensor` = `held\|empty\|unknown\|stale miss=N raw=0x········` in **lowercase** hex matching the bridge console's `%08lx` (Phase 7 step 2 compares them textually); `empty` reachable only from a valid reading, never-seen renders `unknown (never seen)` rather than fabricating zeros. 10 tests on the paired-node harness; the review catch worth naming is the **inverted active-low raw-word constants** — held ⇒ bit 2 **CLEAR**, empty ⇒ SET — inert to every assertion but the artifact Phase 7 operators read to learn the toggle direction. Same shape as the fix for the BallButler fail-open reload-skip defect (age-only, no vouching term). **Not deployed** — needs `colcon build --packages-select jugglebot_interfaces jugglebot` + relaunch. | [2026-07-29-hand-sensor-ros-surface](2026-07-29-hand-sensor-ros-surface.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 4 (additive uplink message) | **Small phase: Phase 3's snapshot reaches the wire.** New `HandSensor` message at MsgType **0x8B** (next free slot in the reserved 0x89–0x8F telemetry block), 14 B packed exactly per plan (`u64 t_bridge_us` wall-at-last-good-reply, `u32 raw_states` verbatim, `u8 flags`, `u8 miss_count`), emitted by `hand_sensor_uplink_step()` in `telemetry.cpp` beside the hand cmd-echo (no new TU), called from `task_telem`. **Additive ⇒ no `PROTOCOL_VERSION` bump** (LegCmd precedent — the two ends deploy in either order); `test_wire_layout_frozen` re-pinned in the same change (`5ad4fa88cf2b…` → `1e1cf7314377…`), version pin untouched at 4. Three review fixes shape it: `flags` promoted to a **generated `HandSensorFlags` enum** because `test_wire_layout_frozen` hashes field `name:type` and **not comments**, so a literals-plus-prose bitset could let producer and consumer drift with the suite green (`HeartbeatT2JFlags` precedent; Phase 5 reads the same names); a `static_assert` pinning `CHECK_INTERVAL_MS × TELEM_RATE_HZ ≥ 1000`, since nothing else bound poll-period ≥ telem-tick and a Phase 7 retune below it would silently coalesce the raw per-sample bit that approved decision #4 requires reach ROS; and the `controller/teensy_link` re-export landing in **both** `protocol.py` and `__init__.py` (both prior additive messages did — `teensy_bridge_node` imports from the package root, which Phase 5 needs). Freshness is an **equality test** on the reply's wall stamp, never an interval (a wall-anchor step costs one redundant frame, not a wrong verdict), so the wire rate is the **poll** rate 50 Hz, not the 100 Hz telemetry rate; a 1 Hz keepalive makes staleness observable rather than silent. Deliberately **not** gated on `JBBallDetect::ENABLED` — a kill-switched build still keepalives honest-UNKNOWN, so "sensor compiled out" ≠ "no bridge at all". **Not flashed** — ships with Phase 3 at Phase 7 step 1. | [2026-07-29-hand-sensor-uplink-message](2026-07-29-hand-sensor-uplink-message.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 3 (bridge firmware poller) | **The phase where the qualified endpoint id finally leaves the Teensy.** New TU `Teensy_code_canbridge/gpio_poll.{h,cpp}` on `task_homing` (100 Hz) beside `version_check_step`: a two-phase non-blocking request/await machine polling hand node 6 with an RxSdo `OPCODE_WRITE` of `EndpointId::odrive_pro_0_6_11::get_gpio_states`, decoding the TxSdo `uint32` through a new typed decoder (the dead C++ float32 one deleted; the `_u32` suffix **kept** because Python's same-named decoder has float semantics). Every normative semantic implemented and behaviourally tested (console rendering excepted): tri-state UNKNOWN, timeout advances staleness only (never the verdict, never `miss_count`), saturating-and-frozen `miss_count`, asymmetric 5-sample HELD→EMPTY debounce, first-ever-EMPTY ⇒ EMPTY, and a **stale-gap reseed** so a pre-outage HELD cannot survive a 30 s outage for even 100 ms. Three-state `Get_Version` gate — UNRECEIVED ⇒ quiet/no fault, MATCH ⇒ poll, MISMATCH ⇒ latched park printed at 1 Hz — which **explicitly amends** `version_check.h`'s "ZERO version SEMANTICS in firmware": the wrong-build id *answers plausibly*, so the refusal must precede the RxSdo leaving the Teensy, and only firmware can do that. Publication is a PRIMASK critical section, **not** the house seqlock: the reader (`PRIO_UDP_TX 3`) outranks the writer (`PRIO_HOMING 2`), so a preempting reader would spin against a writer that can never be scheduled — livelock, not retry. Stamps captured at **reply arrival** (a review fix; stamping at the consumption tick biases `t_bridge_us` 7–9 ms late, material inside the pre-registered ~35 ms sigma). Runtime `gpio_poll on\|off` toggle required building the bridge's first serial **input** path; the one BLOCKING finding was the off-path failing to drain the state machine, which would republish a mid-off-window reply as fresh on re-enable. Kill switch proven by build matrix — `enabled:false` text returns to the exact 226624 baseline. `FW_VERSION` 3→4; two test layers (xref source-scan + 13-case/100-assertion native behavioural on the real TU), both mutation-validated. **Not flashed** — Phases 3+4 ship in one flash at Phase 7. | [2026-07-29-hand-sensor-bridge-gpio-poller](2026-07-29-hand-sensor-bridge-gpio-poller.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 2 (endpoint-id contract) | **The repo's ODrive endpoint ids were S1 values in a flat table with nothing saying "S1".** Jugglebot's hand is a Pro, where `get_gpio_states` is 726, not 700 — and 700 on a Pro is `encoder_estimator1.status`, a read-only byte that **answers** well-formed TxSdo replies, so the wrong id reads as a live sensor that never changes and "no reply ⇒ wrong endpoint" commissioning is worthless. Fixed as a contract, not a `700→726` edit: `protocol_config.yaml` endpoints restructured into (board, fw)-qualified groups (`odrive_pro_0_6_11.get_gpio_states: 726`, `odrive_s1_0_6_11.get_gpio_states: 700` + `commutation_mapper_pos_abs: 488`), both emitters taught the nested shape (`EndpointId::odrive_pro_0_6_11::get_gpio_states`, `ENDPOINT_ODRIVE_PRO_0_6_11_GET_GPIO_STATES`), every consumer migrated in the same change. 488 moves **verbatim** (production-proven on BB's S1); the Pro's 461 is deliberately **not** emitted — the contract qualifies ids by evidence, not table completeness. Jugglebot's dangling `commutation_mapper.pos_abs` is **deleted, not re-pointed**: re-pointing would hand an S1 id to a Pro-facing module, the exact class defect being closed, one line below the fix. Native golden regenerated (2 of 19 rows, `e801`→`d602`); new YAML→generated round-trip test pins codegen freshness *and* the name-mangling contract Phase 3 quotes. BallButler lands in two commits so `git blame` never blames the endpoint phase for the motor constant. | [2026-07-29-hand-sensor-endpoint-id-contract](2026-07-29-hand-sensor-endpoint-id-contract.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 1 (config groundwork) | **Config only — the block Phase 3 reads from, plus the drift test that keeps it honest.** New `jugglebot_ball_detect` YAML section 23 (sibling of `ball_butler_ball_detect`: `enabled` build-time kill switch, `gpio_pin: 2`, 50 Hz poll, 5-sample HELD→EMPTY debounce, `expected_fw: [0, 6, 11]`), registered in `generate_config.py`'s table as `JB_BD_` / `JBBallDetect`. No generator change needed — a 3-int list already emits as ints in both languages (precedent `ODriveVer::AXIS_0`). Nothing consumes it yet. Shipped as `JBBallDetect`, **not** the plan's literal `JbBallDetect`: all 26 generated namespaces uppercase their initialisms (`JBOp`, `BBGeom`, sibling `BBBallDetect`), and with zero consumers before Phase 3 this was the last free moment to fix the plan's typo. **A Phase 3 trap recorded here**: `ENABLED` emits as a `constexpr bool`, not a macro, so "compiles the TU out" cannot be an `#if` — `#if JB_BD_ENABLED` evaluates an undefined identifier to 0 and silently compiles the poller out *even when enabled*; use constexpr-gated early returns. The drift test has **two** legs because the plan's single flashed-JSON assertion (`gpio2_mode == 1`, keyed off the generated pin) stays green through a "YAML edited, codegen not re-run" divergence; the YAML↔generated leg closes it, and two names mean a failure says whether you have a wrong flash or stale codegen. BallButler's regenerated header is deliberately NOT committed — it already carried ~60 lines of pre-existing drift, and Phase 2's lockstep commit must enumerate it. | [2026-07-29-hand-sensor-ball-detect-config](2026-07-29-hand-sensor-ball-detect-config.md) |
| 2026-07-29 | in-progress | Hand ball-present sensor — Phase 0 (fw-version surfacing) | **The bridge decoded every axis's `Get_Version` reply and threw the numbers away.** `_version_check_poll` fed them to `record_version` / `validate_group`; the PASS log line printed no versions and `/link_status` had no ODrive-version row (only `platform_fw_version`, which is the *Platform Teensy*). Phase 0 renders them — `_odrive_fw_versions_str()` appended as `(fw ...)` to **both** the PASS and FAIL firmware-check lines, plus a new `odrive_fw_versions` KeyValue distinct from `platform_fw_version` — **including `Get_Version`'s fourth byte (`fw_unreleased`, previously discarded as `_unrel`)**, the only wire evidence that could carry the `-1` of `0.6.11-1`. This matters because the sensor plan pins RxSdo endpoint id **726** against the hand ODrive **Pro** at fw `0.6.11`/`0.6.11-1`, and neither fallback is free: `validate_group` checks firmware only for *internal consistency within a hardware group*, so "firmware check PASSED" would pass seven axes agreeing on the WRONG version; and the id the repo carries today (**700**, the S1 value) exists on Pro 0.6.11 as `encoder_estimator1.status`, answering plausibly-wrong with **no timeout**. Approved-decisions row 6 owns the byte; empirical confirmation is Phase 7 step 1. Rendering decisions are both about not lying: absent axes render `axis:?` rather than being dropped (a partial bench rig legitimately sweeps fewer axes), and the fourth byte renders **even when zero**, so "the drive reports no suffix" stays distinguishable from "the bridge doesn't surface it". `MotorStateTracker` is untouched — the byte lives in a node-local `_fw_unreleased` dict, because the tracker has callers outside this node and `validate_group` has no use for it; validation and evidence-rendering stay separate. **The renderer must never fabricate a `-0`**: on any future path that populated the tracker without the byte, a `.get(axis, 0)` default would read as measured evidence ("the drive reports no suffix") for exactly the byte row 6 exists to confirm — so the renderer is total and renders `-?` for a missing byte instead (an earlier direct-indexing draft's "loud `KeyError`" theory was refuted by the pre-commit audit: the exception would go dark inside `_publish_link_status`'s blanket `except` or kill the node from the 1 Hz timer). `_fw_unreleased[axis]` is written BEFORE `record_version`, which keeps `-?` unreachable today (the version-poll and `link_status` timers share the node-default `MutuallyExclusiveCallbackGroup`, so no concurrent read exists — the ordering makes it hold if that changes). The plan's "Done when" test is split in two because a partial rig and a PASS/FAIL log line are mutually exclusive by construction — `_version_check_poll` returns at the `all_jugglebot_versions_received()` gate before either log call — so the partial-rig test asserts the shared renderer plus the `/link_status` row, the surface that exists in that state. A reviewer's throttled "sweep incomplete" log line was rejected because `/link_status` already carries the row at 10 Hz (the Phase 6 runbook points there). No behaviour changes: nothing new is asserted, refused or faulted on. Three parallel read-only reviewers (correctness / plan-conformance / minimalism), **zero blocking findings**, six fixes applied at finalize. `python -m pytest tests/ros/test_teensy_bridge_node_version.py -q`, run 2026-07-29: **8 passed in 2.15 s**. `python -m pytest tests/ -q`, run 2026-07-29: **4256 passed, 3 xfailed in 1421.88 s (0:23:41)**. **Nothing has run on powered hardware** — whether the hand Pro reports a non-zero fourth byte is unknown until Phase 7 step 1. Deploy: `colcon build --packages-select jugglebot` + `source install/setup.bash` + relaunch (the launch runs the INSTALLED copy); no interfaces rebuild, no firmware flash. | [2026-07-29-hand-sensor-fw-version-surfacing](2026-07-29-hand-sensor-fw-version-surfacing.md) |
| 2026-07-25 | in-progress | Self-toss anomaly fixes — Phase F (TossContinuous, the programme finale) | **Repetition ships as a SEQUENCER, and the instrument that gates it was validated on one side only.** `TossContinuous` runs `num_throws` toss-catch cycles from one goal with a configurable dwell; every cycle is an ordinary `Toss` built by `_build_toss_cycle` and ticked by `_run_toss_cycle` — the same two methods the single `Toss` now uses after they were extracted — so a session **cannot** drift from the toss the hardware ladder validated. A pure-Python outer FSM owns only *when* the next cycle starts, *whether* it starts, and the accounting; `stop_on_miss` defaults TRUE on the wire (verified against the real generated message, not just the mock). The dwell is a QUIESCENT WAIT, not a stretched `throw_delay`, because the rejected option leaves `catch/armed` RAISED over a loaded cup for the whole gap with `catch_coordinator`'s reactive path live — verified in the mechanism (`_on_throw_announcement` and `_prime_retry_tick` are both gated on `_catch_armed`). **The brief's 2.0 s dwell floor was REFUTED, not adopted**: it needs `throw_delay <= 1.458 s`, **2.042 s below** the toss FSM's own `MIN_TOSS_THROW_DELAY_S` (3.49 rejects / 3.50 dispatches against the real FSM); shipped floor is derived, `4.10 s` absolute and `5.60 s` at the default delay, and a short dwell is REFUSED rather than silently stretched. **THE CONVERGENT FINDING — two lenses, and it would have burned the sitting**: the CS-1..CS-6 trace checker this phase ships as the bench gate had only ever been shown clean on an **all-CAUGHT** session, while runbook `CONT-STEP-1` makes an **all-MISSED** dry trace the MANDATORY capture *before any ball flies*. That ladder publishes `catch/armed False` FIRST and only then retracts, so the cycle's own mandated retract — **~10 rev** on a dry trace, since no catch stroke re-parks the hand — lands inside the dwell window CS-3 measures. Building the missing `cont_dry` case found two real defects in sequence: CS-3's hand bound was **DT-5's idle-window `0.05 rev`**, which is **1.12x** the worst real post-disarm residual (`0.0446 rev`, measured over the 16 toss-shaped gaps of `temp/logs/toss_trace_2026-07-27_15-39-50.jsonl`) — a coin-flip false STOP on a row the runbook marks a hard STOP; and the first repair, anchoring on the first in-park sample, **still FAILed `cont_dry`** because the `0.5 rev` park band is wide enough that the tail of the retract ramp is already inside it (baseline `0.250` vs settled `0.000`). Landed formulation is **no-ascent, running minimum**, at a measured `0.15 rev` (3.4x the worst real ascent, 10x below `ASCENT_POS_REV`, 66x below the auto-prime ascent it exists to catch), plus a new failure for a window where the hand never reaches park. Validated three ways: `cont_dry` 6/6 PASS, the new `viol_cs3_hand` FAILs CS-3 with zero collateral, **16/16 real windows pass at 3.4x**. **THE EXTRACTION REGRESSED THE SINGLE TOSS, in the direction nobody expected**: `_execute_toss`'s `rclpy`-shutdown branch terminalised NOTHING on the goal handle at HEAD; the discarded `exit_kind` let it fall through to `goal_handle.abort()`, so a status transition on a dying executor could overwrite the `ABORTED_SHUTDOWN` line with `ABORTED_EXCEPTION` and turn a clean Ctrl-C into a fault trace. The *session* handled it correctly and its test asserted parity with "the single Toss does the same" — which the same change had made false; the pre-existing shutdown test asserted the outcome and the safing but never the handle, so it passed both ways. Fixed and now pinned on the single Toss. **THE DWELL FLOOR SIZED ONLY THE HANDOFF THAT COMMANDS NOTHING**: `dwell_margin_s = 0.6` covers the CAUGHT handoff (a verdict, two ticks). A MISSED cycle the session continues past hands over through a whole `SAFE_ABORT` ladder whose every rung returns on a **SERVICE ACK** — `_go_home()` returns when a `2.0 s` recentre profile has been *installed*, `_retract_hand_with_retries()` on the first ack — so at the shipped defaults the naive arithmetic starts cycle N+1 **1.7 s before the recentre lands**, and the docstring's "lateness is absorbed" was true of the FSM and false of the session. Landed `DEFAULT_SESSION_MISS_CLEANUP_S = 2.80 s` as a FLOOR (`CATCH_CONFIRM_WINDOW_S 0.7` + `go_home 2.0` + 2 ticks) that can only lengthen a gap, chosen over a `stop_on_miss`-dependent dwell floor because that would have REJECTED the all-defaults configuration the mandatory dry trace uses. Also: **`REJECTED_THROW_DELAY`**, without which the advertised `4.10 s` absolute floor was an arithmetic identity (a `throw_delay 2.0 / dwell 3.0` goal was ACCEPTED, built a whole cycle's state, then died `REJECTED_CANT_MAKE_LEAD` naming a field the operator never set); `catch/reach_center` removed from CS-3's silence set (published in the SAME FSM tick as the window's own end boundary, **131 us** apart on the real bag — CS-4 already covers the whole gap more strictly, so no coverage is lost); CS-6's docstring corrected to what it proves (release-vs-previous-landing ordering, **not** the requested cadence — a collapse from 8 s to 2 s would PASS); and `ball_possession_contract.md` § 7.1's *"no edit to the coordinator"* upgrade claim **refuted** (cycle N+1's `ball_seated` is a plain read of the sticky latch, `_on_balls` can only ever SET it True, and `judge()` is reachable only from a `/balls` CAUGHT while the tracker prunes the track ~2 s in against a 4.10 s floor) and replaced with the two coordinator edits retention actually needs. **Deferred with the trace recorded**: the unprotected pre-`try` region of `_execute_toss_continuous` (NOT-PROVEN reachability by both reviewers, pre-existing in `_execute_toss`, and the safe restructure needs `session is None` guards on two handlers). `python tools/probes/toss_trace_synth.py --all --verify`, run 2026-07-29: **30/30 OK, matrix CLEAN** (RED at HEAD on `viol_dt7`, a pre-existing vacuity repaired here). `pytest tests/ -q`, run 2026-07-29: **4254 passed, 3 xfailed in 1435.48 s (0:23:55)** (+114 on the `3332bc6` baseline of 4140 — accounted EXACTLY: `--collect-only` reports 70 tests in `test_toss_session.py` and 44 in `test_toss_continuous_node.py`, both new files; xfail unchanged at 3, so no test was weakened to reach green). **NOT DEPLOYED, NOTHING HAS RUN** — `colcon build --packages-select jugglebot_interfaces jugglebot` + relaunch, **BOTH packages**: `reload_coordinator_node` imports `TossContinuous` at module scope, so a `jugglebot`-only build raises `ImportError` before the node is constructed and takes **all three** ball-op actions down. The review found four per-section runbook blocks affirmatively saying "no interfaces rebuild"; underneath them **every** build instruction in the file was the single-package command — all **20** swept. No firmware flash. § SECTION CONT runs LAST. | [2026-07-25-toss-continuous-action](2026-07-25-toss-continuous-action.md) |
| 2026-07-29 | resolved | Self-toss anomaly fixes — Phase E (sim gate seed bugfix) | **The Tier-8b asymmetry map was one trial replicated.** `_run_asymmetry_map` advanced its seed offset only AFTER the list comprehension that consumed it, so all `diag_trials` trials in a cell ran the IDENTICAL seed — which is why every published cell read `0/4` or `4/4` with `err_mean == err_worst` exactly. The other two seeded columns are unaffected (gating uses `cfg.seed + gi` per trial; contact-diag uses `+ i` with `i += 1`). Fixed to `+ i + k`. **The fix does NOT make the map trustworthy and the first draft of the comment wrongly said it did**: on the post-fix re-run **47 of 48 cells still read 0/4 or 4/4** (one reads 1/4), so the near-bimodality is the low-fidelity contact model, not a seeding artefact — a `0/4` cell is still not evidence of a 0 % direction. Consequence: the 2026-07-25 directional map is **refuted, not merely stale** (`+y` now 10/10 at 70 mm, 10/10 at 100, 9/10 at 150; NW 10/10 everywhere; only sub-9/10 cell is SW at the cap), so the plan's `+y`/NW ladder guidance gets a REFUTED banner and Phase E's cap ships as a simple scalar. Changes no shipped robot behaviour. | [2026-07-29-toss-gate-asymmetry-seed](2026-07-29-toss-gate-asymmetry-seed.md) |
| 2026-07-25 | in-progress | Self-toss anomaly fixes — Phase E (displaced throws to ±150 mm) | **The reach envelope was bounding two different things with one number, and only one of them could be judged in time.** Four Tier-8b goals on 2026-07-27 were refused 4/4 — `catch target 146/131/117/117 mm from the armed hold pose exceeds the 80 mm reach envelope` — because the envelope, which exists to bound UNREQUESTED tracker drift, was centred on the pose held at arming and so also capped the REQUESTED `A→B` reach; and since that reach is published at `t_release`, its verdict could only ever arrive **after the irreversible commitment, with the ball airborne**. Closed as a class by contract **C-REACH-1**: the centre moves to the DECLARED catch point `B` (`catch/reach_center`, consumed-and-cleared at the arm raise), the 80 mm radius does not move, and the reload declares nothing so its path stays byte-identical. Throw site `A` is now the platform's LIVE commanded pose (`trajectory/commanded_position`); `toss_throw_site_mm` is RETIRED and `compute_release_state_tilted`'s site argument is REQUIRED — one enforcement point, because a config `A` is not merely a wrong number: POSITIONING obediently TRANSLATES the platform to it before throwing. Cap re-based on its own evidence to **150 mm**; CAUGHT now STAYS (`ACTION_STAY` = the RECENTER ladder minus `go_home`). **A LIVE HAZARD FOUND AT FINALIZE AND FIXED**: the new reload `REJECTED_NOT_CENTERED` gate used the bare 80 mm envelope as its tolerance, leaving **zero** budget for the reload's own pre-tilt swing — `compute_catch_orientation` CLAMPS at `MAX_TILT_DEG` for every real BB arrival (18–40° off vertical), so the centroid shift SATURATES at `64.78·sin(12°) = 13.47 mm` on every reload (measured invariant across 18/25/30/40°). Parks in `(66.5, 80]` mm were ADMITTED then rejected `WORKSPACE` **after `ACTION_SEND_THROW`**, with BB's countdown started and the ball unsavable — the DISP-7.1 E-STOP condition reached through a gate that said yes, and the ladder walks into it (DISP-3 catches at 70 mm and the next rung opens with a Reload). Tolerance is now `envelope − hand_catch_offset·sin(MAX_TILT_DEG)` = **66.53 mm**, derived not magic. **SHIPS WITH A KNOWN LIMITATION, DOCUMENTED NOT PAPERED OVER — two independent lenses converged on it**: chaining is refused AT the cap. The catch deliberately parks the CENTROID outside `B` so the CUP lands ON `B`; measured, the cup ends at exactly `(−150.00, 0)` and the centroid at `(−153.10, 0)`, and `trajectory/commanded_position` publishes the CENTROID — so the next goal reads `A = −153.10` and both surviving gates refuse (`REJECTED_DISPLACEMENT` back to centre, `REJECTED_WORKSPACE` for any other `B`). Root cause is a **frame** error, not an aim error (`A` is nominated, and POSITIONING makes it true): the wire supplies a centroid where `compute_release_state_tilted` documents a CUP xy — identical while level, divergent by `hand_catch_offset·sin(tilt)` = 2.07 % of displacement once `STAY` holds a receive tilt, crossing the box at `|B| ≈ 147 mm`. Not fixed because all three candidate fixes need an operator decision (publishing the cup xy needs the commanded ROTATION, which was deliberately kept off that wire to avoid C-LEVEL-1 double-correction; widening is bidirectional so it either strands harder or loosens an evidence-based cap; lowering the cap violates the ±150 ask). **The runbook was corrected so it cannot mis-route**: as reviewed, DISP-6.2 told the operator a `REJECTED_DISPLACEMENT` means *the throw site did not follow the platform* — the opposite of the truth. Also refuted at finalize: `REACH_*` limits were documented as *pinned by the drift-guard test* while nothing pinned them (harmless at the old 70 mm cap where the bound never bound; **this phase made it live**, so a cautious operator halving `leg_jerk_limit` would have had a 150 mm goal pass CHECKING at 171.5 mm against a real 85.8 mm budget) — now genuinely pinned; DISP-5.3 quoted the **platform-space** closed form (17578) as the expected reading of the **leg-space** `peak_leg_jerk`, measured 9742 max (0.55×); `throw_site_known` defaulted fail-OPEN in a family where every sibling is fail-CLOSED, and flipping it immediately caught a test silently relying on it. `pytest tests/ -q`, run 2026-07-29: **4140 passed, 3 xfailed in 1409.60 s (0:23:29)** (+44 on the 4096 baseline at `956a4b8`; xfail unchanged at 3). **NOT DEPLOYED** — `colcon build --packages-select jugglebot` + relaunch (row DISP-0 greps the installed copy); no flash, no interfaces rebuild. | [2026-07-25-displaced-throws-150](2026-07-25-displaced-throws-150.md) |
| 2026-07-29 | tuned | Self-toss anomaly fixes — Phase 7 (post-release decel) | **The hand touched its end stop because the only open-loop braking term was sized from a model that omits the motor's rotor.** `accelToTorque` models the axis as a pure translating mass on a spool, implying a reflected inertia of **7.3695e-6** kg·m² against a measured **≥1.0126e-5** — so the braking feedforward delivered **~70 %** of what the commanded decel physically needs, and the shortfall fell to a loop whose integrator unwind constant (0.100 s) is **1.1–2.1× the whole 47–93 ms ramp**. `calcThrow` gives that shortfall a lever arm: it allocates the decel **4.046 rev at every speed** (velocity-independent), ending exactly on `x3` with **zero** allowance for tracking error, so it converts 1:1 into end-stop travel — measured +0.074 / +0.063 / +0.347 / **+1.020** rev at 2.74 / 3.44 / 3.97 / 4.86 m/s. Fixed at ONE enforcement point (`throwDecelToTorque`, one caller), decel segment only; **commanded position and velocity are bit-identical on every kind**, so C-HAND-1's window, `_PRIME_INFLIGHT_S` and the timeline probe's verdict model stay valid without moving. Steepening was rejected on **measured authority**: the drive is NOT current-saturated (max |iq| 25.67 A of 50, achieved decel still growing 911→2330), yet the **commanded** decel at `FLIGHT_TIME_MAX_S` already sits at **83–86 %** of the axis's own 4178–4323 rev/s² ceiling. **REVIEW RE-ANCHORED THE SAFETY CLAUSE (two lenses converged):** the cited "accel-phase torque balance → 1.015e-5" is **ball-inclusive** — release is at `x2`, so the ball is in the cup for the whole ascent, and `INERTIA_RATIO = 0.747 = m_hand/(m_hand+m_ball)` makes it worth **2.412e-6, 24 %** of the figure. Ball-corrected it gives **7.74e-6**, *below* the declared 9.5e-6, so "7–10 % below BOTH identifications" was false; 7.74e-6 is *also* refuted as a decel inertia (it implies a flat η≈0.95 where **0.799** was measured). Re-anchored on a genuine **decel-side bound** `J ≥ (τ_FF+τ_grav)/(2π·a_ach) = 1.0126e-5` that uses **no `iq` measurement at all**, so the aliasing that killed a cascade model cannot corrupt it. **THE ONE-SIDED-SAFETY PROOF OMITTED GRAVITY, AND THERE IS NO CLEAN FIX**: on an upward decel gravity brakes in the SAME direction (+1.46e-6 of effective inertia at the band floor, +0.37e-6 at the ceiling), so open-loop the total *exceeds* `J_true` below `a_cmd ≈ 1900 rev/s²` and "can never over-brake" is false at the bottom of the band. Lowering the declared value to satisfy it needs `J_ff ≤ 8.69e-6` → a pessimistic peak of **10.80 rev**, past the 10.60 abort line. The two requirements are **incompatible**, which is the finding: the open-loop inequality is the wrong requirement. Enforcement moved to bench row **H7.4** plus a new **band-floor rung R0** — the only rung that tests the over-braking direction, and below every rung the ladder had, while routine reload work throws there daily. Tradeoff stated plainly rather than hidden behind the algebra. **THE VERDICT INSTRUMENT WAS READING THE WRONG PHASE OF THE STROKE**: `iq_ramp`'s window was anchored on the announcement's `throw_time`, which lands **120.5–166.9 ms** before the commanded stroke end while `t_dec` is only 52.7–93.3 ms ⇒ **zero overlap with the real decel on 13 of 17 tosses**; it reported the ASCENT current (10–23 A) where the true braking current is 3.6–9.9 A, and H7.5 gates the ladder's highest rung on it. Re-anchored on the commanded profile, `t_anchor` + fresh-sample count now printed. The probe also had **no self-check**, alone among the runbook's instruments; it now has a **two-sided `--self-check`** (pre-fix must FLAG, post-fix must ACCEPT) running through the real `analyse()` — mutation-verified: restoring the old window makes it FAIL. **AN UNEXAMINED DRIVE CLAMP COULD MAKE THE PHASE A NO-OP**: `torque_soft_min = −0.0551 N·m` = **exactly −10.00 A** against a `torque_soft_max` of +0.5; if live it truncates the feedforward above ~0.49 m. Probably not binding (achieved decel grows 2.6× across tiers, which a hard clamp forbids) but unresolved ⇒ pre-flight **H7.0c** reads it off the live drive *before* the flash, because otherwise the failure table would call a drive-config fault a physics result. Also: H7.3's flatness gate **0.25 → 0.35 rev** (0.25 sat at **95 %** of the model's own 0.237 prediction — a coin flip that blocks R5 on a working fix); `settle_offset` restated **per tier** (the single span silently dropped the 4.858 m/s tier, the one the phase exists for, where it is ~0); stale **"6055 rev/s²"** corrected at 3 surviving sites (it is the `MAX_EVENT_VEL_MPS` clamp value, not the band top's 3597 — as written it asserted a commanded decel *above* the axis's own ceiling); two sites still calling `platform_fw_version = 1` the flashed reading. `pytest tests/ -q`, run 2026-07-29: **4096 passed, 3 xfailed in 1428.61 s (0:23:48)** (+28 on the 4068 baseline at `ac74c1a` = this phase's two new files; xfail unchanged at 3). **NOT DEPLOYED** — Platform Teensy **flash** (`FW_VERSION` 1 → 2) *and* `colcon build` + relaunch, then the gated **R0→R5** ladder. | [2026-07-29-hand-post-release-decel](2026-07-29-hand-post-release-decel.md) |
| 2026-07-25 | tuned | Self-toss anomaly fixes — Phase C (seat-experiment prep) | **The seat rate becomes a config key, and the A/B that needs it gets an instrument.** Phase 4 left one question open — *does a non-zero seat rate widen the capture basin?* — and left it unanswerable, because moving the rate meant editing `planner.py` at the bench: invisible in the INSTALLED tree (so *which rate did that block run?* is unanswerable from the capture) and forgettable (so it ships as an accidental permanent default). Now `trajectory_op.catch_seat_rate_radps`, bound **once at import** into the same module attribute the 12 existing monkeypatches target — a call-time `hw.` lookup would take the whole C-CATCH-1 block vacuous **while staying green**, so that is pinned executably. Ships at **`0.0`**; commanded motion byte-identical. The A/B's central measurement — arrival offset — **had no runnable command anywhere in the repo** (the sitting computed it by hand, once), so `tools/probes/ball_arrival_offset.py` was built and validated two-sided against the real bag (worst error **0.6 mm**). Review then caught that the instrument's own acceptance was **vacuous on the one bias the design calls disqualifying** — a mutant with the post-contact cut deleted scored `8/8 PASS, exit 0`, because the synthetic bounce-out was grafted *below* the fit plane where the band filter drops it anyway; now grafted inside the band and mutation-verified both ways. Three protocol gates **fired on correct behaviour** and were re-derived from measured numbers: burn-in (`successive change < 10 mm` fires 3-in-7 on the settled plateau -> trend slope), matched arms (flat `5 mm` is 1.1-1.4 se at the expected sd, so identical aim fails 17-28 % of the time -> `max(5 mm, 2*se)`), and the marginal band (one-sided floor passes both under- and over-biased aim -> two-sided). A **refuted number** was corrected at its durable sites: the sitting's *"3.8 mm apart"* basin edge re-scores to **0.02 mm** — degenerate, not narrow, which is *why* the two hypotheses cannot be separated from that capture. Sitting deferred to the operator; the open prerequisite is how to bias BB aim +25-35 mm. `pytest tests/ -q` 2026-07-28: **4068 passed, 3 xfailed in 1422.44 s**. | [2026-07-25-seat-experiment-prep](2026-07-25-seat-experiment-prep.md) |
| 2026-07-28 | tuned | Self-toss anomaly fixes — follow-on B (caught-gate) | **Every ball op the machine has ever run reported MISSED — the verdict was not wrong, it was structurally unreachable.** The CAUGHT gate ANDed two spatial bounds and the vertical half (`z_err <= 150 mm`) cannot be satisfied by a real catch: the tracker declares CAUGHT **because the marker vanished**, so `BallState.position` at that instant is a dead-reckoned free-fall extrapolation, then frozen (post-CAUGHT variance **exactly 0.000 mm** on all 60+ CAUGHT tracks — structural, not a fluke: `_associate_confirmed_balls` skips non-`IN_FLIGHT`). Under free fall the error lands almost entirely in z — measured **305–1007 mm** against xy **0.30–3.88 mm** across the sitting's 17 self-tosses — so `success` was False **by construction on every ball op**. The root cause is *a plausibility bound applied to an observable whose error model was never written down*, and **the same expression carried a second instance**: the 200 mm xy bound sat 4.9 mm under the corrupt-track floor (204.9 mm), a **1.02x** margin against minting a FALSE CAUGHT. Closed as a class: contract **C-POSSESS-1** makes a possession claim two independent parts (ARRIVAL, RETENTION), forbids a source asserting a part it cannot observe, and makes a non-discriminating observable REPORT-ONLY. One enforcement point (`ball_possession.py`, reached only through `_possession_confirmed`), z retained as REPORT-only `plane_drop_mm`, arrival bound single-sourced from `GEOM_ARM_RADIUS_MM`. Self-toss **0/17 → 17/17**; all 18 corrupt reload tracks stay refused (the honest verdict — none carries evidence). **THE FATE TERM THE BRIEF SPECIFIED DID NOT SHIP, and that is recorded as a deviation (§ 7), not buried**: lateral velocity at CAUGHT separates the data **75x** and nearly shipped, but the separation is *geometry, not retention* — a self-toss arrives vertically so its lateral velocity is ~0 by construction, while a **legitimate** reload catch arrives at ~1 m/s and the same threshold would refuse it. **A HYPOTHESIS WITHDRAWN AT FINALIZE**: the justification claimed flatly that "a bounce-out raises no successor track — there is nothing to observe", measured as zero new tracks between CAUGHT and floor arrival — **confounded**, because those tracks were mis-associated so the real marker was *already* on a separate track and no *new* one could appear; `_detect_parabolic` (≥3 frames) is a real unevaluated successor channel, as are `/mocap_data` and the matcher's own CAUGHT precondition (a still-visible bouncing ball never mints CAUGHT). Narrowed to "unobservable from the *terminal track*". **THE HANDED-OFF TRACKER SIGNATURE WAS FACTUALLY WRONG in five artefacts** — "the filters received **no** measurements at all (lateral velocity constant)" — refuted by re-scanning the bag: all 18 reach `tracking=CONFIRMED` (set only inside `kf.update()`) with `velocity.x` spread **31–516 mm/s**. They are **mis-fed, not starved**; the gate to start from is the adaptive announced-ball threshold capped at 400 mm. That wording would have sent the tracker investigation after the wrong defect. **THE NEW BOUND IS KNOWINGLY UNDER-SIZED FOR RELOAD** and was deliberately NOT re-tuned: three reviewers converged from three starting points, and finalize measured the reload era's *real-marker* untagged tracks at **34.4 / 34.9 / 37.6 / 68.4 mm** — a genuine reload catch sits **1.6 mm inside** the 70 mm bound, a **1.02x** margin, *the identical defect shape § 1 condemns* — plus up to 80 mm of catch-reach the reference point does not follow. Re-tuning needs a *fixed* tracker; sizing against the 204.9 mm corrupt floor would be sizing a bound against a bug. Pinned by test + bench row `POSS-1.6`. `landing_position.xy` measured as an alternative and **refuted** (140.0/120.3 mm — worse). The FSMs' long-dead `ball_caught` branch goes live: a caught toss terminates at the CAUGHT tick with `RECENTER` (no retract) instead of the settle deadline with `SAFE_ABORT` — hand already parked to **±0.045 rev** at that instant, worst 3 s excursion **0.069 rev** vs a ±0.5 rev band. Two residuals sent to the bench rather than argued away: `RECENTER` is the one terminal with no telemetry-verified hand step, and the ball's settle time before the 0.26–0.50 s-earlier `go_home` was argued from *hand* `pos_meas`, which cannot see the ball (new row `POSS-2.4`). `pytest tests/ -q`, run 2026-07-28: **4059 passed, 3 xfailed in 1405.75 s (0:23:25)** (+92 on the `a5972ec` baseline of 3967; xfail unchanged at 3). **NOT DEPLOYED** — `colcon build` + relaunch; `ball_possession.py` is a NEW module (the one shape that lands half-applied), so pre-flight **PF-7** runs before the capture. No flash, no codegen. | [2026-07-28-caught-gate-xy-plausibility](2026-07-28-caught-gate-xy-plausibility.md) |
| 2026-07-28 | in-progress | Self-toss anomaly fixes — toss_tier 8b default | **Tier 8b becomes the shipped default — and the flip quietly falsified four operator documents, none of them findable by grepping the symbol.** The one-line change is `jugglebot_operational.toss_tier: "8a" → "8b"`, an operator decision on this run's own T4 evidence (**11/11** displaced throws accepted out to the 70 mm cap; `ZSEAT-4` reading `0.0000°` tilt span and `0.0000 mm` position span pre-release on every one). The code has been in the tree since Phase 4. **But a tier is not a value — it selects which of two shipped choreographies every toss goal runs**, and the direction of risk cuts both ways: the CHECKING gate gets strictly TIGHTER (refuses above 70 mm where 8a took 150 mm) while the CHOREOGRAPHY does not — 8b pre-tilts the platform at throw site `A`, suppresses the stock announcement pre-tilt via `catch/pretilt_hold`, and puts an A→B platform reach **into ball flight**. The claim that the change "can only cause the machine to refuse more goals, never command a larger motion" is half true and is corrected here. **THE GUARD WAS RE-PINNED, NOT WIDENED** — the sitting entry's own Open Question 1 recommended `JB_OP_TOSS_TIER in (8a, 8b)`, and that is retracted: a set-membership assertion cannot detect DRIFT, only an unimplemented tier, which the FSM already rejects at runtime AND `[tier-…]` already covers by driving `9z`. Widening would have duplicated an existing check while silently dropping the only check on WHICH tier ships. Mutation-checked: reverting the YAML to `8a` turns both the tier guard and the new `displacement` row red. **A PLAN HEDGE THAT WAS EMPIRICALLY FALSE**: the brief said to build the WORKSPACE row laterally, but with `A=(0,0)` a displacement ≤ 70 forces `|x|,|y| ≤ 70 < 150`, so the box's lateral half is **structurally unreachable** under 8b — the cap always answers first. The `z` band is the only reachable branch, built at `x=60, z=300` so the 8b gates genuinely run and PASS before `z` rejects (a zero-displacement row would still pass if the cap collapsed to zero and swallowed every real goal). **THE INSTRUMENT WOULD HAVE SCORED A CORRECT CAPTURE AS A FAILURE**: the handoff prescribed `toss_trace_recorder.py check --reject` for the new rejection rows, calling RJ-1 "the ordered-CHECKING-gates check" — it is not. RJ-1 hard-requires *exactly one* `REJECTED_NO_BALL`, so a correct `REJECTED_DISPLACEMENT` capture prints **FAIL … "that precondition wire is unhealthy"** with an empty hint. Caught before transcription; the runbook scores off the node's own outcome line instead. **Four falsified documents repaired in the same commit**: rung `T3` of the phase-8 runbook still said "±60 mm corners (throw = catch site, **Tier 8a**)" while 60 mm is inside the 70 mm cap — so it is ACCEPTED as an 8b displaced throw, the platform never makes the POSITIONING move the rung exists to test, and its `(0,+60)` corner fires an **un-gated** throw into the `+y` hemisphere the asymmetry map flags weak at `T≈0.70 s`, below the `T ≥ 0.80 s` **T4 itself stipulates**; `REJECT_WIRE_MAP` told the operator `JB_OP_TOSS_TIER must be 8a` — before the flip a no-op, after it **an instruction to revert this commit mid-sitting**; the plan's "score from a clean 8a worktree" stopped being obtainable from `HEAD`; and the "nobody has asked the operator" item was answered. **Also**: the co-located `B == A` 8b path has **never run on hardware** (all 11 validated throws were displaced) yet is the shape every other toss row sends — new `TIER-D` scores it; the handoff's `TIER-C` was labelled "NO MOTION" but traces to a **full throw**, rewritten to run un-armed. New § SECTION TIER is two-sided by construction (`TIER-A`/`B` must be FLAGGED, `TIER-C`/`D`/`E` must be ACCEPTED) with measured thresholds, not control-flow artefacts. `pytest tests/ -q`, run 2026-07-28: **4059 passed, 3 xfailed in 1405.75 s (0:23:25)** (+92 on the `a5972ec` baseline of 3967; xfail unchanged at 3). **NOT DEPLOYED** — `colcon build` + relaunch required; `TIER-PREREQ` reads the installed file because a skipped rebuild is otherwise invisible. No firmware flash. | [2026-07-28-toss-tier-8b-default](2026-07-28-toss-tier-8b-default.md) |
| 2026-07-28 | tuned | Self-toss anomaly fixes — hardware validation sitting (2026-07-27) | **The anomaly-fix run meets hardware: every levelling and hand row passes, and the one genuinely open question came back ABORT.** First hardware validation of the eleven landed phases. Levelling frame exact (`LVL-3` err **±0.0000°** on all three stages; mocap validates the chain to **0.6 %** against a known commanded step), the gate works **across processes** (`LG-5` reconstructed: refusal → flip **+30.401 s** → second outcome **+149.778 s**, with a literally motionless refusal), FK clean (**0 failures / 105,318 solves**), and the post-throw dip is **gone** — 0.000–0.026 rev on 15 of 17 tosses against a pre-fix 0.339–1.748 rev (**40–70×**), with Phase 4's velocity-continuous branch firing on hardware for the first time. **`ZSEAT-2` ABORTs**: 3 bounce-outs, all 3 consecutive — **operator-confirmed by eye** (all three touched the rim and bounced out), rate arm passes at 13/16 = 0.8125. Attribution: the proximate trigger is a measured **and eye-witnessed** ~26–39 mm BB warm-up drift, but the **capture-basin width stays OPEN** — attempt 3 dropped at x=−10.7 and attempt 4 caught at x=−14.5, **3.8 mm apart**, and the capture holds no throws at ~+30 mm offset with a non-zero seat; the A/B is pre-registered for the next sitting. Two things nobody was looking for: the hand **made light physical contact with its end-stop** on ~1.2 m tosses (operator-confirmed 2026-07-28 — telemetry read only **1.2 mm** of margin, so the true stop may sit *below* the declared 11.1 rev guard, and the shipped `FLIGHT_TIME_MAX_S` is past a limit already reached), and the persisted levelling offset is **truncated to int16 milliradians**, costing 0.0264° — with `LVL-3` structurally blind to it. The auto-push race is settled: **7 of 7**, 0.43–3.50 ms, structurally rigged by a 2.7–4.3 s head start, so `LG-3`'s precondition is unreachable. Extremity tilt at ±150 mm quantified (0.15–0.60°, revisit sd 0.001–0.014°, **not** commanded and **not** leg tracking) as the seed of the lookup-table decision. Eleven instrument/runbook defects, several of which would false-ABORT a healthy machine. | [2026-07-28-anomaly-fixes-validation-sitting](2026-07-28-anomaly-fixes-validation-sitting.md) |
| 2026-07-27 | resolved | Self-toss anomaly fixes — hand-command-continuity Phase 6 | **The one deployment in the stack that failed silently — and the reviewers found the same defect class one layer up, in this phase's own code.** The Platform Teensy carried no version, so an un-flashed board was indistinguishable from a flashed one from the Jetson, and § CHECK HAND-4's fix is a **no-op on the clean path by design** — a stale board produces plausible rows that silently re-measure the pre-fix baseline. It now declares `FW_VERSION = 1` and reports it in **bytes 5-6 of the 0x6E0 RobotState reply it already sends**, chosen on ONE property: every firmware ever built zero-filled those bytes at the same dlc 8, so a **pre-versioning board ANSWERS with 0** rather than going silent. A new query frame/RPC was rejected because an old board would not answer at all, making the un-flashed signature an *absence* — indistinguishable from a CAN3 hiccup or an unpowered board — and would have needed **a can-bridge flash to detect a stale Platform flash**: a second silent deployment introduced to detect a silent deployment. Zero new CAN frames, dlc unchanged, no can-bridge change. **WARNS, never refuses**: the 0x6D0 path carries the kind-3 retract, the only un-arm mechanism the Teensy offers, so a gate there converts a skipped flash into "the abort stopped working with a ball about to launch"; and a detector is monotonic where a refusal cannot be un-shipped mid-sitting. **FINALIZE — three convergent reviewer findings, all verified before acting.** (1) A comment claimed plain-attribute assignment makes a stale `jugglebot_interfaces` build "die loudly" — **false**: the assignment is inside `_publish_robot_state`'s own `try/except`, so unlike `trajectory_node` (no handler ⇒ rclpy re-raises ⇒ process exits) it degrades to ONE throttled ERROR per 5 s and a silently-dead `/robot_state`, while the node still lists as healthy and the orchestrator stalls in BOOT blaming **power/CAN**. Worse on a *partly* stale tree, where `trajectory_node` does not exit and the promised row-B signature never appears. Rather than delete the claim — the same silent-deployment class this phase exists to close, one layer up — it was made TRUE: `INTERFACES_STALE` at construction, naming the missing fields and the rebuild command (logs, never raises: a refusal to start is the shape this phase argues must be earned). (2) **The instrument mis-scored a HEALTHY board.** `UNKNOWN` routed to a hard ABORT into a CAN3 investigation — but `UNKNOWN` is exactly what this robot's *documented benign boot-read transient* produces, and the contract doc cited that transient as a reason not to enforce while its own table treated it as a fault. Traced independently: the verdict is captured only in `relay_read_robot_state` and re-read only on UDP-reconnect or CAN3-recovery **edges**, so a clean launch that missed its boot read stays `unknown` all session. All three reviewers offered the same code retry; **declined with reason** — it cannot read the version without also refreshing the cold-start cache, flipping `is_homed` False→True mid-session after the boot path already committed to the conservative fallback: a new homing discontinuity introduced to improve a diagnostic. Doc fix removes the false abort; code option recorded open. (3) The test pinning "warn, never refuse" **did not pin it** — it entered below `teensy_hand_traj_cmd`, the single funnel both hand services call and exactly where this repo's own one-enforcement-point convention would put a gate. Reproduced, then repaired **two-sided**: 26 pass clean, and the kind-3 assertion FAILS with a gate at the funnel. **REFUTED** the SRX_DIS hardening: "ignore a reply matching the last write" would discard *correct* reads, since the next honest read after `_write_is_homed` matches the written bytes by construction. Also: first-ever compile gate on this sketch (`platformio.ini`, **no `upload_command`** — its image needs `-fno-exceptions` + a patched linker script and is not what this board runs; switching toolchain in the sitting that validates Phase 4 would confound the two). `pytest tests/ -q`, run 2026-07-27: **3966 passed, 3 xfailed in 1411.32 s** — `+19` on the `2b0f3f0` baseline of 3947, exactly the 10 xref + 8 coldstart + 1 finalize tests added (xfail unchanged at 3). **NOT FLASHED** — same flash Phase 4 required, now confirmable. Bench § Section FW: INST-4/5, FW-1, FW-2, H4.0d. | [2026-07-27-platform-teensy-fw-version](2026-07-27-platform-teensy-fw-version.md) |
| 2026-07-27 | resolved | Self-toss anomaly fixes — run close-out | **A criterion that fires on CORRECT behaviour is worse than no criterion — the runbook had four, and building the missing instrument found the fifth.** Eleven phases appended to `tests/hardware/session_anomaly_fixes.md` without anyone reading it top-to-bottom; rows written early contradicted code that landed later, so a healthy sitting would have aborted on CCATCH-3's `peak off the park` (moves `0.292407°` = **2.9x** its own `> 0.10°` ABORT), on row 3's `hard abort at > 10.5 rev` against H4.4/H4.5's `10.60` (one reading, two responses, one E-STOP-grade), and on standing rule 2 telling the operator to `level` before the very relaunch LG-3 needs un-levelled. **The fifth was found by building the instrument, not by reading**: LVL-4 gated parked mocap tilt at `±0.10°` with NO probe in the repo reading `/rigid_body_poses` at all — and the new reader (46 161 samples, 0 failures) showed the parked *uncorrected* Platform sits `0.087°` off `Base` while the inclinometer read `0.782°` off gravity at the same instant, so **`Base` is ~0.78° off gravity** and a correctly-levelled platform reads LVL-4's ABORT verbatim. Demoted to REPORT; LVL-3 keeps the gate. Also: ZSEAT-2 — the run's ONE open experiment — had an unscored band (`7/12 = 0.583` satisfies neither `>= 0.63` nor `<= 0.58`), found independently by two lenses, now a RATE with an INCONCLUSIVE branch in both the runbook and the plan gate; the probe case-9 comment claimed a `~2.3°` excursion that measures `0.000000°` (C-CATCH-1 bounds the fallback to `2.5*scale/T`, exactly the no-wrong-side ratio, so the segment-count and settle conjuncts are the load-bearing ones); and CLAUDE.md's new docs-only-exemption clause cited two examples that both pass green (`INDEX.md` is in `_SKIP_FILES`, malformed front matter is silently `continue`d) — rewritten to the traced mechanism, own commit. `pytest tests/ -q`, run 2026-07-27: **3947 passed, 3 xfailed in 1383.73 s** (baseline `1e78f3f` was 3943 + 3; the `+4` is exactly four `seat_rate` parametrisations, collected 3946 -> 3950). | [2026-07-27-anomaly-run-closeout.md](2026-07-27-anomaly-run-closeout.md) |
| 2026-07-27 | resolved | Self-toss anomaly fixes — hand-command-continuity Phase 4 | **The velocity was sitting right there, declared and never read — and the two bounds that decide when honouring it is affordable were both wrong on the first pass.** `Trajectory.h::makeSmoothMove` seeded every hand profile `v = a = 0` from `current_hand_position` while `current_hand_velocity` sat declared `extern volatile` **two lines above it**, unread — so any command landing mid-motion commanded a velocity STEP. That unread `extern` IS the 2026-07-25 post-throw dip (catch arm at release+8-18 ms froze the setpoint at 6.20-7.78 rev with the hand through it at ~120 rev/s; coast to 10.17-10.32 rev, 0.775 rev off the guard; yank 0.34-1.75 rev = **10.7-55.3 mm** below the stroke end). Now `(x0, v0, a=0) → (target, 0, 0)`, decomposing EXACTLY into `pos = x0 + δ·s(τ) + (v0·T)·h(τ)`, `h = τ(1−τ)³(3τ+1)` — `h(0)=h(1)=0` so the live velocity never moves the endpoints, `h ≥ 0` so its whole effect is a one-sided bulge of `|v0|·T·16/81`, which IS the legitimate overshoot and is what gets clamped. **THE CONVERGENT BLOCKING FINDING — the commanded-position floor was ON the bottom hard stop.** It shipped as `Homing::HAND_ABS_POS_REV = −0.1` rev, 0.1 rev below the plan's own `[0, 11.1]` bound, justified as *"any margin would make every abort retract infeasible by construction"* — **refuted two lines away in the same function**: the bound is relaxed to `min(FLOOR, start, target)`, so the retract target is an endpoint and is admitted at ANY floor value; re-run at 0.0 every retract still lands on 0.0 and the two undershooting cases take the fallback, which is today's behaviour and is SHORTER. What −0.1 actually bought was permission to command onto the stop the axis homes downward into at −3 rev/s — below the floor the HOST declares (`teensy_bridge_node` rejects a target < 0, `can/odrive.py` clips and warns), so firmware and host disagreed on the legal range. Brute-forced over the honoured branch it admitted troughs to **−0.0999 rev** on the retract, prime AND catch-descent paths, and a PRIME dispatched mid-retract (5.0 rev, −24.6 rev/s) dived to **−0.057 rev — 5 rev BELOW its start**, for a move whose target is the top of the stroke. The ceiling keeps 0.5 rev for exactly the +0.186 rev position-loop undershoot the floor had **zero** allowance for. Floor → `JBOp::HAND_RETRACT_REV = 0.0`; the asymmetry that remains is now deliberate and documented. A 1e-4 rev (3.2 µm) float32 slack had to ship with it — the interior turning point is evaluated in `float`, so a monotone profile can report a trough a few ulps below its OWN endpoint (5.0→0.0 at −7.5 rev/s reads −2.4e-6), and without the slack the tighter bound would have silently disabled the feature it was protecting on the whole retract path. **A SECOND cannot-fit test, added at finalize: DURATION.** Arresting `v0` costs time as well as travel and the two scale differently — duration grows linearly in `|v0|`, excursion as `v0²` — so mid-stroke, where there is room for a big bulge, the excursion clamp alone lets a prelude run arbitrarily long. `catch_coordinator._PRIME_INFLIGHT_S = 1.2 s` is the anti-stutter window (the 2026-07-23 stutter: 5/12 ascents stalled 60-70 ms, reversals to −4 rev/s) and its pinning test's own comment says it exists so *"a Phase-4 duration change cannot outgrow it silently"* — while evaluating only the `v0 = 0` branch. A prime into a live retract (which never stamps `_prime_dispatch_mono`, so it is unsuppressed) at the retract quintic's own peak — start ≈5.04 rev, −24.6 rev/s — solves to **1.206 s**, past the window outright. Raising the host constant needs 1.81 s; chosen instead: bound the FIRMWARE at `smoothMoveMaxDuration()` = the longest **rest-to-rest** move the stroke admits (0.8005 s), derived from constants already in the header, so an honoured prelude can never outlast a profile this firmware **already emitted before the change** — the fallback's own argument, extended from magnitude to duration, keeping every host window valid without moving any of them. Costs the top of the band (20.9 → 20.3 rev/s) against a target settle tail of ≤ 0.25 rev/s. **THE INSTRUMENT WOULD HAVE SCORED A WORKING FIX AS A FAILURE.** The Phase-0 probe separates a `makeSmoothMove` brake from the armed catch descent at 0.5 rev below x3 — sized on the seven observed tosses' brakes (0.206-0.365 rev), **every one of them the settle-from-above case**. A brake seeded from a DOWNWARD velocity dives `0.00778·v0²` rev: past 0.5 rev at just **8.05 rev/s**, honoured out to **3.213 rev**. Synthetic capture at the clamp's reach, shipped probe, old separator restored: `catch_desc` lands on the brake (2.05 s vs the real 3.03 s), `peak` under-reports 9.9602 vs 9.9794, and `dip_below_x3` reads **0.0 rev on a capture that dove 3.21 rev = 102 mm below the stroke end** — a false PASS on the row guarding the 11.1 rev end stop, whose bench row is a hard abort with E-STOP. Re-anchored on the catch region (`x5 + 0.33` rev, which no honoured brake can reach); `deep-brake` is now a permanent gate case, biased deep on purpose because too shallow fails SILENTLY and too deep fails loudly. **The velocity and torque feedforward streams were entirely unvalidated** — the compiled-header driver printed `tr.v.front()` and stopped, so `30.0f*t4 → 3.0f*t4` in `quinticS1` left the full-stroke prime commanding **−354 rev/s** of velocity FF at its final sample with **164/164 green**; a `quinticH1` sign flip (preserving `h1(0)=1`, so `tr.v[0]` still reads `v0`) and any `quinticS2`/`quinticH2` mutation (reaching only `tr.tor`) were equally invisible. Driver now emits every sample; all four mutations caught, each by exactly one test. **And the excursion sweep asserted the code's own acceptance predicate back at itself** (`min(HAND_HOME_ABS_POS_REV, start, target)`) — tautological on the floor side, which is why a 60-case sweep never saw the floor defect; now written against the PHYSICAL limits with two states from shipped profiles added to the grid. REJECTED both of the plan's cannot-fit options with the failure each causes (refusing means a kind-3 does not clobber — the only un-arm mechanism — and refusing via an EMPTY trajectory is worse, since `Teensy_code.ino:472-475` returns BEFORE `packedMsgs.clear()`; braking to the limit needs ~28 000 rev/s² near a target, 280× the declared limit — a figure the first draft over-generalised to every retract and which finalize scoped back to the near-target corner). WITHDRAWN mid-finalize: the `smooth_move_v0_deadband_rps = 6.0` **value**, because the two available measurements disagree — the 5.39 rev/s anchor is a top-park **p99** (max never published) while a stationarity-filtered re-read of the 2026-07-25 traces gives p99 0.134-0.144, max 0.96, suggesting 5.39 is a position-band artefact that swallowed transit samples. Its arithmetic claims were wrong and are fixed (0.2802 rev is the branch's **infimum**, not a bound; the H3.5 argument compares a COMMANDED excursion against a MEASURED-position band); the value is the operator's. Rest-to-rest is bit-identical in every commanded position (the one difference is `−0.0f → +0.0f` on the first velocity sample for δ < 0, same `int16` 0 on the wire); the empty branch is strictly NARROWER, so the un-arm path fires in strictly more cases. **NOT FLASHED** — `Teensy_code.ino` byte-untouched, and the Platform Teensy carries no `FW_VERSION`, so an un-flashed board is indistinguishable from a fixed one at the bench (row H4.0 is the only guard) — *superseded 2026-07-27 by Phase 6: the board now reports `FW_VERSION` on the 0x6E0 reply and run-sheet row **FW-1** detects the un-flashed case directly; this entry's flash is still required, FW-1 is what confirms it.* § CHECK HAND-4, rows H4.0a-c + H4.1-H4.10. Full-suite triple in the entry (xfail unchanged at 3). | [2026-07-27-velocity-continuous-prelude](2026-07-27-velocity-continuous-prelude.md) |
| 2026-07-26 | in-progress | Self-toss anomaly fixes — catch-reach-degenerate-overshoot Phase 3 | **The trajectory builder stops manufacturing platform motion nobody asked for — and most of the work was stopping the contract that guards it from going silently vacuous.** Operator decision: `planner._CATCH_TILT_THROUGH_RATE_RADPS` `0.07 → 0.0`, so an unopinionated catch is now **reach + quiescent hold** — two segments, rim stationary at ball contact, settle exactly on the commanded target, plan 0.15 s shorter. The framing is load-bearing and is recorded verbatim: platform motion during a catch or throw is **PERMITTED but never MANDATED** — it should be an output of the planner deciding it is optimal, never a constant in the builder. So this is a **default, not a rule**: `tilt_through_rate_radps` is unchanged, the decay path is unchanged, C-CATCH-1 still returns an explicitly-requested rate **verbatim and unbounded** (the seam a future optimising planner uses), and **no assertion anywhere says a catch commands no motion**. THE VACUITY TRAP, which is most of the diff: at a zero default the C-CATCH-1 bound is unreachable two independent and silent ways — `rate == approx(_CATCH_TILT_THROUGH_RATE_RADPS)` degenerates to `0 == 0`, and passing the kwarg takes the deliberately-**unbounded** requested branch — so the whole `test_ccatch1_*` block would have stayed **green while proving nothing**, which is worse than deleting it. Every such test now restores a non-zero **module constant** via `_set_seat_rate(monkeypatch)`, never via the kwarg. **Verified by mutation, not by reading**: both bound halves deleted ⇒ **5 of 7 `ccatch1` tests fail**, the 2 survivors being negative cases by construction (`..._leaves_a_request_alone...`, `..._keeps_the_seat_on_an_on_pose_supersede`); tree restored and re-verified by md5. Same trap closed twice more — the ROS tests capture `_PRE_FIX_SEAT_RATE_RADPS = 0.07` as a **record** not a mirror, and the replay probe's single mirror splits into a *live default* (`0.0`, compared against production) and a *capture record* (`0.07`, pinned to itself), because syncing it would make every pre-2026-07-26 bag score `NOT-REPRODUCED` — the harness reporting history as broken rather than itself as re-baselined. THE RISK, surfaced and deliberately **not** solved in code: the `0.07` existed because *a parked **tilted** rim deflects the ball* (bb-sim geometry finding). The self-toss catch seats **level** so zero costs it nothing (its seat rate was already zero under C-CATCH-1); the **reload** catch seats at **11.08°** — exactly that geometry — and its rim is now stationary at contact. Two facts bound it without removing it: `0.07` has **never** been hardware-validated (its own leg-velocity note was 2× wrong until measured — 14.24 mm/s, not ~7), and until `407154f` the seat aimed off the **plan-frame** tilt, so no bench impression was ever formed on a correctly-aimed one. The reload is the experiment; § Section ZSEAT scores it, ZSEAT-2 setting PASS/ABORT by **binomial sigma** against sitting 4's 15/19 and separating *bounce-out* (the deflection signature) from *missed-arrival* (BB scatter, not evidence). Also caught: § CHECK CCATCH-3 told the operator to ABORT if the commanded tilt is **flat** over the last 0.8 s — flat is now the **PASS**, inverted outright; corrected by a superseded banner listing all four affected rows rather than by rewriting a sibling section, since the runbook is append-only so parallel phases cannot clobber each other. Landed as its own phase because it changes commanded motion on the **shipping reload path**; rollback is the one-line default with C-CATCH-1 already in force to bound whatever replaces it. colcon + relaunch, no flash, no codegen. | [2026-07-26-catch-through-seat-rate-zero](2026-07-26-catch-through-seat-rate-zero.md) |
| 2026-07-25 | in-progress | Self-toss anomaly fixes — levelling-frame-contract Phase 3 | **The toss now refuses an un-levelled launch — and the gate had to observe the node that APPLIES the correction, because the plan's own step 3 would have passed in exactly the state it exists to refuse.** Phase 0's Table C found the levelling correction is per-PROCESS and observable nowhere: `/gravity_offset` is VOLATILE with one latched push per orchestrator boot and no re-request path, so the `colcon build` + relaunch this very plan mandates empties `trajectory_node`'s correction while the Teensy-persisted `RobotState.levelling_complete` still reads `true`. Verified against ground truth (`orchestrator_node._on_robot_state` assigns it straight from the Teensy), so the plan's step 3 was OVERRIDDEN: `TrajectoryStatus` gains `bool gravity_correction_loaded`, published 5 Hz by the applier; the coordinator stamps every arrival and feeds `platform_levelled = status_fresh AND loaded` into `TossObservations`; `toss_sequencer._step_checking` refuses with `REJECTED_NOT_LEVELLED`, ordered after mocap/streaming and before the hand chain. Justified GEOMETRICALLY, not procedurally — drift is exactly `4·h·sin θ`, i.e. **43 mm** at the ~0.79 m default apex against a **35 mm** cup, so the catch is impossible before the ball is airborne. Three wrong predicates ruled out and pinned: `levelling_complete` (false assurance after a relaunch), `R != I` (a genuinely level machine measures zero tilt and must not be refused), and a sticky cache (no message flips it between a death and its replacement's first status). Transient-local QoS REJECTED — its latch lives in the publisher, so a whole-graph relaunch gains nothing, and a TRANSIENT_LOCAL subscriber silently refuses to connect to a VOLATILE publisher. `_TRAJ_STATUS_STALE_S = 1.0` is probe-sized, not copied: at 5 Hz the sibling 0.5 s is 2.5 periods and the reference bags already breach it once (median 200.0 ms, max **508.5 ms**, four gaps > 0.3 s over 420 s, zero > 1.0 s). Contract **C-LEVEL-1.O** in `ros_ws/docs/levelling_frame.md`. FINALIZE WITHDREW A HYPOTHESIS THE IMPLEMENTER, THE PLAN AND A PRODUCTION COMMENT ALL CARRIED: a half-rebuild does **not** make the topic "go silent" — rclpy re-raises timer-callback exceptions out of `spin()` and `main()` catches only `KeyboardInterrupt`, so **`trajectory_node` EXITS** and `activate` fails at the A2 arm; the operator never reaches a toss, and the documented symptom would have sent them hunting the wrong subsystem. Also added at finalize: non-finite offsets no longer count as loaded (`correction_from_offset` does no validation, so a NaN would have passed CHECKING and surfaced only as a POSITIONING rejection **after** the platform was claimed). DEFERRED with reason, not scope-protection: a dead `trajectory_node` reports `NOT_LEVELLED` rather than naming itself — the toss-only fix would give one node two independent answers to "is trajectory_node alive", and the one-enforcement-point fix (a stamp on `streaming`) changes when a RELOAD refuses. Both Phase-8 toss runbooks swept for the now-mandatory `level` step, and the LG-5 analysis command was validated **two-sided** against four synthetic captures plus two decoy rosout lines. colcon needs **`jugglebot_interfaces` too** — the one phase of this run that does. Hardware deferred — § Section LVLGATE, LG-0..LG-5. Full-suite triple in the entry (xfail unchanged at 3). | [2026-07-25-toss-rejected-not-levelled](2026-07-25-toss-rejected-not-levelled.md) |
| 2026-07-26 | in-progress | Self-toss anomaly fixes — catch-reach-degenerate-overshoot Phase 2 | **The catch through-seat was aiming at the levelling correction, not at the ball — and the bound that fixes it nearly parked the reload rim at contact.** `build_catch` read `catch_pose[3:5]` as "the receive tilt", a premise that dies the moment a C-LEVEL-1 correction is loaded: a gravity-level catch's commanded tilt IS the correction, so the seat aimed 100% wrongly, the reach swung `+2.32°` opposite a `−0.78°` request, and the platform settled `0.3008°` off gravity and held it through release (16.5 mm of throw-direction error). FIX: the gravity-referenced receive tilt becomes its own `build_catch` argument (level catch ⇒ zero arrival twist **by construction**, not by a threshold), plus contract **C-CATCH-1** (`ros_ws/docs/catch_arrival_contract.md`) bounding every departure a *derived* arrival twist creates — reach excursion AND settle overshoot — to `40/81` of the catch's physical tilt scale, at one enforcement point. The factor is the quintic's own geometry (`min ψ/|φ| = 5/2`, measured), and it **corrects a published repo number**: the sign-reversal threshold is `40/81 = 0.4938`, not `ψ(2/3) = 0.790` — 1.6× too permissive. ⚠️ The finalize review's convergent HIGH: sizing that bound on the *residual* seed→target travel collapsed it on the shipping reload path, where the coordinator re-installs the catch 9–11 times in the last 0.7 s from an already-on-pose seed — arrival rate at **ball contact** `0.070000 → 0.004460 rad/s`, a **15.7× de-rate** that parks the rim at the one instant the seat exists for, with segment count, tests and probe all still reading green. The scale is therefore `max(residual travel, receive-tilt magnitude)`: the displacement-only reading degenerates exactly where its own meaning does, since once the target IS the seed every arrival velocity is "wrong-side" by definition. Reference bag: 5 self-toss reaches `2.32° → 0.0000°`, settle on target; both reload reaches keep the full seat with the aim rotated `4.0997°` and the settle moved `+0.021°`. Two advisory Tier-8b sim points do bind (`0.070000 → 0.059469`) — correct, pinned, outside the gate ring. Separate commit: `peak_leg_*` report-less installs now publish `0.0` instead of the superseded plan's peaks. Hardware sitting deferred — § Section CCATCH. | [2026-07-26-catch-reach-overshoot-fix](2026-07-26-catch-reach-overshoot-fix.md) |
| 2026-07-26 | in-progress | Self-toss anomaly fixes — catch-reach-degenerate-overshoot Phase 2 (separate deliverable) | **`trajectory/diagnostics` was attributing one plan's predicted leg peaks to another.** The three `peak_leg_*` fields carry the GATE-PREDICTED peaks of the last install holding a `FeasibilityReport`; six install paths have none (`_svc_hold`, `_svc_go_home`, `_install_guard_descent`, `_retry_pending_stop`, `_install_graceful_stop`, the follower's input-loss stop) and left the fields untouched — so a `hold` after a `go_to_pose` published the `go_to_pose`'s numbers under the hold's already-advanced `move_seq`. This is the misreading that cost time on 2026-07-25, when `14.2 / 142.4 / 3950` read identically before and after a catch install and the field was inferred to be cached: it is not cached, it is per-plan for report-carrying installs and stale for the rest, and a bag cannot tell the two apart. FIX: `_install` clears the predicted peaks, so a report-less install publishes `0.0` = "this install carried no prediction" (honest, and distinguishable — a real accepted move always has non-zero peak velocity). The subtler half: `_svc_go_to_pose`'s write moved AFTER its `_install`, because adding the clear alone would have turned a stale-diagnostic bug into a blank-diagnostic one; all four report-carrying write sites were enumerated and checked. Diagnostics-only, no commanded-motion change, no new lock scope. Closes the PREDICTED half of `mvp_bench_runbook.md` open item 7 — the realized/rolling-window half stays open. Bench: § CHECK CCATCH-5. | [2026-07-26-peak-leg-predicted-staleness](2026-07-26-peak-leg-predicted-staleness.md) |
| 2026-07-26 | resolved | Self-toss anomaly fixes — hand-command-continuity Phase 3 | **The hand-catch prime becomes the derived stroke top — and the review found the profile model three of its neighbours were sized against was the wrong one.** `jugglebot_operational.hand_catch_prime_rev` was a hand-maintained `9.858` rev while the quantity its own YAML comment claimed it was — x3, the throw stroke's end AND the catch trajectory's **first sample** — is `9.95940313273228`. It had drifted **0.101403 rev = 3.207 mm** for the life of the constant, and `Trajectory.h`'s `makeSmoothMove` prepends a prelude from wherever the hand physically is to the first sample of whatever is packed, so every "catch from rest at the top" opened with a real **3.2 mm / 76.5 ms** move — the no-op case was never a no-op. FIX: `generate_config.py:595` emits `HAND_STROKE_TOP_REV` (x3, in **rev** — deliberate unit break from its x2/x5 siblings, because arithmetic inside a drift guard is where a rev/mm slip hides and this project has already shipped a 14× one); the YAML key stays an **explicit override** (derived-only would either delete the operator's bench escape hatch or make a future `9.9594 → 9.5` edit a **silent no-op**, strictly worse than the drift; hard-failing codegen was rejected because it blocks every unrelated YAML edit with no escape); guarded by three independent routes — generated constant, host model, and x3 re-derived from the **parsed shipped firmware header**, the only route that survives a hand-edit bypassing codegen. **The plan's park-band risk did not bind and nothing was widened**: near-band margin **0.2156 rev = 6.8 mm** pessimistic, bridge headroom **1.1406 rev = 36.1 mm**, and `toss_sequencer`'s `hand_parked` gate is keyed to `JB_OP_HAND_RETRACT_REV = 0.0` — the **bottom** band — so the kind-0 off-band hazard is structurally independent of the prime (now pinned so a re-key is caught). Peak commanded **acceleration is UNCHANGED at 100 rev/s²** (the duration formula solves for it), jerk *falls* 1377.5→1370.5, so a taller stroke buys duration, never a harder command. THE REVIEW'S OWN FINDING, outside this phase's charter and converged across two lenses: the `√(a·Δx)` / `2·√(Δ/a)` closed forms three neighbours are sized against are **bang-bang** forms and `makeSmoothMove` is a **quintic** — both a duration (`MIN_THROW_EVENT_DELAY_S`'s "~0.63 s" vs the real **0.758 s**, an upper bound violated by 20 % in the *unsafe* direction) and a velocity (`_THROW_STROKE_VEL_RPS`'s "peak ascent 31.56 rev/s" vs the real **24.63**) were wrong in opposite directions, and this phase propagated both while updating them for the new prime. Root-caused to a **named model** (`hand_stroke.smooth_move_peak_vel_rps` / `_bound_rps`) all four sites now quote, rather than three corrected literals that would leave the fourth site free to re-derive the triangle again. The code guard was **NOT** re-sized — clearing a conservative bound is what a guard should do, and re-sizing to 24.63 would tighten a safety threshold for tidiness — but bench row **H3.7 was recalibrated** `PASS <= 35` → `<= 30` / DEBRIEF 30-40 / ABORT `>= 40`, because at 35 it scored a **26 % overspeed as "on model"** and overspeed on a smooth move is the re-seeded/clobbered-profile signature this whole plan exists to catch. Also fixed: `test_prime_at_the_stroke_top...` pinned the YAML's 4-decimal **rounding artefact**, so a *more precise* value (`9.959403` ⇒ residual under `makeSmoothMove`'s 1e-6 dead-band ⇒ duration 0.0) would have gone RED on an improvement — now a property, two-sided-verified (old 9.858 RED, 4-dp/6-dp/full-float GREEN, 1e-4 rev drift RED, 1e-5 GREEN). `_PRIME_INFLIGHT_S = 1.2` found **unpinned**: sized from the ascent duration but never naming the constant, so a grep-of-the-constant sweep misses it structurally (0.44 s model margin; a future overrun re-dispatches a kind-3 mid-ascent ⇒ the 2026-07-23 stutter). TWO HYPOTHESES WITHDRAWN: the "unexplained stale-YAML codegen run" that left the external BallButler header at `9.858f` was a **reviewer's own mutation test** (`git checkout` cannot restore a delivery target outside this repo — re-running codegen fixed it and proved determinism); and "the 26.5 ms returns to Phase 1's arm-fit budget" is **false** — `PRELUDE_ALLOWANCE_S` is only consulted on the **self-toss** path, where the hand reaches x3 via the throw stroke and the prime never entered, so the saving accrues on the **primed** path to the firmware's own `:533` fit check. Restating `prelude_peak == approx(31.4 → 31.56)` adjudicated **ACCEPT** (tolerance `abs=0.1` unchanged, both inequalities and both latch samples unchanged, one assertion ADDED, zero xfail/skip in the diff). colcon + relaunch, **no flash** (`JBOp::HAND_CATCH_PRIME_REV` is a dead `constexpr` no sketch reads); § CHECK HAND-3 pending. Full-suite triple in the entry (xfail unchanged at 3). | [2026-07-26-hand-prime-rev-derived](2026-07-26-hand-prime-rev-derived.md) |
| 2026-07-25 | in-progress | Self-toss anomaly fixes — catch-reach-degenerate-overshoot Phases 0-1 | **The `+2.32°` excursion from a `−0.78°` target is one plan doing what `build_catch` specifies — and the amplification goes as `1/|tilt|`, so plan 1 makes it WORSE.** All **seven** catch reaches in `2026-07-25_15-17-48` now reproduce offline through the production `planner.build_catch`, seeded only from recorded inputs (FK seed, `levelling.correct_pose` of the wire target, `landing − _PRETILT_EARLY_S` arrival; ONE nuisance scalar fitted, zero-lag residual printed beside it): 5 self-toss pre-tilts at rx rms 0.0049–0.0062° and both reload pre-tilts at 0.0021–0.0025°, 0 FK failures. THE THREE FEATURES, closed-form: the opposite-sign excursion is the quintic ACQUIRING its specified terminal arrival rate (`p = p0 + Δ·ψ + v1·T·φ`, `φ = −4s³+7s⁴−3s⁵`, extremum **−16/81 at s = 2/3** — verified equal to the production `QuinticSegment` to **2.8e-14**; twist term `+2.925°` + displacement term `−0.615°` = `+2.32°` against the echo's `+2.3224°` at the same knot); the 1.385× settle is the through-seat overshoot `1 + 0.5·rate·decay/|tilt|` = **1.384732** (`−1.078408°` vs bag `−1.0784`); the acc/jerk spike is the 0.150 s decay segment (`142.4 / 3950` against the 3.707 s reach's `13.9 / 35` — 10.2× and 113×), landing at the arrival = release − 0.700 s, which is also when the tilt goes flat. ROOT CAUSE: `build_catch` reads `catch_pose[3:5]` as "the receive tilt", true only while the commanded frame IS the gravity frame — with a correction loaded a gravity-level catch arrives as a plan-frame tilt, so the through-seat aims along the correction. **STOP FIRED**: not general to the C2-replan-to-fixed-arrival pattern (`build_timed`/`build_follow`/chase all take a CALLER-supplied twist; only `build_catch` manufactures one), but general to every catch with a correction loaded **including the shipping reload path** — plan re-rated LIVE, item 9 blocked on operator re-prioritisation. Amplification `(16/81)·rate·T/|tilt|` = **3.76** toss vs **0.17** reload, one code path 22× apart — and since C-LEVEL-1 drives the requested tilt toward zero, plan 1 makes this worse, reversing the premise that rated it lowest of four. THE BRIEF'S MANDATORY RE-DERIVATION: single-install reading **CORRECT**, staleness worry **REFUTED** (`move_seq` 47 published `0.0/0.0/0`, 48 published `14.2/142.4/3950` — the field is WRITTEN; the repetition is real lead-invariance). BUT THE EVIDENCE NARRATIVE WAS WRONG THOUGH THE VERDICT WAS RIGHT, and it had already reached a production comment, the probe, the README and the plan: the statistic called *decisive* (plan-end drift 1.9 ms / 21 samples) is **structurally blind to the exact hypothesis it was credited with refuting** — `_plan_and_install_catch` derives its lead from a fixed `arrival_perf`, so a re-plan to the SAME arrival leaves the plan end untouched, and the same bag hands over the counterexample (post-release burst `move_seq` **53→64**, 14 accepted feedbacks, drift **1.02 ms** — LESS than the zero-install window); and there is no status-vs-diagnostics rate advantage, one 5 Hz timer publishes both (1453 each / 290.6 s). Census regated on `move_seq` + **`plan_kind`** (a `HoldPlan` reads `hold` — the direct per-sample detector of the three paths that install by direct assignment) + accepted `target_feedback`. THREE WAYS THE INSTRUMENT WOULD HAVE FAILED A **HEALTHY** CAPTURE, all closed: (1) `_last_rejection` is a lifetime latch (assigned `''` in `__init__` ONLY), so one unrelated early rejection would force `NOT-REPRODUCED`/exit 1 on every later reach — invisible on the reference bag (0 non-empty across 1453 samples); (2) `--self-check` was blind to **2 of 5** mirrored constants — `build_replay` forced its own `tilt_decay_s` into every production call, so mutating `build_catch`'s default 0.15→0.25 left it passing 7/7 (mutation-verified), and nothing compared `_PRETILT_EARLY_S` at all; (3) `/gravity_offset` was read at end-of-bag, not at ingest, so a mid-session re-`level` would score earlier reaches with a later correction. Also corrected: lead-invariance is **NOT** a general property of catch plans (a reload-sized reach runs `82.5→30.1→24.7→15.2→14.0 mm/s` over leads 0.8→8.0 s; the bag's two reload installs published `23.6`/`23.8` vs the toss catches' `14.2`) — losing that qualifier recreates the very inference error the annotation exists to kill; the plan's "phase-swept band brackets the recorded values" was false on both axes and mixed a truncated running max (2416) with a full-plan band (2558–3141); "bit-identical" → identical at the published precision (`3949.7` → `:.0f` → `3950`); and a parenthetical contradicting the landed contract `ros_ws/docs/levelling_frame.md` on the 16 mm was resolved **in favour of the contract** by lever arm (~1 mm is the seat geometry; **16.5 mm** is the throw direction, valid because `hold_after` holds the settle pose through release — confirmed by the bag's own flat `−1.0784°` from −0.50 to +2.00 s). C-CATCH-1's withdrawn stationarity clause had been justified by an "operator principle of 2026-07-26" that exists **nowhere in the repo** — appeal deleted, root cause kept, ratification parked for the operator. `peak_leg_*` staleness ANNOTATED not fixed (the fix reorders `_svc_go_to_pose`'s write to after its `_install`, safety-adjacent; handed over **symbolically** because the first draft's line numbers were already 2 stale and following them would ship a worse defect). `ros_ws/` diff is comment-only, zero executable lines; colcon for parity, no flash, no YAML. Full-suite triple in the entry (+10 cases, xfail unchanged at 3). | [2026-07-25-catch-reach-overshoot-repro](2026-07-25-catch-reach-overshoot-repro.md) |
| 2026-07-26 | resolved | Self-toss anomaly fixes — hand-command-continuity Phases 1-2 | **The catch arm stops clobbering a live throw stroke (C-HAND-1 host half) — and the review found five ways the bench would have believed the wrong thing.** The Platform Teensy holds ONE packed queue and rebuilds it on any kind-0/1/2 command, seeding the replacement prelude from `current_hand_position` with `v=0` — the live velocity is `extern` at `Trajectory.h:47` and **never read**. During a self-toss the kind-1 catch arm landed **8-18 ms after release**, inside the throw's 65 ms decel ramp, so the queue was cleared with the hand at ~120 rev/s: overshoot to **10.17-10.33 rev** (0.775 rev = 24.5 mm from the 11.1 rev guard), yanked **0.34-1.75 rev = 10.7-55.3 mm below** the stroke end, ~300 ms to recover. FIX: a stroke-busy window **derived** from the toss's own announcement (`throw_time + t_dec(|initial_velocity|) + 40 ms`; both inputs already on the wire) enforced at **exactly one point** — `_throw_stroke_gate_ok`, called from `_arm_hand_catch`, the node's only kind-0/1/2 dispatch — so the arm is **deferred, never dropped** (latch left open, dispatch counter untouched). That also makes Phase 2 free: the ack-failure retry re-enters the same gate, turning "further repacks just churn" from an *assumption* (true for a reload, false for a toss) into an enforced precondition. A fixed delay was rejected on `t_dec`'s 2× spread (94.5 ms at 0.55 s vs 47.4 ms at 1.10 s). The model is **shared, not duplicated-and-pinned**: the Phase-5 verdict probe's local `StrokeModel` MOVED into `motion/trajectory/hand_stroke.py` and is imported back, because a pin only catches the quantities it asserts and a `t_dec` divergence would have the bench scoring the fix against a different model than shipped (precedent: `JB_OP_HAND_CATCH_PRIME_REV` is a hand-maintained 9.858 against a 9.9594 stroke top). Contract written down at **`ros_ws/docs/hand_command_continuity.md`** rather than left in a comment a plan-archival would orphan. THE FIVE FINDINGS, two converged across lenses: (1) the closed-window branch promises an **attempt, not a catch** — its fit check budgets the *at-rest* prelude while the hand is mid-stroke (real prelude 0.37-0.76 s), so `:533` may refuse it; the proposed "drop the arm instead" was **REJECTED** after reading the firmware — `:533`'s `return` sits **before** `packedMsgs.clear()` at `:539`, so a refusal leaves the live stroke **intact** and the cost is a lost catch, never a clobbered stroke; (2) `required_arm_lead_s` excludes the **downstream** bridge→Teensy transit (only the upstream leg cancels, via the bridge's own re-stamp) — a 7 ms shortfall at the tightest point against the **115 ms** of slack the gate actually dispatches with, since it fires at the FIRST tick after the window opens, not the last; (3) **`catch/vel_scale` closes the window on its own** — scale **0.45 ⇒ −15 ms** at the 0.55-0.56 s flight with a healthy tracker, inside the shipped `[0.3, 1.5]` range, and that is exactly the corner the optional short-flight check runs in; (4) the **flight-time error is probably not this phase's** — the ball separates at the decel ONSET (`x2` 5.9138 rev) and all seven truncations sit *past* it (6.1965-7.7825), so a null result is expected and must not re-open the queue-clobber question; (5) the deferral is **tick-dependent** (nothing re-enters the gate on a timer), instrumented as new runbook row **H1.7** rather than fixed on an unproven trigger. Also caught: `test_stroke_window_cleared_on_both_latch_edges` exercised only the *disarm* edge — deleting the arm-edge clear left the file green; now drives both, mutation-verified. And the `JB_OP_TOSS_RELEASE_LATENCY_MS == 0.0` pin became a **residual** property, so Phase-5 T0 filling that slot (which makes the window strictly *more* conservative) no longer reds an unrelated module. Two Phase-0 hedges corrected in place: the prelude is never empty (dead-band 1e-6 rev = 0.03 µm; `fmaxf(T, 0.05f)` floor ⇒ 50-76 ms), and `_MIN_EVENT_DELAY_S = 0.3` binds ahead of the Teensy budget ⇒ real windows **395/115 ms**, not 546/208. kind-3 exemption deliberate + pinned (the only un-arm mechanism the Teensy offers). Instrument re-verified two-sided after the model move (25/25 flag + 4-shape accept, fixture byte-identical). colcon + relaunch, **no flash**; hardware validation pending (§ Section HAND, H1.1-H1.7 + H2.1-H2.4). Full-suite triple in the entry (+33 cases, xfail unchanged at 3). | [2026-07-26-hand-command-continuity-arm-gating](2026-07-26-hand-command-continuity-arm-gating.md) |
| 2026-07-26 | resolved | Self-toss anomaly fixes — levelling-frame-contract Phases 1-2 | **One levelling frame lands (C-LEVEL-1) — and the swing the plan promised to remove turns out to be `build_catch`'s, not the frame's.** `trajectory_node` applied the gravity correction on **2 of its 6** external pose-ingest surfaces, so *"level" meant two things inside one node*: the toss's positioning `go_to_pose` parked at plan-frame `rx = 0` while the pre-tilt `catch/dynamic_target` asked for `−0.7788°`, and `build_catch` planned a min-jerk reach **between two frames**. FIX: one shared pure-Python `motion/levelling.py` (sign convention + the non-commutative `R_gravity @ R_target` composition — the two orders differ by `1.268e-3 rad` *entirely in `rz`*, invisible in leg-space telemetry, which is why a second copy is a silent error); normative contract at `ros_ws/docs/levelling_frame.md`; all six E surfaces + `mpc_bridge_node`'s verbatim second copy migrated (per-row grep gate 0/0/0, stored `R` 2 assignment sites). Extraction verified **bit-identical numerically** (36 offset×rotvec pairs incl. rotvec ≈ π, `max|Δ| = 0.000e+00`), not by reading the diff. Enumerated sites rather than one choke point, and the reason is countable: **9 derived seeds + 4 direct plan installs vs 6 external ingests reach the same planner entries**, so a choke point double-applies on every FK seed (`−0.7788°` → `−1.5576°`). Neutral corrected **at use**, never at construction (a stored corrected constant double-applies the instant a new `/gravity_offset` lands — and the operator levels *after* launch). THE FINDING (all three review lenses converged on the label, one on the physics): **the plan's headline claim was false and its bench instrument would have failed a working fix.** `build_catch` specifies a non-zero **arrival twist**, so a reach with `p0 == p1` still collapses to `v1·T·φ(s)`, `φ = −4s³+7s⁴−3s⁵`, `|φ|max = 16/81` at `s = 2/3` ⇒ `peak_above_park = 0.789132° per second of catch lead` **exactly linear, and independent of how well the machine is levelled**. The reference bag's `+2.3204°` pins the lead at **3.70 s**; at that lead this fix moves the physical peak tilt from **+3.0992° to +2.9198°** — 0.18° of a 2.9° swing — while the quantity the runbook gated on **rises** (`+2.3204 → +2.9198`), because pre-fix the park itself sat 0.7788° high and hid part of it. The retired `≥ +3.099° ⇒ ABORT` line fires on a healthy system at any lead ≥ 3.93 s and sat 0.18° from firing at the lead the session actually ran, while the *same bullet* quoted `+0.17…+0.80°` (absolute peaks at 1.2/2.0 s) and the probe printed a third number under the same label — three conventions, one name. What the fix DOES close is the **park**: the platform rested 0.78° off gravity and the two surfaces disagreed. Instrument made two-sided (`--self-check`: post-fix must PASS, pre-fix must FAIL, and a post-fix session contaminated by a long pre-`go_home` ACTIVATE hold must FAIL *with an ambiguity note* — that hold is commanded at `rx ≈ 0` and is CORRECTLY uncorrected, so on a healthy session it can win the longest-plateau vote and read exactly like the pre-fix frame; `--t0` now mandatory in LVL-3), plus sign-aware settle selection (a hard-coded `min()` returns the PARK for a positive-rx correction) and an `implied lead` cross-check that replaces the threshold (pre-fix bag reads 2.94 s against a true 3.70 s). Guard hardened at three mutation-proven blind spots: a new ingest writing `self._follower_target` with no tracked call at all; `from jugglebot.motion.levelling import correct_pose`, which un-instruments both AST manifests AND the behavioural counting fixture at once; and the endpoint-only regression assertion the plan had twice forbidden (now a rate-per-second-of-lead pin at `s = 2/3`, asserted against the pre-fix geometry at the identical lead). Six seeded regressions, each caught, each reverted. SURFACED AND DELIBERATELY NOT FIXED: `build_catch` aims its through-seat residual along `catch_pose[3:5]` — with a correction loaded that IS the correction — settling **0.3008° off gravity-level at ball contact** (closed form `−1.078408°/−0.095775°`; bag `−1.0784/−0.0958`; probe `−1.0775°`, err 0.0009°) ⇒ the 16 mm catch error. Four-branch design fork, changes commanded motion at ball contact on the shipping reload path ⇒ operator decision, pinned as a characterisation test. One reviewer finding REFUTED with the trace (`stow_to_global_mm` is a pure z translation, so the cup-swing compensation's frame is the base frame, not gravity — the ~1.3 mm IS newly introduced, and is worth ~42 mm of release-direction error it removes). colcon + relaunch, no flash; a manual `level` after the relaunch is mandatory until Phase 3. Full-suite triple in the entry (+55 cases, xfail unchanged at 3). | [2026-07-26-levelling-frame-contract](2026-07-26-levelling-frame-contract.md) |
| 2026-07-26 | tuned | Self-toss anomaly fixes — hand-command-continuity Phase 0 | **The post-throw hand dip measured, and the instrument taught to score a FIXED capture — the half of a probe's charter that had never been exercised.** `tools/probes/hand_stroke_timeline.py` lands (offline, read-only, no production code changed): reproduces **25/25** Context-table rows from the reference trace, worst instant delta **0.4 ms** against a 20 ms tolerance, and characterises the defect across the whole evidence base — **7 self-tosses, all truncated mid-stroke** (`pos_cmd` freezes at 6.1965–7.7825 rev instead of running the decel ramp to `x3` 9.9594), the hand coasting to 10.1653–10.3248 rev with as little as **0.775 rev = 24.5 mm** of headroom to the 11.1 rev guard, then yanked **0.339–1.748 rev = 10.7–55.3 mm BELOW** the stroke end at −17.9…−42.4 rev/s. Mechanism traced in firmware: any kind-0/1/2 `HAND_TRAJ_CMD` re-preludes from the LIVE encoder with a `v=a=0` quintic (`Trajectory.h:242-301`, `current_hand_velocity` extern-and-never-read) then clears the whole queue (`Teensy_code.ino:539`), so a kind-1 catch arm landing in the 65 ms decel ramp replaces it with a rest-to-rest ramp computed from a position the hand is passing at ~120 rev/s — corroborated independently by every seed reading **0.0000 rev** from the live `pos_meas`. Dispatch latency measured **+12.8…+21.9 ms** (bag) / up to **+23.4 ms** (trace, a systematic 1.5–1.7 ms clock-path offset), so `t_release + t_dec` alone expires mid-ramp and Phase 1's 40 ms margin is 1.7× the worst case, not optional slack; `_MAX_ARM_DISPATCHES = 2` honoured (15 dispatches / 10 tosses) — **no separate defect**, and 5 of 10 tosses dispatched only ONCE, so the dip is caused by any mid-stroke command, not by the retry. THE FINDING (all three review lenses converged): **every gated criterion was an artefact of the probe's own control flow, not a measured threshold** — the probe's own model of a *perfect* post-fix capture printed `dip 0.6 mm` / `pullback −0.3 rev/s` while the runbook said PASS was "no dip printed", so an operator would have scored a working fix as FAILED and burned a powered sitting. Root cause: `dip` is peak-minus-bottom, hence non-zero on any healthy overshoot-and-settle — 20.2 mm at the 10.60 rev coast Phase 4 step 2 *charters*, **larger than the smallest real defect** (20.3 mm), so no `dip` threshold can separate the populations. Fixed at the enforcement point, not in four places of prose: new `dip_below_x3` (pre-fix 0.339–1.748 rev vs healthy 0.000–0.001, nothing between), band `<= 0.10` rev = 3.2 mm — the *same* allowance the plan grants for overshoot above `x3`, so symmetric — with the honest margin stated (~100× healthy side, only **3.4×** on the tightest defect); `pullback` demoted to a bounded report (ABORT `< −5.0` rev/s, *conditional on `peak` passing*) because both reviewers' proposed −1.0/−2.0 would falsely abort a healthy settle, measured −0.31/−1.58/−10.03 rev/s at 0.02/0.10/0.64 rev of overshoot. Two more instrument defects: `catch_desc` was ANY downward command, so a `makeSmoothMove` **brake** — which Phase 4 step 3 specifically charters — spoofed it, and since it bounds both the truncation scan and the dip/peak window it put `catch_desc` **567 ms early** and mis-read the end-stop `peak` row (now requires the command to travel 0.5 rev below `x3`, 7.7× clear of the descent / 1.4× clear of the largest brake, with the brake surfaced as `first_neg_cmd`); and the fixed-shape gate ran only at `tof=0.80` where the 0.6 s window binds first, so neither `catch_desc` cap was load-bearing (added a `FLIGHT_TIME_MIN_S=0.55` case — deleting the dip cap now reports **3.83 rev** of dip on a clean capture). All three mutation-verified; the truncation-scan cap recorded as still-unpinned belt-and-braces rather than papered over. HYPOTHESIS WITHDRAWN: the Outcome's "+20 mm, sim from the physical bottom, firmware from the bottom of the effective stroke" is wrong — `Homing::HAND_ABS_POS_REV = −0.1` homes DOWN so encoder zero is **3.16 mm** up, and the 11.1 rev guard lands 0.76 mm *below* the 355 mm top (incoherent at 20 mm: the guard would sit 16 mm PAST the hard stop). Phase 4 would have converted the `[0, 11.1]` rev bound **20 mm = 0.63 rev too permissive** (0.532 rev past the guard) and flashed it; the mapping, the constraint and a "see Confirmation 2" pointer now live *inside* Phase 4 steps 2 and 4. Also: `/rosout` now DETECTED not assumed on both input paths (the runbook's own Recording command records it); the runbook's recorder env corrected to system `python3` **not** the venv (contradicted the recorder's own docstring on the one mandatory capture step); `--json` timestamped so two `CHECK HAND-n` analyses in one sitting no longer clobber; the two conventions this probe deviates from scoped **in the convention documents first**. Deferred: the catch time-origin question, the kind-3 clobber gap, and all bench validation (§ Section HAND carries the 7-row numeric PASS/ABORT table + baseline). No colcon, no relaunch, no flash. Full-suite triple in the entry. | [2026-07-26-hand-stroke-timeline-probe](2026-07-26-hand-stroke-timeline-probe.md) |
| 2026-07-25 | in-progress | Self-toss anomaly fixes — levelling-frame-contract Phase 0 | **Enumeration phase: the ingest audit found a SIXTH external pose path, a volatile correction, and a guard blind to the class of defect it was specified to catch.** The plan closes "`level` means two different things inside one node" — `trajectory_node` corrects some pose-ingest paths and not others — so Phase 0's whole deliverable is the enumeration itself. Built from the ROS entry surfaces DOWN (6 subs, 7 services, 2 timers, the emitter thread, `follower.py`) rather than by grepping the existing call sites, because the grep method **cannot** find E6. Result: **6 external / 9 derived / 4 direct plan installs**, keyed per POSE not per surface — the shape decision that mattered, since `timed_target` is ONE surface carrying TWO independent external poses. **E6** = `timed_target(hold_after=False)` passes `_neutral_pose` into `build_timed` as the RETURN target (`:2223`→`:1895`→`planner.py:560`), so correcting only `go_home` would have shipped a half-fix. Review then found two more: (a) the correction's own DELIVERY is VOLATILE — one latched publish per orchestrator boot, in-memory, no re-request, absent from `TrajectoryStatus` — so Phase 3's gate as wired to the Teensy-persisted `levelling_complete` would PASS in exactly the state it exists to refuse (Phase 3 rewired; the wire format is deliberately left to Phase 3); (b) the structural bypass test's manifest key `(enclosing fn, target-arg source)` is blind to the whole E6 class — `build_timed(...)` at `:1892-1895` is ONE `ast.Call` with TWO external poses, and `build_catch` already takes `neutral_pose` too, so a one-line future change adds a SEVENTH ingest that every assertion passes. Key widened to pose-bearing arguments + the file set frozen as data (Table A cites planner entries in `follower.py`, which a `trajectory_node.py`-only AST pass cannot see). C-LEVEL-1 restated ("once per external pose, one shared implementation, enumerated sites") with the root cause a choke point can't work: derived poses reach the same planner entries and would double-apply. Resolved benign: nothing trusts an uncorrected neutral (`motor_guard` uses the pose for a condition number + workspace check only; ACTIVATE/stow are leg-space). Excursion measured 2.7736 mm = 0.03908 rev. All 52 `file:line` citations re-resolved mechanically and every headline number re-derived independently at finalize; 4 numeric/reference defects corrected. No code, no tests — full-suite triple in the entry. AWAITING OPERATOR REVIEW (hard gate on Phase 1). | [2026-07-25-levelling-frame-enumeration](2026-07-25-levelling-frame-enumeration.md) |
| 2026-07-25 | resolved | Self-toss anomaly fixes — fk-convergence-tolerance Phases 0-1 | **A fully converged FK solve could be declared a failure, and `trajectory_node`'s seed path turned that into a hard refusal to stream.** `leg_lengths_to_pose` tested a bare ABSOLUTE residual of 1e-10 mm, but the residual is `\|leg_vector\| - init_leg_length` — a cancellation of two ~650–870 mm operands — so its achievable floor scales as `2.01·eps·L·cond(J)` (bounded every one of 765 Phase 0 poses) and EXCEEDS 1e-10 mm in part of the workspace. Consequence: 26 consecutive `seed FK failed … not streaming until a valid state` ERRORs across 286 ms (2026-07-24 09:08:55), plus 514/28953 `/robot_state` and 37/10453 `/leg_setpoint_echo` vectors of one session unreconstructable offline. The rejected pose was exact to ≤ 4.60e-13 mm = 5.3e-11 of one encoder dead-band; and the failure is spatially CLUSTERED (±1 dead-band around the burst pose fails 96.8 %, vs 0.0 % at z=50/110 flat) so the 10 ms re-seed retried *inside* the failing pocket. FIX: mixed absolute/relative test (`scale` = largest ABSOLUTE leg length, bounded ≥648 mm so it cannot collapse near stow — the risk register's concern, closed structurally) + a stagnation exit gated on a 1e-6 mm absolute physical-irrelevance ceiling (NOT `k·tol`, which would give the MPC plant 12 dead-bands of slack), both in ONE predicate `_fk_accepted`, divergence raise untouched (unsatisfiable sets raise at ≥ 6.5e8× the ceiling). Both mechanisms ship because they fail differently — the finalize sweep found 85 poses worse than the grid the relative term was sized against (max floor 2.18e-10 mm over 13001 poses, 4.1× headroom not 6×). Review caught a REAL regression the implementation introduced: a ±inf target made the derived threshold `inf`, so `inf <= inf` returned the INITIAL GUESS as converged — suppressing the only FK-failure signal (`_fk_fail_count`/`fk_convergence_failure` e-stop unreachable; seed installs a hold at z=0 while parked at 170) — fixed by rejecting a non-finite `scale` (0.126 µs, no allocation). Also fixed: the final Newton step's work was discarded; the RuntimeError quoted the PRE-step residual (up to 3.00× over-stated); the mkdocs criterion table's unescaped in-cell pipes silently dropped every default value. Honest correction: the exhaustion path DID change for the 40 Hz caller (returns where it raised when the last step lands within tol) — kept, but the "bit-for-bit unchanged" comment is withdrawn and the semantics pinned. Full-suite triple in the entry. | [2026-07-25-fk-convergence-tolerance](2026-07-25-fk-convergence-tolerance.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss: Toss.action height goal + operator-guaranteed ball | **Two operator-facing Toss.action changes ahead of Phase-5 hardware (operator's direction).** (C) The goal nominates a THROW HEIGHT (`throw_height_m`, apex above release — the juggling-relevant variable; `h ∝ T²`, not linear) instead of a flight time; a single tested `toss_release.flight_time_from_height` converts once at ingestion, internal `flight_time_s` plumbing untouched (grep-verified: zero remaining goal-field consumers). (B) The ball-evidence CHECKING gate defaults OFF (`toss_require_ball_evidence: false`) — there is NO ball-in-cup sensor, "possession" is only an unreliable tracker belief (false-MISSED corruption), so a physically-loaded ball must not be blocked; the operator guarantees the ball, the hand-parked hazard gate still stands, a forgotten ball just fires a benign empty stroke. Config constant (a hardware fact), not a per-session waiver. Consequence recorded: the Phase-3 Capture R now needs the gate re-enabled (Phase 3 is CLOSED). Phase-5 runbook `session_phase8_toss_hardware.md` (T0–T4) written on top. Toss subset 199 passed; full-suite triple in the entry. | [2026-07-25-toss-action-height-and-operator-guaranteed-ball](2026-07-25-toss-action-height-and-operator-guaranteed-ball.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 3 VALIDATED (trace + checker contract fix) | **Phase 3 real-ordering trace VALIDATED on hardware; the dry checker's no-ball assumption corrected.** Three captures over two ACTIVATE cycles: all cross-process ORDERING invariants pass (DT-1/2/5/6/8/10/11). A ball/no-ball A/B confirmed DT-9 (stroke↔BALL_IN_FLIGHT) + DT-12 as ball-in-hand artifacts — both flip FAIL→PASS with the hand empty (the reframe: DT-9 first looked like a real Phase-7-class timing bug, but `toss_sequencer.py:824`'s `throw_stroke_seen OR ball_track_confirmed` fired early on the in-hand ball; retired by the clean re-run). ROOT of the residual fails: a *self-announced* toss always seeds its OWN predicted track (`ball_prediction_node` synthesises a `destination=jugglebot` `BallState` from the announcement — `tracking=0`, no mocap detection, drives the catch pre-position), so DT-7/DT-13/DT-14's "no ball ⇒ quiet tracker" assumption is unpassable by construction. Fixed as a CONTRACT correction (not a weakening): DT-14 keys on `tracking`/destination (fails only on a real `tracking=1`/foreign detection — adversarially verified it still catches the real-ball run's `id=35`); DT-7 drops "exactly 1" for "whole stream within 30 mm of (0,0,170)" (stronger — catches a real-ball reach-wander); DT-13 keeps streaming/mode, drops the mis-specified return-to-hold. Corrected `check --dry` on `toss_trace_2026-07-25_15-24-25` = **12 PASS / 0 FAIL**. Two real tosses occurred (a ball present) — both MISSED — early Phase-5 T0/T1 data. | [2026-07-25-toss-phase3-trace-validated](2026-07-25-toss-phase3-trace-validated.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 4 (Tier 8b displaced throw) | **Tier 8b lands (behind `JB_OP_TOSS_TIER='8a'`): tilt-aimed displaced throw from a config site A to a nominated catch B ≤70 mm away.** `tilt_to_throw` ported to the production `tilt_geometry` (sim re-exports); `compute_release_state_tilted` = swing-compensated pre-tilt at A + tilt-aimed launch with a loud `ThrowTiltInfeasible` clamp gate; the coordinator pre-positions TILTED at A, raises a new `catch/pretilt_hold` gate (else the stock announcement pre-tilt translates A→B and un-tilts BEFORE release — aim destroyed), and publishes the ONE deferred A→B reach at `t_release`. **Salvaged**: the Fable-5 build workflow died on a spend limit leaving a half-wired coordinator + absent safety gate + zero tests + a latent None-deref; finished + tested on Opus 4.8. The re-run adversarial review caught a **BLOCKING** gap invisible to both mocked tests (they monkeypatched positioning) and the sim gate (it models the correct pre-tilt-at-A): production `_position_platform_for_toss` sent the LEVEL pose at B for all tiers, orphaning the tilted A pose — an 8b hardware toss would have thrown straight up. Fixed + pinned. 8b binding ring (50 mm, T=0.80) PASS 9/9, 0 feas_viol; asymmetry map (detach column, T=0.80 s) shows a +y-hemisphere weakness (−y+−x seat to 100 mm; +y/NW fail at 70 mm) — the data for the T4-at-100 mm fork. 8a + reload byte-identical. ci-deep + sweep triples in the entry. | [2026-07-25-toss-phase4-tier8b-displaced-throw](2026-07-25-toss-phase4-tier8b-displaced-throw.md) |
| 2026-07-25 | prepared-pending-hardware | MVP trajectory bringup — Phase 8 / single-ball toss Phase 3 prep (trace harness + runbook) | **Phase 3 PREPARED (not executed): real-ordering trace harness + operator runbook land.** `tests/hardware/toss_trace_recorder.py` — observe-only rclpy recorder (16 wires, correct QoS, `/rosout` as the service-evidence channel, `uptime_ms` via link_status) + pure-stdlib offline checker: 14 dry-trace + 4 reject invariants (prime_hold→armed→announcement→dispatch orderings, no-prime-under-hold, single-dispatch, hold-released-last), 5 ms same-wait-set epsilon band reported AMBIGUOUS never silently PASS; self-verified both ways on synthetic traces (injected violations fail exactly their invariants, no collateral). PLAN ERROR FOUND AND AMENDED: the plan's "unpowered bench" dry trace is unreachable (activation faults + 10 Hz mode republish ⇒ `REJECTED_WRONG_MODE` before the waiver matters) — the runbook specifies a POWERED no-ball bench, framing the dry capture honestly as the first-ever real empty-cup throw stroke (≈3.93 m/s, T=0.8 s) and recommending it fold into the T0 sitting; un-waived `REJECTED_NO_BALL` capture runs FIRST; waiver set/unset discipline (this runbook is the one sanctioned set point); `toss_mocap_body` QTM bench task included. `session_phase8_toss_trace.md` mirrors the phase-7 runbook voice. Not pytest-collected. Operator captures pending. | [2026-07-25-toss-phase3-prep-trace-harness](2026-07-25-toss-phase3-prep-trace-harness.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 2 (sim gate + Tier-8a sweep) | **`sim/toss_gate.py` lands; the Tier-8a sweep PASSES both binding bands** (plan's 2–3 m/s: 10/10 at all 12 points; the amended hardware-relevant T=0.80@z170 band: 10/10 ×4 + 9/10 ×1) with 0 feasibility violations and 100% pump acceptance (45,950 frames, asserted non-vacuously). Advisory long-flight tail (T≥0.95 s) FAILS under the 1% placeholder release noise — drift ∝ v·T vs the 80 mm envelope class, worst landing 90.7 mm; signal for T2's 1.1 s rung, re-gated after T0 measures real scatter. Production-in-the-loop: real `build_move`→emitter→pump pre-position (worst err 0.08 mm), real `toss_release` announcement (worst −43 ms), real `build_catch` + arm-and-forget catch. Review caught pre-sweep: BLOCKING silent-dead-arm `core_clean` (ball into the statically-parked cup gate-passed; now requires `catch_armed`), vacuous empty-band PASS, the pump's silent `(None,None)` path making the knot-acceptance invariant vacuous (also inherited in `reload_gate` — follow-up). Kinematic ejector = `Ball.release` (mode-correct for the `contact_carry=False` gating plant; plan-named `ballistic_release` runs verbatim in the diag column’s opt-in kinematic mode; the recorded sweep’s diag column ran detach: 8/20 seat-caught, landing err ≤4.2 mm — the seat, not the aim, is the knife-edge). `gate_common.py` extraction behaviour-neutral (`test_reload_gate.py` unmodified, green). | [2026-07-25-toss-phase2-sim-gate-sweep](2026-07-25-toss-phase2-sim-gate-sweep.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 1 (action + sequencer + coordinator) | **Toss Phase 1 lands software-complete: `Toss.action` + `toss_sequencer` FSM + ball-ops coordinator (Tier 8a; 8b = `REJECTED_TIER`, config-selected).** `reload_coordinator_node` extends into a two-action ball-ops node (measured shared surface 76.5% > the 70% threshold; cross-action busy gate claimed under `_lock` closes a pre-existing reload accept→install race); the control-system walk-through confirms ZERO `trajectory_node` changes for 8a; firmware ground truth dissolves the prime-timing fear (throw stroke 0→9.959 rev starts at the post-catch rest and ends at the catch-prime position). NEW hazard found and closed: `catch_coordinator`'s armed-edge/retry-tick auto-prime would move the ball-laden hand mid-toss — explicit `catch/prime_hold` gate (default-off, reload bit-identical) + raise-side ordering (hold one tick before the bundle, `prime_dispatched` belt). Adversarial panel caught a BLOCKING frame bug the unit tests had enshrined (mocap arrival check vs a nonexistent 'jugglebot' QTM body, target global vs measurement platform_start — the z double-add class in the verification path; now config-keyed `toss_mocap_body`, default disabled, platform_start-frame compare) + phantom-track poisoning, NaN-goal post-arm detonation, stroke-signature 15→40 rev/s. Release math: one STOW→global conversion point, `HAND_THROW_OFFSET_MM` 58.044, full ballistic inverse (T=0.8 s ⇒ 3930.82 mm/s, apex 787.85 mm). Thrower-agnosticism verified with the sharper invariant: gates are `target_id=='jugglebot'`, never `thrower_name` — an empty target_id is silently uncatchable (integration test drives the real tracker+coordinator path). tests/ros 1157 passed; full-suite triple in the entry. | [2026-07-25-toss-phase1-action-sequencer-coordinator](2026-07-25-toss-phase1-action-sequencer-coordinator.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 0 (post-merge reconciliation) | **Toss Phase 0 closes: post-merge doc pointers reconciled, suite green as the gate.** The 4 never-indexed ladder entries (2026-06-26 contact-mechanics, 3× 2026-07-03 motion-quality arc) get their INDEX rows backfilled (129 → 134 with this row; ordering + link integrity script-verified); `sim/JUGGLE_DEMO.md` gains a paused-offline-demo vs merged-online-ladder status banner; `plans/archived/bb-led-two-ball-juggle-demo.md` gains a merged-location note (20fcc9e, branch + worktree retired); toss plan phase table → COMPLETE. No code changes. Full suite (`pytest tests/ -q`, 2026-07-25): 3153 passed / 3 xfailed in 1325.20 s — identical counts to the post-merge baseline. First phase of the 2026-07-25 unattended workflow-orchestrated build run. | [2026-07-25-toss-phase0-postmerge-reconciliation](2026-07-25-toss-phase0-postmerge-reconciliation.md) |
| 2026-07-24 | resolved | MVP trajectory bringup — Phase 8 kickoff (branch merge + toss plan) | **BB juggle branch merged (`20fcc9e`); Phase 8 expanded into `plans/active/single-ball-toss.md`.** `demo/bb-led-two-ball-juggle` (71 commits: online tilt-ladder Rungs 0–2b, controller/demo planner, 16 test files, 10 probes, ladder logbook/plans) lands on `mvp-trajectory-bringup`; conflict surface = 2 doc tables + 1 comment + ONE semantic break (reload_gate vs the multi-ball BallManager rework, fixed in-merge; suite 3153 passed / 3 xfailed post-fix). New plan: two-tier `Toss.action` — goal = nominated catch state ⟨position, flight time⟩; 8a toss-at-position (level platform), 8b tilt-aimed displaced throw→catch; production-in-the-loop `sim/toss_gate.py`; hardware T0–T4 with T0 = bench release characterisation. Worktree + branch retired. | [2026-07-24-phase8-kickoff-bb-branch-merge.md](2026-07-24-phase8-kickoff-bb-branch-merge.md) |
| 2026-07-24 | resolved | MVP trajectory bringup — Phase 7 reload: fourth hardware session (open-loop pivot) | **Phase-7 fourth sitting — 15/19 caught; platform goes OPEN-LOOP, hand ladders learn telemetry, stale-track guards, log truth.** Fresh-bridge session, bag `09-07-53`: 24 goals = 2 instant rejects (WRONG_MODE, BB_BUSY), 2 BB refusals (PV_STALE + the SECOND yaw NOT_SETTLED in two sittings), 1 FALSE `ABORTED_PRIME_FAILED`, and 19 real throws (~15 caught by settle-current). Four-phase Opus workflow (3 investigators → adversarial verifier → implementer → auditor). Confirmed: (1) the one "platform moved at the last second" miss = Ball 30 — the corrupt track's target sweep crossed the 80 mm envelope at 78 mm ONCE, accepted at land−0.67 s, re-anchored the reach 78 mm off a centre-landing ball (all 18 other throws <2 mm late motion); (2) the "hand jump up-down then abort" = all four prime-ladder acks LIED while each frame transmitted — the ladder yanked its own live ascent four times then reported a false abort; (3) the arm-failed WARN spam = the ack epidemic at 59 % on kind-1, all attempts eventually armed, re-arms benign (absolute wall-time stroke); (4) the 4th-attempt no-throw = BB PV_STALE refusal, with ~62 mm pre-throw platform motion from the reactive path chasing the corrupt track. REFUTED: leftover prior-attempt tracks (census: every corrupt track is the current ball's own, dead ~10 s before the next throw); garbage-timed arms (all 21 arms sane, stroke at land+0.10..0.15 uniformly). The 3 "unknown" misses are inherent limits, NOT bugs: one ball 0.31 s late (hand already retracted), one 105 mm BB scatter, one unrecoverable. FIXES: config-backed open-loop reload platform (announcement pre-tilt held; reactive platform targets suppressed; hand-arm stays tracker-timed — announced landing runs early); telemetry-verified prime/retract ladders (re-dispatch ONLY on positively-stationary hand; probe-validated thresholds incl. park-band qualifiers against 5.39 rev/s top-park vel noise); ±0.75 s arrival-window arm guard + prior-id exclusion; arm re-dispatch cap 2; ONE authoritative outcome line per reload (was ZERO) + spam demotions. DEFERRED: ERR_TIMEOUT epidemic (own investigation), BB yaw/PV refusals (BB-side), late/scatter misses (ball-held-sensor era). ADDENDUM (post-session): announced-landing timing error measured across both sittings' bags — pooled n=20, bias +0.115 s ±0.017 (tracker-mediated), core σ ≈ 0.035 s, discrete ~+0.31 s outlier mode ONE per sitting (+0.314/+0.320, likely a BB release mode → BB-side review); **BB's 44 ms firmware release-timing constant STAYS untouched** (tracker-latency confound, single-ToF release-vs-flight ambiguity, nothing consumes announced time critically today) — re-tune pre-registered for the ball-held-sensor era (source-timestamped contact, ≥2 ToFs, explain the outlier mode first). | [2026-07-24-phase7-fourth-sitting-openloop-telemetry-ladders.md](2026-07-24-phase7-fourth-sitting-openloop-telemetry-ladders.md) |
| 2026-07-24 | in-progress | MVP trajectory bringup — GUI observability | **BB telemetry restored to the GUI charts — 9-axis robot_state + bus V/I on the Diagnostic wire (UDP protocol v4).** The BB Pitch/BB Hand charts and fault dots had been structurally dead since the can-bridge cutover: they read `robot_state.motor_states[7]/[8]`, but the bridge published only 7 axes (can_node's 9-axis `ALL_AXES` layout was narrowed at the handoff). Fix: bridge appends BB pitch/hand entries (pos/vel from the 100 Hz `BB_AXIS_ESTIMATES`, state/errors/Iq/temps/bus V+I from the 1 Hz BB DIAGNOSTIC stash) under all-or-nothing honest-silence gating (`heartbeat_seen` + not stale + 3 s/0.5 s freshness — dark BB ⇒ 7 axes, never phantom zeros or frozen flatlines). DIAGNOSTIC frame grew 36→40 B with `bus_current` (+`heartbeat_seen` flags bit1) — UDP PROTOCOL_VERSION 3→4, FW_VERSION 2→3, flashed 2026-07-24 — which also revives the legs'/hand's silently-dead "Bus I" chart signal. Zero GUI changes needed to revive the charts (positional indexing); one GUI fix (Opus review LOW) clears BB fault dots on the 9→7 shrink so a fault can't freeze red. Full suite 2954 pass (+1 known alloc flake, passes isolated); live wire probe: v4 decodes 0-error at 100 Hz, legs `seen=True`, dark BB `seen=False` all-zero — the exact phantom case the gate refuses. OPEN: powered-BB sitting to see the charts render live, then flip to resolved. | [2026-07-24-bb-robot-state-restoration.md](2026-07-24-bb-robot-state-restoration.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: third hardware session (catch_vel_scale sweep) | **Phase-7 third sitting — FIRST REAL CATCHES (8/10 at scale 0.8); false MISSED_INFEASIBLE verdicts, prime-retry stutter, hand-dispatch epidemic, just-in-time pre-tilt — all fixed.** Fresh-reboot session, bag `17-51-09`: 13 goals, 2 throws aborted BY BB (yaw `THROW_ABORTED_NOT_SETTLED` — never flew), 8 of the 10 real throws caught (hand holding-current +0.8 A). Anomaly 1: all 12 goals read `MISSED_INFEASIBLE_WORKSPACE` — the corrupt split-track drove 27–46 envelope WORKSPACE rejects per flight and `note_catch_feasibility` was last-writer-wins, while the plausibility gate rightly vetoed the corrupt track's below-floor CAUGHT; fix = infeasibility latches only while NO target was ever accepted (gate on `_throw_sent`, not phase — the pre-tilt accept lands during AIMING). Anomaly 2: 5/12 prime ascents stuttered — the 0.5 s retry tick re-dispatched 0.5 s into a 0.7–1.05 s ascent whose ack had failed (every stall phase-locked to the tick grid); fix = 1.2 s dispatch-anchored in-flight window shared across BOTH prime owners via new `catch/prime_dispatched` topic (ack channel demonstrably lies — frames observed transmitted after failed acks). Anomaly 3: goal-1 `ABORTED_PRIME_FAILED` — the operator's ODrive settle-window theory REFUTED by the bag (hand axis IDLE throughout); real cause = `HAND_TRAJ_CMD: ERR_TIMEOUT` bridge CAN-enqueue failures at ~40–60 % per call ALL SESSION on a fresh reboot (7/12 safe-abort retracts also failed silently); mitigation = 4-attempt dispatch ladders (prime + retract); the epidemic itself is a NEW OPEN investigation (suspect FlexCAN TX contention vs partner-gate; `[canhealth] tx_gated` disambiguates). Anomaly 4: pre-tilt arrival was scheduled exactly AT landing (one 3.9 s min-jerk crawl, tilt >1° off until ~0.3 s before contact); fix = arrival at landing −1.5 s (clamped ≥ now+1.0). Also: `catch_vel_scale` default locked to 0.8 (`JB_OP_CATCH_VEL_SCALE_DEFAULT`), GUI RELOAD button (Trigger relay in orchestrator — Foxy rosbridge has no action transport). OPEN: dispatch epidemic; BB yaw-settle aborts (+ FSM doesn't consume throw completion); tracker corruption (ball-held sensor imminent = clean verdict source); envelope centred on hold pose rejects real off-centre refinements (A07 caught 124 mm off-centre on feed-forward alone). | [2026-07-23-phase7-third-sitting-verdicts-stutter-pretilt.md](2026-07-23-phase7-third-sitting-verdicts-stutter-pretilt.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: second hardware session (re-test) | **Phase-7 re-test — catch strokes erased by a prime-vs-arm race; tracker split-track corruption fakes CAUGHT; pre-tilt + catch_vel_scale land.** The morning's z fix WORKED (platform reached+tilted on all 6 throws; every ball met the hand) but 0 real catches: strokes on attempts 2/3/5 were ERASED by the coordinator's same-tick re-prime racing the catch arm on the Platform Teensy's single packed queue (kind-3 and kind-1 handlers both clear+repack — last writer wins); 5/6 reloads spuriously reported CAUGHT off corrupt tracker tracks coasting BELOW THE FLOOR near BB while the real ball spawned as a separate untagged track (landing 29-39 mm from centre); the 80 mm envelope did its job holding the good reach. Fixes: NO smooth-move ever sent while a catch sequence is live (prime = armed edge + off-path retry timer with a 1.5 s quiet window); arm one-shot latches only on actual dispatch; CAUGHT plausibility gate (200 mm xy / 150 mm z of the catch point) + pre-existing-flight exclusion make outcomes honest; prime dispatch retries once (attempt 0 died to a transient). NEW: pre-tilt at announcement (~3.9 s early, predicted from the announced landing state, single-sourced pose math, never arms the hand) and the operator's catch_vel_scale action knob (= firmware CATCH_VEL_RATIO re-tune without reflash; clamp [0.3, 1.5], resets on disarm). OPEN: the tracker split-track corruption itself; the Teensy uptime reboot experiment (67.6 h at this bag). | [2026-07-23-phase7-retest-stroke-race-tracker-corruption.md](2026-07-23-phase7-retest-stroke-race-tracker-corruption.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: first hardware validation of the action-driven catch | **Phase-7 reload first hardware session — z frame double-add rejected every catch reach; prime raced the flight; abort retract was a silent no-op.** 7a passed; 7b was a coin-flip (hand prime vs 0.878 s flight); 7c never tilted — every `catch/dynamic_target` was gate-rejected `leg 3 out of stroke (275.2 mm)`. Root cause 1: `trajectory_node` added the 170 mm active-z to an already-STOW-relative wire (the pre-registered Phase-7 open question — the gate did its job); the wire z≈171 is now declared normative in `DynamicTargetCommand.msg`, the lift removed, and an 80 mm reach envelope enforced (`catch_reach_envelope_mm`) so the whole frame-regression class rejects loudly. Root cause 2: the prime waited for a tracked ball (7b) or the throw-accept (7c); it now fires at COMMAND time (`ACTION_PRIME_HAND` + the `catch/armed` rising edge — measured smooth-move 0.68–0.73 s (0.75 s predicted)). Root cause 3: arming results were ignored and BB's firmware countdown has NO abort opcode — the FSM now arms EVERYTHING before `bb/throw_at_target` (new PREPARING phase; prime/prepare failures abort with the throw unsent). Latent bugs fixed: the SAFE_ABORT retract targeted −0.1 rev (below the bridge's [0, 11.1] validation → silently rejected, hand stayed at top); a standing infeasibility finished the FSM mid-flight (tearing down the hand under the falling ball — it now resolves at settle, and the parked cup's two passive catches count as CAUGHT). OPEN: BB-ball landing estimates drift 435–605 mm/s toward BB (tracker issue, own session needed; the envelope bounds the chase meanwhile). | [2026-07-23-phase7-reload-first-hardware-session.md](2026-07-23-phase7-reload-first-hardware-session.md) |
| 2026-07-20 | resolved | MVP trajectory bringup — Phase 7 reload: action-driven catch + arm_catch latch (retire CATCH & SHELL) | **RELOAD becomes a self-contained action; the persistent CATCH mode is retired for a catch-armed latch (SHELL retired too; 809.08 cup-plane aim).** The operator questioned whether a bespoke CATCH control mode is needed "just to catch a ball" — it is not. CATCH did exactly one load-bearing thing: flip the `trajectory_node._on_dynamic_target` gate that lets the tracked-ball tilt reach `planner.build_catch`. So "RELOAD supersedes CATCH" = **raise that gate from the `jugglebot/reload` action via a catch-armed latch** (`trajectory/arm_catch`, mirrored on `catch/armed` which gates the hand), not from a mode the operator holds. The catch *mechanics* don't move; only the *trigger*. The action runs **within ACTIVE:TRAJECTORY** (streaming) — it can't copy LEVELLING's stream-suspend because the live catch IS a streaming trajectory — and it now **owns the platform + hand**: on throw-accept it proactively primes the hand to top (9.858 rev) + raises the latch; on CAUGHT it lowers the latch + re-centers (`go_home`, keeps the ball); on any abort it retracts the hand (≈0 rev) + re-centers. Hand-ownership split: the action owns the proactive prime + abort-retract; `catch_coordinator` keeps the reactive fire + gains but is **latch-gated** so it never arms on a stray ball. Phase-1 re-keyed the CATCH motion-transition safety logic (graceful decel-to-rest, `_catch_arrival_perf` freeze reset) to the latch edges via two shared helpers — every graceful stop seeded C2 off `_current_state()`, no discontinuity at the arm/disarm seams. **Ruled out**: the orchestrator-state form (re-introduces the mode-partition duplication for one gate); the LEVELLING stream-suspend pattern (RELOAD needs the live stream up). SHELL was a clean 1:1 GUI alias delete. The Q1 groundwork fix (`bdbd186`) aims BB at the 809.08 mm cup plane (centroid 744.3 + `HAND_CATCH_OFFSET_MM` 64.78), not the centroid — aiming low ate tilt/reach margin. Commits `bdbd186` (Q1) / `4572925` (SHELL) / `caea3ec` (latch) / `790e943` (action) / `5d5f4ae` (CATCH delete) + this docs phase. Scoped gates (`pytest tests/ros tests/motion -q`, 2026-07-21): Phase 1 1634 / Phase 2 1648 / Phase 3 1641 passed. Phase 5 hardware validation is operator-run + deferred. | [2026-07-20-reload-action-catch-latch](2026-07-20-reload-action-catch-latch.md) |
| 2026-07-18 | open | MVP trajectory bringup — wobble regression root cause | **Leg tracking lag grows MONOTONICALLY with can-bridge Teensy UPTIME — 10 ms at 2 min post-boot → ~40 ms at 1.15 h → 130–160 ms at 4.5 h → 230–250 ms at 24–30 h (saturating) — and the dominant term of the "silky vs bumpy" regression is HARDWARE/DRIVER STATE, not code (pending reboot confirmation; the one real code term, the mesh-lattice traverse shift, is ~11 %).** Four-agent sweep + telemetry mining: commit bisect found the real-but-minor mesh-lattice traverse change (~11 % of the effect per cascade sim); emit path proven bit-identical; deployment diff clean; vbus flat 45.0–45.3 V all sessions; temps anti-correlated; cadence/initial-state refuted; hold floors identical. The `uptime_ms` field cracked it: the healthy 10 ms session (bag `17-38-15`, devs 0.012–0.061 at 3.5 rev/s commands = the NOMINAL 40/0.20/0.32 cascade) ran 1.8 min after the guard-raise flash rebooted the Teensy; every session since sits on the uptime curve; Jetson up 13 days (its uptime is not the epoch). **If the pre-registered reboot experiment confirms: the 2026-07-16 "velocity-loop lag" root cause, the 1.0 rev guard raise, the chase-ceiling 2.2–2.7 rev/s, the unshaped-traverse "out of envelope", the retime-OFF decision, and the accel-FF sizing premise were all measured on a degraded plant** — the post-flash "validation" was actually the reboot fixing the lag. Isolation protocol pre-registered: Arm 0 degraded reference → Arm A `/reboot_odrives` (ODrive-side) → Arm B `ip link` bounce of the J↔T dongle (Jetson transport) → Arm C Teensy reboot — B before C because a Teensy reboot also bounces the PHY; PASS = lag ≤20 ms + unshaped traverse ~0.1–0.2 rev no-latch. Firmware audited for the accumulator (see entry Addendum). | [2026-07-18-teensy-uptime-tracking-degradation.md](2026-07-18-teensy-uptime-tracking-degradation.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — retime model first hardware A/B | **The retiming model's HONEST durations exceed the TRACKING envelope on lean traverses — lean-0.6 x-traverse deviation 0.45 → 0.73 rev (above the ~0.6 in-move ABORT line); `retime_model` shipped FALSE the same day it shipped ON.** Operator's second session (bag `19-32-03`): lean-0.0 latched the same cliff a third time (1.076, unchanged); lean-0.6 "smoother but not as smooth as yesterday". Both operator hypotheses KILLED by data: compute bottleneck NO (emit gaps ≤ 28.8 ms), ringing NO (post-arrival zero-crossings 1–2 both days, no oscillation train). Real cause: yesterday's lean-0.6 traverses were timed by the LEGACY loop whose overshoot is largest exactly on high-lean moves (peak jerk 12000/11534 of the 30000 budget ≈ 2× padding — far beyond the SPE corpus's +1.2–14.7 % tail); the model prices them honestly (27406/27276, +52–62 % peak accel, 1.60→1.42 s) and the velocity-loop tracking deficit converts the difference into deviation. Non-traverse plans near-identical; their dev deltas (+6–80 %, median ~+43 %, small-denominator noisy) are plant-side/sticky-day, not plan changes. **Key lesson: duration slop was load-bearing** — the gate's invariant is kinematic feasibility, NOT trackability, and "gate-feasible but untrackable" is the same class as the unshaped cliff. Fix: YAML `retime_model: false` + regen (legacy timing ALGORITHM restored — but per the 07-18 bisect the 1b mesh commit shifted the legacy lattice, so the post-OFF traverse plans jpk ~20928, not the validated 12000; shaped planning ~0.23 s on the batched gate, still 5× pre-SPE; re-enable = YAML flip + regen + colcon build + relaunch). Re-enable gated on accel FF or a tracking-aware duration floor; corpus gap noted (add the ±150 traverse family). | [2026-07-17-retime-model-tracking-envelope.md](2026-07-17-retime-model-tracking-envelope.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — S4 working point on hardware | **Two MAX_DEVIATION latches on the first post-SPE session were the KNOWN unshaped-traverse cliff, NOT an SPE regression** (bag `17-35-14`: both trips on the ±150 x-traverse, leg 1, dev 1.02–1.08 vs guard 1.0). Exoneration is numeric: matched moves' planned peak vel/acc/jerk **identical to the decimal** to the validated 2026-07-16 runs (diff per-move `peak_leg_*` diagnostics across sessions BEFORE reading code — the cheap high-power test); emit clean; retime model never engaged. Yesterday's own record had the same move at **0.936 rev = 94 % of guard**; today's +10–30 % at matched commands (identical peak chase vel, higher iq — first-moves-cold/stickier day) crossed the latch. **Both runs were unshaped**: `traj_ramp_battery.py --lean-gain` defaulted 0.0 = the EXPLICIT-OFF arm under the b6391c1 convention, so "defaults vs --lean-gain 0.0" compared identical arms and the shipped lean 0.6 (traverse ≤ ~0.45 rev — a LEGACY-TIMED figure; the retime model pushed it to 0.73 the same evening and shipped OFF, see the retime row above) never ran → battery default now **-1.0 = defer-to-config**. Margin lesson: **a guard margin must beat run-to-run variance, not zero** — 0.064 rev never did; unshaped ±150 traverses at jerk 30000 are OUT of envelope until accel FF lands (expected-latch framing in runbook + battery docstring). Working point stands, validated for the SHIPPED (lean 0.6) config only. | [2026-07-17-wobble-latch-unshaped-traverse.md](2026-07-17-wobble-latch-unshaped-traverse.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — shaped-planning efficiency | **Shaped `go_to_pose` planning ~1.1 s → ~91 ms (~12×) AND the binding leg-jerk constraint measured 7× more honestly — accuracy and speed were not in tension.** Three composing phases, all software-verified (hardware behaviour unchanged bit-for-bit from the gate; only the planned duration is shorter + planning faster). **1a (`4acaefe`):** numpy-vectorised twin of the shaped feasibility gate at the current n=200 mesh — pure parity refactor (sampler 7.1e-15/5.7e-14/4.5e-13; gate 1.41e-10 at equal mesh), per-pass **185.6 → 7.5 ms**. **1b (`54c1e75`):** `_SHAPED_VALIDATE_SAMPLES` **200 → 1600** (now affordable) — shaped FD-jerk under-measurement **23 % → 3 %** vs a 6400-ref; jerk-bound lateral moves plan **+4.9 % longer** = the intended fix for the silent under-stretch of the "sharp" lean; two plan hedges corrected as empirically FALSE (@6400 is a convergence reference NOT an acceptance oracle; only FD jerk is mesh-monotone, not vel/acc point-samples). **2 (`806e8fb`):** retiming-model duration search (`retime.py`; per-sample `u=1/T` polynomial, cap-free `T_ref`, monotone bisection, **ONE** verify pass through the UNCHANGED gate) behind `JB_TRAJ_RETIME_MODEL` (default ON — **flipped OFF the same evening** after the first hardware A/B: honest durations on lean traverses exceed the tracking envelope, see the retime-model row above) — shaped `build_move` **228.7 → 90.9 ms (2.52×)**, durations uniformly **+2.0 %** over optimum (legacy bisection +1.2–14.7 %), **0 verify rejections** over 54 optimality + 120 fuzz cases; every returned plan still gate-verified (proved from code). **Key insight:** vectorisation decoupled the mesh from the cost (the 1600 bump would have been ~300 ms/pass and unaffordable pre-1a); the model never bypasses the gate (it only *chooses* T). Builder deviations accepted: 1b's `_batched_at` re-anchoring of the parity tests after the clamp interaction; Phase-2 module/API naming drift (`retime.py`/`build_model`/`RetimeModel` vs the plan's `retime_model.py`). Full suite (`pytest tests/ -q`, 2026-07-17): 1a **2860/1 xfailed in 584.45 s**, 1b **2862/1 in 608.38 s**, 2 **2872/1 in 937.31 s**. Deployment: `colcon build --packages-select jugglebot` + relaunch (Python-only; YAML flag regenerated). | [2026-07-17-shaped-planning-efficiency-implemented](2026-07-17-shaped-planning-efficiency-implemented.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — S4 CLOSED (step-5 persistence) | **S4 closed: the working point (1000, 5000, 30000) + lean_gain 0.6 persisted to YAML — with the GoToPose.srv field default (-1.0) that makes the gain actually reachable.** Operator picked vel=1000 (velocity was 14–23% utilized in the A/B — costs nothing, shortens a runaway's runway). The catch: an unset srv float64 reads 0.0 = EXPLICIT lean-off, so a YAML flip alone would have been silently unreachable for default-constructed requests — the srv field now defaults −1.0 (defer-to-config); explicit 0.0 stays the A/B baseline arm; pinned by test_shipped_trajectory_defaults_are_the_s4_working_point (regex-pins the srv default too) + the defer choreography tripwire. **Consequences: a relaunch now reverts to the HOT working point (runbook escape-hatch note updated — set_limits DOWN for cautious probing); default service moves are now SHAPED (~1.2–1.5 s planning until the efficiency work lands); go_home/hold/timed/follower stay unshaped; the battery instrument's --lean-gain default 0.0 does NOT pick up the config default (framed then as correct A/B semantics — **superseded 2026-07-17 evening: battery default now −1.0 = defer-to-config**, see the wobble-latch row above and the entry Addendum).** 8 mechanism tests re-pinned to the gentle limits their geometry was designed around. Deployment: colcon build BOTH jugglebot_interfaces AND jugglebot + relaunch. | [2026-07-17-s4-closed-working-point-persisted](2026-07-17-s4-closed-working-point-persisted.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — Phase 5 timed targets: operator-facing interface | **`trajectory/timed_target` now takes a relative `lead_time_s` instead of an absolute ROS-clock `arrival_time`** (operator-requested ahead of S5). The node anchors arrival internally at handler entry (`perf_counter + lead`, same clock domain as the emitter — no clock crossing left on the service path); non-positive leads flow through the single feasibility gate (loud `TOO_FAST` with the *achievable* `min_duration_s` + target_feedback), only non-finite values are handler-guarded (`UNREACHABLE`). The `/catch/dynamic_target` TOPIC and `TargetFeedback.arrival_time` (perf-domain, machine-to-machine) are deliberately unchanged; `_ros_time_to_perf` + clock-offset plumbing retained but consumer-less on the service path (docstrings say so). S5 session doc + runbook examples now typeable (`lead_time_s: 3.0`). **Deployment: `colcon build --packages-select jugglebot_interfaces jugglebot` + full relaunch (interface package changed).** `pytest tests/ros/ -q` (2026-07-16): **905 passed** incl. 2 new reject-path tests. | [2026-07-16-timed-target-lead-time](2026-07-16-timed-target-lead-time.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — S4 lean A/B: planning latency + boundary vel_ff step | **The lean-0.3 A/B pauses were planning COMPUTE (not motion) and the "sharp tilt" was a boundary vel_ff STEP — both operator hypotheses confirmed, both fixed.** Three-agent investigation (code map + bag forensics on the 929 s `2026-07-16_18-45-29` + offline profiling) on why `--lean-gain 0.3` felt worse than `0.0`. **(1) PAUSES = PLANNING.** Matched A/B at `(1500,5000,20000)` seq111-121 vs 122-132: between-move pause **1.70 → 4.33 s (+2.64 s), ~94 % planning** (settle grew only +0.13 s); offline `build_move` 0.73 → 2.6 s/move. Root chain: a shaped `_ShapedPlan` is barred from the fast vectorized gate (`feasibility.py:403-407` TypeError) → falls to the dense analytic gate, runs 6 `validate()` passes vs 2 (`_SHAPED_REFINE_ITERS=4` bisections), and `compute_jacobian` was **68 % of validate wall time** via `np.cross` numpy generic dispatch (3 Jacobians/sample). **(2) SHARP TILT = boundary vel_ff STEP.** Lean tilt ∝ base accel ⇒ tilt_rate ∝ base jerk, and a rest-to-rest quintic has `j(0)=j(T)=60d/T³ ≠ 0` ⇒ a **~70–182 mm/s (1.0–2.6 rev/s)** leg vel_ff step at every shaped-move boundary (**0.00** unshaped); bag-corroborated (+43 % peak iq 5.93→8.48 A at matched limits, 1.52× first-60 ms vel). `validate()` is structurally blind (never differences across the hold→move seam) — the trajectory-layer analogue of the K5 twist-discontinuity class; the "disconnected" feel is structural (lean is a self-cancelling pulse peaking at ~0.211 T, upright at every stop). **(3) SAFETY** at `(1500,5000,40000)`: peak dev 0.359 rev = 36 % of the 1.0 rev guard, peak leg vel 285 mm/s = 54 % of clamp cap, 0 faults / 143 moves; jerk is the binding constraint. **FIXES (this commit):** `ik_solver.compute_jacobian` vectorized to a component-form cross (numerically identical — bit-equal to np.cross on the same operands, ≤2.84e-14 ≈1 ulp vs the old per-leg loop; unshaped build_move 736→234 ms, shaped 2680→1183 ms); `feasibility.py` default sampling 200→**80** for unshaped with `_VALIDATE_JERK_MARGIN=1.05` covering the ≤3.7 % FD under-measurement on the BINDING jerk constraint (adopting `validate_follow`'s pre-existing idiom), shaped floored at `_SHAPED_VALIDATE_SAMPLES=200` (its FD jerk under-converges — window-edge curvature post-window); `shaping.py` multiplies the ENTIRE lean contribution (tilt + xy shift + all derivatives) by a **C2 plateau window** `w(s)=S(s/EDGE)·S((1−s)/EDGE)`, EDGE=0.15, applied by full product rule in `state_at` (window-then-cap) — self-consistency preserved. **Net offline (x+150): unshaped ~0.73→~0.10 s (~7×), shaped ~2.6→~1.2 s (~2.3×); boundary vel_ff step 0.00 mm/s at both ends for all tiers.** Low-pass FF REJECTED (breaks pose/twist/accel self-consistency, the 2026-05-08 class); septic base REJECTED (blast radius); fewer refine iters REJECTED (`_stretch_factor` assumes 1/Tⁿ vs lean's 1/T³ → durations balloon 1.04→6.9 s). **Status in-progress — operator must re-run the lean A/B (colcon build + relaunch):** expect pauses ~4.3 → ~2 s (settle-dominated), onset iq spikes gone; the open A/B question is whether lean *helps* realized smoothness at matched limits (pre-fix it WORSENED peak iq). Remaining for later: vectorized shaped gate (~100× endgame), analytic shaped stretch seed, carry-tilt-across-moves (the true fluidity fix). Scoped `pytest tests/motion/test_trajectory_feasibility.py tests/motion/test_trajectory_shaping.py tests/motion/test_kinematics.py -q` (2026-07-16) **60 passed**; `pytest tests/motion/ -q` (2026-07-16) **675 passed in 147.09 s**; full suite (`pytest tests/ -q`, 2026-07-16) **2826 passed / 1 xfailed in 620.43 s**. **RE-RUN CONFIRMED same night (bags 21-58-59 + 22-06-30): pauses 1.7 s (planning-dominated ~1.47 s — endgame items still worthwhile); onset spikes GONE (leaned onset 4.1-4.3 A now BELOW gain-0's 6.9 A); lean helps DECISIVELY at matched limits (gain 0 hit 0.94 rev = 94% of guard + 7.94 A; gains 0.6/1.0 → 0.25 rev / 4.4 A (corrected 2026-07-17: not like-for-like — the same x-traverse re-extracts to 0.45/0.46 rev at 0.6/1.0, see the wobble-latch entry); pre-registered realized-jerk criterion MET at 0.3/0.6: 19.3k→13.5k/14.9k mm/s³, ~tied at 1.0; mechanism: the shaped gate stretches leaned moves ~1.5× — slower+tilted reads calm); 5° cap never bound even at gain 1.0 (1.85° peak; binds above ~856/gain mm/s² lateral accel); recommended default lean_gain 0.6 (not yet persisted — YAML still ships 0.0).** | [2026-07-16-lean-planning-latency-and-boundary-step](2026-07-16-lean-planning-latency-and-boundary-step.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — S4 limit ramp: MAX_DEVIATION guard + vel_limit headroom | **The MAX_DEVIATION guard latched on legitimate velocity-loop lag, not runaway — guard raised 0.5→1.0 rev, leg vel_limit 4.0→6.0 rev/s, per-leg latch attribution wired.** The S4 limit ramp PASSed at 100/660/10500 then latched the Teensy guard three times at vel=200 mm/s (bags `2026-07-16_13-17-58` @528.371 s + @621.567 s; `2026-07-16_13-34-58` @72.502 s), always leg 1 (0-indexed) first, frozen trips −0.5196/−0.5603/−0.5515 rev, always at move onset of the same asymmetric sweep; jerk 8000→10500 made no difference. Root cause (six-agent parallel investigation + adversarial cross-check): **legitimate velocity-loop lag under coordinated-move load, NOT runaway.** The guard watches the RAW streamed 40 Hz knot u0 vs encoder PRE lead-clamp (`fault_machine.cpp:367-371`), so it counts exactly the command-space lag the lead clamp (`enc ± MAX_LEAD 0.10`) deliberately tolerates. Peak \|dev\| is superlinear in realized leg velocity (v^1.19 all-legs / v^1.39 leg 1: 0.079 rev @35-50 mm/s → 0.544 @170-200, where all 6 move records belong to the 3 latch events), implied lag τ grows 112→208 ms (NOT a fixed 6-15 ms transport delay); deficit-integral closes it (−0.594 rev computed vs −0.516 accumulated at the last 10 Hz pre-trip sample; frozen snapshot −0.5196). Drives never railed: peak iq_setpoint 8.73 A of 10 A, peak encoder vel 2.23 of 4.0 rev/s, active_errors 0 throughout. H1 transport-delay / H2 vel_limit-saturation / H4 lead-clamp-limit-cycle / H5 current-rail / H6 stroke-clamp / H7 command-discontinuity all REFUTED; H3 (accel-phase velocity-loop lag) is root. Withdrawn mid-investigation: the vel_limit-saturation framing AND the 'encoder capped at vel_limit under clamp' corollary (setpoint saturates, encoder does not). Operator-approved response (all four): **(1)** `MAX_DEVIATION_REV` 0.5→1.0 rev (firmware, `canbridge_config.h`; FW_VERSION 1→2 human-facing only — sole consumer is the boot Serial banner, nothing runtime); **(2)** ODrive leg `vel_limit` 4.0→6.0 rev/s (config→codegen→runtime CAN push at `teensy_bridge_node._run_configure`:3035, fires after every /home /configure /activate) — un-saturates Kp·MAX_LEAD=40×0.10=4.0 ≤ 6.0 so vel_ff regains catch-up authority (was 4.0=vel_limit exactly); bench envelope ~48 rev/s, overspeed guard 16.5 rev/s unchanged; **(3)** per-leg latch attribution from the firmware's frozen `max_dev_leg` snapshot → bridge log + `/link_status guard_fault_leg` + GUI minimap tooltip `(MAX_DEVIATION, leg N)` (zero Python re-thresholding — auto-tracks the firmware threshold); **(4)** runbook ABORT rules recalibrated (0.1 rev at HOLDS only; moves may run to ~0.6 rev but must collapse at arrival). Raising the guard adds ZERO physical excursion for command-side faults (lead + stroke clamp + ODrive clip bound the executed command); accepted cost = encoder-side-runaway detect distance 35→70 mm. `MAX_LEAD` stays 0.10. **Deployment is two independent halves, neither on hardware yet:** (A) guard needs a Teensy REFLASH (`pio run -e teensy41 -t upload`; built clean, Flash 3.2%/RAM 27.2%); (B) vel_limit needs `colcon build --packages-select jugglebot` THEN relaunch (a stale install keeps pushing 4.0). Next chapter = accel FF, sized on a MEASURED coordinated-move inertia (1-3e-3+ kg·m²), NOT the bench single-leg J_eff≈3e-4. Scoped `python -m pytest tests/motion/ tests/teensy_link/ tests/firmware/ -q` (2026-07-16): **1018 passed in 110.58 s**; firmware `pio run -e teensy41` SUCCESS; full suite (`pytest tests/ -q`, 2026-07-16): **2811 passed / 1 xfailed in 772.17 s**. **EVENING ADDENDUM: both halves VALIDATED on hardware — bag 17-38-15 ramped vel 100→200→280 mm/s, 22 moves, ZERO latches (the battery that latched 3× at vel=200 that morning). Envelope then opened further: vel_limit 6→12 rev/s, set_limits ceilings 280/4000→5000/5000 (jerk 200000 unchanged; ceilings now administrative). Clamp-engaged catch-up is VELFF-cap-bound at 7.5 rev/s ≈ 529 mm/s hard cap (ramp guidance ~450–500 mm/s) until the VELFF flash item + accel FF land. Accel-FF chapter opened as plans/parked/accel-ff-inertia.md: the FF is already fully wired behind torque_ff_platform_inertia:false; hard prereq = firmware stale-hold torque decay; measured coordinated inertia median 7.7e-4 = 2.8× rotor, lateral/z 1.68× (operator hypothesis confirmed; the '5–20×' here was a lag figure — clean inertia is 2–5×).** | [2026-07-16-max-deviation-guard-tracking-lag](2026-07-16-max-deviation-guard-tracking-lag.md) |
| 2026-07-16 | resolved | Leg feedforward — ARC COMPLETE: gravity FF armed + shipped ON | **The gravity feedforward is armed, sign-verified on hardware, and ships enabled.** A/B sessions (FF off `2026-07-16_10-12-33` / on `2026-07-16_10-17-45`): banner wire scale 0.967251; ramp 0→1 in exactly 2.00 s from the arm instant; zero motion at the arm edge (0.0014 rev max); **S3 sign check NO INVERSION** — per-leg hold-iq delta leg-mean −0.115 A (the ideal falls-or-flat signature; abort signature absent); setpoints_without_ff=0 (all 5058), firmware clamp never bound, zero faults. Dynamic A/B statistically identical over 11 matched moves — exactly the pre-registered expectation (FF < friction floor on the unloaded platform; its value is the integrator no longer carrying gravity, for loaded/dynamic work). torque_ff_enabled: true now SHIPS; tripwire test inverted (test_shipped_config_has_the_feature_on). Bonus: first hardware validation of the arming contract (BOOT pre-flight cleared last night's stale latch; auto-arm 230 ms post-activate; clean armed stows). Follow-ups: /leg_torques_diagnostic gap CLOSED same day (trajectory_node 5 Hz republish of the emitter torques, gated on streaming); B's first move drew 7.35 A + all-legs lead clamp (isolated — watch); hold-current hysteresis ±1.5 A dwarfs FF signatures (compare identical-approach-history holds only). | [2026-07-16-gravity-ff-armed.md](2026-07-16-gravity-ff-armed.md) |
| 2026-07-15 | resolved | MVP trajectory bringup — arming lifecycle contract (A1–A5) | **One evening, three arming failures, one root cause — the ARMING CONTRACT.** First S1 attempt hit: (1) a stale prior-session guard latch (Jetson-5V Teensy survives relaunches) wedging HOMING at the guard-gated HOME verb — twice; (2) a fully-'accepted' trajectory battery with ZERO motion and zero warnings (manual arm step skipped; a disarmed bridge holds no :5557 subscription — setpoints_sent=0, setpoints_rejected=0, /link_status OK); (3) enable_setpoint_output:=true self-E-STOPing at boot (zero-precondition arm before anything can stream) then dead-ending in 'reseed refused'. Root cause: 'armed' was a cross-process invariant with no owner. Fix: ARMING_CONTRACT.md (A1–A5) — orchestrator auto-arms on ACTIVE entry (bridge's 5-precondition stream-then-arm stays the gate) and disarms on real faults; disarm-before-stow enforced in-process with T2J-bit3 wire confirmation; mode outlives the armed window (A4); boot-arm removed; BOOT stale-latch pre-flight; loud disarmed-wire accepts; guard-only classification requires ACTIVE origin AND wire_armed. 27-agent adversarial review (21 confirmed findings incl. a BLOCKING staged-disarm-vs-RPC race) + /audit round (arm-lock reentrancy, TOCTOU recheck) all fixed pre-commit. The operator's proposed fix (stream only in ACTIVE+TRAJECTORY) analysed and rejected — the armed STANDBY hold is load-bearing (250 ms staleness watchdog). | [2026-07-15-arming-contract.md](2026-07-15-arming-contract.md) |
| 2026-07-15 | resolved | Leg feedforward — Kt CONFIRMED + SHIPPED (0.0570), tff channel verified | **First Kt measurement: 0.0577 ± 0.0012 Nm/A — NEITHER candidate** (ODrive's 0.0551 excluded +2.1σ low, the historical 0.0624 excluded −3.8σ high — the stiction-bias prediction confirmed). Recovered from the operator's four-mass friction-cancelling traverse session (0.5/1.0/1.5/2.25 kg) against ACTUAL masses — the harness had mislabelled every run "1.00 kg" (`--hold-mass` is a Mode-2 flag, silently ignored by Mode 1); fit R²=0.99909, intercept ≈0. PROVISIONAL: masses operator-recalled, and stiction-confounded hold-currents pull mildly toward ~0.062 — the weighed-mass confirmation run settles it; YAML keeps 0.0624 until then (updating it auto-corrects the FF wire scale — the double-correction-proof design). **Bonus: friction GROWS with load** (half-diff 1.16→1.51 A over 0.5→2.25 kg; cancels in the fit; unmodelled in Stribeck). **The torque_ff channel is SETTLED**: positive wire tff = extension torque, production chain correct as built, **NO negation** (3 inverted-rig runs, ≥10σ); decay-corrected gain ≈18.2 A/Nm vs 18.14 expected (±15–20%). The 20:55 "MUST NEGATE / −21 A/Nm" is dead: **drift-aliasing** — a monotonic rung ladder at fixed 3.2 s cadence makes time and torque perfectly collinear (slope_alias = 160×drift; that run's hand-contamination drift was 2× what's needed), injection path byte-identical across harness versions. **The static-hold Mode 2's steady-state signal is EXACTLY ZERO by construction** (the integrator re-pins total iq at the load value) — what the runs measured was the transient at ~1.3 s of decay (τ_locked ≈ 8–10 s); the orchestrator's traverse-A/B replacement was ALSO refuted in review (during-motion τ = vel_gain/vel_int = 0.625 s absorbs ~83%) → **Mode 2 rebuilt as EDGE-CAPTURE** (settle gate → 15 s pre-soak → ±X square wave inside the friction band → matched-filter edges → decay-corrected jump/(2X); drift tested BEFORE the null test; tri-state channel_live). **The friction-band load-transfer transient** poisons the first ~7 s of any static hold: amplitude ≈ τ_c (1.2–1.4 A, LOAD-INDEPENDENT), τ ≈ 3.2 s — 15 s pre-soak kills it; no rung re-ordering does (0/5040 permutations). **Mode-1 failure chain, one root cause**: starved steady gate (accel sub-gate rejected ~95% of steady samples) → mass "FAILED" → park ran a bring-up helper ARMED AND SILENT (hard 0.3 s sleep) → guaranteed MPC_STALE latch → re-arm tripped → "CANNOT PARK". All fixed + 135 kt_lib tests: parse-time mode/flag refusals, reworked steady selector (n 70–168 on the real CSVs with the landed gate; the recovery-analysis gate kept 47–159), keep-alive flat-knot pump across armed gaps, full stream-then-arm park re-arm (+ armed fast path that never drops the load), abort reasons survive NaN snapshots, **manifests persist per-traverse immediately** (an abort can no longer cost the data), REQUIRED `--rig-orientation` (the inverted rig had silently wrong-way sign bookkeeping), conditioning floor downgraded to a warning (the 0.5 kg point empirically refuted it). **Firmware ingest clamp BUILT (review: SHIP, zero findings)**: |torque_ff| ≤ 0.25 wire-Nm at UDP ingest (clamp-don't-reject), per-leg torque_clamp_mask on heartbeat flags bits 8–13 (free bits — no size change, no version bump), mirrored on /link_status; three-layer chain pump 0.1325 → firmware 0.25 → ODrive 10 A; both pio envs build, codegen idempotent, 210 firmware/parser/bridge tests — **DORMANT UNTIL FLASHED**. SAME-DAY CONFIRMATION (weighed masses ±10 g, 2 reps, flashed): harness's own fit **Kt = 0.0564 ± 0.0011** (R²=0.99999) → **pooled 0.0570 ± 0.0008 SHIPPED to YAML** (0.0624 excluded 6.6σ; ODrive's 0.0551 at +2.2σ — the nameplate was nearly right); wire scale auto-corrected **0.8835 → 0.96725** with pump/tests/docs rippled in lockstep. Edge-capture's first hardware contact found one more design bug — the amplitude bound checked the LEVEL but a square wave toggles a SWING of 2X (±0.035 swing = 0.070 ≈ 1.5× the band → chaos; ±0.020 swing = 0.040 → 12% low) AND the pool gated globally, refusing a t=147 measurement over one bad amplitude → **fixed** (swing-safe defaults ±0.005/0.010/0.015; per-amplitude data-quality exclusion, t-stat stays pooled so a NULL still reads DEAD). Replayed verdict: **trustworthy, +18.351 ± 0.224 A/Nm (t=82) vs 18.14 expected — channel verified end-to-end**, and the bench drive's flashed torque_constant verified live (implied 0.0545 ± 0.0007, 1σ from 0.055133). **Gravity-FF arming session is next with nothing in front of it.** | [2026-07-15-kt-first-measurement-and-tff-channel](2026-07-15-kt-first-measurement-and-tff-channel.md) |
| 2026-07-14 | in-progress | Leg feedforward — Kt + gravity FF | **The Kt mismatch is a STEALTH GAIN CHANGE, not a stale constant — reconciled on the wire; gravity feedforward re-applied (DEFAULT OFF).** Follow-on to the 2026-07-13 gain-hunt closure. **(1)** On ODrive 0.6.x the velocity controller outputs TORQUE and the drive computes `Iq = torque/torque_constant`, so the loop's authority is `vel_gain/torque_constant` — meaning **"just set torque_constant to the measured Kt" would silently DETUNE the shipping, bench-validated velocity loop by −11.7 %** with no error and no telemetry. That is the one action that must never happen. Confirmed three ways: operator + the version-pinned 0.6.11 API ref (`vel_gain : [Nm/(rev/s)]`) + **the robot's own stability** (amps-semantics would need 30.5 rev/s of velocity error to explain an observed 6.09 A peak against a 4.0 rev/s vel_limit, and would put ω_v at 1.26 Hz — BELOW the 6.37 Hz position loop, an inverted cascade that rings unconditionally). **Resolution: do NOT touch the drives.** The YAML now carries BOTH constants (`motor_kt_nm_per_a` = physical truth, `motor_kt_odrive_config_nm_per_a` = what is flashed) and codegen derives `ODRIVE_LEG_TORQUE_WIRE_SCALE = 0.8835`; pre-scaling torque_ff on the wire makes the DELIVERED CURRENT correct at zero risk to the loop, and is **double-correction-proof** (flash the true Kt later, update the mirror, the scale auto-becomes 1.0). If it IS ever changed it must land ATOMICALLY on three surfaces (YAML gains, FLASHED gains, FLASHED torque_constant) — homing/activate run before configure and use the flashed values, so two-of-three is a silent ±13 % loop change. **(2)** The same division **falsified yesterday's cascade table**: ω_v = vel_gain/(2πJ) omitted it. Corrected **ω_v 22.8 Hz / ratio 3.58** (was 20.2/3.17) — healthier, not worse — and **ω_v is INVARIANT to the true Kt** (J_eff scales with Kt, the measured slope as 1/Kt), so it survives however Kt lands. **(3) Kt itself is NOT settled**: 0.055133 is ODrive's uncalibrated nameplate `8.27/Kv` to 15 s.f. with zero empirical backing (and the datasheet CANNOT adjudicate — 3.86 Nm/70 A = 0.05514 = the formula, circularly), but 0.0624 has **two** problems: a **lever-arm BUG fixed this session** (`single_leg_test.py:841` used the STANDARD leg's 11.221 mm spool radius on the BENCH leg's 11.391 mm ⇒ Kt came out 1.51 % LOW ⇒ corrected **0.0633**, i.e. FURTHER from ODrive's value) and an **unquantified stiction bias** (a position-hold fit; τ_c ≈ 1.1 A supports part of a static load ⇒ Kt biased HIGH). **Both 2026-07-13 plant-ID conclusions survive at either Kt** — τ_c is Kt-INVARIANT (raw regression coefficient in amps; the 2026-04-27 corroboration is amps-vs-amps), and the J_eff ≈ 1× J_rotor refutation of "~10× rotor" holds by an order of magnitude either way. **(4) Gravity FF re-applied, DEFAULT OFF** (`motion/torque_ff.py`): SetpointPump is the single wire enforcement point — **clamp → Kt scale → ramp**. The clamp (0.15 Nm) is load-bearing because **there is NO other clamp at any layer** (firmware saturates only at int16 ≈ 59 A of demand) and the ODrive ADDS input_torque BEFORE the torque limit, so a large FF saturates the current clamp, **the position loop loses ALL authority**, and the leg runs open-loop until MAX_DEVIATION E-STOPs it mid-motion. The **ramp-in** exists because `vel_integrator_limit` is genuinely **inf** and the integrator has already wound up to carry gravity — a step FF would command ~2× gravity until it unwinds. Platform-inertia term gated OFF: the firmware HOLDS torque_ff **undecayed** through a stale-link window (`leg_interp.cpp:366` sits after the velocity-decay block), so an accel-proportional FF would keep pushing at full magnitude while commanded velocity decays — gravity, being static, is correct to hold. **SIGN VERIFIED BY INDEPENDENT ENERGY BALANCE** (not by reading the code): FF virtual work per 1 mm platform lift = **mgh exactly, ratio 1.00000 at four poses** incl. tilted + extreme; all six torques positive; magnitudes land in the historic 0.013–0.041 Nm/leg band; **mutation-testing the sign fails 11 tests**. Flag-OFF proven inert (even a NaN torque_Nm neither leaks nor rejects the frame). **The iq SIGN-FRAME TRAP**: `can_buses.cpp:92` stores iq RAW while pos/vel go through `leg_sign` — a code-read says extending torque should read NEGATIVE iq, which would make the plant-ID sign-broken. **The data refutes it** (corr(iq,accel)=+0.39, corr(iq,sign v)=+0.81; +36 rev/s² draws +0.89 A) — the drive's own direction calibration inverts it back. Per-axis calibration is a property of each DRIVE, not the firmware — re-verify before porting to the robot's legs. **Kt experiment ready** (`kt_bench_test.py`): constant-velocity up/down traverse under known masses, where `(iq_up+iq_down)/2 = τ_g/Kt` **cancels friction EXACTLY** and the half-difference yields a free independent friction measurement; 1.0–3.0 kg at 0.6 rev/s; resolves 0.0551-vs-0.0624 at ~7.5σ (cannot resolve 0.0624-vs-0.0637, and says so); **requires BENCH_SYSID_BUILD** (DIAGNOSTIC gates on iq_setpoint not iq_measured, so stock v3 gives 2–3 samples per traverse) and hard-refuses below 150 Hz telemetry. Run `--mode torque_ff_check` FIRST — it settles the torque_ff sign through `leg_sign` empirically. Suite `pytest tests/ -q` (2026-07-14): **2701 passed, 1 xfailed** (+ the known load-flaky alloc test, which passes isolated). No firmware change, no gain change, no ODrive re-flash. **ADVERSARIAL REVIEW COMPLETED same day (after the spend limit lifted): 4/4 lenses FIX_FIRST, 15 findings (6 HIGH), all 14 software fixes landed same-session** — the recovery-step hole (the FF ramp missed the guard-latch → /recover path, the most common integrator-rewind event → `SetpointPump.restart_torque_ramp()` wired into both /recover clear paths), the ungated THIRD producer (HardwarePlant read NO config flags; enabling the master switch would have armed the MPC path's accel-proportional inertia term → per-term skip flags threaded through the whole chain + pinned), the **missing Newton–Euler transport moment** in `compute_inertia_wrench` (`r_com × m·a_com` — a 4 m/s² vertical throw demanded ~0.33 N·m of moment and got ZERO; pre-existing since March, fixed + pinned by independent rotational virtual work), a 2π-mutation test gap closed, a clamp ceiling added (0.30 Nm — the ODrive adds input_torque BEFORE its torque limit), the TRAJECTORY producer's singular/ill-conditioned-Jacobian FF now holds-then-decays instead of stepping to zero (the doubly-gated-off MPC producer still zero-steps — known-open) (with a 0.5 Nm physical sanity bound, since solve() returns huge FINITE forces on ill-conditioned J), and the Kt harness rebuilt (see below). One LOW deferred to the firmware sitting (stroke-clamp FF pulses). **FIRST HARDWARE CONTACT validated the review, not the feature**: the operator's messy torque_ff_check run (0.8 kg, odd angle, hand support) produced R²=−0.099 yet the harness confidently printed 'MUST NEGATE / MISMATCH −63%' — review finding #1 demonstrated live. ~~De-noised reading~~ **⚠️ SUPERSEDED 2026-07-15**: the "in-band |slope| 20–22.5 A/Nm, ~17% high" and the "negative bench sign" were BOTH the same **drift-alias + hand-contamination** artifact — three clean 2026-07-15 runs settle the channel at **positive wire tff = extension, gain ≈18.2 vs 18.14 expected, ≥10σ, matching the production chain (NO negation)**; the 1/21.3=0.047 torque_constant speculation is resolved (drive consistent with 0.055133) — see the 2026-07-15 row. Still true from this row: the ±0.10 rungs saturated/re-absorbed exactly as the review predicted — per-drive ODrive direction calibration differs bench-vs-platform, so **a bench sign result must NEVER be copied to the platform; the platform-validated production sign stands**. Harness now refuses verdicts below R²≥0.9 / t≥3, defaults its ladder inside the friction band (±0.035 Nm max), actually parks on abort paths, shapes the traverse start, and frames bench-sign results with the per-drive context. | [2026-07-14-kt-reconciliation-and-gravity-ff](2026-07-14-kt-reconciliation-and-gravity-ff.md) |
| 2026-07-13 | resolved | S4 gain-retune — CLOSED | **The leg gain hunt ends — a rev/mm units bug manufactured the "accuracy knee", and the plant-ID says feedforward, not feedback, is the lever.** The operator ran the missing sustained S4 datapoint, said he was **unconvinced the gain search had found the right answer**, and asked to question the methodology instead of running more points. He was right. Three findings, all from data **already on disk** (no new hardware): **(1)** `bench_leg_sysid.py:2548` printed `err_rms * 1e3` under an `mm` label, but `err_rms` is in **REVOLUTIONS** — that is milli-revs, **~14× too large** (the bench leg is 71.5708 mm/rev). The "10.3 → 6.9 mm accuracy knee at pos 70" is really **0.74 → 0.49 mm**; and against the operator's spec — **±1 mm judged AT THE CATCH/THROW INSTANT** — production `40/0.20/0.32` measures **0.054 mm median / 0.192 mm worst (0/21 strokes over 1 mm), ~5× inside**, so the entire accuracy case for raising pos_gain evaporates. Four rounds of adversarial verification missed it because every check was *internal to the number* — nobody asked whether **10 mm RMS was plausible on a 280 mm-stroke leg**. **(2)** Plant-ID by torque-balance regression (`2πJ·a = Kt·iq − τ_c·sgn(v) − b·v`) on the same CSVs: **J_eff = 0.7–1.2 × J_rotor** (5 fits; two highest-R² both 0.97×) — **refuting the unsourced "J_eff ~10× rotor"** guess carried since 2026-04-27 by an order of magnitude; τ_c = 0.88–1.22 A brackets the 2026-04-27 bench's 1.094 A. Implied production cascade **ω_v 22.8 Hz / ω_p 6.4 Hz, ratio 3.58 — already healthy** (scatter across the 5 fits: ω_v 17.7–28.8 Hz, ratio 2.78–4.53). NOTE the cascade must include the ODrive's `torque_constant` division (vel_gain is Nm/(rev/s), and Iq = torque/torque_constant using the drive's CONFIGURED 0.0551, not the true Kt) — so the shipping velocity loop runs ~13% stiffer than its nominal vel_gain implies. ω_v is INVARIANT to the true Kt, so this survives the open Kt question; the ladder's `vel_gain ∝ √pos_gain` rule holds that ratio *constant*, so climbing it never improved damping, it only slid both loops toward the resonance (which is what the operator's ear heard at pos 110). **(3)** Error budget (contributions of correlated regressors — they do NOT sum): against 0.83 mm pooled RMS, **the delay-shaped term alone contributes 0.57 mm and is gain-independent** (largely the metric's own sample-and-hold — `cmd_u0` is the knot at the START of the segment the firmware interpolates THROUGH), 0.17 mm inertial + 0.15 mm friction (the two switched-off FF terms), 0.44 mm noise floor. **Reframe:** Jugglebot's reference is known in advance with exact derivatives (`plan.state_at` → pose/twist/accel; exact velocity + acceleration IK already computed), so **feedforward follows the trajectory and feedback only cleans up model error** — the ladder used the stability-limited lever to chase an accuracy target the accuracy-limited lever had already met. `vel_ff` (a feedforward) was the single biggest historical win (lag 31→6 ms) and is *why* the error is already sub-mm. **SpaceMouse** is architecturally identical to a ball-tracked catch (same 40 Hz quintic replan path) so it is not off-path — but it is a **worst-case soak** (chase clamp rides 85 % of limits indefinitely) and its harshness has a **non-gain cause**: a fresh quintic every 25 ms restarts *jerk* at 40 Hz, and the firmware's *cubic* Hermite makes executed accel step at every knot ⇒ pass/fail robustness screen, **never a gain selector**. **Outcome: production `40/0.20/0.32` STANDS**; Stage-2 candidate `70/0.35/0.56` + `MAX_LEAD 0.057` **RETRACTED** (MAX_LEAD stays 0.10, already shipped — no firmware change); the gain ladder is **demoted to a stability-envelope probe**; the ±1 mm spec becomes the **stopping rule the methodology never had**; the loaded S4 replay is **reframed as a v3-firmware regression test at production gains**. Effort moves to **feedforward** — `torque_ff` is plumbed end-to-end to the ODrive and carrying zeros (one line: `setpoint_pump.py:263`), leg accel is already on the wire as `acc_mm_s2`, and J_eff is now measured. **Open/blocking:** the **gravity+inertia FF appears to have been dropped by ACCIDENT** (the cutover claimed `:5557` carries `torque_Nm = zeros`; `hardware_plant.py:488` publishes it live — though note MPC is dormant on this branch, and the magnitude 0.013–0.041 Nm/leg is cited from a 2026-05-08 archived plan, not re-measured) and a **13 % Kt mismatch** (ODrive 0.0551 vs measured 0.0624) is latent under any torque FF. New probe `tools/probes/bench_leg_plant_id.py`; enforcement point `sysid_lib.format_stroke_error` + 3 regression tests. Full suite `pytest tests/ -q` (2026-07-13): **2578 passed, 1 xfailed in 601.77 s**. No firmware change, no production gain change. | [2026-07-13-leg-plant-id-and-the-units-bug](2026-07-13-leg-plant-id-and-the-units-bug.md) |
| 2026-07-12 | superseded | S4 gain-retune (Stage 1) | **⚠️ SUPERSEDED 2026-07-13 — the "accuracy knee" and the Stage-2 recommendation are RETRACTED (rev/mm units bug, ~14×; see the 2026-07-13 entry). Still valid: the stability envelope, the servo-vs-structure discriminant, the friction describing-function, the measurement-ceiling lessons, the instrument-gap lesson, and every harness/firmware fix.** **Bench-leg gain-tuning Stage 1 — bench system-ID on the can-bridge path, an escalate-until-unstable ladder, and the motion-excited vibration the operator's ears caught that the instruments missed.** Four adversarially-verified analysis rounds on the acceptable-loss 7th bench leg (Path BRIDGE, CAN3). The unloaded leg **never reproduced the S4 ~6 Hz limit cycle** in any regime (ladder, sustained tracking, or deliberate clamp-riding at 46 %) — arrive-and-settle stimuli are the wrong excitation class; given the v2→v3 clamp change (vel_ff kept, MAX_LEAD 0.10) the signature may already be fixed on the robot, so **the loaded robot replay decides**. The first ladder "winner" **110/0.50/0.72 was OVERTURNED** — the operator heard/saw motion-excited HF vibration the quiescent + settle instruments structurally miss; the chirp peak/DC then measured effective **ζ≈0.49–0.52 at the 17.5 Hz crossover** (step-fits overstate damping — ramp-limited steps never excite the resonance), 3.7× baseline 20–45 Hz iq energy, ζ non-reproducible (0.78→0.66–0.68), iq railing 1.26 % at 4.4 rev/s. Withdrawn mid-arc by the verify stage: the "18:03 mode-all ran at winner gains" misread (code-confirmed BASELINE at bench_leg_sysid.py:1800) and the chirp "~15 Hz corner" (friction describing-function, not a loop corner; chirp gain ~0.5 at 0.02 rev = real Coulomb friction, 2026-04-27 tau_c=1.094 A corroborated). Round-4 strokes battery + vel_int de-confound: the sustained-stroke triple `MAX_DEVIATION` latch was a **harness sizing bug** (2A swings timed for A → 7.6 rev/s, infeasible at any gains; fixed db05670 + a series-level velocity gate closing the class); baseline HF onsets on 99 ms strokes are commanded-content **false positives** (velHF FALLS with gain), real vibration GROWS with gain on long strokes (iqHF to 5.6× at pos 90). Bench envelope: **pos 40 clean / 70 accuracy knee (mean errRMS 10.3→6.9 mm, −33 %) / 90 aggressive edge / 110+ out** [**2026-07-13: those errRMS values are milli-REVS, not mm — really 0.74→0.49 mm; the "accuracy knee" is retracted, the stability envelope stands**]; pos 130 dead at all vel_int (the boundary itself). Stage-2 robot recommendation ~~**70 / 0.35 / 0.56 + MAX_LEAD re-pin 4.0/pos_gain=0.057**~~ **[RETRACTED 2026-07-13 — no gain change warranted; MAX_LEAD stays 0.10]**, vel_int by ratio-rule not the bench-frozen 0.72; acceptance gate = loaded S4 replay under v3 **[reframed 2026-07-13 as a v3 regression test at production gains — there is no candidate to escalate to]**. Firmware: bench-only `BENCH_SYSID_BUILD` variant (250 Hz telemetry, 100 Hz knots, un-gated axis-0 iq; flag-OFF binary sha256-proven byte-identical; never-flash-to-robot). No production gain shipped (committed gains stay Level-1 40/0.20/0.32). Six commits; full suite `pytest tests/ -q` (2026-07-12) 2502→2531→2541→2572→2575 passed + 1 xfailed for 1e03348/0a271f8/30cc786/8cbf4f0/db05670, follow-up 0182a11 2575+1. Instrument gap + operator-ear lesson ties the CLAUDE.md physical-intuition rule. Plan: `leg-gain-tuning-methodology.md`. | [2026-07-12-bench-leg-gain-tuning-stage1](2026-07-12-bench-leg-gain-tuning-stage1.md) |
| 2026-07-12 | resolved-with-open-items | GUI + catching cone | **Cone "only syncs after a colcon build" — zombie AND build-correlation hypotheses BOTH REFUTED by bag ground truth; a REAL but LATENT UDP co-bind hazard found; the morning failures point at CONE MCU RESTARTS.** Follow-up to [[2026-07-12-gui-cone-catch-correlation-regression]]. Operator report: *"cone syncs perfectly immediately after a colcon build, but subsequent launches don't sync — zombie ROS2 processes?"* **No zombies** (live forensics: only the systemd GUI daemon + the `ros2` CLI daemon). **No build-correlation**: decoding `/cone/heartbeat` from EVERY bag of the day (**17 sessions**) shows **14/17 `connected=True` AND `time_synced=True` FROM THE FIRST PUBLISHED HEARTBEAT** — the `19-16-18` (pre-rebuild) / `19-17-31` (post-rebuild) / `19-19-12` (next launch, the one the hypothesis says must FAIL) triple is identical (**a colcon boundary with NO signature change**), and three further launches 45 min–3 h later with no build between all sync from their first HB. The operator's model was a CORRECT lesson from finding (1) — the `catch_correlation_node` orphan GENUINELY needed a rebuild (`ros2 launch` runs the INSTALLED copy) — **over-generalised from launch-graph membership to device time-sync**; the defence is to restate the *mechanism*, not the correlation, when reusing a prior lesson. **FINDING 1 — a real structural hazard (HIGH confidence, read straight off the code):** `teensy_bridge_node` and the parallel session's **bench system-ID harness** each construct a `TeensyLinkClient` binding the SAME can-bridge ports (`0.0.0.0:5005` stream + `:5006` RPC) with `SO_REUSEADDR` (`controller/teensy_link/client.py:174-180`); the second bind succeeds SILENTLY and the kernel resolves each unicast datagram to **exactly ONE** socket (no `SO_REUSEPORT`, no load-balancing) ⇒ **one process gets EVERYTHING, the other gets NOTHING**, winner decided at bind time ⇒ since the bench harness **does not subscribe `CONE_FRAME`**, a healthy cone would present as a disconnected one with **no error, no warning, no log line on either side**. **FINDING 2 — that hazard did NOT cause this incident (attribution DOWNGRADED to LOW / candidate-only, NOT supported by the timing artifacts).** The draft blamed the two dead morning sessions (`10-37-19`, `10-42-51`: `connected=False` for **0/882 and 0/1606** heartbeats) on the co-bind because bench runs "bracketed" them — but **a bracket is ADJACENCY, and the mechanism needs CONCURRENCY**: bench run 1 ran 10:36:57→**10:37:00** and the dead bag's first message is **10:37:23** (23 s AFTER it exited); the second dead bag's last message is **10:45:38** and bench run 2 starts **10:46:00** (22 s later). Across **all 17 bags × all 22 `bench_sysid_*` runs of the day there is NOT ONE overlapping pair** — the co-bind event never occurred. Both morning bench runs are **3-second aborted runs** (manifest-only, `stages: {}`) and could not have starved an 89 s / 163 s session even if they HAD overlapped. The signature also **flips mid-session** in three other bags (impossible for an all-or-nothing bind race) and **appears in the `cone_test_*` bags of 2026-05-24 — six weeks BEFORE the bench harness existed** (`f218acf`, 2026-07-11; `TeensyLinkClient` itself 2026-06-02). **FINDING 3 — the LEADING explanation is cone-side power/wiring intermittency:** the cone MCU **restarted at least 3× on 2026-07-12** (`18-34-09` dropout t+32.3→t+50.3 s; `18-44-56` t+12.4→t+26.7 s; `20-03-36` **down at launch**, up at t+43.9 s — each returning with `last_catch_seq=0` + `have_any_catch` cleared), and `20-03-36` (connected **155/593**, self-healing, **no bench process within 54 min**) is the partial-connectivity signature a port race CANNOT produce. The morning sessions look like the same fault held down ~9 min instead of ~20 s. **NO CODE LANDED**, deliberately: no defect reproduced in the reported path, and the interlock closing the (real, latent) co-bind hazard lives in shared transport the parallel gain-tuning workstream is actively depending on — a **coordinate-first change, not a drive-by**. Probe hardened alongside (`tools/probes/cone_bag_decode.py`): reboot-aware `taps` (a seq reset was silently bridged ⇒ ~250 phantom taps had a catch followed either reboot — so the `taps == catch_ev` 17/17 transport validation is **conditional on no post-reboot catch**, now stated), a genuine ZERO no longer collapses to "topic absent" (which had silently suppressed the *exact* catch-frames-lost case the probe exists to detect — on BOTH the statistics and decode paths), and a clean error on a missing `--root`. READ-ONLY investigation — evidence is the 17-bag decode + a 22×17 bench/session overlap check (0 pairs) + `git log --diff-filter=A` provenance + `~/.ros/log` node rosters/teardown records + live process forensics; **no test-count triple is cited because none applies to an entry that lands no code** (inventing one would be fabricating evidence). Operator confirms cone syncing works post-investigation — but note that is **consistent with an INTERMITTENT fault not currently manifesting**, and is NOT evidence the single-owner discipline cured anything. OPEN ITEMS: (a) the single-owner `flock` interlock in `TeensyLinkClient.start()` so a second binder **fails LOUDLY naming the current owner** — closing the whole **two-owners-of-one-link** class at the single point where ownership is established (needs coordination with the bench/gain-tuning workstream; **latent, never observed to fire** — fix it because a silent failure mode with no observable is a bad thing to keep, not because it is currently hurting us; interim mitigation: never run `jugglebot_launch.py` concurrently with the bench harness, pre-flight `pgrep -af bench_leg_sysid; ss -lup \| grep -E ":5005\|:5006"`); (b) `teensy_bridge_node.on_shutdown` **exceeds the launch's 5 s SIGINT→SIGTERM window in EVERY evening session** and is killed mid-profiled-stow (**exit `-15`**; its budget is ~8 s = settle + up to 6 s `DEACTIVATE`) — firmware deferred-stow is the backstop so no immediate danger, but the clean teardown is truncated every session; fixing it moves shutdown timing ⇒ needs the CLAUDE.md control-system-implications analysis, deliberately NOT done here; (c) **PROMOTED to the leading explanation** — the cone MCU restarts are unexplained if the operator did not power-cycle the cone; next steps are (1) ask the operator about ~18:35/~18:45/~20:03, (2) log a firmware **reset cause** (brown-out vs watchdog vs external) — one heartbeat field that distinguishes a power fault from a firmware hang, (3) then investigate physically. **Do not re-open the co-bind attribution without direct evidence of two live `TeensyLinkClient` processes.** | [2026-07-12-cone-sync-udp-link-contention](2026-07-12-cone-sync-udp-link-contention.md) |
| 2026-07-12 | resolved | GUI + ball-butler observability | **BB panel stale "Calibrated" across ROS sessions — latched calibration_result never reset when the GUI outlives the session.** Operator report: the long-lived browser tab shows a previous session's Calibrated in fresh, uncalibrated sessions; operator asked whether calibration can be read from a topic directly. Ground truth: the display is driven by the LATCHED `bb/calibration_result` from mocap_node (published only on calibration attempts, never startup); `bb/heartbeat` (teensy_bridge, relayed BB device CAN heartbeat) carries NO calibration flag — adding one REJECTED by root cause: calibration lives in mocap_node (cross-node subscription + .msg change + lockstep rebuild), it's a category error coupling device-liveness to mocap-software state, AND it still wouldn't fix the stale browser DOM, which needs a disconnect reset regardless. The latched topic IS the right source; the gap was no reset on session death. Fix: `resetBBCalibration()` in panels.js called from main.js's `onConnectionStateChange` 'disconnected' branch — same pattern as the cone entry's `setCatchingConeDisconnected`; **third instance this week of the "GUI outlives the ROS session" state-latching class** (pos_cmd flatline cache 2026-07-11, cone catch baseline, now BB Calibrated). Self-healing: a same-session blip resets then self-restores (rosbridge resubscribe re-delivers the still-latched success=true); a fresh session has no latched sample → honestly Not Calibrated. Also rejected: reset on BB-heartbeat connected=false (mocap calibration independent of the BB CAN link; flickers on transient dropouts); mocap publishing success=false on startup (spurious failure event, no discriminator). Residual accepted: a lone mocap_node restart with rosbridge up isn't covered (websocket never drops) — not the deployment topology. `pytest tests/ros/test_gui_geometry.py tests/ros/test_ball_butler_node.py tests/ros/test_ball_butler.py tests/ros/test_teensy_bridge_node_bb.py -q` (2026-07-12) **138 passed** incl. 4 new tripwires (TestBBCalibrationStaleLatchReset); node module checks clean; headless-chromium baseline renders Not Calibrated + uncalibrated dot. Full suite immediately pre-commit (`pytest tests/ -q`, 2026-07-12): **2545 passed, 1 xfailed in 590.43 s** | [2026-07-12-gui-bb-stale-calibrated](2026-07-12-gui-bb-stale-calibrated.md) |
| 2026-07-12 | resolved | GUI + catching cone | **Catching-cone catches invisible in the GUI — `catch_correlation_node` was a Phase-13 launch orphan.** Operator report (live GUI, 2026-07-12): a piezo tap on the cone surfaces NOTHING in the GUI, though `cone/heartbeat`'s `last_catch_seq` increments correctly. Root cause: the GUI's ONLY catch-surfacing input is `cone/timing_result`, published exclusively by `catch_correlation_node`, whose sole launcher (`catching_cone_test.launch.py`) was DELETED in the Phase-13 SocketCAN decommission (7c7f61b, as "the last launcher of can_node"; operator chose delete over rewire) and the node was NEVER added to `jugglebot_launch.py`. The transport half migrated (`teensy_bridge_node` publishes `cone/catch_event` + `cone/heartbeat`); the correlation half was orphaned — silently dead since 7c7f61b, validated-working on 2026-05-23. Evidence: `git show 7c7f61b` (node creation + LaunchDescription membership both deleted), `git log -S catch_correlation_node -- launch/` (symbol never existed in jugglebot_launch.py), GUI subscribes `cone/timing_result` + rosbag records it (consumers wired to a publisher-less topic → bags silently empty). **Same silent-orphan class as yesterday's pos_cmd fix (2026-07-11-gui-leg-setpoint-echo-poscmd) — two instances in two days, both rooted in the Phase-13 migration; dead topics look identical to quiet topics.** Fix (2 parts): (1) `catch_correlation_node` restored to `jugglebot_launch.py` as a bare declaration exactly as 7c7f61b deleted it (no params/remaps — the deleted launch's cone params belonged to ball_butler_node), with an in-place comment telling the story; (2) defensive UX — `panels.js updateConeHeartbeat` now surfaces `last_catch_seq` directly (`last catch #<seq> · <N>s ago` + 800 ms flash on change; first-connected-heartbeat baseline → no phantom, reconnect re-baselines, wrap-safe inequality) using `CatchingConeHeartbeat`'s `have_any_catch`/`last_catch_seq`/`ms_since_last_catch` — per-tap feedback INDEPENDENT of the correlation pipeline; deliberately NOT an event-store CATCH type (EVENT_TYPES is a frozen 4-type enum feeding chart overlays/filters). Mitigation: new string-level launch tripwire `tests/ros/test_launch_nodes.py` pins the node DECLARED and MEMBERED in the LaunchDescription; docstring names the regression class. A future systematic sweep (every GUI-subscribed topic has a live producer in the launch graph) would close the whole class — open question. Post-fix: every tap → heartbeat line updates + flashes (independent of correlation); with the stack RELAUNCHED (picks up the restored node) → unmatched tap shows an `Actual` clock + `unmatched catch` footer, a matched catch (throw within 0.5 s) shows a predicted-vs-actual delta + sound-bar bar + `last: <thrower>` (sound bar stays matched-only by design). `pytest tests/ros/test_launch_nodes.py tests/ros/test_gui_geometry.py -q` (2026-07-12) **56 passed** (2 new tripwires + 54); panels.js/jugglebot_launch.py syntax clean; headless-chromium harness (temp, deleted): no-phantom baseline / silent reconnect re-baseline / seq-change renders `last catch #8 · 0s ago` all as designed. DOM cone scenario **10/10 ×2** + scenario1 17/17 + minimap 33/33 regression (2026-07-12); full suite (`pytest tests/ -q`, 2026-07-12): **2533 passed, 1 xfailed in 620.89 s**. CONFIRMED live (2026-07-12, session 18-37-27 rosbag: 5 piezo taps → 5 catch_events → 5 matched timing_results → GUI updated on every tap) — after a colcon rebuild, NOT relaunch alone: the same-day addendum refutes the operator's topic-interference hypothesis (catch_event count == genuine taps in all 10 bagged sessions, zero drops) and pins the stale-install root cause (ros2 launch runs the INSTALLED launch copy; the node entered the roster only post-rebuild); residuals flagged: a stale top-level install/ tree (2026-07-09 launch copy lacks the node's launch entry — sourcing it silently resurrects the regression) + suspected cone MCU restarts (seq resets in two sessions). | [2026-07-12-gui-cone-catch-correlation-regression](2026-07-12-gui-cone-catch-correlation-regression.md) |
| 2026-07-11 | resolved | GUI + orchestrator control surface | **GUI orchestrator state-machine minimap — safe click-to-state navigation with a runbook-enforcing sequencer and hold-to-confirm.** User-specified: a translucent minimap of the orchestrator state machine bottom-left of the 3D scene above the command strip; header click → full scene height, opaque, pushes scene + command strip right (fixed-vertical-FOV camera + existing ResizeObserver — ZERO viewer.js changes). Current state highlighted (FAULT pulses); unreachable states greyed with per-node reasons; clicking a node drives the system there in the fewest SAFE steps via 800 ms hold-to-confirm; contextual button surfaces the currently-necessary command (Arm Setpoints/Disarm/Recover/Clear Errors/Retry Boot) with service rejects verbatim. New ES module `state-minimap.js` (~1500 lines) + css; architect-designed plan reviewed pre-implementation; the **safety-critical transition table is reviewable DATA, not BFS** (no emergent graph-legal-but-runbook-illegal routes; review audits rows, not an algorithm). Inputs: orchestrator_state/control_mode_topic/robot_state/link_status (router slot the CAN panel reserved) + `leg_setpoint_echo` (sibling entry) as go_home quiescence proxy. **Sequencer enforces the runbook Sharp Edges**: teardown = standby-if-needed → go_home (best-effort) → anchored echo-quiescence wait → set_setpoint_output(false) ALWAYS → HARD verify-gate (fresh link_status tick, mpc_active exactly 0; ABORT otherwise — deactivate NEVER published unverified; kills the MPC_STALE + un-stowed-legs worst silent failure) → deactivate. Recovery entry ALWAYS the bridge /clear_errors Trigger (self-routes to converge-first /recover on the bridge's authoritative in-process _mpc_active — stale-GUI-read immune); orchestrator clear_errors ONLY for boot-timeout FAULT (F-C Retry Boot). Plans computed at confirm time from a fresh snapshot; per-step pre re-checks; single-flight lock; global abort on FAULT-mid-sequence/disconnect/timeout. Reachability replicates orchestrator ground truth (7-axis heartbeats, firmware_validated, is_homed, error[], guard latch) + freshness guards; BOOT/HOMING/LEVELLING grey ALL ("commands discarded" — deliberately stricter than the command strip's known BOOT-Home silent no-op); unknown state ⇒ all grey fail-safe. Adversarial review (4 lenses + adversarial verification of HIGHs): **2 HIGH CONFIRMED, both fixed in-session** — (1) echo-quiescence wait false-passed on pre-go_home hold samples (no anchor to the wait's start; the 40 Hz hold stream trivially satisfied it → disarm ~100 ms into the 2 s descent, freezing moving legs; fixed: window anchored to wait-step activation, ≥400 ms NEW samples; synthetic probe 7/7) — the class lesson: **freshness ≠ relevance**; (2) vacuous mid-hold action-identity guard (renders rewrote renderedActionKey during the hold — a Disarm hold could fire Arm; fixed: identity pinned at pointerdown + isConnected at confirm). **1 HIGH refuted by mechanism** (armed submode change vs S4.3: all six submodes are in trajectory_node's streaming set; Sharp Edge #1's mechanism is the emitter stopping, which doesn't happen — S4.3 blanket wording flagged as doc note). 18 medium/low triaged, most fixed — worst-case-defaults cluster (bridge_link as actuation guard — frozen fault_state=NONE lies while the uplink is down, same deception class as the CAN panel HIGH; >2 s staleness ⇒ all grey; FAULT branches freshness-gated; mpc_active tri-state, unparseable ⇒ UNVERIFIED never 'disarmed'; stale link + last-known armed ⇒ 'ARMED?' badge + enabled best-effort Disarm; Arm requires fault_state exactly NONE) + pane-relative width clamp min(280, paneWidth*0.55); greyed-label contrast ≥4.3:1 both themes; BOOT→IDLE skip-if-homed edge + arrowheads; T_MODE/T_DEACT 5→15 s (ActiveHandler queues until ACTIVATE completes; queued-command-after-abort residual documented); tripwires derive consumed keys from source; init idempotence. Deliberately not fixed: keyboard/AT (pointer-only holds are the GUI idiom); status-line precedence over the disconnected note. Probe-vs-review complementarity: the probe's fake go_home has no echo dynamics (only review could catch the anchor bug); only the probe pins the live teardown order (recorded timelines: negative case aborts naming Sharp Edge #6, deactivate NEVER published; positive publishes deactivate 62 ms after the armed flag clears). `pytest tests/ros/test_gui_geometry.py -q` (2026-07-11) **54 passed** (+2 EXPECTED_FILES, +4 tripwires incl. disarm-before-deactivate + go_home-before-disarm); JS module checks clean; quiescence probe 7/7; DOM probe (`--scenario minimap`) 27/27 twice pre-fixes, then post-fix **33/33 twice** (32 falsifiable assertions + 1 recorded-timeline info row; new checks: G_BRIDGE uplink-lost greying, ARMED?+enabled-Disarm worst case) + scenario1 regression **17/17** (2026-07-11; anchored gap 0.50/0.64 s visible in the timelines, deactivate ≤65 ms after the armed flag clears); canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2478 passed, 1 xfailed in 597.42 s** | [2026-07-11-gui-orchestrator-minimap](2026-07-11-gui-orchestrator-minimap.md) |
| 2026-07-11 | resolved | GUI + can-hub observability | **GUI CAN TRAFFIC panel rebuilt — per-bus (CAN1/CAN2/CAN3) traffic + health from the can-hub /profile + /link_status uplink.** The panel was DEAD (its `can_traffic` topic lost its only publisher when can_node was deleted in the 2026-07-06 decommission; the decommission-precheck parity audit — 2026-07-06-can-node-parity-reconcile-decommission-precheck Row 63 — accepted the loss naming `/profile` successor) and the metric obsolete (single-bus count from the Platform Teensy on the old shared bus; the network is now three buses through the can-hub Teensy: CAN1=Ball Butler, CAN2=catching cone, CAN3=Jugglebot core). Rebuilt (user-approved): per-bus rows with clickable label (toggles its chart series; readouts keep updating), msg/s, kbit/s, util%, health dot + one uPlot msg/s chart with 30/60/120 s presets (chosen over 5/10/30 s given the 1 Hz cadence); new ES module `can-traffic.js`, old panels.js CAN section deleted, dead subscription removed. **Zero backend work**: `/profile` (1 Hz KeyValues can1/can2 rx/tx/util_pct) + `/link_status` (10 Hz healths + bridge_link). ***THE SLOT TRAP***: wire slot can1_* = physical CAN3, can2_* = physical CAN1 (deliberate firmware remapping, profiling.cpp:24-28 / Teensy_code_canbridge.ino:100-101) — labeling off field names would swap the two most important buses; triple-guarded (registry comment citing firmware; KeyValue drift-pin test producer⊇consumer in test_gui_geometry.py; end-to-end synthetic-stack assertion that the CAN3 row shows the can1_* numbers); mapping independently verified through all four layers. Adversarial review, **1 HIGH confirmed by two lenses, fixed in-session**: the bridge republishes cached /profile at 1 Hz + frozen healths at 10 Hz FOREVER after the can-hub uplink dies (bridge :922/:2101-2107, :898/:2045-2050) → arrival-watchdogs alone would fabricate a live-looking flat line with green dots during the exact outage the panel exists to surface. Fix GUI-side (bridge republish serves other consumers; the honest signal is already in-band: bridge_link + heartbeat_age_ms): three-cause staleness {profileStale, healthStale, linkDown} derived in one place (applyStaleUI) — linkDown drops cached payloads, injects exactly one latched NaN gap per episode (re-armed at UP), forces `--` readouts + UNKNOWN dots + cause-naming badge; profileStale no longer stomps the health dots. CAN2 rendered permanently greyed "n/a — not on uplink" (cone slot = acknowledged firmware TODO; lighting it = PROTOCOL_VERSION 3→4 + wire-freeze re-pin + lockstep flash, 18A precedent; registry-driven so v4 is a two-field change) — chosen over full-stack-now (v3 flashed yesterday; cone bus often disconnected) and over omitting the row. Derivations documented as approximations: msg/s=(rx+tx)/1.0 s vs the firmware's ~1 s LED-cadence window (no window KeyValue on the wire), kbit/s via the firmware's own pre-bit-stuffing 111-bit estimate, util% verbatim; constants from GENERATED geometry-config.js. Other review fixes: STALE badge light-mode ~2.2:1 → filled amber ~9.8:1; RING_CAP 120→132; isolated-sample visibility via uPlot points.filter (17/17 probe assertions); `nanGaps` EXPORTED from telemetry-charts.js (single source — uplot_nan_gap_probe.js still 21/21). NEW reusable harness tools/probes/gui_dom_probe.py + gui_synthetic_stack.py (real rosbridge + scripted rclpy fake stack + headless-chromium CDP DOM assertions). `pytest tests/ros/test_gui_geometry.py -q` (2026-07-11) **48 passed** (+2 drift-pin); JS module checks clean; DOM probe (`python3 tools/probes/gui_dom_probe.py --scenario scenario1`, 2026-07-11, twice) **17/17 PASS** incl. the keystone slot-mapping + `can-linkdown` deception stages; canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2472 passed, 1 xfailed in 582.61 s** | [2026-07-11-gui-can-traffic-per-bus-panel](2026-07-11-gui-can-traffic-per-bus-panel.md) |
| 2026-07-11 | resolved | GUI + can-bridge observability | **GUI "Pos (cmd)" leg series restored — teensy_bridge setpoint echo (`leg_setpoint_echo`).** The missing series was an **architecture-migration orphan, not a chart bug**: commanded leg positions flow trajectory_node →ZMQ:5557→ bridge SetpointPump →UDP→ can-hub Teensy with NO ROS topic on that path; the GUI's datasource (`leg_lengths_topic`) is only published by motion_bridge_node while the dormant run_mpc stack streams, and its old consumer (can_node) was deleted 2026-07-06 — so commanded leg position was unobservable for ALL current-topology runs (uPlot renders an all-NaN series silently absent, pill still lit). Fix (user-approved over trajectory_node-publishes and reuse-leg_lengths_topic): the bridge echoes the last ACCEPTED per-leg u0 on new `leg_setpoint_echo` (Float64MultiArray, 6 motor-rev floats) — covers BOTH sources; setpoint thread only stashes under lock, publish rides the 100 Hz robot_state timer (0.5 s freshness gate, seq-deduped ~40 Hz); stash-before-send deliberate. GUI swapped to it (50 ms throttle, 1 s staleness-clear replacing the never-cleared flatline) — last GUI dependency on motion_bridge_node's **leg output** removed (clears the leg half of the MVP motion_bridge retirement check; `motion/diagnostics` → motion panel still consumes the node); latent bug killed (hand tracking error read `latestCommandedLegs[6]` = leg-0 vel_ff garbage → now `hand_telemetry.pos_cmd`). Adversarial review: 2 HIGH fixed (vendored uPlot 1.6.31 honours only strict null → NaN stale windows drew a **false bridging ramp** — custom per-series `nanGaps` hook, validated against the REAL vendored uPlot in node 21/21; production echo trigger unpinned — mocked-ROS choreography blind spot — now driven via `_publish_robot_state` with robot_state silent) + rosbag record parity, tracking-panel decoupling + hand `--`-unknown, NaN-safe sparklines, try-containment + OSError fault-injection test. Accepted (documented): MutuallyExclusive-callback-group echo gaps during blocking services, setTimeout GUI watchdog, first-echo phantom hand error, no latestHandTelemetry watchdog. Scoped: `pytest tests/ros/test_teensy_bridge_node_setpoint.py tests/ros/test_gui_geometry.py -q` (2026-07-11) 73 passed; `pytest tests/ros/ -q` (2026-07-11) 853 passed; canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2469 passed, 1 xfailed in 574.97 s** | [2026-07-11-gui-leg-setpoint-echo-poscmd](2026-07-11-gui-leg-setpoint-echo-poscmd.md) |
| 2026-07-10 | resolved (bench validation pending) | S4 | **S4 stutter forensics + guard-latch recovery stack.** The evening S4 session's stutter is a **~6 Hz leg-servo limit cycle** (fundamental 5.9–6.1 Hz on all six legs, speed-independent, = the `pos_gain/2π` bandwidth; ODrives actively drive it with ±4–6 A current slams; gains are HOLD-tuned per the config's own comments) — **40 Hz is exonerated with numbers** (22 µm interp fidelity, 6 mm/s join step, zero 40 Hz spectral power, CAN3 load knot-rate-independent). The MAX_DEVIATION latch was **legitimate** (raw u0 ran ~0.49 rev ahead of the stalling leg; the guard watches unclamped u0, the ODrive sees only the lead-clamped command) and the failed `/clear_errors` was a designed-in catch-22 (guard-blind trajectory_node kept advancing to 2.4–2.8 rev divergence → instant re-latch, invisible at 10 Hz). Landed: guard-latch recovery stack (freeze + gate-validated **profiled descent** onto the frozen encoder — an instant reseed cannot pass the pump's 0.3 rev step gate, discovered by the implementing agent — + mode-preserving orchestrator FAULT + one-call `/recover`); **firmware v3 FLASHED** (vel_ff kept at lead-clamp engage, MAX_LEAD 0.15→0.10, per-leg deviation/lead-clamp telemetry + latch snapshot); bridge-owned Ctrl-C disarm→settle→stow; log ergonomics; rosbridge/GUI fix (venv-invisible tornado). Adversarial review caught a BLOCKING choreography deadlock (guard-FAULT publishing ERROR would silence the emitter and kill the recovery it routes) — second cross-process bug in three days invisible to mocked per-node tests. **Root fix pending**: S4b gain-retune session (3-point pos_gain discrimination {25/40/55}, then vel_gain damping A/B). Full suite pre-pin-fix: 2366 passed / 1 failed (the intended v3 version pin); scoped `tests/teensy_link` 188/188 green after the pin fix (2026-07-10). | [2026-07-10-s4-stutter-guard-forensics-recovery-stack](2026-07-10-s4-stutter-guard-forensics-recovery-stack.md) |
| 2026-07-08 | resolved | 1–7 (meta) | **MVP Trajectory Bringup — autonomous build run (closing meta-entry).** Closes the `mvp-trajectory-bringup` arc: the MPC 40 Hz CasADi hot loop removed from the leg path (dormant, source retained), replaced by a simple gate-guarded Jetson-side trajectory generator streaming 40 Hz waypoints on :5557 into the validated `SetpointPump` → can-hub-Teensy Hermite chain. **7 phases implemented autonomously** (a fresh Opus 4.8 agent per phase under Fable 5 orchestration), each followed by an **independent `/audit` fix-and-land round**; **1 phase-agent death salvaged (Phase 4, documented in-entry) + 2 auxiliary agents (Phase-5 fixer, Phase-7 auditor) resumed from transcript**. Suite **1956 → 2274 (+318 tests)**, all new tests only; **ci-deep green twice** (Phase-5 backfill 2164/1 + Phase-7 2274/1). The load-bearing section is the **decision digest** — every decide+document fork across the seven Discussions + audit rounds, grouped by phase, plus the orchestrator-level calls (fixed 40 Hz; Phase-2 `BUSY`-then-lift-in-5; pending-stop retry over the deferred retargeted stop; document-not-fix the ≤1.5 % jerk bias; 12° tilt clamp; light-scope vel-match deferral; `juggle_bb_catch` port deferred to 8/9; audit-fix packages skip a second `/audit`). **Audit-arc retrospective**: 7 rounds, ~62 findings, 7 BLOCKING (P2 ×1, P4 ×1, P7 ×5) — the Phase-7 lesson that mocked-ROS unit tests are blind to cross-process choreography (target_id routing, announcement ordering, ToF arithmetic, goal concurrency, feedback semantics all only visible to an auditor tracing the real ordering); production-ordering tests now pin those seams. **Honest limits**: no hardware validation yet; contact quality has no sim criterion (MuJoCo contact→instant-hold capture model — "caught" is geometric, separation vacuously 0), so 7b's two-consecutive-bounce-out abort is the operative hardware guard. Full suite `pytest tests/ -q` (2026-07-08) = **2274 passed, 1 xfailed in 553.60 s**; ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, 2026-07-08) = **2274 passed, 1 xfailed, 198 warnings in 3024.70 s**. All hardware sessions deferred to `tests/hardware/mvp_bench_runbook.md` (S1→S8). | [2026-07-08-mvp-autonomous-build-run](2026-07-08-mvp-autonomous-build-run.md) |
| 2026-07-08 | resolved | 7 | **MVP Phase 7 — BB→Jugglebot reload action (software-complete; hardware deferred).** The reload (MVP goal 4): BB aims at Jugglebot's ACTIVE catch point and throws; Jugglebot (CATCH mode, hand armed by the existing coordinator) tilts to receive and catches, exposed as `jugglebot/reload` (new `Reload.action`). **Integration gap closed**: `trajectory_node`'s `catch/dynamic_target` now routes through `planner.build_catch` (reach with zero translational arrival velocity → tilt-through-seat decay → literal quiescent hold) not the Phase-5 reach-only `build_timed` that parked the tilted rim — without this a hardware reload catches WITHOUT tilt. The receive tilt is already collinear with the arrival velocity (the hardware-validated `catch_coordinator.compute_catch_orientation` ships it in `target_quat`; `build_catch` reads it — decide+document fork: NOT moved to `target_vel`, which is always zero, to avoid a cross-subsystem contract change to a proven node); reach-freeze + `FROZEN` semantics preserved. New pure-Python `reload_sequencer` FSM (CHECKING → AIMING → THROW_PENDING → BALL_IN_FLIGHT → CATCHING → SETTLING; loud rejects: WRONG_MODE/BB_DISCONNECTED/BB_BUSY/MOCAP_STALE/NOT_STREAMING/NO_BALL/CANT_MAKE_LEAD/BB; aborts: NO_ANNOUNCEMENT/MODE_CHANGED/BB_ERROR/MISSED_INFEASIBLE) + thin `reload_coordinator_node` that **orchestrates only** (trajectory plans motion, catch_coordinator arms the hand, ball_butler throws; never bypasses the gate). `BallButlerThrow` point-target extension (`use_target_point` skips the QTM lookup; `aim_only` = speed-0 7a fast-path; default-zero preserves callers). Announcement→correlation→coordinator integration test drives the REAL BallTracker + CatchCoordinator with synthesized messages (bags predate the pipeline; no rosbag2 reader in mocked CI). Separable Phase-6 follow-up: `sim/hand/trajectory.py` `CATCH_VEL_RATIO` 0.9→0.6 (config truth) — tiny blast radius (symbolic test ref); five reload-gate JSONs refreshed at 0.6, all **CORE PASS** unchanged (caught 20/20, hold ≤0.02 mm, limits 156/660/10331), deferred vel-match rose as expected (nominal worst 0.343→0.440). Full suite `pytest tests/ -q` (2026-07-08) = **2261 passed, 1 xfailed in 541.51 s** (baseline 2223/1 at bf5b46e; net +38 = new tests only: 3 trajectory-node catch + 3 BB-node + 19 sequencer + 10 coordinator + 3 integration); `colcon build --packages-select jugglebot_interfaces jugglebot` = 2 packages, 0 errors. **Deferred**: staged operator sessions 7a (aim-only frame + 744.3 mm catch-z verification) / 7b (throw + static catch, two-bounce-out abort) / 7c (full reload action) — `tests/hardware/session_phase7_reload.md`. **Audit-fix round (2026-07-08, last before hardware; 5 BLOCKING + WARNING/NOTE)**: fixed five cross-process choreography bugs the mocked-ROS per-node tests couldn't see — `target_name` omitted → BB announced `target_id='point'` (whole catch pipeline dropped the ball); the announcement is published *inside* the throw handler (AIMING) so the phase-gated `note_announcement` discarded it (regated on the throw being commanded); the settle deadline omitted the 0.61–0.73 s ToF so a nominal catch read MISSED (now anchored on the announced landing, ROS→perf converted); concurrent goals double-threw (now REJECTed); `FROZEN`/`STALE_STATE` catch feedback latched `MISSED_INFEASIBLE` (now ignored; a later accept clears). Plus: receive tilt **clamped at `MAX_TILT_DEG=12°`** (single source with `tilt_geometry`) not rejected above 30° (real BB arrivals are 18–40° off vertical → were silently dropped/blacklisted); non-finite-point guard; tracker-id-correlated CAUGHT; 7b rewritten to TRAJECTORY-mode static catch. `pytest tests/ -q` (2026-07-08) = **2274 passed, 1 xfailed in 553.60 s** (net +13); ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, 2026-07-08) = **2274 passed, 1 xfailed, 198 warnings in 3024.70 s (0:50:24)**. | [2026-07-08-mvp-phase7-reload-action](2026-07-08-mvp-phase7-reload-action.md) |
| 2026-07-08 | resolved | 6 | **MVP Phase 6 — Catch trajectory + sim reload gate (LANDED LIGHT; vel-match deferred).** The reload catch trajectory + a production-in-the-loop sim gate. New pure `motion/trajectory/tilt_geometry.py` (`tilt_to_receive` collinear-catch tilt, ≤12° cup-axis anti-parallel to the arrival velocity; re-exports the Phase-4 `shaping.py` cup lever arm 1.66 mm/deg @ 744.3 mm centre — single source of truth) and `ballistics_bc.py` (touchdown quadratic + launch/arrival BCs, copied-not-imported from `controller/ballistics.py`, overlap regression-pinned). `planner.build_catch`: reach (translational arrival velocity **forced zero** — velocity matching is the hand's job) → **tilt-through-seat** decay (small residual tilt rate `0.07 rad/s` so the rim isn't parked, decayed to rest over 0.15 s) → **literal quiescent hold** → optional return; fixed-lead loud `TOO_FAST`; gated by the fast `validate_follow` (fork owned: matches the Phase-5 catch path, catch is never lean-shaped, step-bound bit-identical so pump-acceptance holds). Ported `sim/juggle_noise.py`; new headless seeded harness `sim/reload_gate.py` drives the **real** `build_catch` + `KnotEmitter` + a real `SetpointPump` into the MuJoCo plant, ballistic ball under §3 noise, arm-and-forget `HandCatchSequence` sampled at the physics-substep rate (Teensy runs the hand high-rate); JSON report. **Six-file diff-audit**: the bb divergences are two-ball features (Phases 8/9), NOT catch-critical — nothing ported but `juggle_noise`; one HIGH finding surfaced + deferred (`sim/hand/trajectory.py` `CATCH_VEL_RATIO = 0.9` is stale vs config `0.6`). **Reload gate** (`python sim/reload_gate.py --trials 20`, 2026-07-08) = **CORE PASS**: caught 20/20, held 20/20, core_clean 18/20, hold travel ≤0.01 mm, tilt ≤0.01°, separation 0 ms, feasibility violations 0, pump rejects 0; all 4 robustness sweeps (arm ±30 ms, event_vel ±10 %) CORE PASS 20/20 each (nominal core_clean 18/20). **Required leg limits published to Phase 4**: vel ≈156 mm/s, acc ≈660 mm/s², jerk ≈10 331 mm/s³. **Deferred (light-scope, operator-approved)**: hand-contact vel-match ≤15% floors ~0.26 — root-caused (the ~14 ms Teensy velocity-hold is narrower than the ±20 ms capture-timing alignment in MuJoCo's contact→instant-hold model; cup axis IS aligned — the mismatch is uniform ~0.74× magnitude) and inconsistent with the hardware 0.6 catch ratio (designed ~40% first-contact mismatch, absorbs over the stroke). No ROS/config/codegen change → no colcon/codegen gate. Full suite `pytest tests/ -q` (2026-07-08) = **2222 passed, 1 xfailed in 526.04 s** (baseline 2179/1 at 22ed9cf; net +43 = new tests only: 15 tilt + 7 ballistics + 12 catch + 9 harness). | [2026-07-08-mvp-phase6-catch-trajectory-sim-gate](2026-07-08-mvp-phase6-catch-trajectory-sim-gate.md) |
| 2026-07-08 | resolved | 5 | **MVP Phase 5 — Timed target states (code-complete; hardware deferred).** MVP goal 3: reach a pose+orientation at a nominal velocity at an ABSOLUTE arrival time, with a too-tight lead **loudly rejected** (`TOO_FAST` + the minimal feasible lead), never silently slowed. New pure `planner.build_timed`: a **fixed-lead reach** to `(target_pose, target_twist)`; the plan is ALWAYS rest-terminating (a nonzero arrival velocity gets a decel-to-rest continuation — a final nonzero-velocity segment would be an unbounded-jerk terminal-hold snap); `hold_after` = hold-at-target vs return-to-neutral. Gated by the **fast** `validate_follow`, which is what enables the mid-plan **supersede** — a C2 replan installs single-digit ms typical after its seed, guard-bounded — install-continuity rejects `STALE_STATE` on drift >0.06 rev; worst case tens of ms with the appended stop-stretch (the TOCTOU class the Phase-2 install-continuity guard closed on the ~377 ms analytic path), so the temporary Phase-2 `BUSY` restriction is **lifted for the timed path** (`go_to_pose` keeps `BUSY` — it uses the analytic gate for shaped plans; the asymmetry is deliberate + tested). New interfaces `TimedTarget.srv` (response `code` = the feasibility **string** enum, matching GoToPose) + `TargetFeedback.msg`; `trajectory/timed_target` (TRAJECTORY mode) + `catch/dynamic_target` (CATCH mode) BOTH route through `build_timed` (+ a reach-freeze window holding the committed catch reach through the last `JB_TRAJ_CATCH_REACH_FREEZE_S`); `trajectory/target_feedback` carries accept/reject to `catch_coordinator_node`, which **swaps** its dormant ZMQ :5559 `TargetFeedbackSub` for the topic (feasibility blacklist semantics preserved). One ROS-clock→perf_counter conversion point; the catch path is already perf-domain (system-wide `CLOCK_MONOTONIC`). The catch z is lifted by the active-z (170) from the MPC-offset convention (0 = active) to the trajectory STOW-relative convention — flagged for Phase-7 hardware verification (the gate rejects an out-of-stroke z loudly meanwhile). Every emitted timed knot pump-accepted (invariant re-asserted). No `hardware_config.yaml`/codegen change (Phase-5 constants landed in Phase 1). Full suite `pytest tests/ -q` (2026-07-08) = 2164 passed, 1 xfailed in 535.82 s (baseline 2128/1 at 1c0f9c1; net +36 = new tests only: 12 planner-timed + 17 node + 7 catch-coordinator); ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, run 2026-07-08) = **2164 passed, 1 xfailed, 198 warnings in 3001.75 s (0:50:01)**; `colcon build --packages-select jugglebot_interfaces jugglebot` clean (`TimedTarget.srv`, `TargetFeedback.msg`). **Deferred**: the ±25 ms mocap timed-move battery + too-tight rejection + mid-plan supersede demo (`tests/hardware/session_phase5_timed.md`). **Audit-fix round (same day, 3 WARNING + 6 NOTE)**: `trajectory_op.max_timed_lead_s` 60 s clock-domain guard in `build_timed` (an epoch-magnitude lead previously → ~7e10-element `np.arange` MemoryError killing the node); CATCH added to `_MOTION_MODES` (graceful stop on leaving-CATCH-mid-reach AND entering-CATCH-mid-move); reach-freeze releases after `arrival + JB_TRAJ_CATCH_SETTLE_HOLD_S` (was latched forever, silently dropping post-catch targets) with a distinct `FROZEN` service-level feedback code; coordinator feedback now source-filtered (`'catch'` only) and `STALE_STATE`/`FROZEN` excluded from blacklist counting; `_min_feasible_timed` fall-through validated; timed installs record the accepting report's predicted peaks for `/diagnose`; stale ZMQ-era comments/docs updated; +15 tests. | [2026-07-08-mvp-phase5-timed-targets](2026-07-08-mvp-phase5-timed-targets.md) |
| 2026-07-08 | resolved | 4 | **MVP Phase 4 — Lean shaping + limit-ramp tooling (code-complete; ramp deferred).** The *software* for the multi-session hardware limit ramp — observability + the one opt-in refinement — landing **no limit values** (the ramp is operator bench work). (1) **Lean shaping** `motion/trajectory/shaping.py`: a translation move superposes a small tilt `∝` its lateral accel (`rx += −k·a_y/g`, `ry += +k·a_x/g`, gain `JB_TRAJ_LEAN_GAIN` **default 0.0**, 5° cap) to keep a ball seated, with a cup-height-derived lever-arm xy compensation ported from `Jugglebot-bb/sim/juggle_tilt.py::realize_tilted` (`centroid = target − shift`, arm **+95.1 mm ⇒ 1.66 mm/deg**, `+ry→+x`/`+rx→−y`, sign pinned to the physics not prose). Runs **before** `validate` (planner wraps the plan; node passes the shaper into `build_move`) so the single-gate invariant holds and the stretch loop sizes the shaped motion; gain≤0 / HoldPlan are the identity. Shaped `(pose,twist,accel)` are analytic-consistent (no FF mismatch); **every shaped knot stays pump-accepted**. (2) **Per-call `GoToPose.lean_gain`** (<0 ⇒ config default, ≥0 ⇒ clamp [0,1], default 0.0 ⇒ OFF) for the A/B. (3) **Realized-peak observability**: node tracks realized leg vel/acc/jerk off the emitted knots (reset per move) beside the gate's predicted peaks + session limits + active `lean_gain` on `trajectory/diagnostics`; `sim/analysis/diagnose.py::summarise_trajectory_moves` → per-move `peaks + headroom` table. (4) Operator harness `tests/hardware/traj_ramp_battery.py` (service-calls-only) + protocol `session_phase4_ramp.md`. **Salvage**: implemented by a prior agent that died mid-run leaving the diff uncommitted + unverified; this session independently verified every invariant vs the reference geometry, ran the gates, and applied the audit. Adopted forks (documented): 0.0=explicit-OFF so a ramped config gain never silently leans bare callers; fixed nominal cup arm (per-pose arm deferred to Phase 6); the position-continuous-but-C1-seam tilt-*rate* boundary transient (bounded by the gate, an A/B watch-item — windowed lean deferred). Corrected (real bugs the predecessor's tests missed): (a) the `/diagnose` summariser collapsed the holdless ramp battery into ONE row (`plan.kind` stays `'move'` across a completed move's terminal hold) — fixed with a monotonic `move_seq` diagnostics key segmenting per accepted move; (b) jerk headroom used the coarse realized peak — now also surfaces `used_pct_predicted` (gate-authoritative fine peak) and the `/diagnose` doc ramps against it; (c) a broken logbook link + imprecise port line-refs. No `hardware_config.yaml`/codegen change (no limit values). Full suite `pytest tests/ -q` (2026-07-08) = 2120 passed, 1 xfailed in 518.69 s (baseline 2094/1 at e039cc0; net +26 = new tests only: 14 shaping + 5 diagnose + 7 node); `colcon build --packages-select jugglebot_interfaces jugglebot` clean (`GoToPose.lean_gain`). **Deferred**: the per-step limit ramp + the lean A/B (operator bench sessions). | [2026-07-08-mvp-phase4-shaping-ramp-tooling](2026-07-08-mvp-phase4-shaping-ramp-tooling.md) |
| 2026-07-08 | resolved | 3 | **MVP Phase 3 — SpaceMouse streaming (fast follower gate + graceful stop).** Continuous target following through the validated streaming layer. Meets the two Phase-2-audit prerequisites. (1) **Fast follower gate** `feasibility.validate_follow`: profiling showed the analytic gate's ~377 ms is ~90 % the per-sample analytic Jacobian chain (not the ~9 % condition SVD), so the fast gate sidesteps the Jacobian — vectorised pose→leg-extension sampling (`QuinticSegment.eval_pose_batch` + batched IK, equal to the scalar chain to 1e-9) with leg vel/acc/jerk by finite difference + a decimated condition SVD → **~1.5–4 ms**; vel/acc/step match analytic to < 0.1 %, jerk held **strictly conservative** (≤ 1.5 % under-measure at 300 samples, ×1.05), knot-step-bound **bit-identical** so pump-acceptance is preserved. Same module, same `FeasibilityReport`, reached only through `planner` — the single-gate contract holds. (2) **Duration-stretched graceful stop** `planner.build_graceful_stop`: a decel-in-place that converges for gate-limited seeds by lengthening the horizon, EXCEPT a near-boundary outward seed whose margin is under the decel overshoot ~0.2·v·T (audit-corrected — the node retries from the decaying live state via a pending-stop flag, not a latch); replaces the Phase-2 STANDBY-exit catch-and-complete fallback and backs follower input-loss. New pure `TargetFollower` (clamp → deadband → replan / keep-last-plan) + `planner.build_follow`; `trajectory_node` gains `platform_pose_topic` publisher-field mode gating + `gravity_offset` composition (verbatim ports), the follower replan inside the 40 Hz emitter tick (drain-to-latest), input-loss stop, and a **ray-clamp saturation policy** (clamp to the nearest reachable pose along the current→target ray — uses the EXISTING stroke envelope, invents no new limit; throttled WARN). No new interfaces/config/launch → no colcon/codegen gate. Software complete; **manual-flight hardware session DEFERRED** (gentle fly / saturation shove / mid-flight unplug). Full suite `pytest tests/ -q` (2026-07-08) = 2079 passed, 1 xfailed in 502.33 s (baseline 2047/1 at f38153f; net +32 = new tests only). **Audit-fix round (2026-07-08)**: 4 WARNINGs + 6 NOTEs applied — TOCTOU-safe conditional follower install, `validate_follow` now gates zero-segment `HoldPlan`s, pending-stop retry replacing the always-valid overclaim, entry grace window, sustained-reject skip, per-replan `t0`, hold/go_home rejected in follower modes; `pytest tests/ -q` (2026-07-08) = **2094 passed / 1 xfailed, 0 real failures** (the sole reported failure is the load-flaky `test_hot_loop_allocation_contract`, which passes isolated; net +15 = new audit-fix tests only). | [2026-07-08-mvp-phase3-spacemouse-streaming](2026-07-08-mvp-phase3-spacemouse-streaming.md) |
| 2026-07-07 | resolved | 2 | **MVP Phase 2 — Waypoint moves at very low limits.** The trajectory generator gains its arbitrary-target profiled-move surface + the full feasibility gate. `feasibility.validate` completes to: geometry (non-finite→`UNREACHABLE`, stroke→`WORKSPACE`, Jacobian condition backstop→`UNREACHABLE`) → leg vel/acc/jerk peaks (jerk = per-segment finite-diff of the analytic leg accel) → per-knot `u0` step bound (25 ms spacing, 20% margin) — all peaks measured in one pass so the planner reads the worst ratio off one report. `planner.build_move`: rest-terminating quintic; `duration_s` None/0 ⇒ minimal-feasible via a duration-stretch loop (leg peaks scale exactly `1/Tⁿ` from rest ⇒ converges in ≤2 `validate` calls), an explicit too-tight duration ⇒ loud `TOO_FAST` with `min_duration_s`, a spatial `WORKSPACE`/`UNREACHABLE` re-raised immediately. New interfaces `GoToPose.srv` (response `code` = feasibility **string** enum, deviating from the plan's `int32` — anti-drift), `SetTrajectoryLimits.srv` (ceiling-clamped session ramp), typed `TrajectoryStatus.msg` (migrated off the Phase-1 `DiagnosticStatus` stand-in). `trajectory/go_to_pose` (TRAJECTORY-mode-only, else `WRONG_MODE`; not-seeded ⇒ `STALE_STATE`) + `trajectory/set_limits` + `trajectory/diagnostics`; `ActiveMode.TRAJECTORY` + `'trajectory'` orchestrator command. Every emitted frame still pump-accepted; nothing bypasses the single gate. Software complete; **hardware move-battery + loud-rejection demo DEFERRED** to an operator bench session. Full suite `pytest tests/ -q` (2026-07-07) = 2041 passed, 1 xfailed in 500.17 s (baseline 1996/1; net +45 = new tests only); `colcon build --packages-select jugglebot_interfaces jugglebot` clean. | [2026-07-07-mvp-phase2-waypoint-moves](2026-07-07-mvp-phase2-waypoint-moves.md) |
| 2026-07-07 | resolved | 1 | **MVP Phase 1 — Streaming foundation (hold via the new trajectory path).** Removes the compute-marginal MPC 40 Hz hot loop from the leg path (dormant, source retained) and replaces it with a simple Jetson-side trajectory generator. New pure package `jugglebot.motion.trajectory` (quintic copies pinned bit-for-bit to `controller/`, `TrajectoryLimits`/`QuinticSegment`/`TrajectoryPlan`/`HoldPlan`, the single canonical `feasibility.validate` gate, `planner` constructors, `KnotEmitter`), `motion/ipc.MpcCommandPub` (sole-binder :5557 interlock), the thin `trajectory_node` (dedicated 40 Hz emitter thread, measured-telemetry-seeded hold, `trajectory/hold`+`go_home`, `trajectory/status` as DiagnosticStatus), and the bridge `set_setpoint_output` (SetBool) runtime-arming service (link + fresh-stream + 0.25 rev u0-vs-encoder preconditions, stream-then-arm). Streams only a HOLD in Phase 1; every emitter frame is tested-accepted by a real `SetpointPump` (the production-in-the-loop invariant). `JB_TRAJ_*` config + `HW_SECTIONS` row; launch drops `mpc_bridge_node`, adds `trajectory_node`. Software complete; **hardware arm+120s-hold DEFERRED** to an operator bench session (`tests/hardware/session_phase1_hold.md` + read-only probe `tools/probes/traj_stream_probe.py`). Full suite `pytest tests/ -q` (2026-07-07) = 1993 passed, 1 xfailed in 625.44 s (baseline 1956/1; net +37 = new tests only). | [2026-07-07-mvp-phase1-streaming-foundation](2026-07-07-mvp-phase1-streaming-foundation.md) |
| 2026-07-06 | resolved | post-port code-clarity cleanup | **De-opaque CAN-port docstrings/comments — strip plan-document vocabulary (Phase N, Tier, item, Flash-A/B, U3-iv, Fable-5, decision-ids, the α/β path codenames, plan slugs) from the code so a collaborator reading only the code understands it.** Scope: all CAN-port-arc code (files touched on/after 2026-06-03; ~130 files, ~825 rewrites) in six gated, path-scoped commits — generator+regen (`63feaef`), `teensy_link/` lib (`f13be2b`), firmware (`b60e0c6`), `teensy_bridge_node`+launches (`eeb76a9`), test suite (`f1e5ac3`), probes/tools/benches (`235514d`). Comment/string text only — no wire-format/logic/behaviour change. Operator decisions: α→"Jetson-relay" / β→"Teensy-side"; ADR refs KEPT (durable); handoff decision-ids + Fable-5 attributions stripped; logbook pointers only at defining sites; self-explanatory local "Phase 1/2 / Step 1/2" markers preserved (22 sites). Pre-port May-MPC + friction-FF plan vocabulary deliberately OUT of scope (different older plans; clean 2026-05-21→06-03 date cut). Surfaced+fixed 2 stale comments in-pass (`rpc.h` "hoist at Phase 10" already done; `setup.py` "NOT in jugglebot_launch" now false) + a `can_buses.h` D7 reword. Completeness backstop: the initial gathering regex under-caught (lowercase phase, bare row/D/9a/gap tokens, letter-Phase, 5 uninventoried files) → a final complete-pattern whole-tree sweep caught every straggler. A concurrent session's `SET_LEAD_ACCEL_LIMIT` feature shared the working tree (additive, non-conflicting) → all commits staged by explicit path with `:!` exclusions; **3 test files it had open are DEFERRED** to a follow-up. Full suite 1955 pass / 1 xfail (2026-07-07, 639.44 s; one load-flaky `test_decay_boundary_continuity`, passes isolated) | [2026-07-06-canport-docstring-deopaque](2026-07-06-canport-docstring-deopaque.md) |
| 2026-07-06 | resolved | teensy-can-offload Phase 13 | **Legacy SocketCAN stack DECOMMISSIONED**: `can_node.py` (1725 lines) + `can/bus.py` (206) + their 98 tests + the `can_node` console-script + `catching_cone_test.launch.py` (last launcher of can_node; bridge owns the cone/BB conduit — operator chose delete over rewire) all deleted in one atomic commit, gated on the same-day parity pre-check (scan clean) + explicit operator approval. KEPT: `can/odrive.py`/`ball_butler.py`/`catching_cone.py`/`motor_state.py`/`throw_ballistics.py` (live bridge/orchestrator + xref ground truth) — so `python-can` STAYS pinned (odrive.py:14/ball_butler.py:13 `import can`; the plan's "remove python-can" assumed can_node was the only consumer — disproved); `cantools` also stays but has NO live importer (audit-corrected: a legacy pin, droppable without refactor). The naive-delete trap caught: `can/__init__.py` eagerly imported `CANBus`, which would have broken every `from jugglebot.can import …`. Ripples: setup.py entry point, launch comment + `/platform_target_reached` rosbag entry, provenance docstring notes (can_node.py:NNN citations → git history), matrix Phase-13 caveat → DONE, plan Phase-13 block rewritten. can0 teardown = disabling `init-can.service` (the ONLY loader; stock NVIDIA denylist blocks autoload) — **operator-run + verified 2026-07-06 11:39 (lsmod shows no CAN modules; can0 device gone)**; revert is one systemctl command | [2026-07-06-phase13-socketcan-decommission](2026-07-06-phase13-socketcan-decommission.md) |
| 2026-07-06 | resolved | teensy-can-offload Phase 13 (pre-check) | **Parity-matrix reconcile vs the canhub-hardening arc + Phase-13 decommission pre-check — counts 79/12/23/3 → 85/9/20/3, PARTIAL safety scan CLEAN (no dropped can_node safety behaviour open), 3 GAP dispositions confirmed (row 18 deliberately open for Phase 12; rows 47/63 no live consumer — not blockers)**. Rows flipped: 53 + 57 PARTIAL→validated (`559c6dc` deactivate-at-STOW guard HW-validated; `83ac938` clear_disarm_reasons mirror + native test); 19 PARTIAL→unvalidated (`25557e2` stow-on-shutdown, Ctrl-C HW confirm pending); 1/17/31/55 unvalidated→validated (the native harness compiles the REAL fault machine + cold-start TUs — `5f07ad5`/`431ea89` — asserting the rearm-on-redrop golden, mid-op CAN3-loss aborts, mutual-exclusion rejects, homing abort gates; [18A] `2b749e3` host trusts the firmware HomingResult; 2026-07-04 sitting CHECKs 4/5). Stale global caveats struck ("no compiled-C++ firmware test", frozen-robot_state-on-link-loss, clean-shutdown stow, six-leg stow single-leg-only, unimplemented bus-off register read — each with its closing commit). The one genuine dropped safety semantic found in this family since the audit (clean-shutdown auto-stow) was operator-surfaced 2026-07-04 and closed same-day. Verdict: **Phase-13 decommission NOT blocked** | [2026-07-06-can-node-parity-reconcile-decommission-precheck](2026-07-06-can-node-parity-reconcile-decommission-precheck.md) |
| 2026-07-06 | resolved | canhub-hardening (Tier-2 residual) | **health_of() bus-off wiring — live FLTCONF joins RX staleness in the per-bus health classification**, closing the last shipped `TODO(bench)` in the bus-health path. Previously staleness-only: a bus in error-passive/bus-off with frames still flowing classified OK and accepted motion; `BusHealth::BUS_OFF` was unreachable, so the host's fatal-CAN OR-term, shutdown-stow skip, and part of the recovery-edge trigger were dead code. Fix: `BusRxHealth.flt_live` (live ESR1.FLTCONF, clamped 0/1/2) sampled per 1 kHz service tick from the SAME ESR1 read as `synced` (zero new register traffic, non-sticky — recovers with the bus); pure header-inline `classify_bus_health()` (never-seen→UNKNOWN; bus-off→BUS_OFF; passive OR stale>2s→WARN; else OK), natively pinned. WARN keys on error-PASSIVE (128) not the CAN warning level (96) — the measured 12 V-ramp REC burst (~121) sits between them, so 96 would flag every normal power-on. Strictly per-bus (operator-stipulated): a CAN1 fault cannot gate CAN3. `[canhealth]` gains `fltNow=` beside the sticky `flt=`. Bench-validated (2 operator-run checks): BB unplug/replug → `bb` fltNow active→passive→active with CAN3 untouched; CAN3 12 V cycle → WARN through the outage, clean recovery + exactly one host conservative re-read. Check 2 surfaced a **new known-benign signature** (documented, deliberately NOT hardened): the 12 V reconnection ramp punches sticky `flt=BUSOFF`/`tec=254` via a real ms-scale self-healing bus-off (gate reopens on the first valid frame while the medium is still dirty; BIT1/STUFF errors +8 TEC apiece from the 128 pin through 256; auto-recovery BOFFREC=0) — `fltNow` never shows it, no gate misbehaves, fresh-BOOT flt/tec rule unchanged. Native 16/16 (170.23 s, 2026-07-05); full suite 2054 pass / 1 xfail (651.63 s pre-commit re-run, 2026-07-06); flashed 2026-07-05 | [2026-07-06-canhub-health-of-busoff-wiring](2026-07-06-canhub-health-of-busoff-wiring.md) |
| 2026-07-05 | resolved | deactivate (operator request) | **Deactivate is a no-op when the platform is already at STOW** (hardware-validated 2026-07-05: no-op when stowed, still lowers when not). Operator ask: the legs should NOT deactivate if already stowed (all target legs IDLE with pos near stow). Previously `/deactivate` (and the activate-or-deactivate service + stow-on-shutdown) always ran the firmware DEACTIVATE — re-arming each leg to CLOSED_LOOP (a jolt), lowering to a no-op STOW, then IDLE. Guard added at `_run_deactivate` (the single enforcement point all three callers route through): `_legs_already_stowed(axes)` returns True iff every target leg is IDLE and `|pos| <= _STOW_POS_MAX_REV` (0.2 rev; STOW=0.0 in the DeactivateMonitor convention, `abs()` so it's sign-convention-safe — a bare `pos<=0.2` would wrongly skip when an active pose reads negative, leaving the platform UP). Fail-SAFE: missing telemetry / a missing diagnostic / any leg not-IDLE-or-extended / a NaN position (adversarial-review catch: `abs(NaN)>0.2` is False, so a faulted-active-leg-at-NaN would else look stowed) → False → the deactivate proceeds (only skips on a POSITIVE all-stowed confirmation; the no-op still idles the HAND for de-energise-all-axes parity). Host-only, no reflash. +3 node tests (no-op fires no leg RPC but idles hand; still-extended proceeds; predicate incl. NaN + missing-diag). `pytest tests/ -q` 2054 pass / 1 xfail (645.50 s, 2026-07-05) | [2026-07-05-deactivate-at-stow-noop-guard](2026-07-05-deactivate-at-stow-noop-guard.md) |
| 2026-07-05 | fix-landed-pending-hardware-confirm | canhub-hardening Tier-2 [18A] | **HomingResult uplink closes the homing silent-abort false-success class**. Firmware `finish()` IDLEs a leg on BOTH a successful trip AND every abort (bus-down / guard-E-STOP mid-move); only success also runs `set_absolute_position`. So a mid-drive abort was INDISTINGUISHABLE from success to the host's CLOSED_LOOP→IDLE state-cycle observer → it declared the leg homed while the encoder reference was never set (uncalibrated zero, orchestrator proceeds). Fix: uplink the firmware's real per-axis `HomingResult` (NONE/RUNNING/OK/FAILED) by reusing the Diagnostic's 3-byte pad as `homing_result` (u8) + 2-byte pad — size-preserving (36 B) but a layout change → **`PROTOCOL_VERSION` 1→2** (freeze `_EXPECTED` re-pinned + version-freeze bumped; firmware+host lockstep). Host `HomingMonitor` now TRUSTS the result (OK→DONE, FAILED→FAILED), gated on seeing HOMING_RUNNING this run (a stale prior-home result can't terminate the new one); the state-cycle success inference dropped, timeout + active_errors backstops kept. Firmware `send_diag` populates it + a `homing_result` transition force-emits a Diagnostic (delivering the RUNNING transition promptly, decoupled from the lagging ODrive axis_state, so `saw_running` arms reliably). +7 host tests incl. the false-success regression; 4 home-flow ros tests + freeze/version tests updated. Suite 2051 pass / 1 xfail (469.60 s, 2026-07-05); `pio` green (225600 text). NOT yet flashed (consolidates with item 20 into ONE Phase-2 flash) | [2026-07-05-canhub-hardening-18a-homing-result-uplink](2026-07-05-canhub-hardening-18a-homing-result-uplink.md) |
| 2026-07-05 | fix-landed-pending-hardware-confirm | canhub-hardening Tier-2 item 20 | **Eight small firmware follow-ups** (Fable-5 item 20), landed on top of the CAN3 presence-gate work (nothing reverted). **(1)** version-sweep re-query — a lost Get_Version reply no longer leaves an axis's version permanently missing (re-query present-but-unreceived axes, paced 1 s). **(2)** `REBOOT_ODRIVES` rejected while `fault_stow_pending()` (operator-decided) — a reboot mid-stow disarms the raised legs → gravity drop; mirrors the cold-start reject. **(3)** documented that the deferred stow deliberately completes through a guard E-STOP / ODrive-fatal (aborting would gravity-drop the raised legs). **(4)** `SETTLE_STOP_US` sourced from a new `Homing::STOP_SETTLE_MS` (`jugglebot_homing.stop_settle_ms:5`) — was a hardcoded `5000ull` whose comment named a `Homing::` constant that never existed (the real 5 ms lived only in `BBHand::`). **(5)** documented HOME's axis-6 policy (owned by `homing_request`, consistent with `hand_axis6_permitted`). **(6)** `RESULT_BUF_CAP` static_assert vs `ResultAxisVersions`. **(7)** `encode_frame` nullptr guard (`len>0` + null payload returned a garbage frame → now 0; codegen, no wire-layout / PROTOCOL_VERSION impact). **(8)** cmd-result ring → drop-OLDEST (operator-decided) so the loud command-outcome channel keeps the freshest results. Native tests for items 1/2/7 (+ a version-cache test-isolation fix in `version_check_init`). Suite 2049 pass / 1 xfail (481.29 s, 2026-07-05); `pio` green (225600 text); NOT yet flashed (consolidates with [18A] into the Phase-2 flash) | [2026-07-05-canhub-hardening-item20-firmware](2026-07-05-canhub-hardening-item20-firmware.md) |
| 2026-07-05 | resolved | canhub-hardening (marginal-CAN3 follow-up) | **canhealth `err=` now counts wire errors** — observability follow-through on the marginal-CAN3 mislabel (the 1 Hz headline field printed the raw FlexCAN_T4 ESR1-change snapshot counter, which climbs ~200/s in NORMAL sync-active operation on benign IDLE/RX/TX activity flips — the root of the multi-session "bus erroring 10-15/s" ghost hunt). Second REAL defect found designing the fix: the 8-deep raw-ESR1 evidence ring recorded EVERY snapshot, so at ~200 benign flips/s a genuine error word was flushed within ~40 ms — self-erasing under exactly the traffic where you'd want it. Fix (~30 lines, 4 files): new `wire_errs` counter (snapshots with ≥1 ACK/CRC/FORM/STUFF/BIT0/BIT1 bit; documented lower bound during identical-error storms — tecInc/recInc stay the rate-accurate signals); `err=` prints it (0 on a healthy bus), raw counter renamed `chg=` (capture-machinery liveness only — frozen chg=0 means err=0 unmeaningful); ring filtered to wire errors + one-shot TWRN/RWRN/BOFF interrupt crossings (NOT the latched level bits 8/9 — re-flood risk); `[canerrs]` gated on (wire\|tecInc\|recInc)≠0 so healthy buses print no [canerrs]/[canesr1] at all; README field reference updated + cross-build capture-comparison note (pre-2026-07-05 captures printed the raw counter as err=). Native suite 16/16 (157.35 s, 2026-07-05); flashed same day; 45 s bench capture: all buses err=0 flags=0x00 tec=0 rec=0, chg climbing only on active CAN3, zero [canerrs]/[canesr1] lines, 7/7 ODrives heartbeating. Overflow non-issue (uint32, print-only, ~248-day wrap) | [2026-07-05-canhub-canhealth-err-wire-errors](2026-07-05-canhub-canhealth-err-wire-errors.md) |
| 2026-07-05 | resolved | canhub-hardening (marginal-CAN3 follow-up) | **Marginal CAN3 bus reframed, mechanism power-cycle-verified, presence-gate contract landed**; the Tier-2 carried-out item (`err` 10-15/s, `tec=255`, sticky `flt=BUSOFF`) decomposes into (a) sticky high-water marks from ~2.4 days of boot history spanning robot-supply cycles and (b) a snapshot counter that was **never a wire-error rate** — FlexCAN_T4 captures on any masked-ESR1 *change*, benign SYNCH/IDLE/RX phase flips included. Diagnostic firmware (per-type ack/crc/form/stuff/bit0/bit1 counters, live ECR TEC/REC @ 1 kHz + delta sums, raw-ESR1 ring, `[cantiming]` decode — kept permanently) shows the powered bus **wire-clean at the 75% sample point** (zero error bits, `tecInc=recInc=0`, 210+ s @ ~2,000 fps); kills bit-timing/termination/SI. 2 Hz `bad_axis` = Platform 0x7DF TRAFFIC_REPORT (benign). Operator power-cycle experiment: **12 V ramp garbage confirmed quantitatively** (REC burst→121 vs historical high-waters — 119 capture-verified, 121/133 operator-reported; FORM+STUFF+BIT1 = historical flags minus ACK/CRC); assumption corrected — bridge is **TX-silent when unsynced** (0x7DD gated on `time_synced()`), so historical ACK/tec-254/BUSOFF needs a bus-dies-while-link-up window (ordinary teardown ordering); 45 V edges invisible to CAN. **Fix**: cone presence-gate generalised to ALL buses at the `can_*_send()` choke point (`bus_partner_present()`, `BUS_PARTNER_STALENESS_US` 5 s, `tx_gated`/`gated=` observability, native doctest pins the window; README gains a full serial-console field reference); audit-surfaced same-commit hardening — AXIS_ALL CLEAR/REBOOT RPCs now notify their fault-machine side-effects only after ≥1 frame actually enqueued (SYNCH gate passes on a dead-but-idle bus). Native suite 16/16 (156.80 s, 2026-07-05); flashed + 60 s bench-clean. Gate validated live same day (2nd operator session): BB-absent + sync active → `gated` +100/s per absent bus with ZERO error events/tec=0/flt=active (A/B vs the pre-fix err=122/ACK/tec=128/passive baseline on the same scenario); CAN3 `gated=0` under full load (never blocks legitimate TX); gate reopens on the first BB frame (froze at 3053). RESOLVED | [2026-07-05-canhub-marginal-can3-diagnosis](2026-07-05-canhub-marginal-can3-diagnosis.md) |
| 2026-07-04 | fix-landed-pending-hardware-confirm | canbridge-foundation (Phase-4 follow-up) | **Stow-on-shutdown ported from can_node → teensy_bridge_node** (β migration gap surfaced during the Tier-2 powered sitting). Ctrl-C of `jugglebot_launch` while un-stowed left the platform holding — `can_node.on_shutdown` always did a profiled stow (`_gently_move_to_setpoint(0.0, deactivating=True)`); the α→β migration only ported transport teardown. **NOT a Tier-2 firmware regression** (git-blame `0ad3d25^..192e6af`: the flashed changes touched stow EXECUTION, never a trigger; the stow-on-link-loss path never existed). Deliberately fixed **host-side, not firmware**: an autonomous firmware stow-on-LINK_LOST would re-open the 2026-05-19 hazard (dropping a healthy holding platform on a transient host stall); a clean Ctrl-C is a controlled host event → the stow belongs in `on_shutdown`, as in α. Fix: `_shutdown_stow()` called before RPC/link teardown (MPC already quiesced) → guarded `_run_deactivate(deactivate_axes)` (TRAP_TRAJ lower to STOW + IDLE, hand idled), guards mirroring can_node (skip on CAN3 `BUS_OFF`/no-telemetry — deferred stow covers reconnect — or a pending deferred stow); self-bounded + best-effort. New `stow_on_shutdown` ctor kwarg (default True; tests pass False via `_build_paired_node`). +5 guard tests; suite 2046 pass/1 xfail (2026-07-04). Operator workaround until deployed: `/deactivate` before Ctrl-C. Sibling sitting findings: `enable_setpoint_output` launch-gate (mpc_active=0, by-design opt-in) + an `is_homed` boot-read bug (separate, under diagnosis) | [2026-07-04-canbridge-stow-on-shutdown](2026-07-04-canbridge-stow-on-shutdown.md) |
| 2026-07-04 | resolved | Online-juggle tilt re-architecture — motion-quality course-correction (the co-design catch rung: implementing the 2026-07-03 design basis) | Co-design catch — a CONTINUOUS velocity-matched sub-tick command makes the single-ball self-catch SMOOTH (cup path 1026→322 mm/cycle, ZERO slider clamps, ball received DOWN at −0.67 m/s) WITHOUT a gentler throw; the same insight (kinematic release imposes v_takeoff regardless of cup velocity) retires the vestigial v_takeoff-overshoot carry; the Rung-2b MAKE is regenerated 12/12 on all 6 seeds and is *tighter* (in-cup offset 0.6→0.3 mm) — dissolves the P2 "runway = slam" tension (an artefact of the constant-per-tick command) | [2026-07-04-codesign-catch-continuous-velocity-matched](2026-07-04-codesign-catch-continuous-velocity-matched.md) |
| 2026-07-03 | resolved | Online-juggle tilt re-architecture — motion-quality course-correction (catch control-formulation design basis for the co-design rung) | Catch control-formulation design basis — the non-smooth catch is NEITHER a hand-acceleration limit NOR a "gentler throw" problem: the catch commanded a CONSTANT position per whole 40 Hz tick into an ultra-stiff position actuator (jump-and-settle; cup vz −33…−37 mm/s at the sample tick), so the ceiling-overshoot "static catch" was a workaround, not a runway; the sim already runs the hand at 1 kHz (`plant.step` calls `hand_cmd_fn` every substep), and a CONTINUOUS velocity-matched sub-tick command co-moves the cup smoothly at −2700 mm/s — receive on a *descending* cup, no ceiling overshoot, no gentler throw; both operator hypotheses (max-accel setting, "match 500 Hz") refuted literally yet load-bearing (the hand does tens of g unthrottled; the command-continuity axis was the bug); mirrors the hardware's 40 Hz plan / 500 Hz track split; the design basis the 2026-07-04 co-design rung implements | [2026-07-03-catch-control-formulation-design-basis](2026-07-03-catch-control-formulation-design-basis.md) |
| 2026-07-03 | blocked | Online-juggle tilt re-architecture — motion-quality course-correction (P2 re-unification attempted, blocked by MAKE fragility) | P2 self-catch re-unification on `plan_cup_cycle` — STOPPED with evidence, no code landed (module + 12/12 MAKE intact): the smooth cycle and the fragile MAKE are in DIRECT tension because the robust seat of a −2.7 m/s arrival gets its ~60–100 mm downward runway FROM the ceiling slam (all 93 ceiling ticks live in `_catch`; the stitch's ~700 mm/cycle is easy to remove, the catch's is not); across ~35 probe runs every clean-motion variant punches through (0–2/12) and every restructuring — even a config-only gentler throw or byte-identical-catch stitch removal — breaks the MAKE (0/12); concluded "co-design a gentler throw + apex-rendezvous catch as its own rung" — SUPERSEDED same day by the design-basis entry (the tension is real only for the constant-per-tick command) | [2026-07-03-p2-selfcatch-reunification-tension](2026-07-03-p2-selfcatch-reunification-tension.md) |
| 2026-07-03 | in-progress | Online-juggle tilt re-architecture — motion-quality course-correction (P0 landed; P2 re-unification pending) | Motion-quality review — the single-ball toss/catch is NOT smooth (cup path 1026 mm/cycle vs ~550–650 momentum-shaped, full 350/355 mm stroke, 93 ceiling-clamp ticks, 60 mm/tick command step, contact on an UP-moving cup) and the celebrated Rung-2b MAKE actually seats the ball 145 mm ABOVE `catch_z` on a ceiling-parked cup — the logbook's "co-moving descent seat" never executes (the kinematic seat has no descent gate); root cause is architectural: the stitched quintic harness vs the codebase's own whole-cycle min-accel planner (`plan_cup_cycle`); the obvious P1 descent gate empirically non-separable (no aggregate improvement + broke seed 5, 12→8 — reverted); P0 metrics probe landed as the gate (`tools/probes/juggle_motion_quality.py`); P2 (re-unify on `plan_cup_cycle`) named the fix | [2026-07-03-motion-quality-review](2026-07-03-motion-quality-review.md) |
| 2026-07-02 | hardware-validated | canhub-hardening Tier-2 | Firmware safety/concurrency/parity pass from the Fable-5 review — landed, adversarially reviewed, and **FLASHED** to the can-hub Teensy (2026-07-03), awaiting one powered re-validation. **[13]** (`0ad3d25`) guard E-STOP now LATCHES until an operator CLEAR_ERRORS (motor_guard semantics; released only via fault_notify_clear_errors, never the internal soft-reset/UV auto-retry) — pre-fix it auto-recovered the instant the transient cleared. **[14]** (`138aa11`) monotonic `micros64()` for ALL interval/staleness arithmetic (one root cause: steppable `now_wall_us()` corrupts every staleness interval + the 500 Hz trajectory phase); wall clock kept for wire timestamps only; paired stamp+read conversions across ~8 fw files + host client.py; native clock-step test. **[15]** (`b562825`) NetLock around the lwIP `Ethernet.loop()` pump (was unlocked vs prio-3 TX → pbuf corruption) + a bounded UDP RX drain (drop-deferred). **[16]** (`c8ba247`) Flash-A 7 sub-items (ISR-level MPC↔cold-start mutual exclusion, hand-traj↔homing interlock, homing presence check, STATE_WRITE isfinite, setpoint isfinite + wrap-safe seq guard, notify-after-gate, AXIS_ALL send-result accum). **[17]** (`83ac938`) Flash-B 5 sub-items (atomic last_heartbeat_us, interp_begin_stow PRIMASK barrier, ISR priority 32→16 audited-FreeRTOS-free, clear_disarm_reasons mirror, **hand-axis fault-eval → NUM_AXES — OBSERVABLE: a hand fault now E-STOPs the legs**). **[18B]** (`6fe1ec9`) inverted HomingMonitor timeout docstring; **[18A]** false-success uplink DEFERRED (wire-protocol change). An **adversarial review** (`192e6af`) caught + fixed two real regressions before flash: the seq guard bricked MPC control for ~13 min after ~50% of `run_mpc` restarts (persisted firmware high-water vs host-reset counter → phantom MPC_STALE E-STOP), and a mis-applied `!coldstart` on the stow-descent TX → an unarmed gravity drop if a DEACTIVATE landed mid-stow. `pio` green (222528 text, fits). Suites green (2040 + known flaky alloc test, 2026-07-03). **Powered validation PASSED 2026-07-04**: all 7 checks pass (CHECK 2 seq-guard operator-confirmed PASS; the guard tool's report is verdict-only). Two tools ran it: `tools/probes/canhub_tier2_hw_validation.py` (read-only serial observer, 5/6/7) + `tests/hardware/teensy_guard_validation.py` (MPC-free ZERO-MOTION driver arming mpc_active with legs IDLE, 1/2/3). Carried out one open item: a **marginal CAN3 bus** (`err` climbing, `tec`→255, sticky `flt=BUSOFF` — decorative to the staleness gate, but a real error source) → own bench-diagnosis. Remaining: item 19 (partial — checklist runners committed; automated jitter/soak gate still open), item 20, [18A], the CAN3 investigation, then archive the parity plan | [2026-07-02-canhub-hardening-tier2](2026-07-02-canhub-hardening-tier2.md) |
| 2026-07-02 | resolved | canhub-hardening Tier-1 | Host/test/doc hardening pass from the Fable-5 fresh-eyes review of the whole can-hub firmware/UDP pipeline. **A** (`f8397c7`) host `teensy_link`: setpoint-thread exception-containment (build() rejects-not-raises, OSError-guarded send), non-idempotent RPCs never auto-retried (firmware has no dedup) + retry-race fix, `SetpointPump.reset()` wired at reconnect/mpc-edge, host bundle (ActivateMonitor requires CLOSED_LOOP, scale_to_bench floor>ceiling raise, capped drain, rpc_args DEACTIVATE). **B** (`c7425e9`) `teensy_bridge_node`: UDP-link-loss→`has_fatal_can_error` (dropped can_node parity, observable); cross-axis disarm-while-CLOSED_LOOP predicate `any_disarmed AND any_cl` (the per-leg conjunction never fired, observable); hand vel/curr-limit topic parity (observable); bounded cone queue; **RLock** spanning the whole STATE_WRITE read-modify-write (closes the lost-update, not just serializing wire writes); async cold-start re-reads sharing one inflight guard with CAN3-edge preservation. **C** codegen+tests: PROTOCOL_VERSION + wire-layout freeze + codegen lints + firmware lib pins (`37a81d4`, plan item 9); compile leg_homing/activate/deactivate.cpp into the native harness via a self-contained `coldstart_hal` + retire the tautological Python xrefs (`431ea89`, plan item 3, +130 byte-parity assertions); execute the C++ UDP framing codec natively (`8821451`, plan item 8 / gap 4). Host/test/doc only, NO re-flash. Suites green through each commit (2015 pass / 1 xfail at the framing commit, 2026-07-03). *(Plan item numbers here are canhub-hardening.md's 1–21 list, NOT the Fable-5 review-finding numbers the commit messages carry.)* **Tier-1 CLOSED (2026-07-05)**: item-8 native/golden complete (`6f20fea`+`db4ddf8`), typed CrcError (`2cf3a07`), item-12 doc sweep host/doc side (`0fc2002`; firmware-comment residue → Phase-2 item 20). Observable changes (plan items 2/6/7) still want an operator eyeball (hardware-confirmation checks in the entry) | [2026-07-02-canhub-hardening-tier1](2026-07-02-canhub-hardening-tier1.md) |
| 2026-07-02 | resolved | canbridge-foundation (reboot bugfix) | REBOOT_ODRIVES didn't clear `encoder_search_complete` → orchestrator skipped encoder-search after a reboot → homing on an un-indexed encoder → `ODRIVE_FATAL` FAULT→BOOT→HOMING loop. Found validating the Phase-4 `/reboot_odrives` cold-start clear: the reboot cleared is_homed/levelling/pose but left the sticky `_encoder_search_done_session`, so the DERIVED `encoder_search_complete = is_homed OR session-bit` stayed True; the ODrive MCUs lose their incremental-encoder index on reboot (operator-confirmed not pre-calibrated), so homing entered CLOSED_LOOP un-indexed. **Latent in can_node too** (can_node.py:1552-1566 never cleared it) — only bit once Phase 4 drove homing automatically after a reboot. Fix: clear `_encoder_search_done_session` in the reboot hook (the reboot must clear everything the ODrive loses — references AND the encoder index). Flipped `test_..._monotonic_across_reboot` → `test_reboot_clears_encoder_search_complete`. Confirmation: a plain relaunch cold-starts cleanly (re-index-searches + re-homes) | [2026-07-02-canbridge-reboot-encoder-search-clear](2026-07-02-canbridge-reboot-encoder-search-clear.md) |
| 2026-07-02 | resolved | canbridge-foundation Phase 4 | Orchestrator wiring — the plan's **final** phase; makes the bridge a drop-in for the retired `can_node` from the LOCKED `orchestrator_node`'s view (ZERO edits to orchestrator/state_machine), restoring **automated orchestrator-driven cold-start**. Registers four thin wrappers directly on the bridge (operator design call — no separate `orchestrator_conduit.py`; no existing actions/services module to join): `home_motors` ActionServer(HomeMotors)→ shared `_do_home()` (home legs+hand → persist is_homed → configure) + concurrent-goal guard; `activate_or_deactivate`(ActivateOrDeactivate)→ `_run_activate`+`_run_configure` (the TRAP_TRAJ→PASSTHROUGH **fold**, rows 27/28) / `_run_deactivate`; `get_platform_tilt`(GetTiltReadingService)→ `relay_read_tilt` with retry+validity-bound+**NaN-on-failure** (row 60); `set_level_state`(Float64MultiArray)→ new `_write_level_state` (STATE_WRITE preserving is_homed). All nine blocking cold-start verbs share one `ReentrantCallbackGroup` so multi-second moves never starve the 100 Hz publish (operator-approved: extended beyond the plan's new-verbs-only to close the class). Robustness: a **runtime drift-guard** contract test introspects both nodes' declared `(name,type)` sets (divergence-catch proven) — required a small additive mock-conftest enhancement (it discarded interface types). Host-only (no firmware/codegen change). Suite 1995 pass / 1 xfail (final pre-commit run; +23 Phase-4 tests: 7 drift-guard + 14 behaviour + 2 full-cold-start integration). Matrix rows 6/7/24/27/28/44/45/54/60 → ported+validated. **Powered orchestrator-driven sitting PASSED 2026-07-02** (automatic BOOT→HOMING[legs+hand]→IDLE in ~9 s + 3× levelling cycle; no control_mode='ERROR' → transient-FAULT/ESTOP confirmed benign). Completes the plan's implementation. Residual hardware validations CLOSED 2026-07-02 in a follow-up session: REBOOT_ODRIVES cold-start clear ✓ (+ found/fixed an encoder-search-clear bug), six-leg deferred-stow reconnect ✓ (full-platform CAN-loss safety inversion; legs held, no drop; profiled stow on reconnect). Plan ready to archive; the UDP-link deferred-stow (matrix row 18) is the one out-of-scope open item for autonomous movement | [2026-07-02-canbridge-phase4-orchestrator-wiring](2026-07-02-canbridge-phase4-orchestrator-wiring.md) |
| 2026-07-02 | resolved | Online-juggle tilt re-architecture — Phase 1 / Rung 1 (clean single catch) — fast-catch fidelity follow-up | Fast-catch fidelity — the Rung-1 catch now seats a REAL fast Ball Butler throw (~4.9 m/s vz) via a firmed catch contact (`solref 0.05→0.01`) + a phase-matched descent seat (cup co-moving at ratio·\|v_arr\| @ t_td, ratio 0.55); the operator's physical intuition (a cup at 50–80% of ball speed is normal) reframed a "catch can't do a fast BB" dead-end into a sim-fidelity gap; gentle lob 8/8 AND real BB 8/8 (in reach), self-catch MAKE preserved 12/12 | [2026-07-02-fast-catch-fidelity](2026-07-02-fast-catch-fidelity.md) |
| 2026-07-01 | resolved | canbridge-foundation Phase 5 | Hand command conduit + hand homing + deactivate-idles-hand + cmd-echo telemetry — restores the full hand surface `catch_coordinator` silently no-op'd against (the four hand services, all GAPs). `set_hand_traj_cmd`/`smooth_move_hand` ride one `HAND_TRAJ_CMD` RPC: the **host** builds the exact 8-byte `0x6D0` payload (byte-identical to `can_node`, byte-0 discriminated), the firmware (`hand_ops.cpp`, new) sends the `CLOSED_LOOP`+`POSITION/PASSTHROUGH` preamble to axis 6 then forwards it on the **firmware-owned** `0x6D0` id — **aborting if a preamble send fails**; the **absolute** `wall_time_ms` deadline is Jetson-computed and the firmware CANNOT re-stamp (transit-jitter-immune — the temporal-accuracy contract). `set_hand_state`/`set_hand_gains` ride the Phase-1 axis-6 allow-table. **Hand homing**: `leg_homing.cpp` accepts axis 6 (per-axis `Homing::HAND_*`); the bridge applies hand gains host-side (refuse-flash-defaults) then `HOME(6)`. Also configures + idles the hand (rows 40/51/29) and restores `HAND_CMD_ECHO` (sniffs axis-6 `Set_Input_Pos` → `hand_telemetry` cmd fields, row 58). Native `test_hand_ops` (5 cases, divergence-proven) + byte-parity xrefs. Suite 1972 pass / 1 xfail (+45); `pio` green. **Powered sitting PASSED 2026-07-02** (hand homing + catch-traj fires-after-delay [absolute-deadline confirmed] + HAND_CMD_ECHO populated + deactivate-idles-hand; orchestrator → IDLE on homing-complete corroborates the Phase-2+3+5 chain). Matrix rows 29/32/37-40/51/58 → ported+validated. **Unblocks Phase 4** | [2026-07-01-canbridge-phase5-hand-conduit](2026-07-01-canbridge-phase5-hand-conduit.md) |
| 2026-07-01 | resolved | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK — kinematic-release re-architecture; MAKE) | Rung-2b kinematic-release — the MAKE. The throw→catch→throw loop closes (12/12 cycles, all seeds) once the throw cuts the ball free at the planned take-off velocity instead of letting it emerge from the contact push; contact-parameter tuning ruled out first (frontier scan: gain 2.4–16 everywhere separation holds), then a clean contact-carry↔kinematic hand-off (`Ball.ballistic_release`) rejects the real ~0.6 mm seat offset (loop gain ≪ 1) | [2026-07-01-rung2b-kinematic-release](2026-07-01-rung2b-kinematic-release.md) |
| 2026-07-01 | resolved (by kinematic-release) | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK gate, OPTION-1 re-plan) | Rung-2b OPTION-1 re-plan — the two-point A↔B oscillation is the HONEST tilt test (tilt ENGAGED ~1.4°) and it STILL diverges; loop gain > 1 via the throw's chaotic sensitivity to the throw-ORIGIN pose — a GENUINE tilt-hypothesis BREAK on a non-degenerate geometry (tilt necessary-but-not-sufficient; the contact-detach knife-edge is the binding constraint) | [2026-07-01-rung2b-oscillation-tilt-engaged-diverges](2026-07-01-rung2b-oscillation-tilt-engaged-diverges.md) |
| 2026-07-01 | resolved (by kinematic-release) | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK gate) | Rung-2b single-ball self-catch — MAKE-OR-BREAK BREAK; the pure column diverges (loop gain > 1) for column-specific reasons, and the column is a degenerate case where tilt is inactive (~0° commanded) — does not test the tilt hypothesis | [2026-07-01-rung2b-selfcatch-column-divergence](2026-07-01-rung2b-selfcatch-column-divergence.md) |
| 2026-06-30 | resolved | canbridge-foundation Phase 6 | Robust clear_errors + reboot-in-progress latch — a `REBOOT_ODRIVES` RPC arms a bounded (`REBOOT_WATCHDOG_SUPPRESS_US=6 s`, measured reboot latency 2.3–3.5 s + margin) watchdog-suppression latch (`fault_notify_reboot_started`) so the deliberate reboot silence is not read as a CAN loss; armed ONLY by the reboot RPC (spontaneous loss unaffected — the 2026-05-19 deferred-stow inversion preserved, proven by native test), released on fresh-after-stale OR the bounded deadline (atomic 64-bit). `CLEAR_ERRORS`/`REBOOT` gate on the live ESR1.SYNCH bus-transmittable signal (`jugglebot_bus_transmittable`) not heartbeat-staleness (the just-repowered-bus deadlock), via a single `gate_allows` chokepoint + header-inline policy predicate; AXIS_ALL now loops legs+hand (`i<NUM_AXES`). Three-way (fault_machine.cpp + fault_logic.py + native harness/golden). Bench: staleness-deadlock did NOT reproduce (Platform Teensy keeps CAN3 RX fresh) — SYNCH gate ships as the latent-transient/design-intent fix | [2026-06-30-canbridge-phase6-reboot-latch](2026-06-30-canbridge-phase6-reboot-latch.md) |
| 2026-06-30 | landed | Online-juggle tilt re-architecture — Phase 2 / Rung 2a (single-ball throw to arbitrary targets, open-loop accuracy) | Rung-2a single-ball tilt-aimed throw — tilt-aim is exact (lateral = slider·sin θ), open-loop landing ≤33 mm across the column + 50 mm ring; a directional separation/aim asymmetry caps the clean box at ~±70 mm | [2026-06-30-rung2a-single-ball-tilt-throw](2026-06-30-rung2a-single-ball-tilt-throw.md) |
| 2026-06-30 | landed | Online-juggle tilt re-architecture — Phase 1 / Rung 1 (clean single catch, BB-reload) | Rung-1 clean single catch — constant-decel seat kills the soft-contact bounce; frozen-early reach + ramped tilt; ~3.3 mm in-cup offset under §3 noise | [2026-06-30-rung1-clean-single-catch](2026-06-30-rung1-clean-single-catch.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 3 | Get_Version handshake + Jetson-validated `firmware_validated` — firmware sweeps Get_Version (one frame/tick, bus-paced) + caches raw versions; bridge pulls via `GET_AXIS_VERSIONS` (sync RPC blob, no CAN3 round-trip) + runs the tested `validate_group` (zero firmware version semantics); mismatch → force-FAULT via `robot_state.error`, all-match → `firmware_validated=True` (un-wedges BOOT). Closes the Phase-2 reconnect residual: a CAN3-bus-health (`bus1_health` WARN/BUS_OFF→OK) conservative reconnect re-read (retry, then `is_homed=False`), distinct from the keep-stale UDP-watchdog reconnect | [2026-06-29-canbridge-phase3-version-validated](2026-06-29-canbridge-phase3-version-validated.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 2 | Cold-start state via the relay — `robot_state.is_homed`/`levelling_complete`/`pose_offset` sourced from a cached Platform-Teensy `RobotState` (relay `STATE_READ` at boot + on reconnect; non-blocking publish), `is_homed=True` written on `/home` (read-modify-write preserves levelling), `REBOOT_ODRIVES` clears all three (shared hook), `encoder_search_complete` derived; conservative `is_homed=False` on boot-read failure (the safety invariant); software-only, validated at the Phase-4 powered sitting | [2026-06-29-canbridge-phase2-coldstart-relay-state](2026-06-29-canbridge-phase2-coldstart-relay-state.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 1 | Platform-Teensy relay seam — hybrid (typed STATE_WRITE/TILT_READ/STATE_READ RPCs + verbatim PLATFORM_FRAME reply uplink, host-correlated by (id,dlc)) + narrow generated hand axis-6 allow-table replacing the blanket reject; mechanism-only (cold-start wiring is Phase 2), software-complete to the CAN3 SRX_DIS / reply-latency bench-probe gate | [2026-06-29-canbridge-phase1-platform-relay-seam](2026-06-29-canbridge-phase1-platform-relay-seam.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 0 | Native firmware-test harness (compiles fault_machine/leg_interp host-side, tests the binary) + one codegen-allocation pass (5 RpcMethods + 2 T2J MsgTypes + HeartbeatT2JFlags enum) + RpcMethod dispatch lint; retires the hand-maintained fault-logic transcription via firmware-anchored goldens | [2026-06-29-canbridge-phase0-native-harness](2026-06-29-canbridge-phase0-native-harness.md) |
| 2026-06-29 | in-progress | Online-juggle tilt re-architecture — Phase 0 (characterise tilt/orientation tracking before re-introducing tilt in the planner) | Platform-tilt tracking characterisation — tilt is static-exact (≤0.004° droop), ~1.66 mm/deg lateral lever arm (cup ~95 mm above centroid) + only second-order vertical cross-coupling, >45 mm leg margin even at 24° (not stroke-limited at z=170), and tracks ~0.99/3° at the 1.6 Hz cycle freq (better than lateral translation's 0.95/17°); tilt is viable for throw-aiming; new committed probe `juggle_tilt_bandlimit.py` | [2026-06-29-platform-tilt-tracking-characterisation](2026-06-29-platform-tilt-tracking-characterisation.md) |
| 2026-06-27 | resolved | teensy-can-offload | can_node → Teensy can-bridge feature-parity audit — 117-cap matrix (53 validated / 7 unvalidated / 37 PARTIAL / 20 GAP); headline: automated orchestrator cold-start broken against the bridge (masked by operator-driven cutover) | [2026-06-27-can-node-teensy-parity-audit](2026-06-27-can-node-teensy-parity-audit.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1: online runner integration. Throw fixed, catch axis-split landed (0→5 catches), sustained 2-ball blocked by the throw-amplifies-catch-error cascade | Online juggle integration — throw-separation slam fix (cycle-0 re-plan) + catch axis-split (slider descends moderately *with* the ball; full match slams the floor) take the online runner 0→5 catches (beats the offline demo's 2); sustained 2-ball blocked by the band-limit cascade (catch seats ~15 mm off-centre → throw amplifies to >150 mm, beyond reach → diverge); dwell/coast/symmetric-startup/separation/smoothness/apex all ruled out; fix scoped (less position-sensitive throw / self-centring cup) | [2026-06-27-online-juggle-throw-fix-catch-axis-split-band-limit-cascade](2026-06-27-online-juggle-throw-fix-catch-axis-split-band-limit-cascade.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1: re-architecting to an online cup planner (replacing the offline optimiser); planner built + tested, integration WIP | Pivot to online per-throw task-space planning (Kai-style) re-solved against the observed ball + cup-tracking band-limit characterisation (slider near-perfect → throw stroke; platform static-exact/−3 dB ~5 Hz lateral → smooth positioning); level-platform decoupling drops banking; planner module built + 7 unit tests; catch-geometry shuttle issue documented for integration | [2026-06-27-online-replanning-architecture-and-cup-bandlimit](2026-06-27-online-replanning-architecture-and-cup-bandlimit.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1 (contact) throw-aim: band-limit diagnosis; closed-loop catch WIP | Throw-aim root cause is a platform BAND LIMIT (not angular tracking — ω×r tracks fine, <0.04 m/s; refuted by same-instant decomposition); heavy connect-constraint platform low-pass-filters every fast maneuver (~30 mm+), emergent throw lands ~100-140 mm short; closed-loop catch (observe ball, reach platform xy) → 1→2 catches, short test passes; full ≥30 blocked by band-limit cascade (paused to reconsider planning architecture) | [2026-06-27-throw-aim-band-limit-and-closed-loop-catch](2026-06-27-throw-aim-band-limit-and-closed-loop-catch.md) |
| 2026-06-26 | in-progress | teensy-can-offload | Phase 11 U5 — six-leg β cold-start + /deactivate (validated 06-27); run_mpc-on-β feedback chain confirmed, high-freq hold deprioritized (compute-marginal) → re-scoped to a can_node parity audit | [2026-06-26-phase11-u5-six-leg-cutover](2026-06-26-phase11-u5-six-leg-cutover.md) |
| 2026-06-26 | in-progress | Sim2Real fidelity — concern 1 (contact) infrastructure landed; throw-aim (ω×r + platform angular tracking) WIP | Contact-mechanics integration — the kinematic hold/`release()` replaced with real contact carry + a seat-based catch metric + an emergent physics throw (contact proven faithful: ball leaves at *exactly* the cup velocity); two lasting sim-fidelity fixes: sub-stepped platform command (the 40 Hz staircase rang the stiff leg actuators and shook the ball out) + phase-switched contact stiffness (soft catch / stiff throw — the overdamped soft contact drags the throw cohesively, a sim artifact); the "60% lateral transfer" hypothesis withdrawn on operator pushback (the ball left exactly at the *achieved* 12° tilt, not the commanded 20° — workspace clamp, no transfer loss); throw-aim root-caused to the optimiser's omitted ω×r (~0.2 m/s at ~0.2 m off-centroid, 5 rad/s yaw; added with a left-Jacobian rotvec-rate→ω conversion) but achieved ω ≠ planned ω remains — platform angular tracking deferred to a follow-up; demo 1 catch vs the 30+ target, headline tests `xfail(strict=True)` | [2026-06-26-contact-mechanics-integration](2026-06-26-contact-mechanics-integration.md) |
| 2026-06-26 | in-progress | Sim2Real fidelity — concern 2 landed, concern 1 feasibility proven | Velocity-matched catch (cup at 0.7×ball, colinear) + contact-mechanics feasibility (cup holds & throws a ball via physics; integration is next session) | [2026-06-26-velocity-matched-catch-and-contact-mechanics-feasibility](2026-06-26-velocity-matched-catch-and-contact-mechanics-feasibility.md) |
| 2026-06-25 | complete | teensy-can-offload | Phase 11 U4 — production α→β setpoint switch + friction-FF drop (D9) + /teensy rename | [2026-06-25-phase11-u4-production-cutover](2026-06-25-phase11-u4-production-cutover.md) |
| 2026-06-24 | in-progress | teensy-can-offload | Phase 11 bench cutover — present-axis scoping + synthetic β-knot interp driver (software prep; powered run pending) | [2026-06-24-phase11-bench-cutover](2026-06-24-phase11-bench-cutover.md) |
| 2026-06-24 | resolved | bb-led-two-ball-juggle — Phase 3 → Phase 4 prep | BB-led two-ball juggle sim port-readiness — measured BB throw position, catch_vel_ratio 0.9→0.6 reconciliation, deterministic BB scatter (33/0 held) | [2026-06-24-bb-juggle-sim-port-readiness](2026-06-24-bb-juggle-sim-port-readiness.md) |
| 2026-06-10 | resolved | temporal-accuracy bring-up | Cone catch timestamps glitch +40 s — micros64() false 32-bit wrap from piezo-ISR race | [2026-06-10-cone-micros64-false-wrap](2026-06-10-cone-micros64-false-wrap.md) |
| 2026-06-10 | in-progress | teensy-can-offload | Catching-cone CAN2→UDP uplink (phase-10b) — cone data reaches the Jetson again | [2026-06-10-cone-uplink-phase-10b](2026-06-10-cone-uplink-phase-10b.md) |
| 2026-06-08 | tuned | teensy-can-offload | Ball Butler cutover from can_node to teensy_bridge_node — phase A bench bringup, GUI Calibrate end-to-end | [2026-06-08-bb-cutover-bench](2026-06-08-bb-cutover-bench.md) |
| 2026-06-06 | in-progress | teensy-can-offload | Can-bridge (CAN Hub) hardware bringup — 3-bus wiring, button-free flashing, FlexCAN/FreeRTOS firmware gotchas, live SYNC observability | [2026-06-06-can-hub-bringup](2026-06-06-can-hub-bringup.md) |
| 2026-05-23 | tuned | catching-cone-bringup | Throw director node + trimmed cone-test launch for live BB→cone integration | [2026-05-23-throw-director-and-cone-live-integration](2026-05-23-throw-director-and-cone-live-integration.md) |
| 2026-05-23 | in-progress | catching-cone-bringup | Catching cone hardware bringup | [2026-05-23-catching-cone-hardware-bringup](2026-05-23-catching-cone-hardware-bringup.md) |
| 2026-05-22 | in-progress | hardware diagnostics / compute-capacity assessment | MPC is compute-bound on the Jetson Orin Nano — 17-40% of solves exceed the 40 Hz CPU-time budget; system-profiling harness built | [2026-05-22-mpc-compute-bound-jetson-profiling](2026-05-22-mpc-compute-bound-jetson-profiling.md) |
| 2026-05-20 | in-progress | hardware-safety / fallback redesign | hold_extrap fallback positive-feedback oscillation drove platform into chaotic motion (peak 336.9 mm/s leg velocity, 2.41× soft limit; borderline-E-stop) | [2026-05-20-hold-extrap-positive-feedback-chaotic-motion](2026-05-20-hold-extrap-positive-feedback-chaotic-motion.md) |
| 2026-05-20 | resolved | off-plan | MPC warm-start deadlock escape — failure-driven invalidation breaks chronic-CTE lockup (1769-tick hardware lockup → 21-tick worst burst; offline reproduction within 0.5 pp) | [2026-05-20-mpc-warmstart-deadlock-escape](2026-05-20-mpc-warmstart-deadlock-escape.md) |
| 2026-05-19 | resolved | standalone | SpaceMouse resilient connection handling — keep-alive + throttled-recurring WARNING + auto-reconnect + home-to-ACTIVE on dropout (mocap-pattern transfer) | [2026-05-19-spacemouse-resilient-connection-handling](2026-05-19-spacemouse-resilient-connection-handling.md) |
| 2026-05-19 | tuned | hardware-bringup | CAN-loss fault-response safety inversion — blanket "ERROR ⇒ stow+IDLE" de-energises healthy holding actuators on reconnect | [2026-05-19-can-loss-fault-response-safety-inversion](2026-05-19-can-loss-fault-response-safety-inversion.md) |
| 2026-05-19 | tuned | hardware-bringup | Finding B — the 68 s motor_pos=None was a motor_guard GuardMode.ESTOP sticky latch, NOT a CAN dropout (shared root cause with Finding A) | [2026-05-19-findingb-motor-guard-estop-latch-observability](2026-05-19-findingb-motor-guard-estop-latch-observability.md) |
| 2026-05-18 | resolved | standalone | Mocap QTM connection logging — state-transition + throttled-recurring WARNING, stale qtm_host→mDNS, unbounded-connect bug | [2026-05-18-mocap-qtm-connection-logging](2026-05-18-mocap-qtm-connection-logging.md) |
| 2026-05-18 | tuned | hardware-bringup | z=30 solve-failure root cause — motor_pos=None after CAN drop evades both telemetry watchdogs (Finding 3 resolved) | [2026-05-18-z30-solve-failure-motor-pos-none-watchdog-gap](2026-05-18-z30-solve-failure-motor-pos-none-watchdog-gap.md) |
| 2026-05-18 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 hardware obligations | Hardware bringup — T-H-T2b-1 + T-H-T2a-1 telemetry-stale cascade validation (+2 harness bugs; z=30 solve-failure resolved — see linked entry) | [2026-05-18-hardware-bringup-t2b1-t2a1-cascade-validation](2026-05-18-hardware-bringup-t2b1-t2a1-cascade-validation.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 8 (bugfix) | Runner — on_target_override hook treats None as "keep tc" (Tier 3b bugfix) | [2026-05-12-tier3b-hook-bugfix](2026-05-12-tier3b-hook-bugfix.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 8 (final) | MPC sad-path coverage — Phase 8 Tier 3b time pathologies, resource exhaustion, hooks, races | [2026-05-12-tier3b-time-pathologies](2026-05-12-tier3b-time-pathologies.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 7 (bugfix) | Tier 3a follow-up bugfix — quintic T<=0 guard + DIAG_SCHEMA_CONTRACT | [2026-05-12-tier3a-fuzz-bugfix](2026-05-12-tier3a-fuzz-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 7 | MPC sad-path coverage — Phase 7 Tier 3a numerical + schema fuzz | [2026-05-11-tier3a-numerical-schema-fuzz](2026-05-11-tier3a-numerical-schema-fuzz.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 6 (bugfix) | ZMQ recv resilience — Tier 2c follow-up bugfix (MpcTargetIPC + ZmqTargetSource) | [2026-05-11-tier2c-zmq-recv-resilience-bugfix](2026-05-11-tier2c-zmq-recv-resilience-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 6 | MPC sad-path coverage — Phase 6 Tier 2c ZMQ corruption (real-msgpack harness) | [2026-05-11-tier2c-zmq-corruption](2026-05-11-tier2c-zmq-corruption.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 (bugfix) | HardwarePlant set_pose — once-only singular-FF warning (Tier 2b bugfix) | [2026-05-11-tier2b-set-pose-singular-ff-bugfix](2026-05-11-tier2b-set-pose-singular-ff-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 | MPC sad-path coverage — Phase 5 Tier 2b HardwarePlant telemetry-staleness, set_pose FF, cold-start | [2026-05-11-tier2b-hardware-plant-telemetry-ff](2026-05-11-tier2b-hardware-plant-telemetry-ff.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 4 | MPC sad-path coverage — Phase 4 Tier 2a HardwarePlant FK degradation, singular Jacobian, frozen motor | [2026-05-11-tier2a-hardware-plant-fk-degradation](2026-05-11-tier2a-hardware-plant-fk-degradation.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 3 (bugfix) | MPC fallback — sanitize non-finite q_cur / q_dot at _handle_failure entry (Tier 1c bugfix) | [2026-05-11-tier1c-input-fuzz-bugfix](2026-05-11-tier1c-input-fuzz-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 3 | MPC sad-path coverage — Phase 3 Tier 1c NaN/Inf input fuzz on solve() | [2026-05-11-tier1c-input-fuzz](2026-05-11-tier1c-input-fuzz.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 2 | MPC sad-path coverage — Phase 2 Tier 1b fallback escalation cascade + cold-start IK budget | [2026-05-11-tier1b-fallback-escalation-cascade](2026-05-11-tier1b-fallback-escalation-cascade.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 1 | MPC sad-path coverage — Phase 1 Tier 1a real IPOPT exit codes + fallback-keyword matrix | [2026-05-11-tier1a-real-solver-failures](2026-05-11-tier1a-real-solver-failures.md) |
| 2026-05-10 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 0 | Scheduler — begin_return raises S3 instead of overwriting _next_event (Plan 2 Phase 0, bug 2 of 2) | [2026-05-10-scheduler-begin-return-s3-overwrite](2026-05-10-scheduler-begin-return-s3-overwrite.md) |
| 2026-05-10 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 0 | Scheduler — cancel_next during TRANSITIONING raises (Plan 2 Phase 0, bug 1 of 2) | [2026-05-10-scheduler-cancel-next-during-transitioning](2026-05-10-scheduler-cancel-next-during-transitioning.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 8 (final) | MPC Tier-0 contracts — Phase 8 CI hypothesis profiles wired (Plan 1 closes) | [2026-05-10-mpc-tier0-phase-8-ci-hypothesis-profiles](2026-05-10-mpc-tier0-phase-8-ci-hypothesis-profiles.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 7 | K1–K6 reference contract — Phase 7 hypothesis retroactive expansion | [2026-05-10-k1-k6-hypothesis-phase-7-retroactive-expansion](2026-05-10-k1-k6-hypothesis-phase-7-retroactive-expansion.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 6 | Plant-interface contract — Phase 6 enforcement of P3, P4 | [2026-05-10-plant-interface-contract-phase-6-p3-p4-enforcement](2026-05-10-plant-interface-contract-phase-6-p3-p4-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 5 | Plant-interface contract — Phase 5 enforcement of P1, P2 | [2026-05-09-plant-interface-contract-phase-5-p1-p2-enforcement](2026-05-09-plant-interface-contract-phase-5-p1-p2-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 4 | Plant-interface contract — Phase 4 audit and P1–P4 draft | [2026-05-09-plant-interface-contract-phase-4-audit](2026-05-09-plant-interface-contract-phase-4-audit.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 3 | Scheduler contract — Phase 3 enforcement of S4, S5, S6 + RuleBasedStateMachine | [2026-05-09-scheduler-contract-phase-3-s4-s6-enforcement](2026-05-09-scheduler-contract-phase-3-s4-s6-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 2 | Scheduler contract — Phase 2 enforcement of S1, S2, S3 | [2026-05-09-scheduler-contract-phase-2-s1-s3-enforcement](2026-05-09-scheduler-contract-phase-2-s1-s3-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 1 | Scheduler contract — Phase 1 audit and S1–S6 draft | [2026-05-09-scheduler-contract-phase-1-audit](2026-05-09-scheduler-contract-phase-1-audit.md) |
| 2026-05-08 | resolved | pr-3b-persistent-enable | Friction-FF platform limit cycle — diagnosis and smooth-gate fix | [2026-05-08-friction-ff-platform-limit-cycle](2026-05-08-friction-ff-platform-limit-cycle.md) |
| 2026-04-27 | tuned | post-per-leg-gains-deadband-session | Friction feedforward — bench characterisation, model fit, and FF validation | [2026-04-27-friction-feedforward-bench-validation](2026-04-27-friction-feedforward-bench-validation.md) |
| 2026-04-23 | resolved | hardware-bringup — GC-pause elimination on the MPC 40 Hz hot loop | Hot-loop zero-allocation contract (W1 inventory → contract → enforcement → fixes) | [2026-04-23-hot-loop-zero-allocation-contract](2026-04-23-hot-loop-zero-allocation-contract.md) |
| 2026-04-20 | tuned | post-per-leg-gains-deadband-session | Motion-onset dead-time — cogging-torque-first investigation (continued in 2026-04-27 entry) | [2026-04-20-motion-onset-dead-time-fix](2026-04-20-motion-onset-dead-time-fix.md) |
| 2026-04-20 | resolved | hardware-bringup — platform-only dynamic-motion entry point | Add `--toss-motion` hardware test and `controller/ballistics` helpers | [2026-04-20-toss-motion-hardware-test](2026-04-20-toss-motion-hardware-test.md) |
| 2026-04-20 | resolved | | Add DOCUMENTATION_GUIDE.md as single reference for documentation architecture | [2026-04-20-documentation-guide-single-reference](2026-04-20-documentation-guide-single-reference.md) |
| 2026-04-20 | resolved | hardware-bringup — Phase 4 (moderate motions) | K1–K6 reference-feasibility contract resolves MPC_OVERSHOOT_SATURATION | [2026-04-20-k1-k6-reference-feasibility-resolution](2026-04-20-k1-k6-reference-feasibility-resolution.md) |
| 2026-04-20 | resolved | hardware-bringup — sim-side unlock for Phase 6/7 | Migrate sim catch sources to K1–K6 reference-feasibility contract and lower sim v_max to 500 mm/s | [2026-04-20-sim-catch-source-k1-k6-migration](2026-04-20-sim-catch-source-k1-k6-migration.md) |
| 2026-04-19 | resolved | hardware-bringup — Phase 4 (moderate motions) | Bundle A quintic-ref settling lag + ZMQ live-twist feedback trap | [2026-04-19-bundle-a-quintic-ref-settling-and-live-twist-trap](2026-04-19-bundle-a-quintic-ref-settling-and-live-twist-trap.md) |
| 2026-04-19 | in-progress | hardware-bringup — Phase 4 (moderate motions) | Bundle A — ref-from-plant-state on move transitions (MPC overshoot-saturation fix) | [2026-04-19-bundle-a-mpc-overshoot-saturation-fix](2026-04-19-bundle-a-mpc-overshoot-saturation-fix.md) |
| 2026-04-19 | resolved | post-per-leg-gains-deadband-session | Leg 1 pose-dependent hold-phase twitch at (0,-100,200) | [2026-04-19-leg1-pose-dependent-hold-twitch](2026-04-19-leg1-pose-dependent-hold-twitch.md) |
| 2026-04-18 | tuned | STANDBY-mode MPC (post-64742f2 fallback walk-forward) — tail-latency attribution | MPC-loop overhead spikes trigger fallback bursts; Fixes A+B+C landed (a89a4dd, b5d83df), chronic-CTE phase resolved by warm-start deadlock fix (2026-05-20), singleton-emission jerk tracked separately | [2026-04-18-mpc-overhead-spikes-fallback-bursts](2026-04-18-mpc-overhead-spikes-fallback-bursts.md) |
| 2026-04-18 | in-progress | post-walk-forward-fallback-multiaxis-session-2 | Hold fighting + motion-onset jitter — residual hardware jitter downstream of clean MPC | [2026-04-18-hold-fighting-motion-onset-jitter](2026-04-18-hold-fighting-motion-onset-jitter.md) |
| 2026-04-18 | in-progress | post-walk-forward-fallback-multiaxis-session | Move 5: MPC overshoot-recovery stall + plant-collapse misclassification | [2026-04-18-move5-overshoot-stall-and-plant-collapse](2026-04-18-move5-overshoot-stall-and-plant-collapse.md) |
| 2026-04-17 | in-progress | STANDBY-mode multi-axis MPC | MPC fallback-command sawtooth produces visible stutter on off-Active multi-axis moves | [2026-04-17-mpc-fallback-cmd-sawtooth-stutter](2026-04-17-mpc-fallback-cmd-sawtooth-stutter.md) |
| 2026-04-15 | resolved | | ZMQ telemetry stale on :5556 — RECONNECT_IVL fix and staleness E-STOP | [2026-04-15-zmq-telemetry-stale-reconnect-ivl](2026-04-15-zmq-telemetry-stale-reconnect-ivl.md) |
| 2026-04-01 | resolved | 4.1 | Cold-hold fallback commanded stroke minimum | [2026-04-01-cold-hold-fallback-stroke-minimum](2026-04-01-cold-hold-fallback-stroke-minimum.md) |
| 2026-03-30 | resolved | 3.1 | Velocity feedforward oscillation | [2026-03-30-velocity-feedforward-oscillation](2026-03-30-velocity-feedforward-oscillation.md) |
