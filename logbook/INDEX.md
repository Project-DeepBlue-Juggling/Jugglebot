# Engineering Logbook

Structured record of investigations: symptom → diagnosis → fix → outcome.

Browse with `/logbook`, create entries with `/investigate` or `/logbook --new <title>`.

| Date | Status | Phase | Title | Entry |
|------|--------|-------|-------|-------|
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 3 VALIDATED (trace + checker contract fix) | **Phase 3 real-ordering trace VALIDATED on hardware; the dry checker's no-ball assumption corrected.** Three captures over two ACTIVATE cycles: all cross-process ORDERING invariants pass (DT-1/2/5/6/8/10/11). A ball/no-ball A/B confirmed DT-9 (stroke↔BALL_IN_FLIGHT) + DT-12 as ball-in-hand artifacts — both flip FAIL→PASS with the hand empty (the reframe: DT-9 first looked like a real Phase-7-class timing bug, but `toss_sequencer.py:824`'s `throw_stroke_seen OR ball_track_confirmed` fired early on the in-hand ball; retired by the clean re-run). ROOT of the residual fails: a *self-announced* toss always seeds its OWN predicted track (`ball_prediction_node` synthesises a `destination=jugglebot` `BallState` from the announcement — `tracking=0`, no mocap detection, drives the catch pre-position), so DT-7/DT-13/DT-14's "no ball ⇒ quiet tracker" assumption is unpassable by construction. Fixed as a CONTRACT correction (not a weakening): DT-14 keys on `tracking`/destination (fails only on a real `tracking=1`/foreign detection — adversarially verified it still catches the real-ball run's `id=35`); DT-7 drops "exactly 1" for "whole stream within 30 mm of (0,0,170)" (stronger — catches a real-ball reach-wander); DT-13 keeps streaming/mode, drops the mis-specified return-to-hold. Corrected `check --dry` on `toss_trace_2026-07-25_15-24-25` = **12 PASS / 0 FAIL**. Two real tosses occurred (a ball present) — both MISSED — early Phase-5 T0/T1 data. | [2026-07-25-toss-phase3-trace-validated](2026-07-25-toss-phase3-trace-validated.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 4 (Tier 8b displaced throw) | **Tier 8b lands (behind `JB_OP_TOSS_TIER='8a'`): tilt-aimed displaced throw from a config site A to a nominated catch B ≤70 mm away.** `tilt_to_throw` ported to the production `tilt_geometry` (sim re-exports); `compute_release_state_tilted` = swing-compensated pre-tilt at A + tilt-aimed launch with a loud `ThrowTiltInfeasible` clamp gate; the coordinator pre-positions TILTED at A, raises a new `catch/pretilt_hold` gate (else the stock announcement pre-tilt translates A→B and un-tilts BEFORE release — aim destroyed), and publishes the ONE deferred A→B reach at `t_release`. **Salvaged**: the Fable-5 build workflow died on a spend limit leaving a half-wired coordinator + absent safety gate + zero tests + a latent None-deref; finished + tested on Opus 4.8. The re-run adversarial review caught a **BLOCKING** gap invisible to both mocked tests (they monkeypatched positioning) and the sim gate (it models the correct pre-tilt-at-A): production `_position_platform_for_toss` sent the LEVEL pose at B for all tiers, orphaning the tilted A pose — an 8b hardware toss would have thrown straight up. Fixed + pinned. 8b binding ring (50 mm, T=0.80) PASS 9/9, 0 feas_viol; asymmetry map (detach column, T=0.80 s) shows a +y-hemisphere weakness (−y+−x seat to 100 mm; +y/NW fail at 70 mm) — the data for the T4-at-100 mm fork. 8a + reload byte-identical. ci-deep + sweep triples in the entry. | [2026-07-25-toss-phase4-tier8b-displaced-throw](2026-07-25-toss-phase4-tier8b-displaced-throw.md) |
| 2026-07-25 | prepared-pending-hardware | MVP trajectory bringup — Phase 8 / single-ball toss Phase 3 prep (trace harness + runbook) | **Phase 3 PREPARED (not executed): real-ordering trace harness + operator runbook land.** `tests/hardware/toss_trace_recorder.py` — observe-only rclpy recorder (16 wires, correct QoS, `/rosout` as the service-evidence channel, `uptime_ms` via link_status) + pure-stdlib offline checker: 14 dry-trace + 4 reject invariants (prime_hold→armed→announcement→dispatch orderings, no-prime-under-hold, single-dispatch, hold-released-last), 5 ms same-wait-set epsilon band reported AMBIGUOUS never silently PASS; self-verified both ways on synthetic traces (injected violations fail exactly their invariants, no collateral). PLAN ERROR FOUND AND AMENDED: the plan's "unpowered bench" dry trace is unreachable (activation faults + 10 Hz mode republish ⇒ `REJECTED_WRONG_MODE` before the waiver matters) — the runbook specifies a POWERED no-ball bench, framing the dry capture honestly as the first-ever real empty-cup throw stroke (≈3.93 m/s, T=0.8 s) and recommending it fold into the T0 sitting; un-waived `REJECTED_NO_BALL` capture runs FIRST; waiver set/unset discipline (this runbook is the one sanctioned set point); `toss_mocap_body` QTM bench task included. `session_phase8_toss_trace.md` mirrors the phase-7 runbook voice. Not pytest-collected. Operator captures pending. | [2026-07-25-toss-phase3-prep-trace-harness](2026-07-25-toss-phase3-prep-trace-harness.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 2 (sim gate + Tier-8a sweep) | **`sim/toss_gate.py` lands; the Tier-8a sweep PASSES both binding bands** (plan's 2–3 m/s: 10/10 at all 12 points; the amended hardware-relevant T=0.80@z170 band: 10/10 ×4 + 9/10 ×1) with 0 feasibility violations and 100% pump acceptance (45,950 frames, asserted non-vacuously). Advisory long-flight tail (T≥0.95 s) FAILS under the 1% placeholder release noise — drift ∝ v·T vs the 80 mm envelope class, worst landing 90.7 mm; signal for T2's 1.1 s rung, re-gated after T0 measures real scatter. Production-in-the-loop: real `build_move`→emitter→pump pre-position (worst err 0.08 mm), real `toss_release` announcement (worst −43 ms), real `build_catch` + arm-and-forget catch. Review caught pre-sweep: BLOCKING silent-dead-arm `core_clean` (ball into the statically-parked cup gate-passed; now requires `catch_armed`), vacuous empty-band PASS, the pump's silent `(None,None)` path making the knot-acceptance invariant vacuous (also inherited in `reload_gate` — follow-up). Kinematic ejector = `Ball.release` (mode-correct for the `contact_carry=False` gating plant; plan-named `ballistic_release` runs verbatim in the diag column’s opt-in kinematic mode; the recorded sweep’s diag column ran detach: 8/20 seat-caught, landing err ≤4.2 mm — the seat, not the aim, is the knife-edge). `gate_common.py` extraction behaviour-neutral (`test_reload_gate.py` unmodified, green). | [2026-07-25-toss-phase2-sim-gate-sweep](2026-07-25-toss-phase2-sim-gate-sweep.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 1 (action + sequencer + coordinator) | **Toss Phase 1 lands software-complete: `Toss.action` + `toss_sequencer` FSM + ball-ops coordinator (Tier 8a; 8b = `REJECTED_TIER`, config-selected).** `reload_coordinator_node` extends into a two-action ball-ops node (measured shared surface 76.5% > the 70% threshold; cross-action busy gate claimed under `_lock` closes a pre-existing reload accept→install race); the control-system walk-through confirms ZERO `trajectory_node` changes for 8a; firmware ground truth dissolves the prime-timing fear (throw stroke 0→9.959 rev starts at the post-catch rest and ends at the catch-prime position). NEW hazard found and closed: `catch_coordinator`'s armed-edge/retry-tick auto-prime would move the ball-laden hand mid-toss — explicit `catch/prime_hold` gate (default-off, reload bit-identical) + raise-side ordering (hold one tick before the bundle, `prime_dispatched` belt). Adversarial panel caught a BLOCKING frame bug the unit tests had enshrined (mocap arrival check vs a nonexistent 'jugglebot' QTM body, target global vs measurement platform_start — the z double-add class in the verification path; now config-keyed `toss_mocap_body`, default disabled, platform_start-frame compare) + phantom-track poisoning, NaN-goal post-arm detonation, stroke-signature 15→40 rev/s. Release math: one STOW→global conversion point, `HAND_THROW_OFFSET_MM` 58.044, full ballistic inverse (T=0.8 s ⇒ 3930.82 mm/s, apex 787.85 mm). Thrower-agnosticism verified with the sharper invariant: gates are `target_id=='jugglebot'`, never `thrower_name` — an empty target_id is silently uncatchable (integration test drives the real tracker+coordinator path). tests/ros 1157 passed; full-suite triple in the entry. | [2026-07-25-toss-phase1-action-sequencer-coordinator](2026-07-25-toss-phase1-action-sequencer-coordinator.md) |
| 2026-07-25 | resolved | MVP trajectory bringup — Phase 8 / single-ball toss Phase 0 (post-merge reconciliation) | **Toss Phase 0 closes: post-merge doc pointers reconciled, suite green as the gate.** The 4 never-indexed ladder entries (2026-06-26 contact-mechanics, 3× 2026-07-03 motion-quality arc) get their INDEX rows backfilled (129 → 134 with this row; ordering + link integrity script-verified); `sim/JUGGLE_DEMO.md` gains a paused-offline-demo vs merged-online-ladder status banner; `plans/active/bb-led-two-ball-juggle-demo.md` gains a merged-location note (20fcc9e, branch + worktree retired); toss plan phase table → COMPLETE. No code changes. Full suite (`pytest tests/ -q`, 2026-07-25): 3153 passed / 3 xfailed in 1325.20 s — identical counts to the post-merge baseline. First phase of the 2026-07-25 unattended workflow-orchestrated build run. | [2026-07-25-toss-phase0-postmerge-reconciliation](2026-07-25-toss-phase0-postmerge-reconciliation.md) |
| 2026-07-24 | resolved | MVP trajectory bringup — Phase 8 kickoff (branch merge + toss plan) | **BB juggle branch merged (`20fcc9e`); Phase 8 expanded into `plans/active/single-ball-toss.md`.** `demo/bb-led-two-ball-juggle` (71 commits: online tilt-ladder Rungs 0–2b, controller/demo planner, 16 test files, 10 probes, ladder logbook/plans) lands on `mvp-trajectory-bringup`; conflict surface = 2 doc tables + 1 comment + ONE semantic break (reload_gate vs the multi-ball BallManager rework, fixed in-merge; suite 3153 passed / 3 xfailed post-fix). New plan: two-tier `Toss.action` — goal = nominated catch state ⟨position, flight time⟩; 8a toss-at-position (level platform), 8b tilt-aimed displaced throw→catch; production-in-the-loop `sim/toss_gate.py`; hardware T0–T4 with T0 = bench release characterisation. Worktree + branch retired. | [2026-07-24-phase8-kickoff-bb-branch-merge.md](2026-07-24-phase8-kickoff-bb-branch-merge.md) |
| 2026-07-24 | resolved | MVP trajectory bringup — Phase 7 reload: fourth hardware session (open-loop pivot) | **Phase-7 fourth sitting — 15/19 caught; platform goes OPEN-LOOP, hand ladders learn telemetry, stale-track guards, log truth.** Fresh-bridge session, bag `09-07-53`: 24 goals = 2 instant rejects (WRONG_MODE, BB_BUSY), 2 BB refusals (PV_STALE + the SECOND yaw NOT_SETTLED in two sittings), 1 FALSE `ABORTED_PRIME_FAILED`, and 19 real throws (~15 caught by settle-current). Four-phase Opus workflow (3 investigators → adversarial verifier → implementer → auditor). Confirmed: (1) the one "platform moved at the last second" miss = Ball 30 — the corrupt track's target sweep crossed the 80 mm envelope at 78 mm ONCE, accepted at land−0.67 s, re-anchored the reach 78 mm off a centre-landing ball (all 18 other throws <2 mm late motion); (2) the "hand jump up-down then abort" = all four prime-ladder acks LIED while each frame transmitted — the ladder yanked its own live ascent four times then reported a false abort; (3) the arm-failed WARN spam = the ack epidemic at 59 % on kind-1, all attempts eventually armed, re-arms benign (absolute wall-time stroke); (4) the 4th-attempt no-throw = BB PV_STALE refusal, with ~62 mm pre-throw platform motion from the reactive path chasing the corrupt track. REFUTED: leftover prior-attempt tracks (census: every corrupt track is the current ball's own, dead ~10 s before the next throw); garbage-timed arms (all 21 arms sane, stroke at land+0.10..0.15 uniformly). The 3 "unknown" misses are inherent limits, NOT bugs: one ball 0.31 s late (hand already retracted), one 105 mm BB scatter, one unrecoverable. FIXES: config-backed open-loop reload platform (announcement pre-tilt held; reactive platform targets suppressed; hand-arm stays tracker-timed — announced landing runs early); telemetry-verified prime/retract ladders (re-dispatch ONLY on positively-stationary hand; probe-validated thresholds incl. park-band qualifiers against 5.39 rev/s top-park vel noise); ±0.75 s arrival-window arm guard + prior-id exclusion; arm re-dispatch cap 2; ONE authoritative outcome line per reload (was ZERO) + spam demotions. DEFERRED: ERR_TIMEOUT epidemic (own investigation), BB yaw/PV refusals (BB-side), late/scatter misses (ball-held-sensor era). ADDENDUM (post-session): announced-landing timing error measured across both sittings' bags — pooled n=20, bias +0.115 s ±0.017 (tracker-mediated), core σ ≈ 0.035 s, discrete ~+0.31 s outlier mode ONE per sitting (+0.314/+0.320, likely a BB release mode → BB-side review); **BB's 44 ms firmware release-timing constant STAYS untouched** (tracker-latency confound, single-ToF release-vs-flight ambiguity, nothing consumes announced time critically today) — re-tune pre-registered for the ball-held-sensor era (source-timestamped contact, ≥2 ToFs, explain the outlier mode first). | [2026-07-24-phase7-fourth-sitting-openloop-telemetry-ladders.md](2026-07-24-phase7-fourth-sitting-openloop-telemetry-ladders.md) |
| 2026-07-24 | in-progress | MVP trajectory bringup — GUI observability | **BB telemetry restored to the GUI charts — 9-axis robot_state + bus V/I on the Diagnostic wire (UDP protocol v4).** The BB Pitch/BB Hand charts and fault dots had been structurally dead since the can-bridge cutover: they read `robot_state.motor_states[7]/[8]`, but the bridge published only 7 axes (can_node's 9-axis `ALL_AXES` layout was narrowed at the handoff). Fix: bridge appends BB pitch/hand entries (pos/vel from the 100 Hz `BB_AXIS_ESTIMATES`, state/errors/Iq/temps/bus V+I from the 1 Hz BB DIAGNOSTIC stash) under all-or-nothing honest-silence gating (`heartbeat_seen` + not stale + 3 s/0.5 s freshness — dark BB ⇒ 7 axes, never phantom zeros or frozen flatlines). DIAGNOSTIC frame grew 36→40 B with `bus_current` (+`heartbeat_seen` flags bit1) — UDP PROTOCOL_VERSION 3→4, FW_VERSION 2→3, flashed 2026-07-24 — which also revives the legs'/hand's silently-dead "Bus I" chart signal. Zero GUI changes needed to revive the charts (positional indexing); one GUI fix (Opus review LOW) clears BB fault dots on the 9→7 shrink so a fault can't freeze red. Full suite 2954 pass (+1 known alloc flake, passes isolated); live wire probe: v4 decodes 0-error at 100 Hz, legs `seen=True`, dark BB `seen=False` all-zero — the exact phantom case the gate refuses. OPEN: powered-BB sitting to see the charts render live, then flip to resolved. | [2026-07-24-bb-robot-state-restoration.md](2026-07-24-bb-robot-state-restoration.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: third hardware session (catch_vel_scale sweep) | **Phase-7 third sitting — FIRST REAL CATCHES (8/10 at scale 0.8); false MISSED_INFEASIBLE verdicts, prime-retry stutter, hand-dispatch epidemic, just-in-time pre-tilt — all fixed.** Fresh-reboot session, bag `17-51-09`: 13 goals, 2 throws aborted BY BB (yaw `THROW_ABORTED_NOT_SETTLED` — never flew), 8 of the 10 real throws caught (hand holding-current +0.8 A). Anomaly 1: all 12 goals read `MISSED_INFEASIBLE_WORKSPACE` — the corrupt split-track drove 27–46 envelope WORKSPACE rejects per flight and `note_catch_feasibility` was last-writer-wins, while the plausibility gate rightly vetoed the corrupt track's below-floor CAUGHT; fix = infeasibility latches only while NO target was ever accepted (gate on `_throw_sent`, not phase — the pre-tilt accept lands during AIMING). Anomaly 2: 5/12 prime ascents stuttered — the 0.5 s retry tick re-dispatched 0.5 s into a 0.7–1.05 s ascent whose ack had failed (every stall phase-locked to the tick grid); fix = 1.2 s dispatch-anchored in-flight window shared across BOTH prime owners via new `catch/prime_dispatched` topic (ack channel demonstrably lies — frames observed transmitted after failed acks). Anomaly 3: goal-1 `ABORTED_PRIME_FAILED` — the operator's ODrive settle-window theory REFUTED by the bag (hand axis IDLE throughout); real cause = `HAND_TRAJ_CMD: ERR_TIMEOUT` bridge CAN-enqueue failures at ~40–60 % per call ALL SESSION on a fresh reboot (7/12 safe-abort retracts also failed silently); mitigation = 4-attempt dispatch ladders (prime + retract); the epidemic itself is a NEW OPEN investigation (suspect FlexCAN TX contention vs partner-gate; `[canhealth] tx_gated` disambiguates). Anomaly 4: pre-tilt arrival was scheduled exactly AT landing (one 3.9 s min-jerk crawl, tilt >1° off until ~0.3 s before contact); fix = arrival at landing −1.5 s (clamped ≥ now+1.0). Also: `catch_vel_scale` default locked to 0.8 (`JB_OP_CATCH_VEL_SCALE_DEFAULT`), GUI RELOAD button (Trigger relay in orchestrator — Foxy rosbridge has no action transport). OPEN: dispatch epidemic; BB yaw-settle aborts (+ FSM doesn't consume throw completion); tracker corruption (ball-held sensor imminent = clean verdict source); envelope centred on hold pose rejects real off-centre refinements (A07 caught 124 mm off-centre on feed-forward alone). | [2026-07-23-phase7-third-sitting-verdicts-stutter-pretilt.md](2026-07-23-phase7-third-sitting-verdicts-stutter-pretilt.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: second hardware session (re-test) | **Phase-7 re-test — catch strokes erased by a prime-vs-arm race; tracker split-track corruption fakes CAUGHT; pre-tilt + catch_vel_scale land.** The morning's z fix WORKED (platform reached+tilted on all 6 throws; every ball met the hand) but 0 real catches: strokes on attempts 2/3/5 were ERASED by the coordinator's same-tick re-prime racing the catch arm on the Platform Teensy's single packed queue (kind-3 and kind-1 handlers both clear+repack — last writer wins); 5/6 reloads spuriously reported CAUGHT off corrupt tracker tracks coasting BELOW THE FLOOR near BB while the real ball spawned as a separate untagged track (landing 29-39 mm from centre); the 80 mm envelope did its job holding the good reach. Fixes: NO smooth-move ever sent while a catch sequence is live (prime = armed edge + off-path retry timer with a 1.5 s quiet window); arm one-shot latches only on actual dispatch; CAUGHT plausibility gate (200 mm xy / 150 mm z of the catch point) + pre-existing-flight exclusion make outcomes honest; prime dispatch retries once (attempt 0 died to a transient). NEW: pre-tilt at announcement (~3.9 s early, predicted from the announced landing state, single-sourced pose math, never arms the hand) and the operator's catch_vel_scale action knob (= firmware CATCH_VEL_RATIO re-tune without reflash; clamp [0.3, 1.5], resets on disarm). OPEN: the tracker split-track corruption itself; the Teensy uptime reboot experiment (67.6 h at this bag). | [2026-07-23-phase7-retest-stroke-race-tracker-corruption.md](2026-07-23-phase7-retest-stroke-race-tracker-corruption.md) |
| 2026-07-23 | resolved | MVP trajectory bringup — Phase 7 reload: first hardware validation of the action-driven catch | **Phase-7 reload first hardware session — z frame double-add rejected every catch reach; prime raced the flight; abort retract was a silent no-op.** 7a passed; 7b was a coin-flip (hand prime vs 0.878 s flight); 7c never tilted — every `catch/dynamic_target` was gate-rejected `leg 3 out of stroke (275.2 mm)`. Root cause 1: `trajectory_node` added the 170 mm active-z to an already-STOW-relative wire (the pre-registered Phase-7 open question — the gate did its job); the wire z≈171 is now declared normative in `DynamicTargetCommand.msg`, the lift removed, and an 80 mm reach envelope enforced (`catch_reach_envelope_mm`) so the whole frame-regression class rejects loudly. Root cause 2: the prime waited for a tracked ball (7b) or the throw-accept (7c); it now fires at COMMAND time (`ACTION_PRIME_HAND` + the `catch/armed` rising edge — measured smooth-move 0.68–0.73 s (0.75 s predicted)). Root cause 3: arming results were ignored and BB's firmware countdown has NO abort opcode — the FSM now arms EVERYTHING before `bb/throw_at_target` (new PREPARING phase; prime/prepare failures abort with the throw unsent). Latent bugs fixed: the SAFE_ABORT retract targeted −0.1 rev (below the bridge's [0, 11.1] validation → silently rejected, hand stayed at top); a standing infeasibility finished the FSM mid-flight (tearing down the hand under the falling ball — it now resolves at settle, and the parked cup's two passive catches count as CAUGHT). OPEN: BB-ball landing estimates drift 435–605 mm/s toward BB (tracker issue, own session needed; the envelope bounds the chase meanwhile). | [2026-07-23-phase7-reload-first-hardware-session.md](2026-07-23-phase7-reload-first-hardware-session.md) |
| 2026-07-20 | resolved | MVP trajectory bringup — Phase 7 reload: action-driven catch + arm_catch latch (retire CATCH & SHELL) | **RELOAD becomes a self-contained action; the persistent CATCH mode is retired for a catch-armed latch (SHELL retired too; 809.08 cup-plane aim).** The operator questioned whether a bespoke CATCH control mode is needed "just to catch a ball" — it is not. CATCH did exactly one load-bearing thing: flip the `trajectory_node._on_dynamic_target` gate that lets the tracked-ball tilt reach `planner.build_catch`. So "RELOAD supersedes CATCH" = **raise that gate from the `jugglebot/reload` action via a catch-armed latch** (`trajectory/arm_catch`, mirrored on `catch/armed` which gates the hand), not from a mode the operator holds. The catch *mechanics* don't move; only the *trigger*. The action runs **within ACTIVE:TRAJECTORY** (streaming) — it can't copy LEVELLING's stream-suspend because the live catch IS a streaming trajectory — and it now **owns the platform + hand**: on throw-accept it proactively primes the hand to top (9.858 rev) + raises the latch; on CAUGHT it lowers the latch + re-centers (`go_home`, keeps the ball); on any abort it retracts the hand (≈0 rev) + re-centers. Hand-ownership split: the action owns the proactive prime + abort-retract; `catch_coordinator` keeps the reactive fire + gains but is **latch-gated** so it never arms on a stray ball. Phase-1 re-keyed the CATCH motion-transition safety logic (graceful decel-to-rest, `_catch_arrival_perf` freeze reset) to the latch edges via two shared helpers — every graceful stop seeded C2 off `_current_state()`, no discontinuity at the arm/disarm seams. **Ruled out**: the orchestrator-state form (re-introduces the mode-partition duplication for one gate); the LEVELLING stream-suspend pattern (RELOAD needs the live stream up). SHELL was a clean 1:1 GUI alias delete. The Q1 groundwork fix (`bdbd186`) aims BB at the 809.08 mm cup plane (centroid 744.3 + `HAND_CATCH_OFFSET_MM` 64.78), not the centroid — aiming low ate tilt/reach margin. Commits `bdbd186` (Q1) / `4572925` (SHELL) / `caea3ec` (latch) / `790e943` (action) / `5d5f4ae` (CATCH delete) + this docs phase. Scoped gates (`pytest tests/ros tests/motion -q`, 2026-07-21): Phase 1 1634 / Phase 2 1648 / Phase 3 1641 passed. Phase 5 hardware validation is operator-run + deferred. | [2026-07-20-reload-action-catch-latch](2026-07-20-reload-action-catch-latch.md) |
| 2026-07-18 | open | MVP trajectory bringup — wobble regression root cause | **Leg tracking lag grows MONOTONICALLY with can-bridge Teensy UPTIME — 10 ms at 2 min post-boot → ~40 ms at 1.15 h → 130–160 ms at 4.5 h → 230–250 ms at 24–30 h (saturating) — and the dominant term of the "silky vs bumpy" regression is HARDWARE/DRIVER STATE, not code (pending reboot confirmation; the one real code term, the mesh-lattice traverse shift, is ~11 %).** Four-agent sweep + telemetry mining: commit bisect found the real-but-minor mesh-lattice traverse change (~11 % of the effect per cascade sim); emit path proven bit-identical; deployment diff clean; vbus flat 45.0–45.3 V all sessions; temps anti-correlated; cadence/initial-state refuted; hold floors identical. The `uptime_ms` field cracked it: the healthy 10 ms session (bag `17-38-15`, devs 0.012–0.061 at 3.5 rev/s commands = the NOMINAL 40/0.20/0.32 cascade) ran 1.8 min after the guard-raise flash rebooted the Teensy; every session since sits on the uptime curve; Jetson up 13 days (its uptime is not the epoch). **If the pre-registered reboot experiment confirms: the 2026-07-16 "velocity-loop lag" root cause, the 1.0 rev guard raise, the chase-ceiling 2.2–2.7 rev/s, the unshaped-traverse "out of envelope", the retime-OFF decision, and the accel-FF sizing premise were all measured on a degraded plant** — the post-flash "validation" was actually the reboot fixing the lag. Isolation protocol pre-registered: Arm 0 degraded reference → Arm A `/reboot_odrives` (ODrive-side) → Arm B `ip link` bounce of the J↔T dongle (Jetson transport) → Arm C Teensy reboot — B before C because a Teensy reboot also bounces the PHY; PASS = lag ≤20 ms + unshaped traverse ~0.1–0.2 rev no-latch. Firmware audited for the accumulator (see entry Addendum). | [2026-07-18-teensy-uptime-tracking-degradation.md](2026-07-18-teensy-uptime-tracking-degradation.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — retime model first hardware A/B | **The retiming model's HONEST durations exceed the TRACKING envelope on lean traverses — lean-0.6 x-traverse deviation 0.45 → 0.73 rev (above the ~0.6 in-move ABORT line); `retime_model` shipped FALSE the same day it shipped ON.** Operator's second session (bag `19-32-03`): lean-0.0 latched the same cliff a third time (1.076, unchanged); lean-0.6 "smoother but not as smooth as yesterday". Both operator hypotheses KILLED by data: compute bottleneck NO (emit gaps ≤ 28.8 ms), ringing NO (post-arrival zero-crossings 1–2 both days, no oscillation train). Real cause: yesterday's lean-0.6 traverses were timed by the LEGACY loop whose overshoot is largest exactly on high-lean moves (peak jerk 12000/11534 of the 30000 budget ≈ 2× padding — far beyond the SPE corpus's +1.2–14.7 % tail); the model prices them honestly (27406/27276, +52–62 % peak accel, 1.60→1.42 s) and the velocity-loop tracking deficit converts the difference into deviation. Non-traverse plans near-identical; their dev deltas (+6–80 %, median ~+43 %, small-denominator noisy) are plant-side/sticky-day, not plan changes. **Key lesson: duration slop was load-bearing** — the gate's invariant is kinematic feasibility, NOT trackability, and "gate-feasible but untrackable" is the same class as the unshaped cliff. Fix: YAML `retime_model: false` + regen (legacy timing ALGORITHM restored — but per the 07-18 bisect the 1b mesh commit shifted the legacy lattice, so the post-OFF traverse plans jpk ~20928, not the validated 12000; shaped planning ~0.23 s on the batched gate, still 5× pre-SPE; re-enable = YAML flip + regen + colcon build + relaunch). Re-enable gated on accel FF or a tracking-aware duration floor; corpus gap noted (add the ±150 traverse family). | [2026-07-17-retime-model-tracking-envelope.md](2026-07-17-retime-model-tracking-envelope.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — S4 working point on hardware | **Two MAX_DEVIATION latches on the first post-SPE session were the KNOWN unshaped-traverse cliff, NOT an SPE regression** (bag `17-35-14`: both trips on the ±150 x-traverse, leg 1, dev 1.02–1.08 vs guard 1.0). Exoneration is numeric: matched moves' planned peak vel/acc/jerk **identical to the decimal** to the validated 2026-07-16 runs (diff per-move `peak_leg_*` diagnostics across sessions BEFORE reading code — the cheap high-power test); emit clean; retime model never engaged. Yesterday's own record had the same move at **0.936 rev = 94 % of guard**; today's +10–30 % at matched commands (identical peak chase vel, higher iq — first-moves-cold/stickier day) crossed the latch. **Both runs were unshaped**: `traj_ramp_battery.py --lean-gain` defaulted 0.0 = the EXPLICIT-OFF arm under the b6391c1 convention, so "defaults vs --lean-gain 0.0" compared identical arms and the shipped lean 0.6 (traverse ≤ ~0.45 rev — a LEGACY-TIMED figure; the retime model pushed it to 0.73 the same evening and shipped OFF, see the retime row above) never ran → battery default now **-1.0 = defer-to-config**. Margin lesson: **a guard margin must beat run-to-run variance, not zero** — 0.064 rev never did; unshaped ±150 traverses at jerk 30000 are OUT of envelope until accel FF lands (expected-latch framing in runbook + battery docstring). Working point stands, validated for the SHIPPED (lean 0.6) config only. | [2026-07-17-wobble-latch-unshaped-traverse.md](2026-07-17-wobble-latch-unshaped-traverse.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — shaped-planning efficiency | **Shaped `go_to_pose` planning ~1.1 s → ~91 ms (~12×) AND the binding leg-jerk constraint measured 7× more honestly — accuracy and speed were not in tension.** Three composing phases, all software-verified (hardware behaviour unchanged bit-for-bit from the gate; only the planned duration is shorter + planning faster). **1a (`4acaefe`):** numpy-vectorised twin of the shaped feasibility gate at the current n=200 mesh — pure parity refactor (sampler 7.1e-15/5.7e-14/4.5e-13; gate 1.41e-10 at equal mesh), per-pass **185.6 → 7.5 ms**. **1b (`54c1e75`):** `_SHAPED_VALIDATE_SAMPLES` **200 → 1600** (now affordable) — shaped FD-jerk under-measurement **23 % → 3 %** vs a 6400-ref; jerk-bound lateral moves plan **+4.9 % longer** = the intended fix for the silent under-stretch of the "sharp" lean; two plan hedges corrected as empirically FALSE (@6400 is a convergence reference NOT an acceptance oracle; only FD jerk is mesh-monotone, not vel/acc point-samples). **2 (`806e8fb`):** retiming-model duration search (`retime.py`; per-sample `u=1/T` polynomial, cap-free `T_ref`, monotone bisection, **ONE** verify pass through the UNCHANGED gate) behind `JB_TRAJ_RETIME_MODEL` (default ON — **flipped OFF the same evening** after the first hardware A/B: honest durations on lean traverses exceed the tracking envelope, see the retime-model row above) — shaped `build_move` **228.7 → 90.9 ms (2.52×)**, durations uniformly **+2.0 %** over optimum (legacy bisection +1.2–14.7 %), **0 verify rejections** over 54 optimality + 120 fuzz cases; every returned plan still gate-verified (proved from code). **Key insight:** vectorisation decoupled the mesh from the cost (the 1600 bump would have been ~300 ms/pass and unaffordable pre-1a); the model never bypasses the gate (it only *chooses* T). Builder deviations accepted: 1b's `_batched_at` re-anchoring of the parity tests after the clamp interaction; Phase-2 module/API naming drift (`retime.py`/`build_model`/`RetimeModel` vs the plan's `retime_model.py`). Full suite (`pytest tests/ -q`, 2026-07-17): 1a **2860/1 xfailed in 584.45 s**, 1b **2862/1 in 608.38 s**, 2 **2872/1 in 937.31 s**. Deployment: `colcon build --packages-select jugglebot` + relaunch (Python-only; YAML flag regenerated). | [2026-07-17-shaped-planning-efficiency-implemented](2026-07-17-shaped-planning-efficiency-implemented.md) |
| 2026-07-17 | resolved | MVP trajectory bringup — S4 CLOSED (step-5 persistence) | **S4 closed: the working point (1000, 5000, 30000) + lean_gain 0.6 persisted to YAML — with the GoToPose.srv field default (-1.0) that makes the gain actually reachable.** Operator picked vel=1000 (velocity was 14–23% utilized in the A/B — costs nothing, shortens a runaway's runway). The catch: an unset srv float64 reads 0.0 = EXPLICIT lean-off, so a YAML flip alone would have been silently unreachable for default-constructed requests — the srv field now defaults −1.0 (defer-to-config); explicit 0.0 stays the A/B baseline arm; pinned by test_shipped_trajectory_defaults_are_the_s4_working_point (regex-pins the srv default too) + the defer choreography tripwire. **Consequences: a relaunch now reverts to the HOT working point (runbook escape-hatch note updated — set_limits DOWN for cautious probing); default service moves are now SHAPED (~1.2–1.5 s planning until the efficiency work lands); go_home/hold/timed/follower stay unshaped; the battery instrument's --lean-gain default 0.0 does NOT pick up the config default (framed then as correct A/B semantics — **superseded 2026-07-17 evening: battery default now −1.0 = defer-to-config**, see the wobble-latch row above and the entry Addendum).** 8 mechanism tests re-pinned to the gentle limits their geometry was designed around. Deployment: colcon build BOTH jugglebot_interfaces AND jugglebot + relaunch. | [2026-07-17-s4-closed-working-point-persisted](2026-07-17-s4-closed-working-point-persisted.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — Phase 5 timed targets: operator-facing interface | **`trajectory/timed_target` now takes a relative `lead_time_s` instead of an absolute ROS-clock `arrival_time`** (operator-requested ahead of S5). The node anchors arrival internally at handler entry (`perf_counter + lead`, same clock domain as the emitter — no clock crossing left on the service path); non-positive leads flow through the single feasibility gate (loud `TOO_FAST` with the *achievable* `min_duration_s` + target_feedback), only non-finite values are handler-guarded (`UNREACHABLE`). The `/catch/dynamic_target` TOPIC and `TargetFeedback.arrival_time` (perf-domain, machine-to-machine) are deliberately unchanged; `_ros_time_to_perf` + clock-offset plumbing retained but consumer-less on the service path (docstrings say so). S5 session doc + runbook examples now typeable (`lead_time_s: 3.0`). **Deployment: `colcon build --packages-select jugglebot_interfaces jugglebot` + full relaunch (interface package changed).** `pytest tests/ros/ -q` (2026-07-16): **905 passed** incl. 2 new reject-path tests. | [2026-07-16-timed-target-lead-time](2026-07-16-timed-target-lead-time.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — S4 lean A/B: planning latency + boundary vel_ff step | **The lean-0.3 A/B pauses were planning COMPUTE (not motion) and the "sharp tilt" was a boundary vel_ff STEP — both operator hypotheses confirmed, both fixed.** Three-agent investigation (code map + bag forensics on the 929 s `2026-07-16_18-45-29` + offline profiling) on why `--lean-gain 0.3` felt worse than `0.0`. **(1) PAUSES = PLANNING.** Matched A/B at `(1500,5000,20000)` seq111-121 vs 122-132: between-move pause **1.70 → 4.33 s (+2.64 s), ~94 % planning** (settle grew only +0.13 s); offline `build_move` 0.73 → 2.6 s/move. Root chain: a shaped `_ShapedPlan` is barred from the fast vectorized gate (`feasibility.py:403-407` TypeError) → falls to the dense analytic gate, runs 6 `validate()` passes vs 2 (`_SHAPED_REFINE_ITERS=4` bisections), and `compute_jacobian` was **68 % of validate wall time** via `np.cross` numpy generic dispatch (3 Jacobians/sample). **(2) SHARP TILT = boundary vel_ff STEP.** Lean tilt ∝ base accel ⇒ tilt_rate ∝ base jerk, and a rest-to-rest quintic has `j(0)=j(T)=60d/T³ ≠ 0` ⇒ a **~70–182 mm/s (1.0–2.6 rev/s)** leg vel_ff step at every shaped-move boundary (**0.00** unshaped); bag-corroborated (+43 % peak iq 5.93→8.48 A at matched limits, 1.52× first-60 ms vel). `validate()` is structurally blind (never differences across the hold→move seam) — the trajectory-layer analogue of the K5 twist-discontinuity class; the "disconnected" feel is structural (lean is a self-cancelling pulse peaking at ~0.211 T, upright at every stop). **(3) SAFETY** at `(1500,5000,40000)`: peak dev 0.359 rev = 36 % of the 1.0 rev guard, peak leg vel 285 mm/s = 54 % of clamp cap, 0 faults / 143 moves; jerk is the binding constraint. **FIXES (this commit):** `ik_solver.compute_jacobian` vectorized to a component-form cross (numerically identical — bit-equal to np.cross on the same operands, ≤2.84e-14 ≈1 ulp vs the old per-leg loop; unshaped build_move 736→234 ms, shaped 2680→1183 ms); `feasibility.py` default sampling 200→**80** for unshaped with `_VALIDATE_JERK_MARGIN=1.05` covering the ≤3.7 % FD under-measurement on the BINDING jerk constraint (adopting `validate_follow`'s pre-existing idiom), shaped floored at `_SHAPED_VALIDATE_SAMPLES=200` (its FD jerk under-converges — window-edge curvature post-window); `shaping.py` multiplies the ENTIRE lean contribution (tilt + xy shift + all derivatives) by a **C2 plateau window** `w(s)=S(s/EDGE)·S((1−s)/EDGE)`, EDGE=0.15, applied by full product rule in `state_at` (window-then-cap) — self-consistency preserved. **Net offline (x+150): unshaped ~0.73→~0.10 s (~7×), shaped ~2.6→~1.2 s (~2.3×); boundary vel_ff step 0.00 mm/s at both ends for all tiers.** Low-pass FF REJECTED (breaks pose/twist/accel self-consistency, the 2026-05-08 class); septic base REJECTED (blast radius); fewer refine iters REJECTED (`_stretch_factor` assumes 1/Tⁿ vs lean's 1/T³ → durations balloon 1.04→6.9 s). **Status in-progress — operator must re-run the lean A/B (colcon build + relaunch):** expect pauses ~4.3 → ~2 s (settle-dominated), onset iq spikes gone; the open A/B question is whether lean *helps* realized smoothness at matched limits (pre-fix it WORSENED peak iq). Remaining for later: vectorized shaped gate (~100× endgame), analytic shaped stretch seed, carry-tilt-across-moves (the true fluidity fix). Scoped `pytest tests/motion/test_trajectory_feasibility.py tests/motion/test_trajectory_shaping.py tests/motion/test_kinematics.py -q` (2026-07-16) **60 passed**; `pytest tests/motion/ -q` (2026-07-16) **675 passed in 147.09 s**; full suite (`pytest tests/ -q`, 2026-07-16) **2826 passed / 1 xfailed in 620.43 s**. **RE-RUN CONFIRMED same night (bags 21-58-59 + 22-06-30): pauses 1.7 s (planning-dominated ~1.47 s — endgame items still worthwhile); onset spikes GONE (leaned onset 4.1-4.3 A now BELOW gain-0's 6.9 A); lean helps DECISIVELY at matched limits (gain 0 hit 0.94 rev = 94% of guard + 7.94 A; gains 0.6/1.0 → 0.25 rev / 4.4 A (corrected 2026-07-17: not like-for-like — the same x-traverse re-extracts to 0.45/0.46 rev at 0.6/1.0, see the wobble-latch entry); pre-registered realized-jerk criterion MET at 0.3/0.6: 19.3k→13.5k/14.9k mm/s³, ~tied at 1.0; mechanism: the shaped gate stretches leaned moves ~1.5× — slower+tilted reads calm); 5° cap never bound even at gain 1.0 (1.85° peak; binds above ~856/gain mm/s² lateral accel); recommended default lean_gain 0.6 (not yet persisted — YAML still ships 0.0).** | [2026-07-16-lean-planning-latency-and-boundary-step](2026-07-16-lean-planning-latency-and-boundary-step.md) |
| 2026-07-16 | resolved | MVP trajectory bringup — S4 limit ramp: MAX_DEVIATION guard + vel_limit headroom | **The MAX_DEVIATION guard latched on legitimate velocity-loop lag, not runaway — guard raised 0.5→1.0 rev, leg vel_limit 4.0→6.0 rev/s, per-leg latch attribution wired.** The S4 limit ramp PASSed at 100/660/10500 then latched the Teensy guard three times at vel=200 mm/s (bags `2026-07-16_13-17-58` @528.371 s + @621.567 s; `2026-07-16_13-34-58` @72.502 s), always leg 1 (0-indexed) first, frozen trips −0.5196/−0.5603/−0.5515 rev, always at move onset of the same asymmetric sweep; jerk 8000→10500 made no difference. Root cause (six-agent parallel investigation + adversarial cross-check): **legitimate velocity-loop lag under coordinated-move load, NOT runaway.** The guard watches the RAW streamed 40 Hz knot u0 vs encoder PRE lead-clamp (`fault_machine.cpp:367-371`), so it counts exactly the command-space lag the lead clamp (`enc ± MAX_LEAD 0.10`) deliberately tolerates. Peak \|dev\| is superlinear in realized leg velocity (v^1.19 all-legs / v^1.39 leg 1: 0.079 rev @35-50 mm/s → 0.544 @170-200, where all 6 move records belong to the 3 latch events), implied lag τ grows 112→208 ms (NOT a fixed 6-15 ms transport delay); deficit-integral closes it (−0.594 rev computed vs −0.516 accumulated at the last 10 Hz pre-trip sample; frozen snapshot −0.5196). Drives never railed: peak iq_setpoint 8.73 A of 10 A, peak encoder vel 2.23 of 4.0 rev/s, active_errors 0 throughout. H1 transport-delay / H2 vel_limit-saturation / H4 lead-clamp-limit-cycle / H5 current-rail / H6 stroke-clamp / H7 command-discontinuity all REFUTED; H3 (accel-phase velocity-loop lag) is root. Withdrawn mid-investigation: the vel_limit-saturation framing AND the 'encoder capped at vel_limit under clamp' corollary (setpoint saturates, encoder does not). Operator-approved response (all four): **(1)** `MAX_DEVIATION_REV` 0.5→1.0 rev (firmware, `canbridge_config.h`; FW_VERSION 1→2 human-facing only — sole consumer is the boot Serial banner, nothing runtime); **(2)** ODrive leg `vel_limit` 4.0→6.0 rev/s (config→codegen→runtime CAN push at `teensy_bridge_node._run_configure`:3035, fires after every /home /configure /activate) — un-saturates Kp·MAX_LEAD=40×0.10=4.0 ≤ 6.0 so vel_ff regains catch-up authority (was 4.0=vel_limit exactly); bench envelope ~48 rev/s, overspeed guard 16.5 rev/s unchanged; **(3)** per-leg latch attribution from the firmware's frozen `max_dev_leg` snapshot → bridge log + `/link_status guard_fault_leg` + GUI minimap tooltip `(MAX_DEVIATION, leg N)` (zero Python re-thresholding — auto-tracks the firmware threshold); **(4)** runbook ABORT rules recalibrated (0.1 rev at HOLDS only; moves may run to ~0.6 rev but must collapse at arrival). Raising the guard adds ZERO physical excursion for command-side faults (lead + stroke clamp + ODrive clip bound the executed command); accepted cost = encoder-side-runaway detect distance 35→70 mm. `MAX_LEAD` stays 0.10. **Deployment is two independent halves, neither on hardware yet:** (A) guard needs a Teensy REFLASH (`pio run -e teensy41 -t upload`; built clean, Flash 3.2%/RAM 27.2%); (B) vel_limit needs `colcon build --packages-select jugglebot` THEN relaunch (a stale install keeps pushing 4.0). Next chapter = accel FF, sized on a MEASURED coordinated-move inertia (1-3e-3+ kg·m²), NOT the bench single-leg J_eff≈3e-4. Scoped `python -m pytest tests/motion/ tests/teensy_link/ tests/firmware/ -q` (2026-07-16): **1018 passed in 110.58 s**; firmware `pio run -e teensy41` SUCCESS; full suite (`pytest tests/ -q`, 2026-07-16): **2811 passed / 1 xfailed in 772.17 s**. **EVENING ADDENDUM: both halves VALIDATED on hardware — bag 17-38-15 ramped vel 100→200→280 mm/s, 22 moves, ZERO latches (the battery that latched 3× at vel=200 that morning). Envelope then opened further: vel_limit 6→12 rev/s, set_limits ceilings 280/4000→5000/5000 (jerk 200000 unchanged; ceilings now administrative). Clamp-engaged catch-up is VELFF-cap-bound at 7.5 rev/s ≈ 529 mm/s hard cap (ramp guidance ~450–500 mm/s) until the VELFF flash item + accel FF land. Accel-FF chapter opened as plans/active/accel-ff-inertia.md: the FF is already fully wired behind torque_ff_platform_inertia:false; hard prereq = firmware stale-hold torque decay; measured coordinated inertia median 7.7e-4 = 2.8× rotor, lateral/z 1.68× (operator hypothesis confirmed; the '5–20×' here was a lag figure — clean inertia is 2–5×).** | [2026-07-16-max-deviation-guard-tracking-lag](2026-07-16-max-deviation-guard-tracking-lag.md) |
| 2026-07-16 | resolved | Leg feedforward — ARC COMPLETE: gravity FF armed + shipped ON | **The gravity feedforward is armed, sign-verified on hardware, and ships enabled.** A/B sessions (FF off `2026-07-16_10-12-33` / on `2026-07-16_10-17-45`): banner wire scale 0.967251; ramp 0→1 in exactly 2.00 s from the arm instant; zero motion at the arm edge (0.0014 rev max); **S3 sign check NO INVERSION** — per-leg hold-iq delta leg-mean −0.115 A (the ideal falls-or-flat signature; abort signature absent); setpoints_without_ff=0 (all 5058), firmware clamp never bound, zero faults. Dynamic A/B statistically identical over 11 matched moves — exactly the pre-registered expectation (FF < friction floor on the unloaded platform; its value is the integrator no longer carrying gravity, for loaded/dynamic work). torque_ff_enabled: true now SHIPS; tripwire test inverted (test_shipped_config_has_the_feature_on). Bonus: first hardware validation of the arming contract (BOOT pre-flight cleared last night's stale latch; auto-arm 230 ms post-activate; clean armed stows). Follow-ups: /leg_torques_diagnostic gap CLOSED same day (trajectory_node 5 Hz republish of the emitter torques, gated on streaming); B's first move drew 7.35 A + all-legs lead clamp (isolated — watch); hold-current hysteresis ±1.5 A dwarfs FF signatures (compare identical-approach-history holds only). | [2026-07-16-gravity-ff-armed.md](2026-07-16-gravity-ff-armed.md) |
| 2026-07-15 | resolved | MVP trajectory bringup — arming lifecycle contract (A1–A5) | **One evening, three arming failures, one root cause — the ARMING CONTRACT.** First S1 attempt hit: (1) a stale prior-session guard latch (Jetson-5V Teensy survives relaunches) wedging HOMING at the guard-gated HOME verb — twice; (2) a fully-'accepted' trajectory battery with ZERO motion and zero warnings (manual arm step skipped; a disarmed bridge holds no :5557 subscription — setpoints_sent=0, setpoints_rejected=0, /link_status OK); (3) enable_setpoint_output:=true self-E-STOPing at boot (zero-precondition arm before anything can stream) then dead-ending in 'reseed refused'. Root cause: 'armed' was a cross-process invariant with no owner. Fix: ARMING_CONTRACT.md (A1–A5) — orchestrator auto-arms on ACTIVE entry (bridge's 5-precondition stream-then-arm stays the gate) and disarms on real faults; disarm-before-stow enforced in-process with T2J-bit3 wire confirmation; mode outlives the armed window (A4); boot-arm removed; BOOT stale-latch pre-flight; loud disarmed-wire accepts; guard-only classification requires ACTIVE origin AND wire_armed. 27-agent adversarial review (21 confirmed findings incl. a BLOCKING staged-disarm-vs-RPC race) + /audit round (arm-lock reentrancy, TOCTOU recheck) all fixed pre-commit. The operator's proposed fix (stream only in ACTIVE+TRAJECTORY) analysed and rejected — the armed STANDBY hold is load-bearing (250 ms staleness watchdog). | [2026-07-15-arming-contract.md](2026-07-15-arming-contract.md) |
| 2026-07-15 | resolved | Leg feedforward — Kt CONFIRMED + SHIPPED (0.0570), tff channel verified | **First Kt measurement: 0.0577 ± 0.0012 Nm/A — NEITHER candidate** (ODrive's 0.0551 excluded +2.1σ low, the historical 0.0624 excluded −3.8σ high — the stiction-bias prediction confirmed). Recovered from the operator's four-mass friction-cancelling traverse session (0.5/1.0/1.5/2.25 kg) against ACTUAL masses — the harness had mislabelled every run "1.00 kg" (`--hold-mass` is a Mode-2 flag, silently ignored by Mode 1); fit R²=0.99909, intercept ≈0. PROVISIONAL: masses operator-recalled, and stiction-confounded hold-currents pull mildly toward ~0.062 — the weighed-mass confirmation run settles it; YAML keeps 0.0624 until then (updating it auto-corrects the FF wire scale — the double-correction-proof design). **Bonus: friction GROWS with load** (half-diff 1.16→1.51 A over 0.5→2.25 kg; cancels in the fit; unmodelled in Stribeck). **The torque_ff channel is SETTLED**: positive wire tff = extension torque, production chain correct as built, **NO negation** (3 inverted-rig runs, ≥10σ); decay-corrected gain ≈18.2 A/Nm vs 18.14 expected (±15–20%). The 20:55 "MUST NEGATE / −21 A/Nm" is dead: **drift-aliasing** — a monotonic rung ladder at fixed 3.2 s cadence makes time and torque perfectly collinear (slope_alias = 160×drift; that run's hand-contamination drift was 2× what's needed), injection path byte-identical across harness versions. **The static-hold Mode 2's steady-state signal is EXACTLY ZERO by construction** (the integrator re-pins total iq at the load value) — what the runs measured was the transient at ~1.3 s of decay (τ_locked ≈ 8–10 s); the orchestrator's traverse-A/B replacement was ALSO refuted in review (during-motion τ = vel_gain/vel_int = 0.625 s absorbs ~83%) → **Mode 2 rebuilt as EDGE-CAPTURE** (settle gate → 15 s pre-soak → ±X square wave inside the friction band → matched-filter edges → decay-corrected jump/(2X); drift tested BEFORE the null test; tri-state channel_live). **The friction-band load-transfer transient** poisons the first ~7 s of any static hold: amplitude ≈ τ_c (1.2–1.4 A, LOAD-INDEPENDENT), τ ≈ 3.2 s — 15 s pre-soak kills it; no rung re-ordering does (0/5040 permutations). **Mode-1 failure chain, one root cause**: starved steady gate (accel sub-gate rejected ~95% of steady samples) → mass "FAILED" → park ran a bring-up helper ARMED AND SILENT (hard 0.3 s sleep) → guaranteed MPC_STALE latch → re-arm tripped → "CANNOT PARK". All fixed + 135 kt_lib tests: parse-time mode/flag refusals, reworked steady selector (n 70–168 on the real CSVs with the landed gate; the recovery-analysis gate kept 47–159), keep-alive flat-knot pump across armed gaps, full stream-then-arm park re-arm (+ armed fast path that never drops the load), abort reasons survive NaN snapshots, **manifests persist per-traverse immediately** (an abort can no longer cost the data), REQUIRED `--rig-orientation` (the inverted rig had silently wrong-way sign bookkeeping), conditioning floor downgraded to a warning (the 0.5 kg point empirically refuted it). **Firmware ingest clamp BUILT (review: SHIP, zero findings)**: |torque_ff| ≤ 0.25 wire-Nm at UDP ingest (clamp-don't-reject), per-leg torque_clamp_mask on heartbeat flags bits 8–13 (free bits — no size change, no version bump), mirrored on /link_status; three-layer chain pump 0.1325 → firmware 0.25 → ODrive 10 A; both pio envs build, codegen idempotent, 210 firmware/parser/bridge tests — **DORMANT UNTIL FLASHED**. SAME-DAY CONFIRMATION (weighed masses ±10 g, 2 reps, flashed): harness's own fit **Kt = 0.0564 ± 0.0011** (R²=0.99999) → **pooled 0.0570 ± 0.0008 SHIPPED to YAML** (0.0624 excluded 6.6σ; ODrive's 0.0551 at +2.2σ — the nameplate was nearly right); wire scale auto-corrected **0.8835 → 0.96725** with pump/tests/docs rippled in lockstep. Edge-capture's first hardware contact found one more design bug — the amplitude bound checked the LEVEL but a square wave toggles a SWING of 2X (±0.035 swing = 0.070 ≈ 1.5× the band → chaos; ±0.020 swing = 0.040 → 12% low) AND the pool gated globally, refusing a t=147 measurement over one bad amplitude → **fixed** (swing-safe defaults ±0.005/0.010/0.015; per-amplitude data-quality exclusion, t-stat stays pooled so a NULL still reads DEAD). Replayed verdict: **trustworthy, +18.351 ± 0.224 A/Nm (t=82) vs 18.14 expected — channel verified end-to-end**, and the bench drive's flashed torque_constant verified live (implied 0.0545 ± 0.0007, 1σ from 0.055133). **Gravity-FF arming session is next with nothing in front of it.** | [2026-07-15-kt-first-measurement-and-tff-channel](2026-07-15-kt-first-measurement-and-tff-channel.md) |
| 2026-07-14 | in-progress | Leg feedforward — Kt + gravity FF | **The Kt mismatch is a STEALTH GAIN CHANGE, not a stale constant — reconciled on the wire; gravity feedforward re-applied (DEFAULT OFF).** Follow-on to the 2026-07-13 gain-hunt closure. **(1)** On ODrive 0.6.x the velocity controller outputs TORQUE and the drive computes `Iq = torque/torque_constant`, so the loop's authority is `vel_gain/torque_constant` — meaning **"just set torque_constant to the measured Kt" would silently DETUNE the shipping, bench-validated velocity loop by −11.7 %** with no error and no telemetry. That is the one action that must never happen. Confirmed three ways: operator + the version-pinned 0.6.11 API ref (`vel_gain : [Nm/(rev/s)]`) + **the robot's own stability** (amps-semantics would need 30.5 rev/s of velocity error to explain an observed 6.09 A peak against a 4.0 rev/s vel_limit, and would put ω_v at 1.26 Hz — BELOW the 6.37 Hz position loop, an inverted cascade that rings unconditionally). **Resolution: do NOT touch the drives.** The YAML now carries BOTH constants (`motor_kt_nm_per_a` = physical truth, `motor_kt_odrive_config_nm_per_a` = what is flashed) and codegen derives `ODRIVE_LEG_TORQUE_WIRE_SCALE = 0.8835`; pre-scaling torque_ff on the wire makes the DELIVERED CURRENT correct at zero risk to the loop, and is **double-correction-proof** (flash the true Kt later, update the mirror, the scale auto-becomes 1.0). If it IS ever changed it must land ATOMICALLY on three surfaces (YAML gains, FLASHED gains, FLASHED torque_constant) — homing/activate run before configure and use the flashed values, so two-of-three is a silent ±13 % loop change. **(2)** The same division **falsified yesterday's cascade table**: ω_v = vel_gain/(2πJ) omitted it. Corrected **ω_v 22.8 Hz / ratio 3.58** (was 20.2/3.17) — healthier, not worse — and **ω_v is INVARIANT to the true Kt** (J_eff scales with Kt, the measured slope as 1/Kt), so it survives however Kt lands. **(3) Kt itself is NOT settled**: 0.055133 is ODrive's uncalibrated nameplate `8.27/Kv` to 15 s.f. with zero empirical backing (and the datasheet CANNOT adjudicate — 3.86 Nm/70 A = 0.05514 = the formula, circularly), but 0.0624 has **two** problems: a **lever-arm BUG fixed this session** (`single_leg_test.py:841` used the STANDARD leg's 11.221 mm spool radius on the BENCH leg's 11.391 mm ⇒ Kt came out 1.51 % LOW ⇒ corrected **0.0633**, i.e. FURTHER from ODrive's value) and an **unquantified stiction bias** (a position-hold fit; τ_c ≈ 1.1 A supports part of a static load ⇒ Kt biased HIGH). **Both 2026-07-13 plant-ID conclusions survive at either Kt** — τ_c is Kt-INVARIANT (raw regression coefficient in amps; the 2026-04-27 corroboration is amps-vs-amps), and the J_eff ≈ 1× J_rotor refutation of "~10× rotor" holds by an order of magnitude either way. **(4) Gravity FF re-applied, DEFAULT OFF** (`motion/torque_ff.py`): SetpointPump is the single wire enforcement point — **clamp → Kt scale → ramp**. The clamp (0.15 Nm) is load-bearing because **there is NO other clamp at any layer** (firmware saturates only at int16 ≈ 59 A of demand) and the ODrive ADDS input_torque BEFORE the torque limit, so a large FF saturates the current clamp, **the position loop loses ALL authority**, and the leg runs open-loop until MAX_DEVIATION E-STOPs it mid-motion. The **ramp-in** exists because `vel_integrator_limit` is genuinely **inf** and the integrator has already wound up to carry gravity — a step FF would command ~2× gravity until it unwinds. Platform-inertia term gated OFF: the firmware HOLDS torque_ff **undecayed** through a stale-link window (`leg_interp.cpp:366` sits after the velocity-decay block), so an accel-proportional FF would keep pushing at full magnitude while commanded velocity decays — gravity, being static, is correct to hold. **SIGN VERIFIED BY INDEPENDENT ENERGY BALANCE** (not by reading the code): FF virtual work per 1 mm platform lift = **mgh exactly, ratio 1.00000 at four poses** incl. tilted + extreme; all six torques positive; magnitudes land in the historic 0.013–0.041 Nm/leg band; **mutation-testing the sign fails 11 tests**. Flag-OFF proven inert (even a NaN torque_Nm neither leaks nor rejects the frame). **The iq SIGN-FRAME TRAP**: `can_buses.cpp:92` stores iq RAW while pos/vel go through `leg_sign` — a code-read says extending torque should read NEGATIVE iq, which would make the plant-ID sign-broken. **The data refutes it** (corr(iq,accel)=+0.39, corr(iq,sign v)=+0.81; +36 rev/s² draws +0.89 A) — the drive's own direction calibration inverts it back. Per-axis calibration is a property of each DRIVE, not the firmware — re-verify before porting to the robot's legs. **Kt experiment ready** (`kt_bench_test.py`): constant-velocity up/down traverse under known masses, where `(iq_up+iq_down)/2 = τ_g/Kt` **cancels friction EXACTLY** and the half-difference yields a free independent friction measurement; 1.0–3.0 kg at 0.6 rev/s; resolves 0.0551-vs-0.0624 at ~7.5σ (cannot resolve 0.0624-vs-0.0637, and says so); **requires BENCH_SYSID_BUILD** (DIAGNOSTIC gates on iq_setpoint not iq_measured, so stock v3 gives 2–3 samples per traverse) and hard-refuses below 150 Hz telemetry. Run `--mode torque_ff_check` FIRST — it settles the torque_ff sign through `leg_sign` empirically. Suite `pytest tests/ -q` (2026-07-14): **2701 passed, 1 xfailed** (+ the known load-flaky alloc test, which passes isolated). No firmware change, no gain change, no ODrive re-flash. **ADVERSARIAL REVIEW COMPLETED same day (after the spend limit lifted): 4/4 lenses FIX_FIRST, 15 findings (6 HIGH), all 14 software fixes landed same-session** — the recovery-step hole (the FF ramp missed the guard-latch → /recover path, the most common integrator-rewind event → `SetpointPump.restart_torque_ramp()` wired into both /recover clear paths), the ungated THIRD producer (HardwarePlant read NO config flags; enabling the master switch would have armed the MPC path's accel-proportional inertia term → per-term skip flags threaded through the whole chain + pinned), the **missing Newton–Euler transport moment** in `compute_inertia_wrench` (`r_com × m·a_com` — a 4 m/s² vertical throw demanded ~0.33 N·m of moment and got ZERO; pre-existing since March, fixed + pinned by independent rotational virtual work), a 2π-mutation test gap closed, a clamp ceiling added (0.30 Nm — the ODrive adds input_torque BEFORE its torque limit), the TRAJECTORY producer's singular/ill-conditioned-Jacobian FF now holds-then-decays instead of stepping to zero (the doubly-gated-off MPC producer still zero-steps — known-open) (with a 0.5 Nm physical sanity bound, since solve() returns huge FINITE forces on ill-conditioned J), and the Kt harness rebuilt (see below). One LOW deferred to the firmware sitting (stroke-clamp FF pulses). **FIRST HARDWARE CONTACT validated the review, not the feature**: the operator's messy torque_ff_check run (0.8 kg, odd angle, hand support) produced R²=−0.099 yet the harness confidently printed 'MUST NEGATE / MISMATCH −63%' — review finding #1 demonstrated live. ~~De-noised reading~~ **⚠️ SUPERSEDED 2026-07-15**: the "in-band |slope| 20–22.5 A/Nm, ~17% high" and the "negative bench sign" were BOTH the same **drift-alias + hand-contamination** artifact — three clean 2026-07-15 runs settle the channel at **positive wire tff = extension, gain ≈18.2 vs 18.14 expected, ≥10σ, matching the production chain (NO negation)**; the 1/21.3=0.047 torque_constant speculation is resolved (drive consistent with 0.055133) — see the 2026-07-15 row. Still true from this row: the ±0.10 rungs saturated/re-absorbed exactly as the review predicted — per-drive ODrive direction calibration differs bench-vs-platform, so **a bench sign result must NEVER be copied to the platform; the platform-validated production sign stands**. Harness now refuses verdicts below R²≥0.9 / t≥3, defaults its ladder inside the friction band (±0.035 Nm max), actually parks on abort paths, shapes the traverse start, and frames bench-sign results with the per-drive context. | [2026-07-14-kt-reconciliation-and-gravity-ff](2026-07-14-kt-reconciliation-and-gravity-ff.md) |
| 2026-07-13 | resolved | S4 gain-retune — CLOSED | **The leg gain hunt ends — a rev/mm units bug manufactured the "accuracy knee", and the plant-ID says feedforward, not feedback, is the lever.** The operator ran the missing sustained S4 datapoint, said he was **unconvinced the gain search had found the right answer**, and asked to question the methodology instead of running more points. He was right. Three findings, all from data **already on disk** (no new hardware): **(1)** `bench_leg_sysid.py:2548` printed `err_rms * 1e3` under an `mm` label, but `err_rms` is in **REVOLUTIONS** — that is milli-revs, **~14× too large** (the bench leg is 71.5708 mm/rev). The "10.3 → 6.9 mm accuracy knee at pos 70" is really **0.74 → 0.49 mm**; and against the operator's spec — **±1 mm judged AT THE CATCH/THROW INSTANT** — production `40/0.20/0.32` measures **0.054 mm median / 0.192 mm worst (0/21 strokes over 1 mm), ~5× inside**, so the entire accuracy case for raising pos_gain evaporates. Four rounds of adversarial verification missed it because every check was *internal to the number* — nobody asked whether **10 mm RMS was plausible on a 280 mm-stroke leg**. **(2)** Plant-ID by torque-balance regression (`2πJ·a = Kt·iq − τ_c·sgn(v) − b·v`) on the same CSVs: **J_eff = 0.7–1.2 × J_rotor** (5 fits; two highest-R² both 0.97×) — **refuting the unsourced "J_eff ~10× rotor"** guess carried since 2026-04-27 by an order of magnitude; τ_c = 0.88–1.22 A brackets the 2026-04-27 bench's 1.094 A. Implied production cascade **ω_v 22.8 Hz / ω_p 6.4 Hz, ratio 3.58 — already healthy** (scatter across the 5 fits: ω_v 17.7–28.8 Hz, ratio 2.78–4.53). NOTE the cascade must include the ODrive's `torque_constant` division (vel_gain is Nm/(rev/s), and Iq = torque/torque_constant using the drive's CONFIGURED 0.0551, not the true Kt) — so the shipping velocity loop runs ~13% stiffer than its nominal vel_gain implies. ω_v is INVARIANT to the true Kt, so this survives the open Kt question; the ladder's `vel_gain ∝ √pos_gain` rule holds that ratio *constant*, so climbing it never improved damping, it only slid both loops toward the resonance (which is what the operator's ear heard at pos 110). **(3)** Error budget (contributions of correlated regressors — they do NOT sum): against 0.83 mm pooled RMS, **the delay-shaped term alone contributes 0.57 mm and is gain-independent** (largely the metric's own sample-and-hold — `cmd_u0` is the knot at the START of the segment the firmware interpolates THROUGH), 0.17 mm inertial + 0.15 mm friction (the two switched-off FF terms), 0.44 mm noise floor. **Reframe:** Jugglebot's reference is known in advance with exact derivatives (`plan.state_at` → pose/twist/accel; exact velocity + acceleration IK already computed), so **feedforward follows the trajectory and feedback only cleans up model error** — the ladder used the stability-limited lever to chase an accuracy target the accuracy-limited lever had already met. `vel_ff` (a feedforward) was the single biggest historical win (lag 31→6 ms) and is *why* the error is already sub-mm. **SpaceMouse** is architecturally identical to a ball-tracked catch (same 40 Hz quintic replan path) so it is not off-path — but it is a **worst-case soak** (chase clamp rides 85 % of limits indefinitely) and its harshness has a **non-gain cause**: a fresh quintic every 25 ms restarts *jerk* at 40 Hz, and the firmware's *cubic* Hermite makes executed accel step at every knot ⇒ pass/fail robustness screen, **never a gain selector**. **Outcome: production `40/0.20/0.32` STANDS**; Stage-2 candidate `70/0.35/0.56` + `MAX_LEAD 0.057` **RETRACTED** (MAX_LEAD stays 0.10, already shipped — no firmware change); the gain ladder is **demoted to a stability-envelope probe**; the ±1 mm spec becomes the **stopping rule the methodology never had**; the loaded S4 replay is **reframed as a v3-firmware regression test at production gains**. Effort moves to **feedforward** — `torque_ff` is plumbed end-to-end to the ODrive and carrying zeros (one line: `setpoint_pump.py:263`), leg accel is already on the wire as `acc_mm_s2`, and J_eff is now measured. **Open/blocking:** the **gravity+inertia FF appears to have been dropped by ACCIDENT** (the cutover claimed `:5557` carries `torque_Nm = zeros`; `hardware_plant.py:488` publishes it live — though note MPC is dormant on this branch, and the magnitude 0.013–0.041 Nm/leg is cited from a 2026-05-08 archived plan, not re-measured) and a **13 % Kt mismatch** (ODrive 0.0551 vs measured 0.0624) is latent under any torque FF. New probe `tools/probes/bench_leg_plant_id.py`; enforcement point `sysid_lib.format_stroke_error` + 3 regression tests. Full suite `pytest tests/ -q` (2026-07-13): **2578 passed, 1 xfailed in 601.77 s**. No firmware change, no production gain change. | [2026-07-13-leg-plant-id-and-the-units-bug](2026-07-13-leg-plant-id-and-the-units-bug.md) |
| 2026-07-12 | superseded | S4 gain-retune (Stage 1) | **⚠️ SUPERSEDED 2026-07-13 — the "accuracy knee" and the Stage-2 recommendation are RETRACTED (rev/mm units bug, ~14×; see the 2026-07-13 entry). Still valid: the stability envelope, the servo-vs-structure discriminant, the friction describing-function, the measurement-ceiling lessons, the instrument-gap lesson, and every harness/firmware fix.** **Bench-leg gain-tuning Stage 1 — bench system-ID on the can-bridge path, an escalate-until-unstable ladder, and the motion-excited vibration the operator's ears caught that the instruments missed.** Four adversarially-verified analysis rounds on the acceptable-loss 7th bench leg (Path BRIDGE, CAN3). The unloaded leg **never reproduced the S4 ~6 Hz limit cycle** in any regime (ladder, sustained tracking, or deliberate clamp-riding at 46 %) — arrive-and-settle stimuli are the wrong excitation class; given the v2→v3 clamp change (vel_ff kept, MAX_LEAD 0.10) the signature may already be fixed on the robot, so **the loaded robot replay decides**. The first ladder "winner" **110/0.50/0.72 was OVERTURNED** — the operator heard/saw motion-excited HF vibration the quiescent + settle instruments structurally miss; the chirp peak/DC then measured effective **ζ≈0.49–0.52 at the 17.5 Hz crossover** (step-fits overstate damping — ramp-limited steps never excite the resonance), 3.7× baseline 20–45 Hz iq energy, ζ non-reproducible (0.78→0.66–0.68), iq railing 1.26 % at 4.4 rev/s. Withdrawn mid-arc by the verify stage: the "18:03 mode-all ran at winner gains" misread (code-confirmed BASELINE at bench_leg_sysid.py:1800) and the chirp "~15 Hz corner" (friction describing-function, not a loop corner; chirp gain ~0.5 at 0.02 rev = real Coulomb friction, 2026-04-27 tau_c=1.094 A corroborated). Round-4 strokes battery + vel_int de-confound: the sustained-stroke triple `MAX_DEVIATION` latch was a **harness sizing bug** (2A swings timed for A → 7.6 rev/s, infeasible at any gains; fixed db05670 + a series-level velocity gate closing the class); baseline HF onsets on 99 ms strokes are commanded-content **false positives** (velHF FALLS with gain), real vibration GROWS with gain on long strokes (iqHF to 5.6× at pos 90). Bench envelope: **pos 40 clean / 70 accuracy knee (mean errRMS 10.3→6.9 mm, −33 %) / 90 aggressive edge / 110+ out** [**2026-07-13: those errRMS values are milli-REVS, not mm — really 0.74→0.49 mm; the "accuracy knee" is retracted, the stability envelope stands**]; pos 130 dead at all vel_int (the boundary itself). Stage-2 robot recommendation ~~**70 / 0.35 / 0.56 + MAX_LEAD re-pin 4.0/pos_gain=0.057**~~ **[RETRACTED 2026-07-13 — no gain change warranted; MAX_LEAD stays 0.10]**, vel_int by ratio-rule not the bench-frozen 0.72; acceptance gate = loaded S4 replay under v3 **[reframed 2026-07-13 as a v3 regression test at production gains — there is no candidate to escalate to]**. Firmware: bench-only `BENCH_SYSID_BUILD` variant (250 Hz telemetry, 100 Hz knots, un-gated axis-0 iq; flag-OFF binary sha256-proven byte-identical; never-flash-to-robot). No production gain shipped (committed gains stay Level-1 40/0.20/0.32). Six commits; full suite `pytest tests/ -q` (2026-07-12) 2502→2531→2541→2572→2575 passed + 1 xfailed for 1e03348/0a271f8/30cc786/8cbf4f0/db05670, follow-up 0182a11 2575+1. Instrument gap + operator-ear lesson ties the CLAUDE.md physical-intuition rule. Plan: `leg-gain-tuning-methodology.md`. | [2026-07-12-bench-leg-gain-tuning-stage1](2026-07-12-bench-leg-gain-tuning-stage1.md) |
| 2026-07-12 | resolved-with-open-items | GUI + catching cone | **Cone "only syncs after a colcon build" — zombie AND build-correlation hypotheses BOTH REFUTED by bag ground truth; a REAL but LATENT UDP co-bind hazard found; the morning failures point at CONE MCU RESTARTS.** Follow-up to [[2026-07-12-gui-cone-catch-correlation-regression]]. Operator report: *"cone syncs perfectly immediately after a colcon build, but subsequent launches don't sync — zombie ROS2 processes?"* **No zombies** (live forensics: only the systemd GUI daemon + the `ros2` CLI daemon). **No build-correlation**: decoding `/cone/heartbeat` from EVERY bag of the day (**17 sessions**) shows **14/17 `connected=True` AND `time_synced=True` FROM THE FIRST PUBLISHED HEARTBEAT** — the `19-16-18` (pre-rebuild) / `19-17-31` (post-rebuild) / `19-19-12` (next launch, the one the hypothesis says must FAIL) triple is identical (**a colcon boundary with NO signature change**), and three further launches 45 min–3 h later with no build between all sync from their first HB. The operator's model was a CORRECT lesson from finding (1) — the `catch_correlation_node` orphan GENUINELY needed a rebuild (`ros2 launch` runs the INSTALLED copy) — **over-generalised from launch-graph membership to device time-sync**; the defence is to restate the *mechanism*, not the correlation, when reusing a prior lesson. **FINDING 1 — a real structural hazard (HIGH confidence, read straight off the code):** `teensy_bridge_node` and the parallel session's **bench system-ID harness** each construct a `TeensyLinkClient` binding the SAME can-bridge ports (`0.0.0.0:5005` stream + `:5006` RPC) with `SO_REUSEADDR` (`controller/teensy_link/client.py:174-180`); the second bind succeeds SILENTLY and the kernel resolves each unicast datagram to **exactly ONE** socket (no `SO_REUSEPORT`, no load-balancing) ⇒ **one process gets EVERYTHING, the other gets NOTHING**, winner decided at bind time ⇒ since the bench harness **does not subscribe `CONE_FRAME`**, a healthy cone would present as a disconnected one with **no error, no warning, no log line on either side**. **FINDING 2 — that hazard did NOT cause this incident (attribution DOWNGRADED to LOW / candidate-only, NOT supported by the timing artifacts).** The draft blamed the two dead morning sessions (`10-37-19`, `10-42-51`: `connected=False` for **0/882 and 0/1606** heartbeats) on the co-bind because bench runs "bracketed" them — but **a bracket is ADJACENCY, and the mechanism needs CONCURRENCY**: bench run 1 ran 10:36:57→**10:37:00** and the dead bag's first message is **10:37:23** (23 s AFTER it exited); the second dead bag's last message is **10:45:38** and bench run 2 starts **10:46:00** (22 s later). Across **all 17 bags × all 22 `bench_sysid_*` runs of the day there is NOT ONE overlapping pair** — the co-bind event never occurred. Both morning bench runs are **3-second aborted runs** (manifest-only, `stages: {}`) and could not have starved an 89 s / 163 s session even if they HAD overlapped. The signature also **flips mid-session** in three other bags (impossible for an all-or-nothing bind race) and **appears in the `cone_test_*` bags of 2026-05-24 — six weeks BEFORE the bench harness existed** (`f218acf`, 2026-07-11; `TeensyLinkClient` itself 2026-06-02). **FINDING 3 — the LEADING explanation is cone-side power/wiring intermittency:** the cone MCU **restarted at least 3× on 2026-07-12** (`18-34-09` dropout t+32.3→t+50.3 s; `18-44-56` t+12.4→t+26.7 s; `20-03-36` **down at launch**, up at t+43.9 s — each returning with `last_catch_seq=0` + `have_any_catch` cleared), and `20-03-36` (connected **155/593**, self-healing, **no bench process within 54 min**) is the partial-connectivity signature a port race CANNOT produce. The morning sessions look like the same fault held down ~9 min instead of ~20 s. **NO CODE LANDED**, deliberately: no defect reproduced in the reported path, and the interlock closing the (real, latent) co-bind hazard lives in shared transport the parallel gain-tuning workstream is actively depending on — a **coordinate-first change, not a drive-by**. Probe hardened alongside (`tools/probes/cone_bag_decode.py`): reboot-aware `taps` (a seq reset was silently bridged ⇒ ~250 phantom taps had a catch followed either reboot — so the `taps == catch_ev` 17/17 transport validation is **conditional on no post-reboot catch**, now stated), a genuine ZERO no longer collapses to "topic absent" (which had silently suppressed the *exact* catch-frames-lost case the probe exists to detect — on BOTH the statistics and decode paths), and a clean error on a missing `--root`. READ-ONLY investigation — evidence is the 17-bag decode + a 22×17 bench/session overlap check (0 pairs) + `git log --diff-filter=A` provenance + `~/.ros/log` node rosters/teardown records + live process forensics; **no test-count triple is cited because none applies to an entry that lands no code** (inventing one would be fabricating evidence). Operator confirms cone syncing works post-investigation — but note that is **consistent with an INTERMITTENT fault not currently manifesting**, and is NOT evidence the single-owner discipline cured anything. OPEN ITEMS: (a) the single-owner `flock` interlock in `TeensyLinkClient.start()` so a second binder **fails LOUDLY naming the current owner** — closing the whole **two-owners-of-one-link** class at the single point where ownership is established (needs coordination with the bench/gain-tuning workstream; **latent, never observed to fire** — fix it because a silent failure mode with no observable is a bad thing to keep, not because it is currently hurting us; interim mitigation: never run `jugglebot_launch.py` concurrently with the bench harness, pre-flight `pgrep -af bench_leg_sysid; ss -lup \| grep -E ":5005\|:5006"`); (b) `teensy_bridge_node.on_shutdown` **exceeds the launch's 5 s SIGINT→SIGTERM window in EVERY evening session** and is killed mid-profiled-stow (**exit `-15`**; its budget is ~8 s = settle + up to 6 s `DEACTIVATE`) — firmware deferred-stow is the backstop so no immediate danger, but the clean teardown is truncated every session; fixing it moves shutdown timing ⇒ needs the CLAUDE.md control-system-implications analysis, deliberately NOT done here; (c) **PROMOTED to the leading explanation** — the cone MCU restarts are unexplained if the operator did not power-cycle the cone; next steps are (1) ask the operator about ~18:35/~18:45/~20:03, (2) log a firmware **reset cause** (brown-out vs watchdog vs external) — one heartbeat field that distinguishes a power fault from a firmware hang, (3) then investigate physically. **Do not re-open the co-bind attribution without direct evidence of two live `TeensyLinkClient` processes.** | [2026-07-12-cone-sync-udp-link-contention](2026-07-12-cone-sync-udp-link-contention.md) |
| 2026-07-12 | resolved | GUI + ball-butler observability | **BB panel stale "Calibrated" across ROS sessions — latched calibration_result never reset when the GUI outlives the session.** Operator report: the long-lived browser tab shows a previous session's Calibrated in fresh, uncalibrated sessions; operator asked whether calibration can be read from a topic directly. Ground truth: the display is driven by the LATCHED `bb/calibration_result` from mocap_node (published only on calibration attempts, never startup); `bb/heartbeat` (teensy_bridge, relayed BB device CAN heartbeat) carries NO calibration flag — adding one REJECTED by root cause: calibration lives in mocap_node (cross-node subscription + .msg change + lockstep rebuild), it's a category error coupling device-liveness to mocap-software state, AND it still wouldn't fix the stale browser DOM, which needs a disconnect reset regardless. The latched topic IS the right source; the gap was no reset on session death. Fix: `resetBBCalibration()` in panels.js called from main.js's `onConnectionStateChange` 'disconnected' branch — same pattern as the cone entry's `setCatchingConeDisconnected`; **third instance this week of the "GUI outlives the ROS session" state-latching class** (pos_cmd flatline cache 2026-07-11, cone catch baseline, now BB Calibrated). Self-healing: a same-session blip resets then self-restores (rosbridge resubscribe re-delivers the still-latched success=true); a fresh session has no latched sample → honestly Not Calibrated. Also rejected: reset on BB-heartbeat connected=false (mocap calibration independent of the BB CAN link; flickers on transient dropouts); mocap publishing success=false on startup (spurious failure event, no discriminator). Residual accepted: a lone mocap_node restart with rosbridge up isn't covered (websocket never drops) — not the deployment topology. `pytest tests/ros/test_gui_geometry.py tests/ros/test_ball_butler_node.py tests/ros/test_ball_butler.py tests/ros/test_teensy_bridge_node_bb.py -q` (2026-07-12) **138 passed** incl. 4 new tripwires (TestBBCalibrationStaleLatchReset); node module checks clean; headless-chromium baseline renders Not Calibrated + uncalibrated dot. Full suite immediately pre-commit (`pytest tests/ -q`, 2026-07-12): **2545 passed, 1 xfailed in 590.43 s** | [2026-07-12-gui-bb-stale-calibrated](2026-07-12-gui-bb-stale-calibrated.md) |
| 2026-07-12 | resolved | GUI + catching cone | **Catching-cone catches invisible in the GUI — `catch_correlation_node` was a Phase-13 launch orphan.** Operator report (live GUI, 2026-07-12): a piezo tap on the cone surfaces NOTHING in the GUI, though `cone/heartbeat`'s `last_catch_seq` increments correctly. Root cause: the GUI's ONLY catch-surfacing input is `cone/timing_result`, published exclusively by `catch_correlation_node`, whose sole launcher (`catching_cone_test.launch.py`) was DELETED in the Phase-13 SocketCAN decommission (7c7f61b, as "the last launcher of can_node"; operator chose delete over rewire) and the node was NEVER added to `jugglebot_launch.py`. The transport half migrated (`teensy_bridge_node` publishes `cone/catch_event` + `cone/heartbeat`); the correlation half was orphaned — silently dead since 7c7f61b, validated-working on 2026-05-23. Evidence: `git show 7c7f61b` (node creation + LaunchDescription membership both deleted), `git log -S catch_correlation_node -- launch/` (symbol never existed in jugglebot_launch.py), GUI subscribes `cone/timing_result` + rosbag records it (consumers wired to a publisher-less topic → bags silently empty). **Same silent-orphan class as yesterday's pos_cmd fix (2026-07-11-gui-leg-setpoint-echo-poscmd) — two instances in two days, both rooted in the Phase-13 migration; dead topics look identical to quiet topics.** Fix (2 parts): (1) `catch_correlation_node` restored to `jugglebot_launch.py` as a bare declaration exactly as 7c7f61b deleted it (no params/remaps — the deleted launch's cone params belonged to ball_butler_node), with an in-place comment telling the story; (2) defensive UX — `panels.js updateConeHeartbeat` now surfaces `last_catch_seq` directly (`last catch #<seq> · <N>s ago` + 800 ms flash on change; first-connected-heartbeat baseline → no phantom, reconnect re-baselines, wrap-safe inequality) using `CatchingConeHeartbeat`'s `have_any_catch`/`last_catch_seq`/`ms_since_last_catch` — per-tap feedback INDEPENDENT of the correlation pipeline; deliberately NOT an event-store CATCH type (EVENT_TYPES is a frozen 4-type enum feeding chart overlays/filters). Mitigation: new string-level launch tripwire `tests/ros/test_launch_nodes.py` pins the node DECLARED and MEMBERED in the LaunchDescription; docstring names the regression class. A future systematic sweep (every GUI-subscribed topic has a live producer in the launch graph) would close the whole class — open question. Post-fix: every tap → heartbeat line updates + flashes (independent of correlation); with the stack RELAUNCHED (picks up the restored node) → unmatched tap shows an `Actual` clock + `unmatched catch` footer, a matched catch (throw within 0.5 s) shows a predicted-vs-actual delta + sound-bar bar + `last: <thrower>` (sound bar stays matched-only by design). `pytest tests/ros/test_launch_nodes.py tests/ros/test_gui_geometry.py -q` (2026-07-12) **56 passed** (2 new tripwires + 54); panels.js/jugglebot_launch.py syntax clean; headless-chromium harness (temp, deleted): no-phantom baseline / silent reconnect re-baseline / seq-change renders `last catch #8 · 0s ago` all as designed. DOM cone scenario **10/10 ×2** + scenario1 17/17 + minimap 33/33 regression (2026-07-12); full suite (`pytest tests/ -q`, 2026-07-12): **2533 passed, 1 xfailed in 620.89 s**. CONFIRMED live (2026-07-12, session 18-37-27 rosbag: 5 piezo taps → 5 catch_events → 5 matched timing_results → GUI updated on every tap) — after a colcon rebuild, NOT relaunch alone: the same-day addendum refutes the operator's topic-interference hypothesis (catch_event count == genuine taps in all 10 bagged sessions, zero drops) and pins the stale-install root cause (ros2 launch runs the INSTALLED launch copy; the node entered the roster only post-rebuild); residuals flagged: a stale top-level install/ tree (2026-07-09 launch copy lacks the node's launch entry — sourcing it silently resurrects the regression) + suspected cone MCU restarts (seq resets in two sessions). | [2026-07-12-gui-cone-catch-correlation-regression](2026-07-12-gui-cone-catch-correlation-regression.md) |
| 2026-07-11 | resolved | GUI + orchestrator control surface | **GUI orchestrator state-machine minimap — safe click-to-state navigation with a runbook-enforcing sequencer and hold-to-confirm.** User-specified: a translucent minimap of the orchestrator state machine bottom-left of the 3D scene above the command strip; header click → full scene height, opaque, pushes scene + command strip right (fixed-vertical-FOV camera + existing ResizeObserver — ZERO viewer.js changes). Current state highlighted (FAULT pulses); unreachable states greyed with per-node reasons; clicking a node drives the system there in the fewest SAFE steps via 800 ms hold-to-confirm; contextual button surfaces the currently-necessary command (Arm Setpoints/Disarm/Recover/Clear Errors/Retry Boot) with service rejects verbatim. New ES module `state-minimap.js` (~1500 lines) + css; architect-designed plan reviewed pre-implementation; the **safety-critical transition table is reviewable DATA, not BFS** (no emergent graph-legal-but-runbook-illegal routes; review audits rows, not an algorithm). Inputs: orchestrator_state/control_mode_topic/robot_state/link_status (router slot the CAN panel reserved) + `leg_setpoint_echo` (sibling entry) as go_home quiescence proxy. **Sequencer enforces the runbook Sharp Edges**: teardown = standby-if-needed → go_home (best-effort) → anchored echo-quiescence wait → set_setpoint_output(false) ALWAYS → HARD verify-gate (fresh link_status tick, mpc_active exactly 0; ABORT otherwise — deactivate NEVER published unverified; kills the MPC_STALE + un-stowed-legs worst silent failure) → deactivate. Recovery entry ALWAYS the bridge /clear_errors Trigger (self-routes to converge-first /recover on the bridge's authoritative in-process _mpc_active — stale-GUI-read immune); orchestrator clear_errors ONLY for boot-timeout FAULT (F-C Retry Boot). Plans computed at confirm time from a fresh snapshot; per-step pre re-checks; single-flight lock; global abort on FAULT-mid-sequence/disconnect/timeout. Reachability replicates orchestrator ground truth (7-axis heartbeats, firmware_validated, is_homed, error[], guard latch) + freshness guards; BOOT/HOMING/LEVELLING grey ALL ("commands discarded" — deliberately stricter than the command strip's known BOOT-Home silent no-op); unknown state ⇒ all grey fail-safe. Adversarial review (4 lenses + adversarial verification of HIGHs): **2 HIGH CONFIRMED, both fixed in-session** — (1) echo-quiescence wait false-passed on pre-go_home hold samples (no anchor to the wait's start; the 40 Hz hold stream trivially satisfied it → disarm ~100 ms into the 2 s descent, freezing moving legs; fixed: window anchored to wait-step activation, ≥400 ms NEW samples; synthetic probe 7/7) — the class lesson: **freshness ≠ relevance**; (2) vacuous mid-hold action-identity guard (renders rewrote renderedActionKey during the hold — a Disarm hold could fire Arm; fixed: identity pinned at pointerdown + isConnected at confirm). **1 HIGH refuted by mechanism** (armed submode change vs S4.3: all six submodes are in trajectory_node's streaming set; Sharp Edge #1's mechanism is the emitter stopping, which doesn't happen — S4.3 blanket wording flagged as doc note). 18 medium/low triaged, most fixed — worst-case-defaults cluster (bridge_link as actuation guard — frozen fault_state=NONE lies while the uplink is down, same deception class as the CAN panel HIGH; >2 s staleness ⇒ all grey; FAULT branches freshness-gated; mpc_active tri-state, unparseable ⇒ UNVERIFIED never 'disarmed'; stale link + last-known armed ⇒ 'ARMED?' badge + enabled best-effort Disarm; Arm requires fault_state exactly NONE) + pane-relative width clamp min(280, paneWidth*0.55); greyed-label contrast ≥4.3:1 both themes; BOOT→IDLE skip-if-homed edge + arrowheads; T_MODE/T_DEACT 5→15 s (ActiveHandler queues until ACTIVATE completes; queued-command-after-abort residual documented); tripwires derive consumed keys from source; init idempotence. Deliberately not fixed: keyboard/AT (pointer-only holds are the GUI idiom); status-line precedence over the disconnected note. Probe-vs-review complementarity: the probe's fake go_home has no echo dynamics (only review could catch the anchor bug); only the probe pins the live teardown order (recorded timelines: negative case aborts naming Sharp Edge #6, deactivate NEVER published; positive publishes deactivate 62 ms after the armed flag clears). `pytest tests/ros/test_gui_geometry.py -q` (2026-07-11) **54 passed** (+2 EXPECTED_FILES, +4 tripwires incl. disarm-before-deactivate + go_home-before-disarm); JS module checks clean; quiescence probe 7/7; DOM probe (`--scenario minimap`) 27/27 twice pre-fixes, then post-fix **33/33 twice** (32 falsifiable assertions + 1 recorded-timeline info row; new checks: G_BRIDGE uplink-lost greying, ARMED?+enabled-Disarm worst case) + scenario1 regression **17/17** (2026-07-11; anchored gap 0.50/0.64 s visible in the timelines, deactivate ≤65 ms after the armed flag clears); canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2478 passed, 1 xfailed in 597.42 s** | [2026-07-11-gui-orchestrator-minimap](2026-07-11-gui-orchestrator-minimap.md) |
| 2026-07-11 | resolved | GUI + can-hub observability | **GUI CAN TRAFFIC panel rebuilt — per-bus (CAN1/CAN2/CAN3) traffic + health from the can-hub /profile + /link_status uplink.** The panel was DEAD (its `can_traffic` topic lost its only publisher when can_node was deleted in the 2026-07-06 decommission; the decommission-precheck parity audit — 2026-07-06-can-node-parity-reconcile-decommission-precheck Row 63 — accepted the loss naming `/profile` successor) and the metric obsolete (single-bus count from the Platform Teensy on the old shared bus; the network is now three buses through the can-hub Teensy: CAN1=Ball Butler, CAN2=catching cone, CAN3=Jugglebot core). Rebuilt (user-approved): per-bus rows with clickable label (toggles its chart series; readouts keep updating), msg/s, kbit/s, util%, health dot + one uPlot msg/s chart with 30/60/120 s presets (chosen over 5/10/30 s given the 1 Hz cadence); new ES module `can-traffic.js`, old panels.js CAN section deleted, dead subscription removed. **Zero backend work**: `/profile` (1 Hz KeyValues can1/can2 rx/tx/util_pct) + `/link_status` (10 Hz healths + bridge_link). ***THE SLOT TRAP***: wire slot can1_* = physical CAN3, can2_* = physical CAN1 (deliberate firmware remapping, profiling.cpp:24-28 / Teensy_code_canbridge.ino:100-101) — labeling off field names would swap the two most important buses; triple-guarded (registry comment citing firmware; KeyValue drift-pin test producer⊇consumer in test_gui_geometry.py; end-to-end synthetic-stack assertion that the CAN3 row shows the can1_* numbers); mapping independently verified through all four layers. Adversarial review, **1 HIGH confirmed by two lenses, fixed in-session**: the bridge republishes cached /profile at 1 Hz + frozen healths at 10 Hz FOREVER after the can-hub uplink dies (bridge :922/:2101-2107, :898/:2045-2050) → arrival-watchdogs alone would fabricate a live-looking flat line with green dots during the exact outage the panel exists to surface. Fix GUI-side (bridge republish serves other consumers; the honest signal is already in-band: bridge_link + heartbeat_age_ms): three-cause staleness {profileStale, healthStale, linkDown} derived in one place (applyStaleUI) — linkDown drops cached payloads, injects exactly one latched NaN gap per episode (re-armed at UP), forces `--` readouts + UNKNOWN dots + cause-naming badge; profileStale no longer stomps the health dots. CAN2 rendered permanently greyed "n/a — not on uplink" (cone slot = acknowledged firmware TODO; lighting it = PROTOCOL_VERSION 3→4 + wire-freeze re-pin + lockstep flash, 18A precedent; registry-driven so v4 is a two-field change) — chosen over full-stack-now (v3 flashed yesterday; cone bus often disconnected) and over omitting the row. Derivations documented as approximations: msg/s=(rx+tx)/1.0 s vs the firmware's ~1 s LED-cadence window (no window KeyValue on the wire), kbit/s via the firmware's own pre-bit-stuffing 111-bit estimate, util% verbatim; constants from GENERATED geometry-config.js. Other review fixes: STALE badge light-mode ~2.2:1 → filled amber ~9.8:1; RING_CAP 120→132; isolated-sample visibility via uPlot points.filter (17/17 probe assertions); `nanGaps` EXPORTED from telemetry-charts.js (single source — uplot_nan_gap_probe.js still 21/21). NEW reusable harness tools/probes/gui_dom_probe.py + gui_synthetic_stack.py (real rosbridge + scripted rclpy fake stack + headless-chromium CDP DOM assertions). `pytest tests/ros/test_gui_geometry.py -q` (2026-07-11) **48 passed** (+2 drift-pin); JS module checks clean; DOM probe (`python3 tools/probes/gui_dom_probe.py --scenario scenario1`, 2026-07-11, twice) **17/17 PASS** incl. the keystone slot-mapping + `can-linkdown` deception stages; canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2472 passed, 1 xfailed in 582.61 s** | [2026-07-11-gui-can-traffic-per-bus-panel](2026-07-11-gui-can-traffic-per-bus-panel.md) |
| 2026-07-11 | resolved | GUI + can-bridge observability | **GUI "Pos (cmd)" leg series restored — teensy_bridge setpoint echo (`leg_setpoint_echo`).** The missing series was an **architecture-migration orphan, not a chart bug**: commanded leg positions flow trajectory_node →ZMQ:5557→ bridge SetpointPump →UDP→ can-hub Teensy with NO ROS topic on that path; the GUI's datasource (`leg_lengths_topic`) is only published by motion_bridge_node while the dormant run_mpc stack streams, and its old consumer (can_node) was deleted 2026-07-06 — so commanded leg position was unobservable for ALL current-topology runs (uPlot renders an all-NaN series silently absent, pill still lit). Fix (user-approved over trajectory_node-publishes and reuse-leg_lengths_topic): the bridge echoes the last ACCEPTED per-leg u0 on new `leg_setpoint_echo` (Float64MultiArray, 6 motor-rev floats) — covers BOTH sources; setpoint thread only stashes under lock, publish rides the 100 Hz robot_state timer (0.5 s freshness gate, seq-deduped ~40 Hz); stash-before-send deliberate. GUI swapped to it (50 ms throttle, 1 s staleness-clear replacing the never-cleared flatline) — last GUI dependency on motion_bridge_node's **leg output** removed (clears the leg half of the MVP motion_bridge retirement check; `motion/diagnostics` → motion panel still consumes the node); latent bug killed (hand tracking error read `latestCommandedLegs[6]` = leg-0 vel_ff garbage → now `hand_telemetry.pos_cmd`). Adversarial review: 2 HIGH fixed (vendored uPlot 1.6.31 honours only strict null → NaN stale windows drew a **false bridging ramp** — custom per-series `nanGaps` hook, validated against the REAL vendored uPlot in node 21/21; production echo trigger unpinned — mocked-ROS choreography blind spot — now driven via `_publish_robot_state` with robot_state silent) + rosbag record parity, tracking-panel decoupling + hand `--`-unknown, NaN-safe sparklines, try-containment + OSError fault-injection test. Accepted (documented): MutuallyExclusive-callback-group echo gaps during blocking services, setTimeout GUI watchdog, first-echo phantom hand error, no latestHandTelemetry watchdog. Scoped: `pytest tests/ros/test_teensy_bridge_node_setpoint.py tests/ros/test_gui_geometry.py -q` (2026-07-11) 73 passed; `pytest tests/ros/ -q` (2026-07-11) 853 passed; canonical full suite immediately pre-commit (`pytest tests/ -q`, run 2026-07-11): **2469 passed, 1 xfailed in 574.97 s** | [2026-07-11-gui-leg-setpoint-echo-poscmd](2026-07-11-gui-leg-setpoint-echo-poscmd.md) |
| 2026-07-10 | resolved (bench validation pending) | S4 | **S4 stutter forensics + guard-latch recovery stack.** The evening S4 session's stutter is a **~6 Hz leg-servo limit cycle** (fundamental 5.9–6.1 Hz on all six legs, speed-independent, = the `pos_gain/2π` bandwidth; ODrives actively drive it with ±4–6 A current slams; gains are HOLD-tuned per the config's own comments) — **40 Hz is exonerated with numbers** (22 µm interp fidelity, 6 mm/s join step, zero 40 Hz spectral power, CAN3 load knot-rate-independent). The MAX_DEVIATION latch was **legitimate** (raw u0 ran ~0.49 rev ahead of the stalling leg; the guard watches unclamped u0, the ODrive sees only the lead-clamped command) and the failed `/clear_errors` was a designed-in catch-22 (guard-blind trajectory_node kept advancing to 2.4–2.8 rev divergence → instant re-latch, invisible at 10 Hz). Landed: guard-latch recovery stack (freeze + gate-validated **profiled descent** onto the frozen encoder — an instant reseed cannot pass the pump's 0.3 rev step gate, discovered by the implementing agent — + mode-preserving orchestrator FAULT + one-call `/recover`); **firmware v3 FLASHED** (vel_ff kept at lead-clamp engage, MAX_LEAD 0.15→0.10, per-leg deviation/lead-clamp telemetry + latch snapshot); bridge-owned Ctrl-C disarm→settle→stow; log ergonomics; rosbridge/GUI fix (venv-invisible tornado). Adversarial review caught a BLOCKING choreography deadlock (guard-FAULT publishing ERROR would silence the emitter and kill the recovery it routes) — second cross-process bug in three days invisible to mocked per-node tests. **Root fix pending**: S4b gain-retune session (3-point pos_gain discrimination {25/40/55}, then vel_gain damping A/B). Full suite pre-pin-fix: 2366 passed / 1 failed (the intended v3 version pin); scoped `tests/teensy_link` 188/188 green after the pin fix (2026-07-10). | [2026-07-10-s4-stutter-guard-forensics-recovery-stack](2026-07-10-s4-stutter-guard-forensics-recovery-stack.md) |
| 2026-07-08 | resolved | 1–7 (meta) | **MVP Trajectory Bringup — autonomous build run (closing meta-entry).** Closes the `mvp-trajectory-bringup` arc: the MPC 40 Hz CasADi hot loop removed from the leg path (dormant, source retained), replaced by a simple gate-guarded Jetson-side trajectory generator streaming 40 Hz waypoints on :5557 into the validated `SetpointPump` → can-hub-Teensy Hermite chain. **7 phases implemented autonomously** (a fresh Opus 4.8 agent per phase under Fable 5 orchestration), each followed by an **independent `/audit` fix-and-land round**; **1 phase-agent death salvaged (Phase 4, documented in-entry) + 2 auxiliary agents (Phase-5 fixer, Phase-7 auditor) resumed from transcript**. Suite **1956 → 2274 (+318 tests)**, all new tests only; **ci-deep green twice** (Phase-5 backfill 2164/1 + Phase-7 2274/1). The load-bearing section is the **decision digest** — every decide+document fork across the seven Discussions + audit rounds, grouped by phase, plus the orchestrator-level calls (fixed 40 Hz; Phase-2 `BUSY`-then-lift-in-5; pending-stop retry over the deferred retargeted stop; document-not-fix the ≤1.5 % jerk bias; 12° tilt clamp; light-scope vel-match deferral; `juggle_bb_catch` port deferred to 8/9; audit-fix packages skip a second `/audit`). **Audit-arc retrospective**: 7 rounds, ~62 findings, 7 BLOCKING (P2 ×1, P4 ×1, P7 ×5) — the Phase-7 lesson that mocked-ROS unit tests are blind to cross-process choreography (target_id routing, announcement ordering, ToF arithmetic, goal concurrency, feedback semantics all only visible to an auditor tracing the real ordering); production-ordering tests now pin those seams. **Honest limits**: no hardware validation yet; contact quality has no sim criterion (MuJoCo contact→instant-hold capture model — "caught" is geometric, separation vacuously 0), so 7b's two-consecutive-bounce-out abort is the operative hardware guard. Full suite `pytest tests/ -q` (2026-07-08) = **2274 passed, 1 xfailed in 553.60 s**; ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, 2026-07-08) = **2274 passed, 1 xfailed, 198 warnings in 3024.70 s**. All hardware sessions deferred to `tests/hardware/mvp_bench_runbook.md` (S1→S8). | [2026-07-08-mvp-autonomous-build-run](2026-07-08-mvp-autonomous-build-run.md) |
| 2026-07-08 | resolved | 7 | **MVP Phase 7 — BB→Jugglebot reload action (software-complete; hardware deferred).** The reload (MVP goal 4): BB aims at Jugglebot's ACTIVE catch point and throws; Jugglebot (CATCH mode, hand armed by the existing coordinator) tilts to receive and catches, exposed as `jugglebot/reload` (new `Reload.action`). **Integration gap closed**: `trajectory_node`'s `catch/dynamic_target` now routes through `planner.build_catch` (reach with zero translational arrival velocity → tilt-through-seat decay → literal quiescent hold) not the Phase-5 reach-only `build_timed` that parked the tilted rim — without this a hardware reload catches WITHOUT tilt. The receive tilt is already collinear with the arrival velocity (the hardware-validated `catch_coordinator.compute_catch_orientation` ships it in `target_quat`; `build_catch` reads it — decide+document fork: NOT moved to `target_vel`, which is always zero, to avoid a cross-subsystem contract change to a proven node); reach-freeze + `FROZEN` semantics preserved. New pure-Python `reload_sequencer` FSM (CHECKING → AIMING → THROW_PENDING → BALL_IN_FLIGHT → CATCHING → SETTLING; loud rejects: WRONG_MODE/BB_DISCONNECTED/BB_BUSY/MOCAP_STALE/NOT_STREAMING/NO_BALL/CANT_MAKE_LEAD/BB; aborts: NO_ANNOUNCEMENT/MODE_CHANGED/BB_ERROR/MISSED_INFEASIBLE) + thin `reload_coordinator_node` that **orchestrates only** (trajectory plans motion, catch_coordinator arms the hand, ball_butler throws; never bypasses the gate). `BallButlerThrow` point-target extension (`use_target_point` skips the QTM lookup; `aim_only` = speed-0 7a fast-path; default-zero preserves callers). Announcement→correlation→coordinator integration test drives the REAL BallTracker + CatchCoordinator with synthesized messages (bags predate the pipeline; no rosbag2 reader in mocked CI). Separable Phase-6 follow-up: `sim/hand/trajectory.py` `CATCH_VEL_RATIO` 0.9→0.6 (config truth) — tiny blast radius (symbolic test ref); five reload-gate JSONs refreshed at 0.6, all **CORE PASS** unchanged (caught 20/20, hold ≤0.02 mm, limits 156/660/10331), deferred vel-match rose as expected (nominal worst 0.343→0.440). Full suite `pytest tests/ -q` (2026-07-08) = **2261 passed, 1 xfailed in 541.51 s** (baseline 2223/1 at bf5b46e; net +38 = new tests only: 3 trajectory-node catch + 3 BB-node + 19 sequencer + 10 coordinator + 3 integration); `colcon build --packages-select jugglebot_interfaces jugglebot` = 2 packages, 0 errors. **Deferred**: staged operator sessions 7a (aim-only frame + 744.3 mm catch-z verification) / 7b (throw + static catch, two-bounce-out abort) / 7c (full reload action) — `tests/hardware/session_phase7_reload.md`. **Audit-fix round (2026-07-08, last before hardware; 5 BLOCKING + WARNING/NOTE)**: fixed five cross-process choreography bugs the mocked-ROS per-node tests couldn't see — `target_name` omitted → BB announced `target_id='point'` (whole catch pipeline dropped the ball); the announcement is published *inside* the throw handler (AIMING) so the phase-gated `note_announcement` discarded it (regated on the throw being commanded); the settle deadline omitted the 0.61–0.73 s ToF so a nominal catch read MISSED (now anchored on the announced landing, ROS→perf converted); concurrent goals double-threw (now REJECTed); `FROZEN`/`STALE_STATE` catch feedback latched `MISSED_INFEASIBLE` (now ignored; a later accept clears). Plus: receive tilt **clamped at `MAX_TILT_DEG=12°`** (single source with `tilt_geometry`) not rejected above 30° (real BB arrivals are 18–40° off vertical → were silently dropped/blacklisted); non-finite-point guard; tracker-id-correlated CAUGHT; 7b rewritten to TRAJECTORY-mode static catch. `pytest tests/ -q` (2026-07-08) = **2274 passed, 1 xfailed in 553.60 s** (net +13); ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, 2026-07-08) = **2274 passed, 1 xfailed, 198 warnings in 3024.70 s (0:50:24)**. | [2026-07-08-mvp-phase7-reload-action](2026-07-08-mvp-phase7-reload-action.md) |
| 2026-07-08 | resolved | 6 | **MVP Phase 6 — Catch trajectory + sim reload gate (LANDED LIGHT; vel-match deferred).** The reload catch trajectory + a production-in-the-loop sim gate. New pure `motion/trajectory/tilt_geometry.py` (`tilt_to_receive` collinear-catch tilt, ≤12° cup-axis anti-parallel to the arrival velocity; re-exports the Phase-4 `shaping.py` cup lever arm 1.66 mm/deg @ 744.3 mm centre — single source of truth) and `ballistics_bc.py` (touchdown quadratic + launch/arrival BCs, copied-not-imported from `controller/ballistics.py`, overlap regression-pinned). `planner.build_catch`: reach (translational arrival velocity **forced zero** — velocity matching is the hand's job) → **tilt-through-seat** decay (small residual tilt rate `0.07 rad/s` so the rim isn't parked, decayed to rest over 0.15 s) → **literal quiescent hold** → optional return; fixed-lead loud `TOO_FAST`; gated by the fast `validate_follow` (fork owned: matches the Phase-5 catch path, catch is never lean-shaped, step-bound bit-identical so pump-acceptance holds). Ported `sim/juggle_noise.py`; new headless seeded harness `sim/reload_gate.py` drives the **real** `build_catch` + `KnotEmitter` + a real `SetpointPump` into the MuJoCo plant, ballistic ball under §3 noise, arm-and-forget `HandCatchSequence` sampled at the physics-substep rate (Teensy runs the hand high-rate); JSON report. **Six-file diff-audit**: the bb divergences are two-ball features (Phases 8/9), NOT catch-critical — nothing ported but `juggle_noise`; one HIGH finding surfaced + deferred (`sim/hand/trajectory.py` `CATCH_VEL_RATIO = 0.9` is stale vs config `0.6`). **Reload gate** (`python sim/reload_gate.py --trials 20`, 2026-07-08) = **CORE PASS**: caught 20/20, held 20/20, core_clean 18/20, hold travel ≤0.01 mm, tilt ≤0.01°, separation 0 ms, feasibility violations 0, pump rejects 0; all 4 robustness sweeps (arm ±30 ms, event_vel ±10 %) CORE PASS 20/20 each (nominal core_clean 18/20). **Required leg limits published to Phase 4**: vel ≈156 mm/s, acc ≈660 mm/s², jerk ≈10 331 mm/s³. **Deferred (light-scope, operator-approved)**: hand-contact vel-match ≤15% floors ~0.26 — root-caused (the ~14 ms Teensy velocity-hold is narrower than the ±20 ms capture-timing alignment in MuJoCo's contact→instant-hold model; cup axis IS aligned — the mismatch is uniform ~0.74× magnitude) and inconsistent with the hardware 0.6 catch ratio (designed ~40% first-contact mismatch, absorbs over the stroke). No ROS/config/codegen change → no colcon/codegen gate. Full suite `pytest tests/ -q` (2026-07-08) = **2222 passed, 1 xfailed in 526.04 s** (baseline 2179/1 at 22ed9cf; net +43 = new tests only: 15 tilt + 7 ballistics + 12 catch + 9 harness). | [2026-07-08-mvp-phase6-catch-trajectory-sim-gate](2026-07-08-mvp-phase6-catch-trajectory-sim-gate.md) |
| 2026-07-08 | resolved | 5 | **MVP Phase 5 — Timed target states (code-complete; hardware deferred).** MVP goal 3: reach a pose+orientation at a nominal velocity at an ABSOLUTE arrival time, with a too-tight lead **loudly rejected** (`TOO_FAST` + the minimal feasible lead), never silently slowed. New pure `planner.build_timed`: a **fixed-lead reach** to `(target_pose, target_twist)`; the plan is ALWAYS rest-terminating (a nonzero arrival velocity gets a decel-to-rest continuation — a final nonzero-velocity segment would be an unbounded-jerk terminal-hold snap); `hold_after` = hold-at-target vs return-to-neutral. Gated by the **fast** `validate_follow`, which is what enables the mid-plan **supersede** — a C2 replan installs single-digit ms typical after its seed, guard-bounded — install-continuity rejects `STALE_STATE` on drift >0.06 rev; worst case tens of ms with the appended stop-stretch (the TOCTOU class the Phase-2 install-continuity guard closed on the ~377 ms analytic path), so the temporary Phase-2 `BUSY` restriction is **lifted for the timed path** (`go_to_pose` keeps `BUSY` — it uses the analytic gate for shaped plans; the asymmetry is deliberate + tested). New interfaces `TimedTarget.srv` (response `code` = the feasibility **string** enum, matching GoToPose) + `TargetFeedback.msg`; `trajectory/timed_target` (TRAJECTORY mode) + `catch/dynamic_target` (CATCH mode) BOTH route through `build_timed` (+ a reach-freeze window holding the committed catch reach through the last `JB_TRAJ_CATCH_REACH_FREEZE_S`); `trajectory/target_feedback` carries accept/reject to `catch_coordinator_node`, which **swaps** its dormant ZMQ :5559 `TargetFeedbackSub` for the topic (feasibility blacklist semantics preserved). One ROS-clock→perf_counter conversion point; the catch path is already perf-domain (system-wide `CLOCK_MONOTONIC`). The catch z is lifted by the active-z (170) from the MPC-offset convention (0 = active) to the trajectory STOW-relative convention — flagged for Phase-7 hardware verification (the gate rejects an out-of-stroke z loudly meanwhile). Every emitted timed knot pump-accepted (invariant re-asserted). No `hardware_config.yaml`/codegen change (Phase-5 constants landed in Phase 1). Full suite `pytest tests/ -q` (2026-07-08) = 2164 passed, 1 xfailed in 535.82 s (baseline 2128/1 at 1c0f9c1; net +36 = new tests only: 12 planner-timed + 17 node + 7 catch-coordinator); ci-deep (`pytest tests/ -q --hypothesis-profile=ci-deep`, run 2026-07-08) = **2164 passed, 1 xfailed, 198 warnings in 3001.75 s (0:50:01)**; `colcon build --packages-select jugglebot_interfaces jugglebot` clean (`TimedTarget.srv`, `TargetFeedback.msg`). **Deferred**: the ±25 ms mocap timed-move battery + too-tight rejection + mid-plan supersede demo (`tests/hardware/session_phase5_timed.md`). **Audit-fix round (same day, 3 WARNING + 6 NOTE)**: `trajectory_op.max_timed_lead_s` 60 s clock-domain guard in `build_timed` (an epoch-magnitude lead previously → ~7e10-element `np.arange` MemoryError killing the node); CATCH added to `_MOTION_MODES` (graceful stop on leaving-CATCH-mid-reach AND entering-CATCH-mid-move); reach-freeze releases after `arrival + JB_TRAJ_CATCH_SETTLE_HOLD_S` (was latched forever, silently dropping post-catch targets) with a distinct `FROZEN` service-level feedback code; coordinator feedback now source-filtered (`'catch'` only) and `STALE_STATE`/`FROZEN` excluded from blacklist counting; `_min_feasible_timed` fall-through validated; timed installs record the accepting report's predicted peaks for `/diagnose`; stale ZMQ-era comments/docs updated; +15 tests. | [2026-07-08-mvp-phase5-timed-targets](2026-07-08-mvp-phase5-timed-targets.md) |
| 2026-07-08 | resolved | 4 | **MVP Phase 4 — Lean shaping + limit-ramp tooling (code-complete; ramp deferred).** The *software* for the multi-session hardware limit ramp — observability + the one opt-in refinement — landing **no limit values** (the ramp is operator bench work). (1) **Lean shaping** `motion/trajectory/shaping.py`: a translation move superposes a small tilt `∝` its lateral accel (`rx += −k·a_y/g`, `ry += +k·a_x/g`, gain `JB_TRAJ_LEAN_GAIN` **default 0.0**, 5° cap) to keep a ball seated, with a cup-height-derived lever-arm xy compensation ported from `Jugglebot-bb/sim/juggle_tilt.py::realize_tilted` (`centroid = target − shift`, arm **+95.1 mm ⇒ 1.66 mm/deg**, `+ry→+x`/`+rx→−y`, sign pinned to the physics not prose). Runs **before** `validate` (planner wraps the plan; node passes the shaper into `build_move`) so the single-gate invariant holds and the stretch loop sizes the shaped motion; gain≤0 / HoldPlan are the identity. Shaped `(pose,twist,accel)` are analytic-consistent (no FF mismatch); **every shaped knot stays pump-accepted**. (2) **Per-call `GoToPose.lean_gain`** (<0 ⇒ config default, ≥0 ⇒ clamp [0,1], default 0.0 ⇒ OFF) for the A/B. (3) **Realized-peak observability**: node tracks realized leg vel/acc/jerk off the emitted knots (reset per move) beside the gate's predicted peaks + session limits + active `lean_gain` on `trajectory/diagnostics`; `sim/analysis/diagnose.py::summarise_trajectory_moves` → per-move `peaks + headroom` table. (4) Operator harness `tests/hardware/traj_ramp_battery.py` (service-calls-only) + protocol `session_phase4_ramp.md`. **Salvage**: implemented by a prior agent that died mid-run leaving the diff uncommitted + unverified; this session independently verified every invariant vs the reference geometry, ran the gates, and applied the audit. Adopted forks (documented): 0.0=explicit-OFF so a ramped config gain never silently leans bare callers; fixed nominal cup arm (per-pose arm deferred to Phase 6); the position-continuous-but-C1-seam tilt-*rate* boundary transient (bounded by the gate, an A/B watch-item — windowed lean deferred). Corrected (real bugs the predecessor's tests missed): (a) the `/diagnose` summariser collapsed the holdless ramp battery into ONE row (`plan.kind` stays `'move'` across a completed move's terminal hold) — fixed with a monotonic `move_seq` diagnostics key segmenting per accepted move; (b) jerk headroom used the coarse realized peak — now also surfaces `used_pct_predicted` (gate-authoritative fine peak) and the `/diagnose` doc ramps against it; (c) a broken logbook link + imprecise port line-refs. No `hardware_config.yaml`/codegen change (no limit values). Full suite `pytest tests/ -q` (2026-07-08) = 2120 passed, 1 xfailed in 518.69 s (baseline 2094/1 at e039cc0; net +26 = new tests only: 14 shaping + 5 diagnose + 7 node); `colcon build --packages-select jugglebot_interfaces jugglebot` clean (`GoToPose.lean_gain`). **Deferred**: the per-step limit ramp + the lean A/B (operator bench sessions). | [2026-07-08-mvp-phase4-shaping-ramp-tooling](2026-07-08-mvp-phase4-shaping-ramp-tooling.md) |
| 2026-07-08 | resolved | 3 | **MVP Phase 3 — SpaceMouse streaming (fast follower gate + graceful stop).** Continuous target following through the validated streaming layer. Meets the two Phase-2-audit prerequisites. (1) **Fast follower gate** `feasibility.validate_follow`: profiling showed the analytic gate's ~377 ms is ~90 % the per-sample analytic Jacobian chain (not the ~9 % condition SVD), so the fast gate sidesteps the Jacobian — vectorised pose→leg-extension sampling (`QuinticSegment.eval_pose_batch` + batched IK, equal to the scalar chain to 1e-9) with leg vel/acc/jerk by finite difference + a decimated condition SVD → **~1.5–4 ms**; vel/acc/step match analytic to < 0.1 %, jerk held **strictly conservative** (≤ 1.5 % under-measure at 300 samples, ×1.05), knot-step-bound **bit-identical** so pump-acceptance is preserved. Same module, same `FeasibilityReport`, reached only through `planner` — the single-gate contract holds. (2) **Duration-stretched graceful stop** `planner.build_graceful_stop`: a decel-in-place that converges for gate-limited seeds by lengthening the horizon, EXCEPT a near-boundary outward seed whose margin is under the decel overshoot ~0.2·v·T (audit-corrected — the node retries from the decaying live state via a pending-stop flag, not a latch); replaces the Phase-2 STANDBY-exit catch-and-complete fallback and backs follower input-loss. New pure `TargetFollower` (clamp → deadband → replan / keep-last-plan) + `planner.build_follow`; `trajectory_node` gains `platform_pose_topic` publisher-field mode gating + `gravity_offset` composition (verbatim ports), the follower replan inside the 40 Hz emitter tick (drain-to-latest), input-loss stop, and a **ray-clamp saturation policy** (clamp to the nearest reachable pose along the current→target ray — uses the EXISTING stroke envelope, invents no new limit; throttled WARN). No new interfaces/config/launch → no colcon/codegen gate. Software complete; **manual-flight hardware session DEFERRED** (gentle fly / saturation shove / mid-flight unplug). Full suite `pytest tests/ -q` (2026-07-08) = 2079 passed, 1 xfailed in 502.33 s (baseline 2047/1 at f38153f; net +32 = new tests only). **Audit-fix round (2026-07-08)**: 4 WARNINGs + 6 NOTEs applied — TOCTOU-safe conditional follower install, `validate_follow` now gates zero-segment `HoldPlan`s, pending-stop retry replacing the always-valid overclaim, entry grace window, sustained-reject skip, per-replan `t0`, hold/go_home rejected in follower modes; `pytest tests/ -q` (2026-07-08) = **2094 passed / 1 xfailed, 0 real failures** (the sole reported failure is the load-flaky `test_hot_loop_allocation_contract`, which passes isolated; net +15 = new audit-fix tests only). | [2026-07-08-mvp-phase3-spacemouse-streaming](2026-07-08-mvp-phase3-spacemouse-streaming.md) |
| 2026-07-07 | resolved | 2 | **MVP Phase 2 — Waypoint moves at very low limits.** The trajectory generator gains its arbitrary-target profiled-move surface + the full feasibility gate. `feasibility.validate` completes to: geometry (non-finite→`UNREACHABLE`, stroke→`WORKSPACE`, Jacobian condition backstop→`UNREACHABLE`) → leg vel/acc/jerk peaks (jerk = per-segment finite-diff of the analytic leg accel) → per-knot `u0` step bound (25 ms spacing, 20% margin) — all peaks measured in one pass so the planner reads the worst ratio off one report. `planner.build_move`: rest-terminating quintic; `duration_s` None/0 ⇒ minimal-feasible via a duration-stretch loop (leg peaks scale exactly `1/Tⁿ` from rest ⇒ converges in ≤2 `validate` calls), an explicit too-tight duration ⇒ loud `TOO_FAST` with `min_duration_s`, a spatial `WORKSPACE`/`UNREACHABLE` re-raised immediately. New interfaces `GoToPose.srv` (response `code` = feasibility **string** enum, deviating from the plan's `int32` — anti-drift), `SetTrajectoryLimits.srv` (ceiling-clamped session ramp), typed `TrajectoryStatus.msg` (migrated off the Phase-1 `DiagnosticStatus` stand-in). `trajectory/go_to_pose` (TRAJECTORY-mode-only, else `WRONG_MODE`; not-seeded ⇒ `STALE_STATE`) + `trajectory/set_limits` + `trajectory/diagnostics`; `ActiveMode.TRAJECTORY` + `'trajectory'` orchestrator command. Every emitted frame still pump-accepted; nothing bypasses the single gate. Software complete; **hardware move-battery + loud-rejection demo DEFERRED** to an operator bench session. Full suite `pytest tests/ -q` (2026-07-07) = 2041 passed, 1 xfailed in 500.17 s (baseline 1996/1; net +45 = new tests only); `colcon build --packages-select jugglebot_interfaces jugglebot` clean. | [2026-07-07-mvp-phase2-waypoint-moves](2026-07-07-mvp-phase2-waypoint-moves.md) |
| 2026-07-07 | resolved | 1 | **MVP Phase 1 — Streaming foundation (hold via the new trajectory path).** Removes the compute-marginal MPC 40 Hz hot loop from the leg path (dormant, source retained) and replaces it with a simple Jetson-side trajectory generator. New pure package `jugglebot.motion.trajectory` (quintic copies pinned bit-for-bit to `controller/`, `TrajectoryLimits`/`QuinticSegment`/`TrajectoryPlan`/`HoldPlan`, the single canonical `feasibility.validate` gate, `planner` constructors, `KnotEmitter`), `motion/ipc.MpcCommandPub` (sole-binder :5557 interlock), the thin `trajectory_node` (dedicated 40 Hz emitter thread, measured-telemetry-seeded hold, `trajectory/hold`+`go_home`, `trajectory/status` as DiagnosticStatus), and the bridge `set_setpoint_output` (SetBool) runtime-arming service (link + fresh-stream + 0.25 rev u0-vs-encoder preconditions, stream-then-arm). Streams only a HOLD in Phase 1; every emitter frame is tested-accepted by a real `SetpointPump` (the production-in-the-loop invariant). `JB_TRAJ_*` config + `HW_SECTIONS` row; launch drops `mpc_bridge_node`, adds `trajectory_node`. Software complete; **hardware arm+120s-hold DEFERRED** to an operator bench session (`tests/hardware/session_phase1_hold.md` + read-only probe `tools/probes/traj_stream_probe.py`). Full suite `pytest tests/ -q` (2026-07-07) = 1993 passed, 1 xfailed in 625.44 s (baseline 1956/1; net +37 = new tests only). | [2026-07-07-mvp-phase1-streaming-foundation](2026-07-07-mvp-phase1-streaming-foundation.md) |
| 2026-07-06 | resolved | post-port code-clarity cleanup | **De-opaque CAN-port docstrings/comments — strip plan-document vocabulary (Phase N, Tier, item, Flash-A/B, U3-iv, Fable-5, decision-ids, the α/β path codenames, plan slugs) from the code so a collaborator reading only the code understands it.** Scope: all CAN-port-arc code (files touched on/after 2026-06-03; ~130 files, ~825 rewrites) in six gated, path-scoped commits — generator+regen (`63feaef`), `teensy_link/` lib (`f13be2b`), firmware (`b60e0c6`), `teensy_bridge_node`+launches (`eeb76a9`), test suite (`f1e5ac3`), probes/tools/benches (`235514d`). Comment/string text only — no wire-format/logic/behaviour change. Operator decisions: α→"Jetson-relay" / β→"Teensy-side"; ADR refs KEPT (durable); handoff decision-ids + Fable-5 attributions stripped; logbook pointers only at defining sites; self-explanatory local "Phase 1/2 / Step 1/2" markers preserved (22 sites). Pre-port May-MPC + friction-FF plan vocabulary deliberately OUT of scope (different older plans; clean 2026-05-21→06-03 date cut). Surfaced+fixed 2 stale comments in-pass (`rpc.h` "hoist at Phase 10" already done; `setup.py` "NOT in jugglebot_launch" now false) + a `can_buses.h` D7 reword. Completeness backstop: the initial gathering regex under-caught (lowercase phase, bare row/D/9a/gap tokens, letter-Phase, 5 uninventoried files) → a final complete-pattern whole-tree sweep caught every straggler. A concurrent session's `SET_LEAD_ACCEL_LIMIT` feature shared the working tree (additive, non-conflicting) → all commits staged by explicit path with `:!` exclusions; **3 test files it had open are DEFERRED** to a follow-up. Full suite 1955 pass / 1 xfail (2026-07-07, 639.44 s; one load-flaky `test_decay_boundary_continuity`, passes isolated) | [2026-07-06-canport-docstring-deopaque](2026-07-06-canport-docstring-deopaque.md) |
| 2026-07-06 | resolved | teensy-can-offload Phase 13 | **Legacy SocketCAN stack DECOMMISSIONED**: `can_node.py` (1725 lines) + `can/bus.py` (206) + their 98 tests + the `can_node` console-script + `catching_cone_test.launch.py` (last launcher of can_node; bridge owns the cone/BB conduit — operator chose delete over rewire) all deleted in one atomic commit, gated on the same-day parity pre-check (scan clean) + explicit operator approval. KEPT: `can/odrive.py`/`ball_butler.py`/`catching_cone.py`/`motor_state.py`/`throw_ballistics.py` (live bridge/orchestrator + xref ground truth) — so `python-can` STAYS pinned (odrive.py:14/ball_butler.py:13 `import can`; the plan's "remove python-can" assumed can_node was the only consumer — disproved); `cantools` also stays but has NO live importer (audit-corrected: a legacy pin, droppable without refactor). The naive-delete trap caught: `can/__init__.py` eagerly imported `CANBus`, which would have broken every `from jugglebot.can import …`. Ripples: setup.py entry point, launch comment + `/platform_target_reached` rosbag entry, provenance docstring notes (can_node.py:NNN citations → git history), matrix Phase-13 caveat → DONE, plan Phase-13 block rewritten. can0 teardown = disabling `init-can.service` (the ONLY loader; stock NVIDIA denylist blocks autoload) — **operator-run + verified 2026-07-06 11:39 (lsmod shows no CAN modules; can0 device gone)**; revert is one systemctl command | [2026-07-06-phase13-socketcan-decommission](2026-07-06-phase13-socketcan-decommission.md) |
| 2026-07-06 | resolved | teensy-can-offload Phase 13 (pre-check) | **Parity-matrix reconcile vs the canhub-hardening arc + Phase-13 decommission pre-check — counts 79/12/23/3 → 85/9/20/3, PARTIAL safety scan CLEAN (no dropped can_node safety behaviour open), 3 GAP dispositions confirmed (row 18 deliberately open for Phase 12; rows 47/63 no live consumer — not blockers)**. Rows flipped: 53 + 57 PARTIAL→validated (`559c6dc` deactivate-at-STOW guard HW-validated; `83ac938` clear_disarm_reasons mirror + native test); 19 PARTIAL→unvalidated (`25557e2` stow-on-shutdown, Ctrl-C HW confirm pending); 1/17/31/55 unvalidated→validated (the native harness compiles the REAL fault machine + cold-start TUs — `5f07ad5`/`431ea89` — asserting the rearm-on-redrop golden, mid-op CAN3-loss aborts, mutual-exclusion rejects, homing abort gates; [18A] `2b749e3` host trusts the firmware HomingResult; 2026-07-04 sitting CHECKs 4/5). Stale global caveats struck ("no compiled-C++ firmware test", frozen-robot_state-on-link-loss, clean-shutdown stow, six-leg stow single-leg-only, unimplemented bus-off register read — each with its closing commit). The one genuine dropped safety semantic found in this family since the audit (clean-shutdown auto-stow) was operator-surfaced 2026-07-04 and closed same-day. Verdict: **Phase-13 decommission NOT blocked** | [2026-07-06-can-node-parity-reconcile-decommission-precheck](2026-07-06-can-node-parity-reconcile-decommission-precheck.md) |
| 2026-07-06 | resolved | canhub-hardening (Tier-2 residual) | **health_of() bus-off wiring — live FLTCONF joins RX staleness in the per-bus health classification**, closing the last shipped `TODO(bench)` in the bus-health path. Previously staleness-only: a bus in error-passive/bus-off with frames still flowing classified OK and accepted motion; `BusHealth::BUS_OFF` was unreachable, so the host's fatal-CAN OR-term, shutdown-stow skip, and part of the recovery-edge trigger were dead code. Fix: `BusRxHealth.flt_live` (live ESR1.FLTCONF, clamped 0/1/2) sampled per 1 kHz service tick from the SAME ESR1 read as `synced` (zero new register traffic, non-sticky — recovers with the bus); pure header-inline `classify_bus_health()` (never-seen→UNKNOWN; bus-off→BUS_OFF; passive OR stale>2s→WARN; else OK), natively pinned. WARN keys on error-PASSIVE (128) not the CAN warning level (96) — the measured 12 V-ramp REC burst (~121) sits between them, so 96 would flag every normal power-on. Strictly per-bus (operator-stipulated): a CAN1 fault cannot gate CAN3. `[canhealth]` gains `fltNow=` beside the sticky `flt=`. Bench-validated (2 operator-run checks): BB unplug/replug → `bb` fltNow active→passive→active with CAN3 untouched; CAN3 12 V cycle → WARN through the outage, clean recovery + exactly one host conservative re-read. Check 2 surfaced a **new known-benign signature** (documented, deliberately NOT hardened): the 12 V reconnection ramp punches sticky `flt=BUSOFF`/`tec=254` via a real ms-scale self-healing bus-off (gate reopens on the first valid frame while the medium is still dirty; BIT1/STUFF errors +8 TEC apiece from the 128 pin through 256; auto-recovery BOFFREC=0) — `fltNow` never shows it, no gate misbehaves, fresh-BOOT flt/tec rule unchanged. Native 16/16 (170.23 s, 2026-07-05); full suite 2054 pass / 1 xfail (651.63 s pre-commit re-run, 2026-07-06); flashed 2026-07-05 | [2026-07-06-canhub-health-of-busoff-wiring](2026-07-06-canhub-health-of-busoff-wiring.md) |
| 2026-07-05 | resolved | deactivate (operator request) | **Deactivate is a no-op when the platform is already at STOW** (hardware-validated 2026-07-05: no-op when stowed, still lowers when not). Operator ask: the legs should NOT deactivate if already stowed (all target legs IDLE with pos near stow). Previously `/deactivate` (and the activate-or-deactivate service + stow-on-shutdown) always ran the firmware DEACTIVATE — re-arming each leg to CLOSED_LOOP (a jolt), lowering to a no-op STOW, then IDLE. Guard added at `_run_deactivate` (the single enforcement point all three callers route through): `_legs_already_stowed(axes)` returns True iff every target leg is IDLE and `|pos| <= _STOW_POS_MAX_REV` (0.2 rev; STOW=0.0 in the DeactivateMonitor convention, `abs()` so it's sign-convention-safe — a bare `pos<=0.2` would wrongly skip when an active pose reads negative, leaving the platform UP). Fail-SAFE: missing telemetry / a missing diagnostic / any leg not-IDLE-or-extended / a NaN position (adversarial-review catch: `abs(NaN)>0.2` is False, so a faulted-active-leg-at-NaN would else look stowed) → False → the deactivate proceeds (only skips on a POSITIVE all-stowed confirmation; the no-op still idles the HAND for de-energise-all-axes parity). Host-only, no reflash. +3 node tests (no-op fires no leg RPC but idles hand; still-extended proceeds; predicate incl. NaN + missing-diag). `pytest tests/ -q` 2054 pass / 1 xfail (645.50 s, 2026-07-05) | [2026-07-05-deactivate-at-stow-noop-guard](2026-07-05-deactivate-at-stow-noop-guard.md) |
| 2026-07-05 | fix-landed-pending-hardware-confirm | canhub-hardening Tier-2 [18A] | **HomingResult uplink closes the homing silent-abort false-success class**. Firmware `finish()` IDLEs a leg on BOTH a successful trip AND every abort (bus-down / guard-E-STOP mid-move); only success also runs `set_absolute_position`. So a mid-drive abort was INDISTINGUISHABLE from success to the host's CLOSED_LOOP→IDLE state-cycle observer → it declared the leg homed while the encoder reference was never set (uncalibrated zero, orchestrator proceeds). Fix: uplink the firmware's real per-axis `HomingResult` (NONE/RUNNING/OK/FAILED) by reusing the Diagnostic's 3-byte pad as `homing_result` (u8) + 2-byte pad — size-preserving (36 B) but a layout change → **`PROTOCOL_VERSION` 1→2** (freeze `_EXPECTED` re-pinned + version-freeze bumped; firmware+host lockstep). Host `HomingMonitor` now TRUSTS the result (OK→DONE, FAILED→FAILED), gated on seeing HOMING_RUNNING this run (a stale prior-home result can't terminate the new one); the state-cycle success inference dropped, timeout + active_errors backstops kept. Firmware `send_diag` populates it + a `homing_result` transition force-emits a Diagnostic (delivering the RUNNING transition promptly, decoupled from the lagging ODrive axis_state, so `saw_running` arms reliably). +7 host tests incl. the false-success regression; 4 home-flow ros tests + freeze/version tests updated. Suite 2051 pass / 1 xfail (469.60 s, 2026-07-05); `pio` green (225600 text). NOT yet flashed (consolidates with item 20 into ONE Phase-2 flash) | [2026-07-05-canhub-hardening-18a-homing-result-uplink](2026-07-05-canhub-hardening-18a-homing-result-uplink.md) |
| 2026-07-05 | fix-landed-pending-hardware-confirm | canhub-hardening Tier-2 item 20 | **Eight small firmware follow-ups** (Fable-5 item 20), landed on top of the CAN3 presence-gate work (nothing reverted). **(1)** version-sweep re-query — a lost Get_Version reply no longer leaves an axis's version permanently missing (re-query present-but-unreceived axes, paced 1 s). **(2)** `REBOOT_ODRIVES` rejected while `fault_stow_pending()` (operator-decided) — a reboot mid-stow disarms the raised legs → gravity drop; mirrors the cold-start reject. **(3)** documented that the deferred stow deliberately completes through a guard E-STOP / ODrive-fatal (aborting would gravity-drop the raised legs). **(4)** `SETTLE_STOP_US` sourced from a new `Homing::STOP_SETTLE_MS` (`jugglebot_homing.stop_settle_ms:5`) — was a hardcoded `5000ull` whose comment named a `Homing::` constant that never existed (the real 5 ms lived only in `BBHand::`). **(5)** documented HOME's axis-6 policy (owned by `homing_request`, consistent with `hand_axis6_permitted`). **(6)** `RESULT_BUF_CAP` static_assert vs `ResultAxisVersions`. **(7)** `encode_frame` nullptr guard (`len>0` + null payload returned a garbage frame → now 0; codegen, no wire-layout / PROTOCOL_VERSION impact). **(8)** cmd-result ring → drop-OLDEST (operator-decided) so the loud command-outcome channel keeps the freshest results. Native tests for items 1/2/7 (+ a version-cache test-isolation fix in `version_check_init`). Suite 2049 pass / 1 xfail (481.29 s, 2026-07-05); `pio` green (225600 text); NOT yet flashed (consolidates with [18A] into the Phase-2 flash) | [2026-07-05-canhub-hardening-item20-firmware](2026-07-05-canhub-hardening-item20-firmware.md) |
| 2026-07-05 | resolved | canhub-hardening (marginal-CAN3 follow-up) | **canhealth `err=` now counts wire errors** — observability follow-through on the marginal-CAN3 mislabel (the 1 Hz headline field printed the raw FlexCAN_T4 ESR1-change snapshot counter, which climbs ~200/s in NORMAL sync-active operation on benign IDLE/RX/TX activity flips — the root of the multi-session "bus erroring 10-15/s" ghost hunt). Second REAL defect found designing the fix: the 8-deep raw-ESR1 evidence ring recorded EVERY snapshot, so at ~200 benign flips/s a genuine error word was flushed within ~40 ms — self-erasing under exactly the traffic where you'd want it. Fix (~30 lines, 4 files): new `wire_errs` counter (snapshots with ≥1 ACK/CRC/FORM/STUFF/BIT0/BIT1 bit; documented lower bound during identical-error storms — tecInc/recInc stay the rate-accurate signals); `err=` prints it (0 on a healthy bus), raw counter renamed `chg=` (capture-machinery liveness only — frozen chg=0 means err=0 unmeaningful); ring filtered to wire errors + one-shot TWRN/RWRN/BOFF interrupt crossings (NOT the latched level bits 8/9 — re-flood risk); `[canerrs]` gated on (wire\|tecInc\|recInc)≠0 so healthy buses print no [canerrs]/[canesr1] at all; README field reference updated + cross-build capture-comparison note (pre-2026-07-05 captures printed the raw counter as err=). Native suite 16/16 (157.35 s, 2026-07-05); flashed same day; 45 s bench capture: all buses err=0 flags=0x00 tec=0 rec=0, chg climbing only on active CAN3, zero [canerrs]/[canesr1] lines, 7/7 ODrives heartbeating. Overflow non-issue (uint32, print-only, ~248-day wrap) | [2026-07-05-canhub-canhealth-err-wire-errors](2026-07-05-canhub-canhealth-err-wire-errors.md) |
| 2026-07-05 | resolved | canhub-hardening (marginal-CAN3 follow-up) | **Marginal CAN3 bus reframed, mechanism power-cycle-verified, presence-gate contract landed**; the Tier-2 carried-out item (`err` 10-15/s, `tec=255`, sticky `flt=BUSOFF`) decomposes into (a) sticky high-water marks from ~2.4 days of boot history spanning robot-supply cycles and (b) a snapshot counter that was **never a wire-error rate** — FlexCAN_T4 captures on any masked-ESR1 *change*, benign SYNCH/IDLE/RX phase flips included. Diagnostic firmware (per-type ack/crc/form/stuff/bit0/bit1 counters, live ECR TEC/REC @ 1 kHz + delta sums, raw-ESR1 ring, `[cantiming]` decode — kept permanently) shows the powered bus **wire-clean at the 75% sample point** (zero error bits, `tecInc=recInc=0`, 210+ s @ ~2,000 fps); kills bit-timing/termination/SI. 2 Hz `bad_axis` = Platform 0x7DF TRAFFIC_REPORT (benign). Operator power-cycle experiment: **12 V ramp garbage confirmed quantitatively** (REC burst→121 vs historical high-waters — 119 capture-verified, 121/133 operator-reported; FORM+STUFF+BIT1 = historical flags minus ACK/CRC); assumption corrected — bridge is **TX-silent when unsynced** (0x7DD gated on `time_synced()`), so historical ACK/tec-254/BUSOFF needs a bus-dies-while-link-up window (ordinary teardown ordering); 45 V edges invisible to CAN. **Fix**: cone presence-gate generalised to ALL buses at the `can_*_send()` choke point (`bus_partner_present()`, `BUS_PARTNER_STALENESS_US` 5 s, `tx_gated`/`gated=` observability, native doctest pins the window; README gains a full serial-console field reference); audit-surfaced same-commit hardening — AXIS_ALL CLEAR/REBOOT RPCs now notify their fault-machine side-effects only after ≥1 frame actually enqueued (SYNCH gate passes on a dead-but-idle bus). Native suite 16/16 (156.80 s, 2026-07-05); flashed + 60 s bench-clean. Gate validated live same day (2nd operator session): BB-absent + sync active → `gated` +100/s per absent bus with ZERO error events/tec=0/flt=active (A/B vs the pre-fix err=122/ACK/tec=128/passive baseline on the same scenario); CAN3 `gated=0` under full load (never blocks legitimate TX); gate reopens on the first BB frame (froze at 3053). RESOLVED | [2026-07-05-canhub-marginal-can3-diagnosis](2026-07-05-canhub-marginal-can3-diagnosis.md) |
| 2026-07-04 | fix-landed-pending-hardware-confirm | canbridge-foundation (Phase-4 follow-up) | **Stow-on-shutdown ported from can_node → teensy_bridge_node** (β migration gap surfaced during the Tier-2 powered sitting). Ctrl-C of `jugglebot_launch` while un-stowed left the platform holding — `can_node.on_shutdown` always did a profiled stow (`_gently_move_to_setpoint(0.0, deactivating=True)`); the α→β migration only ported transport teardown. **NOT a Tier-2 firmware regression** (git-blame `0ad3d25^..192e6af`: the flashed changes touched stow EXECUTION, never a trigger; the stow-on-link-loss path never existed). Deliberately fixed **host-side, not firmware**: an autonomous firmware stow-on-LINK_LOST would re-open the 2026-05-19 hazard (dropping a healthy holding platform on a transient host stall); a clean Ctrl-C is a controlled host event → the stow belongs in `on_shutdown`, as in α. Fix: `_shutdown_stow()` called before RPC/link teardown (MPC already quiesced) → guarded `_run_deactivate(deactivate_axes)` (TRAP_TRAJ lower to STOW + IDLE, hand idled), guards mirroring can_node (skip on CAN3 `BUS_OFF`/no-telemetry — deferred stow covers reconnect — or a pending deferred stow); self-bounded + best-effort. New `stow_on_shutdown` ctor kwarg (default True; tests pass False via `_build_paired_node`). +5 guard tests; suite 2046 pass/1 xfail (2026-07-04). Operator workaround until deployed: `/deactivate` before Ctrl-C. Sibling sitting findings: `enable_setpoint_output` launch-gate (mpc_active=0, by-design opt-in) + an `is_homed` boot-read bug (separate, under diagnosis) | [2026-07-04-canbridge-stow-on-shutdown](2026-07-04-canbridge-stow-on-shutdown.md) |
| 2026-07-04 | resolved | Online-juggle tilt re-architecture — motion-quality course-correction (the co-design catch rung: implementing the 2026-07-03 design basis) | Co-design catch — a CONTINUOUS velocity-matched sub-tick command makes the single-ball self-catch SMOOTH (cup path 1026→322 mm/cycle, ZERO slider clamps, ball received DOWN at −0.67 m/s) WITHOUT a gentler throw; the same insight (kinematic release imposes v_takeoff regardless of cup velocity) retires the vestigial v_takeoff-overshoot carry; the Rung-2b MAKE is regenerated 12/12 on all 6 seeds and is *tighter* (in-cup offset 0.6→0.3 mm) — dissolves the P2 "runway = slam" tension (an artefact of the constant-per-tick command) | [2026-07-04-codesign-catch-continuous-velocity-matched](2026-07-04-codesign-catch-continuous-velocity-matched.md) |
| 2026-07-03 | resolved | Online-juggle tilt re-architecture — motion-quality course-correction (catch control-formulation design basis for the co-design rung) | Catch control-formulation design basis — the non-smooth catch is NEITHER a hand-acceleration limit NOR a "gentler throw" problem: the catch commanded a CONSTANT position per whole 40 Hz tick into an ultra-stiff position actuator (jump-and-settle; cup vz −33…−37 mm/s at the sample tick), so the ceiling-overshoot "static catch" was a workaround, not a runway; the sim already runs the hand at 1 kHz (`plant.step` calls `hand_cmd_fn` every substep), and a CONTINUOUS velocity-matched sub-tick command co-moves the cup smoothly at −2700 mm/s — receive on a *descending* cup, no ceiling overshoot, no gentler throw; both operator hypotheses (max-accel setting, "match 500 Hz") refuted literally yet load-bearing (the hand does tens of g unthrottled; the command-continuity axis was the bug); mirrors the hardware's 40 Hz plan / 500 Hz track split; the design basis the 2026-07-04 co-design rung implements | [2026-07-03-catch-control-formulation-design-basis](2026-07-03-catch-control-formulation-design-basis.md) |
| 2026-07-03 | blocked | Online-juggle tilt re-architecture — motion-quality course-correction (P2 re-unification attempted, blocked by MAKE fragility) | P2 self-catch re-unification on `plan_cup_cycle` — STOPPED with evidence, no code landed (module + 12/12 MAKE intact): the smooth cycle and the fragile MAKE are in DIRECT tension because the robust seat of a −2.7 m/s arrival gets its ~60–100 mm downward runway FROM the ceiling slam (all 93 ceiling ticks live in `_catch`; the stitch's ~700 mm/cycle is easy to remove, the catch's is not); across ~35 probe runs every clean-motion variant punches through (0–2/12) and every restructuring — even a config-only gentler throw or byte-identical-catch stitch removal — breaks the MAKE (0/12); concluded "co-design a gentler throw + apex-rendezvous catch as its own rung" — SUPERSEDED same day by the design-basis entry (the tension is real only for the constant-per-tick command) | [2026-07-03-p2-selfcatch-reunification-tension](2026-07-03-p2-selfcatch-reunification-tension.md) |
| 2026-07-03 | in-progress | Online-juggle tilt re-architecture — motion-quality course-correction (P0 landed; P2 re-unification pending) | Motion-quality review — the single-ball toss/catch is NOT smooth (cup path 1026 mm/cycle vs ~550–650 momentum-shaped, full 350/355 mm stroke, 93 ceiling-clamp ticks, 60 mm/tick command step, contact on an UP-moving cup) and the celebrated Rung-2b MAKE actually seats the ball 145 mm ABOVE `catch_z` on a ceiling-parked cup — the logbook's "co-moving descent seat" never executes (the kinematic seat has no descent gate); root cause is architectural: the stitched quintic harness vs the codebase's own whole-cycle min-accel planner (`plan_cup_cycle`); the obvious P1 descent gate empirically non-separable (no aggregate improvement + broke seed 5, 12→8 — reverted); P0 metrics probe landed as the gate (`tools/probes/juggle_motion_quality.py`); P2 (re-unify on `plan_cup_cycle`) named the fix | [2026-07-03-motion-quality-review](2026-07-03-motion-quality-review.md) |
| 2026-07-02 | hardware-validated | canhub-hardening Tier-2 | Firmware safety/concurrency/parity pass from the Fable-5 review — landed, adversarially reviewed, and **FLASHED** to the can-hub Teensy (2026-07-03), awaiting one powered re-validation. **[13]** (`0ad3d25`) guard E-STOP now LATCHES until an operator CLEAR_ERRORS (motor_guard semantics; released only via fault_notify_clear_errors, never the internal soft-reset/UV auto-retry) — pre-fix it auto-recovered the instant the transient cleared. **[14]** (`138aa11`) monotonic `micros64()` for ALL interval/staleness arithmetic (one root cause: steppable `now_wall_us()` corrupts every staleness interval + the 500 Hz trajectory phase); wall clock kept for wire timestamps only; paired stamp+read conversions across ~8 fw files + host client.py; native clock-step test. **[15]** (`b562825`) NetLock around the lwIP `Ethernet.loop()` pump (was unlocked vs prio-3 TX → pbuf corruption) + a bounded UDP RX drain (drop-deferred). **[16]** (`c8ba247`) Flash-A 7 sub-items (ISR-level MPC↔cold-start mutual exclusion, hand-traj↔homing interlock, homing presence check, STATE_WRITE isfinite, setpoint isfinite + wrap-safe seq guard, notify-after-gate, AXIS_ALL send-result accum). **[17]** (`83ac938`) Flash-B 5 sub-items (atomic last_heartbeat_us, interp_begin_stow PRIMASK barrier, ISR priority 32→16 audited-FreeRTOS-free, clear_disarm_reasons mirror, **hand-axis fault-eval → NUM_AXES — OBSERVABLE: a hand fault now E-STOPs the legs**). **[18B]** (`6fe1ec9`) inverted HomingMonitor timeout docstring; **[18A]** false-success uplink DEFERRED (wire-protocol change). An **adversarial review** (`192e6af`) caught + fixed two real regressions before flash: the seq guard bricked MPC control for ~13 min after ~50% of `run_mpc` restarts (persisted firmware high-water vs host-reset counter → phantom MPC_STALE E-STOP), and a mis-applied `!coldstart` on the stow-descent TX → an unarmed gravity drop if a DEACTIVATE landed mid-stow. `pio` green (222528 text, fits). Suites green (2040 + known flaky alloc test, 2026-07-03). **Powered validation PASSED 2026-07-04**: all 7 checks pass (CHECK 2 seq-guard operator-confirmed PASS; the guard tool's report is verdict-only). Two tools ran it: `tools/probes/canhub_tier2_hw_validation.py` (read-only serial observer, 5/6/7) + `tests/hardware/teensy_guard_validation.py` (MPC-free ZERO-MOTION driver arming mpc_active with legs IDLE, 1/2/3). Carried out one open item: a **marginal CAN3 bus** (`err` climbing, `tec`→255, sticky `flt=BUSOFF` — decorative to the staleness gate, but a real error source) → own bench-diagnosis. Remaining: item 19 (partial — checklist runners committed; automated jitter/soak gate still open), item 20, [18A], the CAN3 investigation, then archive the parity plan | [2026-07-02-canhub-hardening-tier2](2026-07-02-canhub-hardening-tier2.md) |
| 2026-07-02 | resolved | canhub-hardening Tier-1 | Host/test/doc hardening pass from the Fable-5 fresh-eyes review of the whole can-hub firmware/UDP pipeline. **A** (`f8397c7`) host `teensy_link`: setpoint-thread exception-containment (build() rejects-not-raises, OSError-guarded send), non-idempotent RPCs never auto-retried (firmware has no dedup) + retry-race fix, `SetpointPump.reset()` wired at reconnect/mpc-edge, host bundle (ActivateMonitor requires CLOSED_LOOP, scale_to_bench floor>ceiling raise, capped drain, rpc_args DEACTIVATE). **B** (`c7425e9`) `teensy_bridge_node`: UDP-link-loss→`has_fatal_can_error` (dropped can_node parity, observable); cross-axis disarm-while-CLOSED_LOOP predicate `any_disarmed AND any_cl` (the per-leg conjunction never fired, observable); hand vel/curr-limit topic parity (observable); bounded cone queue; **RLock** spanning the whole STATE_WRITE read-modify-write (closes the lost-update, not just serializing wire writes); async cold-start re-reads sharing one inflight guard with CAN3-edge preservation. **C** codegen+tests: PROTOCOL_VERSION + wire-layout freeze + codegen lints + firmware lib pins (`37a81d4`, plan item 9); compile leg_homing/activate/deactivate.cpp into the native harness via a self-contained `coldstart_hal` + retire the tautological Python xrefs (`431ea89`, plan item 3, +130 byte-parity assertions); execute the C++ UDP framing codec natively (`8821451`, plan item 8 / gap 4). Host/test/doc only, NO re-flash. Suites green through each commit (2015 pass / 1 xfail at the framing commit, 2026-07-03). *(Plan item numbers here are canhub-hardening.md's 1–21 list, NOT the Fable-5 review-finding numbers the commit messages carry.)* **Tier-1 CLOSED (2026-07-05)**: item-8 native/golden complete (`6f20fea`+`db4ddf8`), typed CrcError (`2cf3a07`), item-12 doc sweep host/doc side (`0fc2002`; firmware-comment residue → Phase-2 item 20). Observable changes (plan items 2/6/7) still want an operator eyeball (hardware-confirmation checks in the entry) | [2026-07-02-canhub-hardening-tier1](2026-07-02-canhub-hardening-tier1.md) |
| 2026-07-02 | resolved | canbridge-foundation (reboot bugfix) | REBOOT_ODRIVES didn't clear `encoder_search_complete` → orchestrator skipped encoder-search after a reboot → homing on an un-indexed encoder → `ODRIVE_FATAL` FAULT→BOOT→HOMING loop. Found validating the Phase-4 `/reboot_odrives` cold-start clear: the reboot cleared is_homed/levelling/pose but left the sticky `_encoder_search_done_session`, so the DERIVED `encoder_search_complete = is_homed OR session-bit` stayed True; the ODrive MCUs lose their incremental-encoder index on reboot (operator-confirmed not pre-calibrated), so homing entered CLOSED_LOOP un-indexed. **Latent in can_node too** (can_node.py:1552-1566 never cleared it) — only bit once Phase 4 drove homing automatically after a reboot. Fix: clear `_encoder_search_done_session` in the reboot hook (the reboot must clear everything the ODrive loses — references AND the encoder index). Flipped `test_..._monotonic_across_reboot` → `test_reboot_clears_encoder_search_complete`. Confirmation: a plain relaunch cold-starts cleanly (re-index-searches + re-homes) | [2026-07-02-canbridge-reboot-encoder-search-clear](2026-07-02-canbridge-reboot-encoder-search-clear.md) |
| 2026-07-02 | resolved | canbridge-foundation Phase 4 | Orchestrator wiring — the plan's **final** phase; makes the bridge a drop-in for the retired `can_node` from the LOCKED `orchestrator_node`'s view (ZERO edits to orchestrator/state_machine), restoring **automated orchestrator-driven cold-start**. Registers four thin wrappers directly on the bridge (operator design call — no separate `orchestrator_conduit.py`; no existing actions/services module to join): `home_motors` ActionServer(HomeMotors)→ shared `_do_home()` (home legs+hand → persist is_homed → configure) + concurrent-goal guard; `activate_or_deactivate`(ActivateOrDeactivate)→ `_run_activate`+`_run_configure` (the TRAP_TRAJ→PASSTHROUGH **fold**, rows 27/28) / `_run_deactivate`; `get_platform_tilt`(GetTiltReadingService)→ `relay_read_tilt` with retry+validity-bound+**NaN-on-failure** (row 60); `set_level_state`(Float64MultiArray)→ new `_write_level_state` (STATE_WRITE preserving is_homed). All nine blocking cold-start verbs share one `ReentrantCallbackGroup` so multi-second moves never starve the 100 Hz publish (operator-approved: extended beyond the plan's new-verbs-only to close the class). Robustness: a **runtime drift-guard** contract test introspects both nodes' declared `(name,type)` sets (divergence-catch proven) — required a small additive mock-conftest enhancement (it discarded interface types). Host-only (no firmware/codegen change). Suite 1995 pass / 1 xfail (final pre-commit run; +23 Phase-4 tests: 7 drift-guard + 14 behaviour + 2 full-cold-start integration). Matrix rows 6/7/24/27/28/44/45/54/60 → ported+validated. **Powered orchestrator-driven sitting PASSED 2026-07-02** (automatic BOOT→HOMING[legs+hand]→IDLE in ~9 s + 3× levelling cycle; no control_mode='ERROR' → transient-FAULT/ESTOP confirmed benign). Completes the plan's implementation. Residual hardware validations CLOSED 2026-07-02 in a follow-up session: REBOOT_ODRIVES cold-start clear ✓ (+ found/fixed an encoder-search-clear bug), six-leg deferred-stow reconnect ✓ (full-platform CAN-loss safety inversion; legs held, no drop; profiled stow on reconnect). Plan ready to archive; the UDP-link deferred-stow (matrix row 18) is the one out-of-scope open item for autonomous movement | [2026-07-02-canbridge-phase4-orchestrator-wiring](2026-07-02-canbridge-phase4-orchestrator-wiring.md) |
| 2026-07-02 | resolved | Online-juggle tilt re-architecture — Phase 1 / Rung 1 (clean single catch) — fast-catch fidelity follow-up | Fast-catch fidelity — the Rung-1 catch now seats a REAL fast Ball Butler throw (~4.9 m/s vz) via a firmed catch contact (`solref 0.05→0.01`) + a phase-matched descent seat (cup co-moving at ratio·\|v_arr\| @ t_td, ratio 0.55); the operator's physical intuition (a cup at 50–80% of ball speed is normal) reframed a "catch can't do a fast BB" dead-end into a sim-fidelity gap; gentle lob 8/8 AND real BB 8/8 (in reach), self-catch MAKE preserved 12/12 | [2026-07-02-fast-catch-fidelity](2026-07-02-fast-catch-fidelity.md) |
| 2026-07-01 | resolved | canbridge-foundation Phase 5 | Hand command conduit + hand homing + deactivate-idles-hand + cmd-echo telemetry — restores the full hand surface `catch_coordinator` silently no-op'd against (the four hand services, all GAPs). `set_hand_traj_cmd`/`smooth_move_hand` ride one `HAND_TRAJ_CMD` RPC: the **host** builds the exact 8-byte `0x6D0` payload (byte-identical to `can_node`, byte-0 discriminated), the firmware (`hand_ops.cpp`, new) sends the `CLOSED_LOOP`+`POSITION/PASSTHROUGH` preamble to axis 6 then forwards it on the **firmware-owned** `0x6D0` id — **aborting if a preamble send fails**; the **absolute** `wall_time_ms` deadline is Jetson-computed and the firmware CANNOT re-stamp (transit-jitter-immune — the temporal-accuracy contract). `set_hand_state`/`set_hand_gains` ride the Phase-1 axis-6 allow-table. **Hand homing**: `leg_homing.cpp` accepts axis 6 (per-axis `Homing::HAND_*`); the bridge applies hand gains host-side (refuse-flash-defaults) then `HOME(6)`. Also configures + idles the hand (rows 40/51/29) and restores `HAND_CMD_ECHO` (sniffs axis-6 `Set_Input_Pos` → `hand_telemetry` cmd fields, row 58). Native `test_hand_ops` (5 cases, divergence-proven) + byte-parity xrefs. Suite 1972 pass / 1 xfail (+45); `pio` green. **Powered sitting PASSED 2026-07-02** (hand homing + catch-traj fires-after-delay [absolute-deadline confirmed] + HAND_CMD_ECHO populated + deactivate-idles-hand; orchestrator → IDLE on homing-complete corroborates the Phase-2+3+5 chain). Matrix rows 29/32/37-40/51/58 → ported+validated. **Unblocks Phase 4** | [2026-07-01-canbridge-phase5-hand-conduit](2026-07-01-canbridge-phase5-hand-conduit.md) |
| 2026-07-01 | resolved | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK — kinematic-release re-architecture; MAKE) | Rung-2b kinematic-release — the MAKE. The throw→catch→throw loop closes (12/12 cycles, all seeds) once the throw cuts the ball free at the planned take-off velocity instead of letting it emerge from the contact push; contact-parameter tuning ruled out first (frontier scan: gain 2.4–16 everywhere separation holds), then a clean contact-carry↔kinematic hand-off (`Ball.ballistic_release`) rejects the real ~0.6 mm seat offset (loop gain ≪ 1) | [2026-07-01-rung2b-kinematic-release](2026-07-01-rung2b-kinematic-release.md) |
| 2026-07-01 | resolved (by kinematic-release) | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK gate, OPTION-1 re-plan) | Rung-2b OPTION-1 re-plan — the two-point A↔B oscillation is the HONEST tilt test (tilt ENGAGED ~1.4°) and it STILL diverges; loop gain > 1 via the throw's chaotic sensitivity to the throw-ORIGIN pose — a GENUINE tilt-hypothesis BREAK on a non-degenerate geometry (tilt necessary-but-not-sufficient; the contact-detach knife-edge is the binding constraint) | [2026-07-01-rung2b-oscillation-tilt-engaged-diverges](2026-07-01-rung2b-oscillation-tilt-engaged-diverges.md) |
| 2026-07-01 | resolved (by kinematic-release) | Online-juggle tilt re-architecture — Phase 3 / Rung 2b (throw-and-self-catch loop, MAKE-OR-BREAK gate) | Rung-2b single-ball self-catch — MAKE-OR-BREAK BREAK; the pure column diverges (loop gain > 1) for column-specific reasons, and the column is a degenerate case where tilt is inactive (~0° commanded) — does not test the tilt hypothesis | [2026-07-01-rung2b-selfcatch-column-divergence](2026-07-01-rung2b-selfcatch-column-divergence.md) |
| 2026-06-30 | resolved | canbridge-foundation Phase 6 | Robust clear_errors + reboot-in-progress latch — a `REBOOT_ODRIVES` RPC arms a bounded (`REBOOT_WATCHDOG_SUPPRESS_US=6 s`, measured reboot latency 2.3–3.5 s + margin) watchdog-suppression latch (`fault_notify_reboot_started`) so the deliberate reboot silence is not read as a CAN loss; armed ONLY by the reboot RPC (spontaneous loss unaffected — the 2026-05-19 deferred-stow inversion preserved, proven by native test), released on fresh-after-stale OR the bounded deadline (atomic 64-bit). `CLEAR_ERRORS`/`REBOOT` gate on the live ESR1.SYNCH bus-transmittable signal (`jugglebot_bus_transmittable`) not heartbeat-staleness (the just-repowered-bus deadlock), via a single `gate_allows` chokepoint + header-inline policy predicate; AXIS_ALL now loops legs+hand (`i<NUM_AXES`). Three-way (fault_machine.cpp + fault_logic.py + native harness/golden). Bench: staleness-deadlock did NOT reproduce (Platform Teensy keeps CAN3 RX fresh) — SYNCH gate ships as the latent-transient/design-intent fix | [2026-06-30-canbridge-phase6-reboot-latch](2026-06-30-canbridge-phase6-reboot-latch.md) |
| 2026-06-30 | landed | Online-juggle tilt re-architecture — Phase 2 / Rung 2a (single-ball throw to arbitrary targets, open-loop accuracy) | Rung-2a single-ball tilt-aimed throw — tilt-aim is exact (lateral = slider·sin θ), open-loop landing ≤33 mm across the column + 50 mm ring; a directional separation/aim asymmetry caps the clean box at ~±70 mm | [2026-06-30-rung2a-single-ball-tilt-throw](2026-06-30-rung2a-single-ball-tilt-throw.md) |
| 2026-06-30 | landed | Online-juggle tilt re-architecture — Phase 1 / Rung 1 (clean single catch, BB-reload) | Rung-1 clean single catch — constant-decel seat kills the soft-contact bounce; frozen-early reach + ramped tilt; ~3.3 mm in-cup offset under §3 noise | [2026-06-30-rung1-clean-single-catch](2026-06-30-rung1-clean-single-catch.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 3 | Get_Version handshake + Jetson-validated `firmware_validated` — firmware sweeps Get_Version (one frame/tick, bus-paced) + caches raw versions; bridge pulls via `GET_AXIS_VERSIONS` (sync RPC blob, no CAN3 round-trip) + runs the tested `validate_group` (zero firmware version semantics); mismatch → force-FAULT via `robot_state.error`, all-match → `firmware_validated=True` (un-wedges BOOT). Closes the Phase-2 reconnect residual: a CAN3-bus-health (`bus1_health` WARN/BUS_OFF→OK) conservative reconnect re-read (retry, then `is_homed=False`), distinct from the keep-stale UDP-watchdog reconnect | [2026-06-29-canbridge-phase3-version-validated](2026-06-29-canbridge-phase3-version-validated.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 2 | Cold-start state via the relay — `robot_state.is_homed`/`levelling_complete`/`pose_offset` sourced from a cached Platform-Teensy `RobotState` (relay `STATE_READ` at boot + on reconnect; non-blocking publish), `is_homed=True` written on `/home` (read-modify-write preserves levelling), `REBOOT_ODRIVES` clears all three (shared hook), `encoder_search_complete` derived; conservative `is_homed=False` on boot-read failure (the safety invariant); software-only, validated at the Phase-4 powered sitting | [2026-06-29-canbridge-phase2-coldstart-relay-state](2026-06-29-canbridge-phase2-coldstart-relay-state.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 1 | Platform-Teensy relay seam — hybrid (typed STATE_WRITE/TILT_READ/STATE_READ RPCs + verbatim PLATFORM_FRAME reply uplink, host-correlated by (id,dlc)) + narrow generated hand axis-6 allow-table replacing the blanket reject; mechanism-only (cold-start wiring is Phase 2), software-complete to the CAN3 SRX_DIS / reply-latency bench-probe gate | [2026-06-29-canbridge-phase1-platform-relay-seam](2026-06-29-canbridge-phase1-platform-relay-seam.md) |
| 2026-06-29 | resolved | canbridge-foundation Phase 0 | Native firmware-test harness (compiles fault_machine/leg_interp host-side, tests the binary) + one codegen-allocation pass (5 RpcMethods + 2 T2J MsgTypes + HeartbeatT2JFlags enum) + RpcMethod dispatch lint; retires the hand-maintained fault-logic transcription via firmware-anchored goldens | [2026-06-29-canbridge-phase0-native-harness](2026-06-29-canbridge-phase0-native-harness.md) |
| 2026-06-29 | in-progress | Online-juggle tilt re-architecture — Phase 0 (characterise tilt/orientation tracking before re-introducing tilt in the planner) | Platform-tilt tracking characterisation — tilt is static-exact (≤0.004° droop), ~1.66 mm/deg lateral lever arm (cup ~95 mm above centroid) + only second-order vertical cross-coupling, >45 mm leg margin even at 24° (not stroke-limited at z=170), and tracks ~0.99/3° at the 1.6 Hz cycle freq (better than lateral translation's 0.95/17°); tilt is viable for throw-aiming; new committed probe `juggle_tilt_bandlimit.py` | [2026-06-29-platform-tilt-tracking-characterisation](2026-06-29-platform-tilt-tracking-characterisation.md) |
| 2026-06-27 | resolved | teensy-can-offload | can_node → Teensy can-bridge feature-parity audit — 117-cap matrix (53 validated / 7 unvalidated / 37 PARTIAL / 20 GAP); headline: automated orchestrator cold-start broken against the bridge (masked by operator-driven cutover) | [2026-06-27-can-node-teensy-parity-audit](2026-06-27-can-node-teensy-parity-audit.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1: online runner integration. Throw fixed, catch axis-split landed (0→5 catches), sustained 2-ball blocked by the throw-amplifies-catch-error cascade | Online juggle integration — throw-separation slam fix (cycle-0 re-plan) + catch axis-split (slider descends moderately *with* the ball; full match slams the floor) take the online runner 0→5 catches (beats the offline demo's 2); sustained 2-ball blocked by the band-limit cascade (catch seats ~15 mm off-centre → throw amplifies to >150 mm, beyond reach → diverge); dwell/coast/symmetric-startup/separation/smoothness/apex all ruled out; fix scoped (less position-sensitive throw / self-centring cup) | [2026-06-27-online-juggle-throw-fix-catch-axis-split-band-limit-cascade](2026-06-27-online-juggle-throw-fix-catch-axis-split-band-limit-cascade.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1: re-architecting to an online cup planner (replacing the offline optimiser); planner built + tested, integration WIP | Pivot to online per-throw task-space planning (Kai-style) re-solved against the observed ball + cup-tracking band-limit characterisation (slider near-perfect → throw stroke; platform static-exact/−3 dB ~5 Hz lateral → smooth positioning); level-platform decoupling drops banking; planner module built + 7 unit tests; catch-geometry shuttle issue documented for integration | [2026-06-27-online-replanning-architecture-and-cup-bandlimit](2026-06-27-online-replanning-architecture-and-cup-bandlimit.md) |
| 2026-06-27 | in-progress | Sim2Real fidelity — concern 1 (contact) throw-aim: band-limit diagnosis; closed-loop catch WIP | Throw-aim root cause is a platform BAND LIMIT (not angular tracking — ω×r tracks fine, <0.04 m/s; refuted by same-instant decomposition); heavy connect-constraint platform low-pass-filters every fast maneuver (~30 mm+), emergent throw lands ~100-140 mm short; closed-loop catch (observe ball, reach platform xy) → 1→2 catches, short test passes; full ≥30 blocked by band-limit cascade (paused to reconsider planning architecture) | [2026-06-27-throw-aim-band-limit-and-closed-loop-catch](2026-06-27-throw-aim-band-limit-and-closed-loop-catch.md) |
| 2026-06-26 | in-progress | teensy-can-offload | Phase 11 U5 — six-leg β cold-start + /deactivate (validated 06-27); run_mpc-on-β feedback chain confirmed, high-freq hold deprioritized (compute-marginal) → re-scoped to a can_node parity audit | [2026-06-26-phase11-u5-six-leg-cutover](2026-06-26-phase11-u5-six-leg-cutover.md) |
| 2026-06-26 | in-progress | Sim2Real fidelity — concern 1 (contact) infrastructure landed; throw-aim (ω×r + platform angular tracking) WIP | Contact-mechanics integration — the kinematic hold/`release()` replaced with real contact carry + a seat-based catch metric + an emergent physics throw (contact proven faithful: ball leaves at *exactly* the cup velocity); two lasting sim-fidelity fixes: sub-stepped platform command (the 40 Hz staircase rang the stiff leg actuators and shook the ball out) + phase-switched contact stiffness (soft catch / stiff throw — the overdamped soft contact drags the throw cohesively, a sim artifact); the "60% lateral transfer" hypothesis withdrawn on operator pushback (the ball left exactly at the *achieved* 12° tilt, not the commanded 20° — workspace clamp, no transfer loss); throw-aim root-caused to the optimiser's omitted ω×r (~0.2 m/s at ~0.2 m off-centroid, 5 rad/s yaw; added with a left-Jacobian rotvec-rate→ω conversion) but achieved ω ≠ planned ω remains — platform angular tracking deferred to a follow-up; demo 1 catch vs the 30+ target, headline tests `xfail(strict=True)` | [2026-06-26-contact-mechanics-integration](2026-06-26-contact-mechanics-integration.md) |
| 2026-06-26 | in-progress | Sim2Real fidelity — concern 2 landed, concern 1 feasibility proven | Velocity-matched catch (cup at 0.7×ball, colinear) + contact-mechanics feasibility (cup holds & throws a ball via physics; integration is next session) | [2026-06-26-velocity-matched-catch-and-contact-mechanics-feasibility](2026-06-26-velocity-matched-catch-and-contact-mechanics-feasibility.md) |
| 2026-06-25 | complete | teensy-can-offload | Phase 11 U4 — production α→β setpoint switch + friction-FF drop (D9) + /teensy rename | [2026-06-25-phase11-u4-production-cutover](2026-06-25-phase11-u4-production-cutover.md) |
| 2026-06-24 | in-progress | teensy-can-offload | Phase 11 bench cutover — present-axis scoping + synthetic β-knot interp driver (software prep; powered run pending) | [2026-06-24-phase11-bench-cutover](2026-06-24-phase11-bench-cutover.md) |
| 2026-06-24 | resolved | bb-led-two-ball-juggle — Phase 3 → Phase 4 prep | BB-led two-ball juggle sim port-readiness — measured BB throw position, catch_vel_ratio 0.9→0.6 reconciliation, deterministic BB scatter (33/0 held) | [2026-06-24-bb-juggle-sim-port-readiness](2026-06-24-bb-juggle-sim-port-readiness.md) |
| 2026-06-10 | resolved | temporal-accuracy bring-up | Cone catch timestamps glitch +40 s — micros64() false 32-bit wrap from piezo-ISR race | [2026-06-10-cone-micros64-false-wrap](2026-06-10-cone-micros64-false-wrap.md) |
| 2026-06-10 | in-progress | teensy-can-offload | Catching-cone CAN2→UDP uplink (phase-10b) — cone data reaches the Jetson again | [2026-06-10-cone-uplink-phase-10b](2026-06-10-cone-uplink-phase-10b.md) |
| 2026-06-08 | tuned | teensy-can-offload | Ball Butler cutover from can_node to teensy_bridge_node — phase A bench bringup, GUI Calibrate end-to-end | [2026-06-08-bb-cutover-bench](2026-06-08-bb-cutover-bench.md) |
| 2026-06-06 | in-progress | teensy-can-offload | Can-bridge (CAN Hub) hardware bringup — 3-bus wiring, button-free flashing, FlexCAN/FreeRTOS firmware gotchas, live SYNC observability | [2026-06-06-can-hub-bringup](2026-06-06-can-hub-bringup.md) |
| 2026-05-23 | tuned | catching-cone-bringup | Throw director node + trimmed cone-test launch for live BB→cone integration | [2026-05-23-throw-director-and-cone-live-integration](2026-05-23-throw-director-and-cone-live-integration.md) |
| 2026-05-23 | in-progress | catching-cone-bringup | Catching cone hardware bringup | [2026-05-23-catching-cone-hardware-bringup](2026-05-23-catching-cone-hardware-bringup.md) |
| 2026-05-22 | in-progress | hardware diagnostics / compute-capacity assessment | MPC is compute-bound on the Jetson Orin Nano — 17-40% of solves exceed the 40 Hz CPU-time budget; system-profiling harness built | [2026-05-22-mpc-compute-bound-jetson-profiling](2026-05-22-mpc-compute-bound-jetson-profiling.md) |
| 2026-05-20 | in-progress | hardware-safety / fallback redesign | hold_extrap fallback positive-feedback oscillation drove platform into chaotic motion (peak 336.9 mm/s leg velocity, 2.41× soft limit; borderline-E-stop) | [2026-05-20-hold-extrap-positive-feedback-chaotic-motion](2026-05-20-hold-extrap-positive-feedback-chaotic-motion.md) |
| 2026-05-20 | resolved | off-plan | MPC warm-start deadlock escape — failure-driven invalidation breaks chronic-CTE lockup (1769-tick hardware lockup → 21-tick worst burst; offline reproduction within 0.5 pp) | [2026-05-20-mpc-warmstart-deadlock-escape](2026-05-20-mpc-warmstart-deadlock-escape.md) |
| 2026-05-19 | resolved | standalone | SpaceMouse resilient connection handling — keep-alive + throttled-recurring WARNING + auto-reconnect + home-to-ACTIVE on dropout (mocap-pattern transfer) | [2026-05-19-spacemouse-resilient-connection-handling](2026-05-19-spacemouse-resilient-connection-handling.md) |
| 2026-05-19 | tuned | hardware-bringup | CAN-loss fault-response safety inversion — blanket "ERROR ⇒ stow+IDLE" de-energises healthy holding actuators on reconnect | [2026-05-19-can-loss-fault-response-safety-inversion](2026-05-19-can-loss-fault-response-safety-inversion.md) |
| 2026-05-19 | tuned | hardware-bringup | Finding B — the 68 s motor_pos=None was a motor_guard GuardMode.ESTOP sticky latch, NOT a CAN dropout (shared root cause with Finding A) | [2026-05-19-findingb-motor-guard-estop-latch-observability](2026-05-19-findingb-motor-guard-estop-latch-observability.md) |
| 2026-05-18 | resolved | standalone | Mocap QTM connection logging — state-transition + throttled-recurring WARNING, stale qtm_host→mDNS, unbounded-connect bug | [2026-05-18-mocap-qtm-connection-logging](2026-05-18-mocap-qtm-connection-logging.md) |
| 2026-05-18 | tuned | hardware-bringup | z=30 solve-failure root cause — motor_pos=None after CAN drop evades both telemetry watchdogs (Finding 3 resolved) | [2026-05-18-z30-solve-failure-motor-pos-none-watchdog-gap](2026-05-18-z30-solve-failure-motor-pos-none-watchdog-gap.md) |
| 2026-05-18 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 hardware obligations | Hardware bringup — T-H-T2b-1 + T-H-T2a-1 telemetry-stale cascade validation (+2 harness bugs; z=30 solve-failure resolved — see linked entry) | [2026-05-18-hardware-bringup-t2b1-t2a1-cascade-validation](2026-05-18-hardware-bringup-t2b1-t2a1-cascade-validation.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 8 (bugfix) | Runner — on_target_override hook treats None as "keep tc" (Tier 3b bugfix) | [2026-05-12-tier3b-hook-bugfix](2026-05-12-tier3b-hook-bugfix.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 8 (final) | MPC sad-path coverage — Phase 8 Tier 3b time pathologies, resource exhaustion, hooks, races | [2026-05-12-tier3b-time-pathologies](2026-05-12-tier3b-time-pathologies.md) |
| 2026-05-12 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 7 (bugfix) | Tier 3a follow-up bugfix — quintic T<=0 guard + DIAG_SCHEMA_CONTRACT | [2026-05-12-tier3a-fuzz-bugfix](2026-05-12-tier3a-fuzz-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 7 | MPC sad-path coverage — Phase 7 Tier 3a numerical + schema fuzz | [2026-05-11-tier3a-numerical-schema-fuzz](2026-05-11-tier3a-numerical-schema-fuzz.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 6 (bugfix) | ZMQ recv resilience — Tier 2c follow-up bugfix (MpcTargetIPC + ZmqTargetSource) | [2026-05-11-tier2c-zmq-recv-resilience-bugfix](2026-05-11-tier2c-zmq-recv-resilience-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 6 | MPC sad-path coverage — Phase 6 Tier 2c ZMQ corruption (real-msgpack harness) | [2026-05-11-tier2c-zmq-corruption](2026-05-11-tier2c-zmq-corruption.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 (bugfix) | HardwarePlant set_pose — once-only singular-FF warning (Tier 2b bugfix) | [2026-05-11-tier2b-set-pose-singular-ff-bugfix](2026-05-11-tier2b-set-pose-singular-ff-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 5 | MPC sad-path coverage — Phase 5 Tier 2b HardwarePlant telemetry-staleness, set_pose FF, cold-start | [2026-05-11-tier2b-hardware-plant-telemetry-ff](2026-05-11-tier2b-hardware-plant-telemetry-ff.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 4 | MPC sad-path coverage — Phase 4 Tier 2a HardwarePlant FK degradation, singular Jacobian, frozen motor | [2026-05-11-tier2a-hardware-plant-fk-degradation](2026-05-11-tier2a-hardware-plant-fk-degradation.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 3 (bugfix) | MPC fallback — sanitize non-finite q_cur / q_dot at _handle_failure entry (Tier 1c bugfix) | [2026-05-11-tier1c-input-fuzz-bugfix](2026-05-11-tier1c-input-fuzz-bugfix.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 3 | MPC sad-path coverage — Phase 3 Tier 1c NaN/Inf input fuzz on solve() | [2026-05-11-tier1c-input-fuzz](2026-05-11-tier1c-input-fuzz.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 2 | MPC sad-path coverage — Phase 2 Tier 1b fallback escalation cascade + cold-start IK budget | [2026-05-11-tier1b-fallback-escalation-cascade](2026-05-11-tier1b-fallback-escalation-cascade.md) |
| 2026-05-11 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 1 | MPC sad-path coverage — Phase 1 Tier 1a real IPOPT exit codes + fallback-keyword matrix | [2026-05-11-tier1a-real-solver-failures](2026-05-11-tier1a-real-solver-failures.md) |
| 2026-05-10 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 0 | Scheduler — begin_return raises S3 instead of overwriting _next_event (Plan 2 Phase 0, bug 2 of 2) | [2026-05-10-scheduler-begin-return-s3-overwrite](2026-05-10-scheduler-begin-return-s3-overwrite.md) |
| 2026-05-10 | resolved | mpc-sadpath-coverage-tiers-1-3 — Phase 0 | Scheduler — cancel_next during TRANSITIONING raises (Plan 2 Phase 0, bug 1 of 2) | [2026-05-10-scheduler-cancel-next-during-transitioning](2026-05-10-scheduler-cancel-next-during-transitioning.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 8 (final) | MPC Tier-0 contracts — Phase 8 CI hypothesis profiles wired (Plan 1 closes) | [2026-05-10-mpc-tier0-phase-8-ci-hypothesis-profiles](2026-05-10-mpc-tier0-phase-8-ci-hypothesis-profiles.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 7 | K1–K6 reference contract — Phase 7 hypothesis retroactive expansion | [2026-05-10-k1-k6-hypothesis-phase-7-retroactive-expansion](2026-05-10-k1-k6-hypothesis-phase-7-retroactive-expansion.md) |
| 2026-05-10 | resolved | mpc-tier0-contracts — Phase 6 | Plant-interface contract — Phase 6 enforcement of P3, P4 | [2026-05-10-plant-interface-contract-phase-6-p3-p4-enforcement](2026-05-10-plant-interface-contract-phase-6-p3-p4-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 5 | Plant-interface contract — Phase 5 enforcement of P1, P2 | [2026-05-09-plant-interface-contract-phase-5-p1-p2-enforcement](2026-05-09-plant-interface-contract-phase-5-p1-p2-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 4 | Plant-interface contract — Phase 4 audit and P1–P4 draft | [2026-05-09-plant-interface-contract-phase-4-audit](2026-05-09-plant-interface-contract-phase-4-audit.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 3 | Scheduler contract — Phase 3 enforcement of S4, S5, S6 + RuleBasedStateMachine | [2026-05-09-scheduler-contract-phase-3-s4-s6-enforcement](2026-05-09-scheduler-contract-phase-3-s4-s6-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 2 | Scheduler contract — Phase 2 enforcement of S1, S2, S3 | [2026-05-09-scheduler-contract-phase-2-s1-s3-enforcement](2026-05-09-scheduler-contract-phase-2-s1-s3-enforcement.md) |
| 2026-05-09 | resolved | mpc-tier0-contracts — Phase 1 | Scheduler contract — Phase 1 audit and S1–S6 draft | [2026-05-09-scheduler-contract-phase-1-audit](2026-05-09-scheduler-contract-phase-1-audit.md) |
| 2026-05-08 | resolved | pr-3b-persistent-enable | Friction-FF platform limit cycle — diagnosis and smooth-gate fix | [2026-05-08-friction-ff-platform-limit-cycle](2026-05-08-friction-ff-platform-limit-cycle.md) |
| 2026-04-27 | tuned | post-per-leg-gains-deadband-session | Friction feedforward — bench characterisation, model fit, and FF validation | [2026-04-27-friction-feedforward-bench-validation](2026-04-27-friction-feedforward-bench-validation.md) |
| 2026-04-23 | resolved | hardware-bringup — GC-pause elimination on the MPC 40 Hz hot loop | Hot-loop zero-allocation contract (W1 inventory → contract → enforcement → fixes) | [2026-04-23-hot-loop-zero-allocation-contract](2026-04-23-hot-loop-zero-allocation-contract.md) |
| 2026-04-20 | tuned | post-per-leg-gains-deadband-session | Motion-onset dead-time — cogging-torque-first investigation (continued in 2026-04-27 entry) | [2026-04-20-motion-onset-dead-time-fix](2026-04-20-motion-onset-dead-time-fix.md) |
| 2026-04-20 | resolved | hardware-bringup — platform-only dynamic-motion entry point | Add `--toss-motion` hardware test and `controller/ballistics` helpers | [2026-04-20-toss-motion-hardware-test](2026-04-20-toss-motion-hardware-test.md) |
| 2026-04-20 | resolved | | Add DOCUMENTATION_GUIDE.md as single reference for documentation architecture | [2026-04-20-documentation-guide-single-reference](2026-04-20-documentation-guide-single-reference.md) |
| 2026-04-20 | resolved | hardware-bringup — Phase 4 (moderate motions) | K1–K6 reference-feasibility contract resolves MPC_OVERSHOOT_SATURATION | [2026-04-20-k1-k6-reference-feasibility-resolution](2026-04-20-k1-k6-reference-feasibility-resolution.md) |
| 2026-04-20 | resolved | hardware-bringup — sim-side unlock for Phase 6/7 | Migrate sim catch sources to K1–K6 reference-feasibility contract and lower sim v_max to 500 mm/s | [2026-04-20-sim-catch-source-k1-k6-migration](2026-04-20-sim-catch-source-k1-k6-migration.md) |
| 2026-04-19 | resolved | hardware-bringup — Phase 4 (moderate motions) | Bundle A quintic-ref settling lag + ZMQ live-twist feedback trap | [2026-04-19-bundle-a-quintic-ref-settling-and-live-twist-trap](2026-04-19-bundle-a-quintic-ref-settling-and-live-twist-trap.md) |
| 2026-04-19 | in-progress | hardware-bringup — Phase 4 (moderate motions) | Bundle A — ref-from-plant-state on move transitions (MPC overshoot-saturation fix) | [2026-04-19-bundle-a-mpc-overshoot-saturation-fix](2026-04-19-bundle-a-mpc-overshoot-saturation-fix.md) |
| 2026-04-19 | resolved | post-per-leg-gains-deadband-session | Leg 1 pose-dependent hold-phase twitch at (0,-100,200) | [2026-04-19-leg1-pose-dependent-hold-twitch](2026-04-19-leg1-pose-dependent-hold-twitch.md) |
| 2026-04-18 | tuned | STANDBY-mode MPC (post-64742f2 fallback walk-forward) — tail-latency attribution | MPC-loop overhead spikes trigger fallback bursts; Fixes A+B+C landed (a89a4dd, b5d83df), chronic-CTE phase resolved by warm-start deadlock fix (2026-05-20), singleton-emission jerk tracked separately | [2026-04-18-mpc-overhead-spikes-fallback-bursts](2026-04-18-mpc-overhead-spikes-fallback-bursts.md) |
| 2026-04-18 | in-progress | post-walk-forward-fallback-multiaxis-session-2 | Hold fighting + motion-onset jitter — residual hardware jitter downstream of clean MPC | [2026-04-18-hold-fighting-motion-onset-jitter](2026-04-18-hold-fighting-motion-onset-jitter.md) |
| 2026-04-18 | in-progress | post-walk-forward-fallback-multiaxis-session | Move 5: MPC overshoot-recovery stall + plant-collapse misclassification | [2026-04-18-move5-overshoot-stall-and-plant-collapse](2026-04-18-move5-overshoot-stall-and-plant-collapse.md) |
| 2026-04-17 | in-progress | STANDBY-mode multi-axis MPC | MPC fallback-command sawtooth produces visible stutter on off-Active multi-axis moves | [2026-04-17-mpc-fallback-cmd-sawtooth-stutter](2026-04-17-mpc-fallback-cmd-sawtooth-stutter.md) |
| 2026-04-15 | resolved | | ZMQ telemetry stale on :5556 — RECONNECT_IVL fix and staleness E-STOP | [2026-04-15-zmq-telemetry-stale-reconnect-ivl](2026-04-15-zmq-telemetry-stale-reconnect-ivl.md) |
| 2026-04-01 | resolved | 4.1 | Cold-hold fallback commanded stroke minimum | [2026-04-01-cold-hold-fallback-stroke-minimum](2026-04-01-cold-hold-fallback-stroke-minimum.md) |
| 2026-03-30 | resolved | 3.1 | Velocity feedforward oscillation | [2026-03-30-velocity-feedforward-oscillation](2026-03-30-velocity-feedforward-oscillation.md) |
