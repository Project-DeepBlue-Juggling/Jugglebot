# Skill-stack invariant checklist (R0)

What this is: every physical fact the machine currently refuses to violate, with the ONE place it is enforced and the ONE test that fails without it. It is the net under `plans/active/two-ball-skill-stack.md` — the FSM choreography is hollowed out at R4 and the Platform Teensy stroke engine deleted at R1, and an invariant that is not on this list dies with the code it was hiding in.
**THE RULE: a code deletion at any rung MUST first check every row that names the deleted path.** A row named by a deleted file is either re-enforced somewhere in the same rung (`PORT`) or is deliberately not an invariant (`RETIRE`, with the reason stated in the row). No third option, and never "we'll re-add it later".
Row vocabulary — `ID`: the existing contract ID where one exists, else `I-<layer>-<n>`. `Invariant`: one normative sentence, one physical fact. `Enforcement`: the one place it is checked (`path::symbol`, or `firmware: file::fn`). `Test`: the one test that fails without it, or `NONE — gap`. `Fate`: `KEEP` / `PORT@R<n>` / `RETIRE@R<n>` (with a reason).

## 1. Transport + firmware guards — the authority nothing on the Jetson overrides

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| I-FW-1 | A Setpoint stream older than 250 ms MUST latch a guard E-STOP that stays latched until an explicit operator clear. | firmware: `fault_machine.cpp` (`SETPOINT_STALENESS_US`) | `NONE — gap` (the LATCH test drives overspeed and MAX_DEVIATION only; no native test ages the clock past `SETPOINT_STALENESS_US` — see Gaps 5) | KEEP |
| I-FW-2 | A leg whose raw commanded position diverges from its encoder by more than `MAX_DEVIATION_REV` (1.0 rev = 70 mm) MUST E-STOP rather than chase the target. | firmware: `fault_machine.cpp` (`canbridge_config.h::MAX_DEVIATION_REV`) | `tests/firmware/native/test_fault_machine.cpp::"guard E-STOP / fb-stale are present-scoped and pre-arm-safe"` | KEEP |
| I-FW-3 | The hand's deviation guard MUST be velocity-compensated on both sides (`2.5 rev` against `fb + fb_vel·age`), because a static bound at 221 rev/s cannot fire before the hard stop. | firmware: `fault_machine.cpp` (`MAX_DEVIATION_HAND_REV`) | `tests/firmware/native/test_fault_machine.cpp::"hand deviation: observe-first reports only; \`hand7 arm\`ed it LATCHES off the 500 Hz tick verdict"` | KEEP |
| I-FW-4 | The emitted leg command MUST be bounded to `encoder ± MAX_LEAD_REV` (0.10) with `vel_ff` passed through capped, never zeroed — zeroing it at clamp engage is what seeded the ~6 Hz limit cycle. | firmware: `leg_interp.cpp` (lead clamp) | `tests/firmware/native/test_leg_interp.cpp::"lead clamp: bounds position, KEEPS vel_ff (capped), sets the clamp mask"` | KEEP |
| I-FW-5 | The LEG guard constants MUST NEVER be applied to axis 6: the hand runs 13× leg speed, so its clamp anchors on the age-extrapolated encoder at `MAX_LEAD_HAND_REV` (2.0) and its `vel_ff` cap is 300 rev/s, not 3.5. | firmware: `leg_interp.cpp` (hand lane) | `tests/firmware/native/test_leg_interp.cpp::"guard separation: the LEG constants never touch axis 6"` | KEEP |
| I-FW-6 | Non-zero hand lead-clamp duty during a throw MUST abort the sitting — a clamp that engages on a legitimate command means the freshness model is wrong. | firmware: `leg_interp.cpp` (`interp_hand_lead_clamp_ticks`) | `tests/firmware/native/test_leg_interp.cpp::"a TRANSMITTING stage does count the lead clamp and the dev_over ticks"` | KEEP |
| I-FW-7 | Every leg command MUST be pinned to that leg's measured hard-stop range, with velocity and torque zeroed at the clamp. | firmware: `leg_interp.cpp` (stroke clamp) | `tests/firmware/native/test_leg_interp.cpp::"stroke clamp: command pinned to [STROKE_MIN, STROKE_MAX], vel/torque zeroed"` | KEEP |
| I-FW-8 | The hand command MUST be clipped to `[0, HAND_MOTOR_MAX_POSITION]` with a NON-ZERO margin below the hard stop — at zero margin both hand guards go blind (command and encoder agree while the slider is jammed in metal). | firmware: `leg_interp.cpp` (`canbridge_config.h::HAND_MOTOR_MAX_POSITION`) | `tests/firmware/native/test_leg_interp.cpp::"guard separation: the LEG constants never touch axis 6"` (SUBCASE "hand stroke clip") | KEEP |
| I-FW-9 | Overspeed MUST be guarded per axis class — legs at 16.5 rev/s, hand at 345 — never the legs' number on axis 6. | firmware: `fault_machine.cpp` (`HAND_MAX_MOTOR_VEL_RPS`) | `tests/firmware/native/test_fault_machine.cpp::"hand overspeed guards at HAND_MAX_MOTOR_VEL_RPS (345), never the legs' 16.5"` | KEEP |
| I-FW-10 | On command silence the interpolator MUST decay: Hermite → Taylor (50 ms) → velocity ramp to zero (60 ms), never a held velocity and never a step. | firmware: `leg_interp.cpp` (Modes 1/2/3) | `tests/firmware/native/test_leg_interp.cpp::"velocity-decay mode: beyond MAX_EXTRAP_DT the velocity decays to zero"` | KEEP |
| I-FW-11 | When `HAS_HAND` falls while leg frames continue, the hand lane MUST DECAY to rest — it MUST NOT hold the last knot velocity. | firmware: `leg_interp.cpp` (falling-edge decay) | `tests/firmware/native/test_leg_interp.cpp::"NORMATIVE falling edge: HAS_HAND falls while leg frames continue ⇒ the hand DECAYS, never holds v1"` | KEEP |
| I-FW-12 | A setpoint carrying a non-finite field MUST be DROPPED whole — never clamped, never partially staged. | firmware: `leg_interp.cpp` (ingest) | `tests/firmware/native/test_leg_interp.cpp::"a setpoint with a non-finite field is DROPPED (never staged / streamed), item 5"` | KEEP |
| I-FW-13 | A bus with no recently-seen partner MUST NEVER be transmitted into; on CAN loss the stow is ARMED and executed only once the bus is confirmed back, with terminal `IDLE` if it never returns. | firmware: `fault_machine.cpp` (deferred-stow latch) | `tests/firmware/native/test_fault_machine.cpp::"never command a dead bus + terminal IDLE on stow complete"` | KEEP |
| I-FW-14 | Encoder-feedback staleness (150 ms) MUST suppress output WITHOUT latching — it is recoverable, unlike I-FW-1. | firmware: `fault_machine.cpp` (`MOTOR_FB_STALENESS_US`) | `tests/firmware/native/test_fault_machine.cpp::"motor-feedback staleness suppresses output recoverably (not latched)"` | KEEP |
| I-FW-15 | Exactly ONE master may command axis 6 at any instant; two writers on one CAN id MUST be structurally impossible, not merely conventionally avoided. | firmware: `hand_source.cpp::hand_source_request` (the latch) | `tests/firmware/native/test_hand_ops.cpp::"hand_source_request gates: mpc_active, unseen/stale/unsettled telemetry all refuse"` | PORT@R1 — the INVARIANT survives; the LATCH does not. R1 deletes the stroke engine, so the bridge becomes the sole TX owner by construction and the interlock's own settle gate (I-FW-16) goes with it. R1 MUST land a native test that the bridge is the only axis-6 writer. |
| I-FW-16 | A mastery switch MUST be refused unless the hand is settled at a rest position (abs(pos−rest) ≤ 0.10 rev, abs(vel) ≤ 0.5 rev/s, fresh telemetry) — a swap mid-stroke hands a moving axis to a master whose timeline starts cold. | firmware: `hand_source.cpp::hand_source_request` | `tests/firmware/native/test_hand_ops.cpp::"hand_source_request gates: mpc_active, unseen/stale/unsettled telemetry all refuse"` | RETIRE@R1 — not an invariant of the machine but of the migration: it guards the act of swapping masters, and after R1 there is only one master to swap to. |
| C-PLATFW-1 | The Platform Teensy MUST declare its firmware version and the host MUST read the board's answer, never infer it; `0` is reserved for a pre-versioning board. | `teensy_link/rpc_args.py::PLATFORM_FW_VERSION_EXPECTED` + `Teensy_code_platform.ino::FW_VERSION` | `tests/firmware/test_platform_fw_version_xref.py::test_firmware_fw_version_matches_the_host_expectation` | KEEP — R1 bumps both ends in lockstep (`PROTOCOL_VERSION 6 → 7`). |

## 2. Wire frame + emitter

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| I-WIRE-1 | The frame MUST be bumpless across a producer switch: `u0` is `motor_rev` verbatim when present (stow offset included), never the `ext × mm_to_rev` fallback, which would jump the leg by the stow offset the instant the source flips. | `teensy_link/setpoint_pump.py::SetpointPump` | `tests/motion/test_trajectory_emitter.py::test_emitter_motor_rev_equals_ext_times_mm_to_rev` | KEEP |
| I-WIRE-2 | The emitted field set MUST stay byte-compatible with what the pump validates — a producer that invents a field set is unvalidated on the wire. | `motion/trajectory/emitter.py::KnotEmitter` | `tests/motion/test_trajectory_emitter.py::test_legacy_frames_byte_identical_to_v5_capture` | KEEP |
| I-WIRE-3 | Hand keys are ALL-OR-NOTHING and any non-finite value in any of them MUST REJECT the whole frame — a silently-degraded lane on a 221 rev/s axis is not a degradation, it is a fault. | `teensy_link/setpoint_pump.py::SetpointPump` | `tests/teensy_link/test_setpoint_pump.py::test_nan_or_junk_in_any_hand_key_rejects` | KEEP |
| I-WIRE-4 | Hand streaming MUST carry the full Mode-1 leg knot set: hand keys aboard a frame whose leg lookahead is absent or flag-cleared is an incoherent producer state and REJECTS. | `teensy_link/setpoint_pump.py::SetpointPump` | `tests/teensy_link/test_setpoint_pump.py::test_hand_without_leg_lookahead_rejects` | KEEP |
| I-WIRE-5 | A commanded per-knot step beyond the axis's own gate MUST be refused host-side, and the hand's gate MUST be independent of the legs' (200 rev/s × 25 ms = 5.0 rev). | `teensy_link/setpoint_pump.py::SetpointPump` | `tests/teensy_link/test_setpoint_pump.py::test_hand_step_gate_rejects_independently_of_legs` | KEEP |
| I-WIRE-6 | The pump is the ONE place a physical torque becomes an ODrive wire value, and it MUST clamp to `±torque_ff_max_nm` per leg — above ~0.55 wire-Nm the position loop loses all authority and the leg runs open-loop into `MAX_DEVIATION`. | `teensy_link/setpoint_pump.py::SetpointPump` (torque_ff clamp) | `tests/motion/test_leg_torque_ff.py::test_pump_torque_ff_max_nm_ceiling` | KEEP |

## 3. Planner / reference feasibility

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| K1 | The reference MUST start at the plant's live state — no step-discontinuity from where the machine actually is. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_k1_anchor_prepended_if_caller_forgot` | PORT@R2 — the skill stack's form is the splice rule: every segment is planned FROM the live state at the splice knot, ≥ 2 knots ahead of the emit cursor. `controller/target.py` keeps K1 for the sim reference path. |
| K2 | Peak reference velocity on any linear axis MUST satisfy `≤ β·v_max` (β = 0.85) — a reference tight at `v_max` is untrackable. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_property_K1_K6_two_event_proposal` | KEEP (sim path); the skill stack's twin is I-PLAN-1. |
| K3 | Peak reference acceleration MUST satisfy `≤ β·(v_max/tau)` — the actuator bandwidth, not merely velocity feasibility. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_large_pose_change_short_T_triggers_K3` | KEEP (sim path); the skill stack's twin is I-PLAN-2. |
| K4 | Event times MUST be strictly increasing, minimum span 50 ms; near-duplicates merge, never raise. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_property_K4_min_span` | KEEP |
| K5 | Two events at the same time MUST carry identical twists — a twist discontinuity is a commanded velocity impulse. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_property_K5_coincident_twist` | KEEP |
| K6 | Each event's linear twist MUST be clipped to `±β·v_max` — FK-derived plant twists spike, and a spike seeds a physically impossible boundary condition K2 cannot stretch out of. | `controller/target.py::make_feasible_events` | `tests/sim/test_make_feasible_events.py::test_fk_noise_spike_always_clamped` | KEEP |
| I-PLAN-1 | A cycle whose leg extension velocity exceeds the session limit MUST be refused `LIMIT_VEL`, and the refusal MUST name the bound. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_trajectory_feasibility.py::test_reject_limit_vel` | KEEP |
| I-PLAN-2 | Likewise acceleration (`LIMIT_ACC`) and jerk (`LIMIT_JERK`), evaluated in priority order vel → acc → jerk so the operator is sent to one knob. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_trajectory_feasibility.py::test_priority_vel_over_acc_over_jerk` | KEEP |
| I-PLAN-3 | The gate MUST be conservative under mesh coarsening: a plan the 80-sample gate accepts MUST also pass a dense 400-sample re-check. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_trajectory_feasibility.py::test_validate_80_never_accepts_what_dense_400_rejects` | KEEP — R2's vectorised runtime assert MUST preserve this, not merely run faster. |
| I-PLAN-4 | A pose outside the workspace, or one whose Jacobian condition number exceeds the hard bound, MUST be refused `WORKSPACE` / `UNREACHABLE` — never clamped into range. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_trajectory_feasibility.py::test_reject_workspace` | KEEP |
| I-PLAN-5 | The catch runway MUST be re-checked with the ACHIEVED catch velocity and MUST measure to the TRUE bottom of hand travel, not to the plan's parked start. | `motion/trajectory/feasibility.py::validate_cycle` (runway re-check) | `tests/motion/test_validate_cycle.py::test_the_catch_runway_still_measures_to_the_TRUE_bottom_of_travel` | KEEP |
| I-PLAN-6 | A hand excursion hidden BETWEEN knots MUST be caught — the gate measures the plan's true demand, never a knot difference. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_validate_cycle.py::test_hand_acceleration_is_the_plans_true_demand_not_a_knot_difference` | KEEP |
| I-PLAN-7 | Every segment MUST end at rest at its site: the tail is the abort path, planned in advance, and is what the machine does if the next skill never arrives. | `motion/trajectory/planner.py::build_catch` (LANDING kind, rest-terminal) | `tests/motion/test_trajectory_planner_catch.py::test_catch_reaches_pose_and_ends_at_rest` | PORT@R2 — becomes `skills/segments.plan_segment`'s post-condition for all three kinds (THROW, CATCH, REST). |

## 4. Levelling frame

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| C-LEVEL-1 | The gravity correction MUST be applied EXACTLY ONCE per external pose, through one shared implementation, at the enumerated ingest sites and nowhere else; it rewrites rotation only. | `motion/levelling.py::correction_from_offset`, called at the enumerated ingests | `tests/ros/test_levelling_frame.py::test_external_surface_applies_the_correction_exactly_once_per_row` | KEEP |
| I-LEVEL-1 | The correction MUST be applied at INGEST, never in the emitter — an emitter-side correction puts the executed plan outside the gate that measured it (`plan == emitted == gated`). | `motion/levelling.py` + the ingest handlers | `tests/ros/test_levelling_frame.py::test_levelling_call_sites_are_exactly_the_enumerated_ingests` | KEEP |
| C-LEVEL-1 (in-flight) | A live plan MUST keep the frame it was built in; a new `/gravity_offset` re-frames only the NEXT install. Re-framing in flight steps the wire by 0.039 rev on one knot — 13 % of the pump's step gate, i.e. invisible to it. | `trajectory_node.py::_on_gravity_offset` (stored, read at ingest) | `tests/ros/test_levelling_frame.py::test_in_flight_plan_keeps_its_frame_when_a_new_offset_arrives` | KEEP |
| E8 | The unified cycle MUST take ONE correction lookup per cycle, at the seed, and carry it through the whole window — a per-knot lookup is forbidden (mixed-frame interpolation). | `motion/trajectory/cup_realize.py::tilt_schedule`, seeded in `motion/unified_cycle.py::_realize` | `tests/ros/test_unified_cycle_levelling.py::test_the_correction_is_BUILT_at_the_seed_and_carries_the_map_residual` | PORT@R2 — `motion/unified_cycle.py::_realize` is deleted with the ring, but `install_segment` inherits the seam: one lookup per SEGMENT, at its splice knot. |
| I-LEVEL-2 | The chained install MUST plan every window of a chain in ONE frame; the frame is recorded on the installed meta and never re-read. | `motion/unified_cycle.py::plan_cycle` / `extend` | `tests/ros/test_unified_cycle_levelling.py::test_the_chained_install_plans_both_windows_in_ONE_frame` | PORT@R2 |
| C-LEVEL-1.O | "Is a correction loaded?" MUST be answered ONLY by the node that applies it, on a FRESH `trajectory/status.gravity_correction_loaded`; `RobotState.levelling_complete` survives the relaunch that empties the correction and MUST NOT be substituted. A zero measured tilt IS loaded. | `trajectory_node.py::_on_gravity_offset` (flag) + `toss_sequencer._step_checking` (gate) | `tests/ros/test_trajectory_node.py::test_zero_offset_counts_as_loaded_even_though_the_correction_is_identity` | KEEP — the gate moves to the skill orchestrator at R4 (see § 8). |

## 5. Possession + arming

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| C-POSSESS-1 | A possession source MUST report ARRIVAL and RETENTION independently and MUST report `UNKNOWN` for any part it does not positively observe — never `True` for a part it cannot see. | `ball_possession.py::PossessionVerdict` | `tests/ros/test_ball_possession.py::test_tracker_source_never_claims_retention` | KEEP — R3's learner reads `caught` from exactly this verdict. |
| C-POSSESS-1 (veto) | `UNKNOWN` retention MUST NOT veto a confirmed arrival; only a positively observed departure (`RETENTION_REJECTED`) vetoes. | `ball_possession.py::PossessionVerdict` | `tests/ros/test_ball_possession.py::test_unknown_retention_does_not_veto_but_rejected_does` | KEEP |
| C-POSSESS-1.C | A possession window MUST NOT outlast the machine's own next scheduled event — a window that does turns every good cycle into a bounce report. | `ball_possession.py::HandBallSensorSource._window` | `tests/ros/test_ball_possession.py::test_the_whole_measured_catch_band_fits_inside_the_window` | KEEP — R5's beat is shorter than every window this was clamped against; re-derive the clamps against β. |
| C-POSSESS-1.D | The LIVE cup query MUST answer from the RAW sensor bit; edges are for windows, not for the precondition. | `ball_possession.py::HandBallSensorSource.evidence` | `tests/ros/test_ball_possession.py::test_evidence_goes_unknown_when_the_node_stops_hearing_the_sensor` | KEEP |
| I-POSS-1 | Possession MUST be asked at ONE seam from every consumer — a second minting site is a second definition of "caught". | `reload_coordinator_node._possession_observed` | `tests/ros/test_reload_coordinator_node.py::test_possession_is_asked_at_one_seam_from_every_consumer` | PORT@R4 — the seam moves to `skill_node`'s outcome capture when the coordinator is hollowed out. |
| A1 | `mpc_active` MUST go 0→1 through ONE path only, whose preconditions include a fresh pump-acceptable frame whose `u0` is within 0.25 rev of every live encoder — so the first applied setpoint commands the pose the ODrives already hold. | `teensy_bridge_node.py::_arm_setpoint_output` | `tests/ros/test_teensy_bridge_node_read.py::test_enable_setpoint_output_true_is_inert_no_boot_arm` | KEEP |
| A2 | The orchestrator MUST own WHEN: it is the sole caller of the arm service, arms after the profiled ACTIVATE completes, and disarms on real-fault FAULT — a guard-only fault deliberately does NOT disarm, because the resume path needs the stream alive. | `state_machine.py::ActiveHandler` / `FaultHandler.on_enter` | `tests/ros/test_state_machine.py::test_guard_only_fault_does_not_disarm` | KEEP |
| A3 | The wire MUST be disarmed, and the firmware's arm-took bit MUST confirm it, BEFORE any DEACTIVATE is fired — staging a disarm is not landing one. | `teensy_bridge_node.py::_run_deactivate` | `tests/ros/test_teensy_bridge_node_deactivate.py::test_run_deactivate_waits_for_wire_disarm_confirmation` | KEEP |
| A4 | The stream MUST outlive the armed window: the control mode stays streaming until the deactivate COMPLETES, so `SETPOINT_STALE` can never beat the disarm. | `state_machine.py::IdleHandler` (deferred blank) | `tests/ros/test_state_machine.py::test_blanks_control_mode_only_after_pending_operation` | KEEP |
| A5 | A motion command accepted while the wire is disarmed MUST be loud — an illegal state that is silent is an invisible one. | `trajectory_node.py` (wire-state WARN + response suffix) | `tests/ros/test_trajectory_node.py::test_accept_while_disarmed_is_loud` | KEEP |

## 6. Hand

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| C-HAND-1 | No hand command may create a discontinuity, in position OR velocity, between the live hand state and the newly commanded trajectory. | (H) `catch_coordinator_node._throw_stroke_gate_ok`; (F) `Teensy_code_platform/Trajectory.h::makeSmoothMove` | `tests/ros/test_catch_coordinator_node.py::test_arm_withheld_while_our_throw_stroke_is_still_decelerating` | PORT@R1 — both enforcement points are deleted at R1 (H's dispatch gate and F's prelude belong to the stroke engine). The invariant is the whole point of the streamed lane and becomes structural: the emitter samples ONE continuous plan, so R1 MUST pin continuity at the splice instead of at the dispatch. |
| C-HAND-2 | The declared reflected inertia behind a post-release decel feedforward MUST be ≤ the measured reflected inertia, so the feedforward alone can never brake harder than the commanded profile. | `Teensy_code_platform/Trajectory.h::throwDecelToTorque` | `tests/sim/test_hand_throw_decel_ff.py::test_declared_inertia_cannot_over_brake` | PORT@R1 — the firmware enforcement point dies with the stroke engine; the physical fact (a feedforward that out-brakes its profile drags the hand backwards) applies to any streamed decel and MUST be restated wherever R1 sizes hand torque. |
| C-HAND-3 | A throw MUST be admitted only if, at the actual commanded release speed, ALL of: end-stop clearance, decel and carry torque authority, current headroom, regen budget, bridge acceptance band, and catch-arm window margin hold — and NO hand-picked flight-time or throw-height boundary may exist anywhere in the admission path. | `motion/trajectory/throw_envelope.py::evaluate` | `tests/motion/test_throw_envelope.py::test_every_declared_input_comes_from_generated_config` | PORT@R2 — its physical bounds become the INPUTS to the offline admissible sweep (plan § 2.6); the per-throw runtime call leaves the beat path. |
| I-HAND-1 | The end-stop margin MUST cover the measured one-sided scatter of the coast — a margin sized on the mean is a margin that has already been crossed. | `motion/trajectory/throw_envelope.py::evaluate` | `tests/motion/test_throw_envelope.py::test_the_end_stop_margin_covers_the_one_sided_scatter` | PORT@R2 |
| I-HAND-2 | The host's coast margin MUST NEVER be looser than the firmware clamp it shadows. | `motion/trajectory/throw_envelope.py::evaluate` | `tests/motion/test_throw_envelope.py::test_the_host_coast_margin_is_never_looser_than_the_firmware_clamp` | PORT@R2 |
| I-HAND-3 | A plan may not dive below the hand's parked floor, and that floor may never be lowered past the firmware retract settle band. | `motion/trajectory/feasibility.py::validate_cycle` | `tests/motion/test_validate_cycle.py::test_the_parked_floor_can_never_be_lowered_past_the_retract_band` | KEEP |
| I-HAND-4 | The hand lane MUST stay INERT until a FRESH `HAS_HAND` knot arrives after an arm edge — a stale latch may never re-master the axis (the idle-axis trap). | firmware: `leg_interp.cpp` (arm-edge latch clear) | `tests/firmware/native/test_leg_interp.cpp::"arm-edge clears a stale hand latch: a hand-less re-armed session is INERT (2026-09-02 fix)"` | KEEP — the trap outlives `hand_source`: the latch is the KNOT latch, not the mastery latch. |
| I-HAND-5 | The hand lane MUST NEVER transmit before the first axis-6 encoder frame has been seen. | firmware: `leg_interp.cpp` | `tests/firmware/native/test_leg_interp.cpp::"hand lane never transmits before the first axis-6 encoder frame (unseen skip, counted)"` | KEEP |

## 7. Catch / arrival / reach

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| C-CATCH-1 | A catch plan MUST NOT command motion nobody asked for: where the planner DERIVES an arrival twist, no departure from the target it creates may exceed `40/81` of the catch's physical tilt scale. Motion the caller asked for is unbounded; motion the planner invented is not. | `motion/trajectory/planner.py::_catch_arrival_rate` | `tests/motion/test_trajectory_planner_catch.py::test_ccatch1_bounds_the_unrequested_excursion_at_a_long_lead` | PORT@R2 — the same distinction (requested vs invented) applies to a CATCH segment's terminal, whose arrival twist comes from the tracker rather than from a caller. |
| C-REACH-1 | The reach envelope bounds UNREQUESTED excursion only — how far a drifting landing estimate may drag the platform from where the catch was planned. Its centre is the DECLARED nominated catch point, captured at the arm-latch raise, and a declaration is scoped to exactly one raise. | `trajectory_node.py::_on_reach_center` / `_svc_arm_catch` / `_on_dynamic_target` | `tests/ros/test_trajectory_node.py::test_creach1_declaration_is_scoped_to_exactly_one_raise` | PORT@R4 — `catch_reach` and the arm-catch latch are deleted at R4; the skill stack's form is a bound on how far a re-sent CATCH terminal may move the segment from the one that was planned. |
| I-CATCH-1 | A reach-envelope centre MUST NEVER be applied mid-catch — moving the envelope out from under an airborne ball. Non-finite declarations are dropped loudly (a NaN centre silently disables the gate). | `trajectory_node.py::_on_reach_center` | `tests/ros/test_trajectory_node.py::test_creach1_declaration_never_applies_mid_catch` | PORT@R4 |
| I-CATCH-2 | A standing post-release infeasible catch target MUST be latched, not acted on: terminating mid-flight retracts the hand into an incoming ball (hardware evidence 2026-07-23, twice). | `trajectory_node.py::_on_dynamic_target` (freeze) | `tests/ros/test_trajectory_node.py::test_dynamic_target_reach_freeze_ignores_late_updates` | PORT@R2 — the skill stack's form is the rest tail: a refused skill ends the attempt through the PREVIOUS segment's planned rest, never through an abort that moves the hand. |
| I-CATCH-3 | A displaced catch reach MUST be judged against the closed-form quintic reach frontier at the LIVE session limits, never a fixed radius. | `motion/trajectory/catch_reach.py::catch_reach_verdict` | `tests/motion/test_catch_reach.py::test_live_session_limits_move_the_verdict` | PORT@R2 — becomes the transit leg of the admissible sweep (site pair × beat), swept offline instead of gated per cycle. |

## 8. Session-level refusals worth keeping as SKILL refusals

Each KEEP/PORT row names one physical fact a skill orchestrator must still refuse on. Every RETIRE row is FSM choreography — a tier gate, a pipeline slot, a displacement cap or a dwell floor — that describes the ring's shape rather than the machine's.

| ID | Invariant | Enforcement | Test | Fate |
|---|---|---|---|---|
| `REJECTED_NO_BALL` | A throw MUST be refused when the cup is observed EMPTY. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_ball_evidence_refusals_name_the_sensor_state` | PORT@R2 |
| `REJECTED_BALL_UNKNOWN` | A throw MUST be refused when the sensor CANNOT ANSWER — a dead sensor refuses, it does not pass. Two codes, because they send the operator to two different subsystems. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_ball_evidence_refusals_name_the_sensor_state` | PORT@R2 |
| `REJECTED_HAND_STALE` | A throw MUST be refused on stale hand telemetry — a dead hand link blinds release verification, and the learner's row is unobservable. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | PORT@R2 |
| `REJECTED_HAND_NOT_PARKED` | A throw MUST be refused when the hand is not where the plan's seed says it is. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | PORT@R2 — the reason CHANGES at R1: today it is that a kind-0 stroke commands ABSOLUTE positions from 0 rev; after R1 it is that the segment was planned from a seed the hand has since left. Same refusal, different physics — restate it, do not copy it. |
| `REJECTED_MOCAP_STALE` | A skill MUST be refused on stale mocap: perception loss ends the attempt, it never delays the schedule. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | PORT@R2 |
| `REJECTED_NOT_LEVELLED` | A throw MUST be refused unless a gravity correction is loaded in the node that applies it: un-levelled the launch is 0.78° off gravity = 43 mm of lateral drift over a 0.8 s flight against a ~35 mm cup. | `toss_sequencer.py::_step_checking` (see C-LEVEL-1.O) | `tests/ros/test_toss_sequencer.py::test_not_levelled_sits_between_the_graph_gates_and_the_hand_chain` | PORT@R2 |
| `REJECTED_TRACK_ACTIVE` | A throw MUST be refused while a live tracker expectation is already destined for us — a phantom track correlates against our own announcement. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | PORT@R4 — R4 is where a second ball first makes this reachable in normal operation. |
| `REJECTED_NOT_STREAMING` / `REJECTED_WRONG_MODE` | A skill MUST be refused unless the machine is in the streaming mode that owns the platform — the wire is the only path to motion. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | PORT@R2 |
| `ABORTED_MODE_CHANGED` | Leaving the owning mode mid-skill MUST end the attempt through the planned rest tail. | `toss_sequencer.py` / `reload_sequencer.py` | `tests/ros/test_reload_sequencer.py::test_mode_change_mid_sequence_aborts_safe_abort` | PORT@R2 |
| `REJECTED_EVENT_VEL` | A release speed outside the bridge's acceptance band MUST be refused, naming the wire bound. | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_event_vel_bound_rejected` | PORT@R2 — folds into C-HAND-3's band term and thence into the admissible box. |
| `REJECTED_THROW_ENVELOPE` | See C-HAND-3 — the refusal MUST name the binding bound and quote the computed envelope. | `motion/trajectory/throw_envelope.py::evaluate` | `tests/motion/test_throw_envelope.py::test_every_declared_input_comes_from_generated_config` | PORT@R2 |
| `ABORTED_NO_RELEASE` | A throw with NO release evidence by its deadline MUST end the attempt; no observation means no learner row. | `toss_sequencer.py` (THROWING) | `tests/ros/test_toss_sequencer.py::test_no_release_evidence_aborts` | PORT@R3 |
| `REJECTED_BB(<message>)` | An external thrower's OWN refusal MUST surface verbatim, not be laundered into a generic abort. | `reload_sequencer.py` | `tests/ros/test_reload_sequencer.py::test_a_bb_busy_through_the_whole_patience_still_rejects` | PORT@R4 |
| `REJECTED_TIER` | — | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_tier_gate_rejects_unknown_tier` | RETIRE@R4 — a config gate over two hard-coded choreographies (8a/8b). The skill stack has one path; there is no tier to be wrong about. |
| `REJECTED_DISPLACEMENT` | — | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_displacement_rejected` | RETIRE@R2 — a pre-throw cap on the A→B displacement that exists because the ring cannot re-plan mid-flight. The skill stack plans each segment from live state and the transit is swept offline; the physical half survives as I-CATCH-3. |
| `REJECTED_TILT_CLAMP` | — | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_tilt_clamp_rejects_in_tier_8a_too` | RETIRE@R2 — a second statement of the tilt bound the cup QP already boxes. Collapse, do not port (listed under Gaps). |
| `REJECTED_POSE_UNKNOWN` | — | `toss_sequencer.py::_step_checking` | `tests/ros/test_toss_sequencer.py::test_pose_unknown_rejected_before_every_displacement_gate` | RETIRE@R2 — a cross-node read of the live commanded pose. In the skill stack `trajectory_node` is the only holder of live state and plans from it directly; the seam that could fail is gone. |
| `REJECTED_POSITION(<code>)` / `ABORTED_POSITION_FAILED` / `ABORTED_POSITION_TIMEOUT` | — | `toss_sequencer.py` (POSITIONING) | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | RETIRE@R4 — the POSITIONING pipeline slot: a profiled `go_to_pose` before the throw. Skills splice into a continuously streamed plan; there is no separate positioning move to time out. |
| `REJECTED_SITE_MOVED` / `REJECTED_REACH_CENTER_DRIFT` | — | `toss_sequencer.py` (PREPARING) | `tests/ros/test_trajectory_node.py::test_creach1_drift_is_still_bounded_around_the_declared_centre` | RETIRE@R4 — staging bookkeeping for a session-scoped latch that R4 deletes; the physical half is C-REACH-1's PORT. |
| `REJECTED_RELEASE_SCHEDULE` / `REJECTED_CANT_MAKE_LEAD` / `ABORTED_CANT_MAKE_RELEASE` | — | `toss_sequencer.py` | `tests/ros/test_toss_sequencer.py::test_throw_delay_floor_is_the_derived_dispatch_budget_not_a_constant` | RETIRE@R4 — dwell floors and release-window arithmetic of the ring beat. The schedule is absolute wall-clock and never delays; a skill that cannot be planned in time is simply refused and the rest tail streams on. |
| `ABORTED_PREPARE_FAILED` | — | `toss_sequencer.py` (PREPARING) | `tests/ros/test_toss_sequencer.py::test_precondition_rejects` | RETIRE@R4 — the PREPARE bundle (soft gains, latch raise, vel_scale, snapshot) is choreography; the skill stack has no prepare phase. |
| `REJECTED_NOT_CENTERED` | — | `reload_sequencer.py::_step_checking` | `tests/ros/test_reload_sequencer.py::test_not_centered_is_refused_before_bb_is_asked_to_throw` | RETIRE@R4 — it exists because BB aims at an ASSUMED pose. At R4 the reload is a CATCH skill whose terminal comes from the announcement, so the site is declared rather than assumed. |
| `REJECTED_BB_BUSY` | — | `reload_sequencer.py` | `tests/ros/test_reload_sequencer.py::test_a_bb_busy_through_the_whole_patience_still_rejects` | RETIRE@R4 — a pipeline-slot fact about an external machine's state machine, not about ours. |

## Gaps

**Rows with no pinning test (`NONE — gap`) or with only adjacent coverage:**

1. **I-FW-15 after R1** — that the bridge is the SOLE axis-6 writer once `hand_source` is deleted has NO test; today the latch's own gate test stands in for it. R1 must land a native test asserting no other TX path reaches axis 6.
2. **I-FW-3 armed behaviour on hardware** — the hand deviation guard's ARMED trip has never been observed on the robot (hand ladder row 18, closed by operator decision 2026-09-05, `tests/hardware/session_unified7_hand_bringup.md`). The native test pins the logic; nothing pins the physics.
3. **C-HAND-1 obligation F** — `Trajectory.h::makeSmoothMove`'s velocity-continuous prelude is pinned only indirectly (`tests/sim/test_hand_trajectory.py::test_a_brake_that_fits_is_velocity_continuous` tests the host twin). When R1 deletes the firmware, the twin's coverage goes with the thing it twins.
4. **C-PLATFW-1's runtime half** — the xref test pins the build; the doc states plainly that runtime enforcement "is the operator's, and it is procedural". No automated check refuses a session against a skewed board.
5. **I-FW-1 (Setpoint staleness → E-STOP latch)** — the native LATCH test drives overspeed and `MAX_DEVIATION` only; nothing ages the mono clock past `SETPOINT_STALENESS_US` and asserts `fault_state() == SETPOINT_STALE` with the guard in ESTOP. R1's can-bridge firmware unit lands that SUBCASE while it is in `test_fault_machine.cpp` anyway.
6. **I-PLAN-7 (rest-terminal) for THROW and REST kinds** — pinned for CATCH only. R2's `plan_segment` must pin all three.

**Invariants stated in two places — collapse before porting:**

7. **The tilt ceiling** — `REJECTED_TILT_CLAMP` in `toss_sequencer` and the tilt box inside the cup QP / `validate_cycle` state the same physical bound. One of them is a duplicate; the QP's is the one that measures what ships.
8. **The reach bound** — `REJECTED_DISPLACEMENT`'s closed-form quintic frontier (`toss_sequencer`) and `catch_reach.catch_reach_verdict` are the same computation reached by two callers. `catch_reach.py` is the shared implementation and the sequencer's copy is the one that dies.
9. **The hand settle band (0.10 rev)** — declared in `canbridge_config.h::HAND_SETTLE_BAND_REV`, mirrored in `motion/trajectory/hand_stroke.py`, and read again as `validate_cycle`'s parked floor. Three statements of one number; R1 deletes `hand_stroke.py`, so the config chain must become the single source in the same rung.
10. **"Is the machine levelled?"** — `trajectory/status.gravity_correction_loaded` (correct) and `RobotState.levelling_complete` (a Teensy-persisted flag that survives the relaunch which empties the correction). C-LEVEL-1.O already forbids the second; the field still exists and still reads True in exactly the state the gate exists to refuse.
