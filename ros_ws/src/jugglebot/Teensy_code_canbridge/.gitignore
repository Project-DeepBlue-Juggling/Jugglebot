# PlatformIO build directory — regenerated by `pio run`, not tracked.
.pio/
# pio's INO→CPP conversion artifact (src_dir = '.', so it lands beside the
# sources) — regenerated by every `pio run`, never hand-edited, never tracked.
Teensy_code_canbridge.ino.cpp
