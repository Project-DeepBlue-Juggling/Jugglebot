# Pose of a single named rigid body from the motion capture system

string name                   # Name of the rigid body (e.g., "Platform", "Base", "Catching_Cone")
geometry_msgs/PoseStamped pose  # Full pose with timestamp and frame_id
