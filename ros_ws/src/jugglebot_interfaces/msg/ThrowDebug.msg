# For debugging the 'throw to catching cone' throws - why are these sometimes missing?

builtin_interfaces/Time stamp
float32 roll_cmd   # Commanded platform roll {rad}
float32 pitch_cmd  # Commanded platform pitch {rad}
float32 v0_mag_cmd # Commanded throw velocity (magnitude) {m/s}
